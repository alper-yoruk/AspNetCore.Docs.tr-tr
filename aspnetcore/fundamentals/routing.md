---
title: ASP.NET Core yönlendirme
author: rick-anderson
description: HTTP isteklerinden eşleşen ve yürütülebilir uç noktalara gönderen ASP.NET Core yönlendirmenin nasıl sorumlu olduğunu öğrenin.
monikerRange: '>= aspnetcore-2.1'
ms.author: riande
ms.custom: mvc
ms.date: 4/1/2020
no-loc:
- Blazor
- Identity
- Let's Encrypt
- Razor
- SignalR
uid: fundamentals/routing
ms.openlocfilehash: f4bba10f7ce0b5a95c2aed8644aed18fa0637457
ms.sourcegitcommit: 4437f4c149f1ef6c28796dcfaa2863b4c088169c
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 06/19/2020
ms.locfileid: "85074515"
---
# <a name="routing-in-aspnet-core"></a><span data-ttu-id="ff86d-103">ASP.NET Core yönlendirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-103">Routing in ASP.NET Core</span></span>

<span data-ttu-id="ff86d-104">[Ryan şimdi ak](https://github.com/rynowak), [Kirk Larkabağı](https://twitter.com/serpent5)ve [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="ff86d-104">By [Ryan Nowak](https://github.com/rynowak), [Kirk Larkin](https://twitter.com/serpent5), and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

::: moniker range=">= aspnetcore-3.0"

<span data-ttu-id="ff86d-105">Yönlendirme, gelen HTTP isteklerini eşleştirmekten ve bu istekleri uygulamanın yürütülebilir uç noktalarına gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-105">Routing is responsible for matching incoming HTTP requests and dispatching those requests to the app's executable endpoints.</span></span> <span data-ttu-id="ff86d-106">[Uç noktalar](#endpoint) , uygulamanın yürütülebilir istek işleme kodu birimleridir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-106">[Endpoints](#endpoint) are the app's units of executable request-handling code.</span></span> <span data-ttu-id="ff86d-107">Uç noktalar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-107">Endpoints are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="ff86d-108">Uç nokta eşleştirme işlemi, isteğin URL 'sindeki değerleri çıkarabilir ve bu değerleri istek işleme için sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-108">The endpoint matching process can extract values from the request's URL and provide those values for request processing.</span></span> <span data-ttu-id="ff86d-109">Uygulamanın uç nokta bilgilerini kullanarak, yönlendirme, uç noktalarıyla eşlenen URL 'Ler de oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-109">Using endpoint information from the app, routing is also able to generate URLs that map to endpoints.</span></span>

<span data-ttu-id="ff86d-110">Uygulamalar, kullanarak yönlendirmeyi yapılandırabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-110">Apps can configure routing using:</span></span>

- <span data-ttu-id="ff86d-111">Denetleyiciler</span><span class="sxs-lookup"><span data-stu-id="ff86d-111">Controllers</span></span>
- Razor<span data-ttu-id="ff86d-112">Sayfaları</span><span class="sxs-lookup"><span data-stu-id="ff86d-112"> Pages</span></span>
- SignalR
- <span data-ttu-id="ff86d-113">gRPC Hizmetleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-113">gRPC Services</span></span>
- <span data-ttu-id="ff86d-114">[Sistem durumu denetimleri](xref:host-and-deploy/health-checks)gibi uç nokta özellikli [Ara yazılım](xref:fundamentals/middleware/index) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-114">Endpoint-enabled [middleware](xref:fundamentals/middleware/index) such as [Health Checks](xref:host-and-deploy/health-checks).</span></span>
- <span data-ttu-id="ff86d-115">Yönlendirme ile kayıtlı temsilciler ve Lambdalar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-115">Delegates and lambdas registered with routing.</span></span>

<span data-ttu-id="ff86d-116">Bu belgede ASP.NET Core yönlendirmenin alt düzey ayrıntıları ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-116">This document covers low-level details of ASP.NET Core routing.</span></span> <span data-ttu-id="ff86d-117">Yönlendirmeyi yapılandırma hakkında bilgi için:</span><span class="sxs-lookup"><span data-stu-id="ff86d-117">For information on configuring routing:</span></span>

* <span data-ttu-id="ff86d-118">Denetleyiciler için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-118">For controllers, see <xref:mvc/controllers/routing>.</span></span>
* <span data-ttu-id="ff86d-119">RazorSayfa kuralları için bkz <xref:razor-pages/razor-pages-conventions> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-119">For Razor Pages conventions, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="ff86d-120">Bu belgede açıklanan uç nokta yönlendirme sistemi, ASP.NET Core 3,0 ve üzeri için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-120">The endpoint routing system described in this document applies to ASP.NET Core 3.0 and later.</span></span> <span data-ttu-id="ff86d-121">Önceki yönlendirme sistemiyle ilgili daha fazla bilgi için <xref:Microsoft.AspNetCore.Routing.IRouter> , aşağıdaki yaklaşımlardan birini kullanarak ASP.NET Core 2,1 sürümünü seçin:</span><span class="sxs-lookup"><span data-stu-id="ff86d-121">For information on the previous routing system based on <xref:Microsoft.AspNetCore.Routing.IRouter>, select the ASP.NET Core 2.1 version using one of the following approaches:</span></span>

* <span data-ttu-id="ff86d-122">Önceki sürümün sürüm Seçicisi.</span><span class="sxs-lookup"><span data-stu-id="ff86d-122">The version selector for a previous version.</span></span>
* <span data-ttu-id="ff86d-123">[ASP.NET Core 2,1 yönlendirme](https://docs.microsoft.com/aspnet/core/fundamentals/routing?view=aspnetcore-2.1)öğesini seçin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-123">Select [ASP.NET Core 2.1 routing](https://docs.microsoft.com/aspnet/core/fundamentals/routing?view=aspnetcore-2.1).</span></span>

<span data-ttu-id="ff86d-124">[Örnek kodu görüntüleme veya indirme](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples/3.x) ([nasıl indirileceği](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="ff86d-124">[View or download sample code](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples/3.x) ([how to download](xref:index#how-to-download-a-sample))</span></span>

<span data-ttu-id="ff86d-125">Bu belgenin karşıdan yükleme örnekleri belirli bir sınıf tarafından etkinleştirilir `Startup` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-125">The download samples for this document are enabled by a specific `Startup` class.</span></span> <span data-ttu-id="ff86d-126">Belirli bir örneği çalıştırmak için, *program.cs* öğesini istenen sınıfı çağırmak üzere değiştirin `Startup` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-126">To run a specific sample, modify *Program.cs* to call the desired `Startup` class.</span></span>

## <a name="routing-basics"></a><span data-ttu-id="ff86d-127">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-127">Routing basics</span></span>

<span data-ttu-id="ff86d-128">Tüm ASP.NET Core şablonları oluşturulan koda yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-128">All ASP.NET Core templates include routing in the generated code.</span></span> <span data-ttu-id="ff86d-129">Yönlendirme, içindeki [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine kaydedilir `Startup.Configure` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-129">Routing is registered in the [middleware](xref:fundamentals/middleware/index) pipeline in `Startup.Configure`.</span></span>

<span data-ttu-id="ff86d-130">Aşağıdaki kod, yönlendirmenin temel bir örneğini göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-130">The following code shows a basic example of routing:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Startup.cs?name=snippet&highlight=8,10)]

<span data-ttu-id="ff86d-131">Yönlendirme, ve tarafından kaydedilen bir dizi ara yazılımı <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> kullanır <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-131">Routing uses a pair of middleware, registered by <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> and <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>:</span></span>

* <span data-ttu-id="ff86d-132">`UseRouting`ara yazılım ardışık düzenine eşleşen rota ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-132">`UseRouting` adds route matching to the middleware pipeline.</span></span> <span data-ttu-id="ff86d-133">Bu ara yazılım, uygulamada tanımlanan uç nokta kümesine bakar ve isteğe bağlı olarak [en iyi eşleşmeyi](#urlm) seçer.</span><span class="sxs-lookup"><span data-stu-id="ff86d-133">This middleware looks at the set of endpoints defined in the app, and selects the [best match](#urlm) based on the request.</span></span>
* <span data-ttu-id="ff86d-134">`UseEndpoints`uç nokta yürütmeyi ara yazılım ardışık düzenine ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-134">`UseEndpoints` adds endpoint execution to the middleware pipeline.</span></span> <span data-ttu-id="ff86d-135">Seçili uç noktayla ilişkili temsilciyi çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-135">It runs the delegate associated with the selected endpoint.</span></span>

<span data-ttu-id="ff86d-136">Önceki örnekte, [Mapget](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*) yöntemi kullanılarak kod uç noktasına tek bir *yol* dahildir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-136">The preceding example includes a single *route to code* endpoint using the [MapGet](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*) method:</span></span>

* <span data-ttu-id="ff86d-137">`GET`Kök URL 'ye BIR http isteği gönderildiğinde `/` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-137">When an HTTP `GET` request is sent to the root URL `/`:</span></span>
  * <span data-ttu-id="ff86d-138">Gösterilen istek temsilcisi yürütülüyor.</span><span class="sxs-lookup"><span data-stu-id="ff86d-138">The request delegate shown executes.</span></span>
  * <span data-ttu-id="ff86d-139">`Hello World!`HTTP yanıtına yazılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-139">`Hello World!` is written to the HTTP response.</span></span> <span data-ttu-id="ff86d-140">Varsayılan olarak, kök URL 'sidir `/` `https://localhost:5001/` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-140">By default, the root URL `/` is `https://localhost:5001/`.</span></span>
* <span data-ttu-id="ff86d-141">İstek yöntemi yoksa `GET` veya kök URL 'si değilse `/` , hiçbir yol eşleşmesi ve bir HTTP 404 döndürülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-141">If the request method is not `GET` or the root URL is not `/`, no route matches and an HTTP 404 is returned.</span></span>

### <a name="endpoint"></a><span data-ttu-id="ff86d-142">Uç Nokta</span><span class="sxs-lookup"><span data-stu-id="ff86d-142">Endpoint</span></span>

<a name="endpoint"></a>

<span data-ttu-id="ff86d-143">`MapGet`Yöntemi, bir **uç noktayı**tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-143">The `MapGet` method is used to define an **endpoint**.</span></span> <span data-ttu-id="ff86d-144">Uç nokta şöyle olabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-144">An endpoint is something that can be:</span></span>

* <span data-ttu-id="ff86d-145">URL ve HTTP yöntemiyle eşleştirerek seçilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-145">Selected, by matching the URL and HTTP method.</span></span>
* <span data-ttu-id="ff86d-146">, Temsilcisi çalıştırılarak yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-146">Executed, by running the delegate.</span></span>

<span data-ttu-id="ff86d-147">Uygulama tarafından eşleştirilecek ve çalıştırılabilen uç noktalar ' de yapılandırılır `UseEndpoints` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-147">Endpoints that can be matched and executed by the app are configured in `UseEndpoints`.</span></span> <span data-ttu-id="ff86d-148">Örneğin,, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*> <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*> ve [benzer yöntemler](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions) , istek temsilcilerinizi yönlendirme sistemine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-148">For example, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*>, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*>, and [similar methods](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions) connect request delegates to the routing system.</span></span>
<span data-ttu-id="ff86d-149">ASP.NET Core Framework özelliklerini yönlendirme sistemine bağlamak için ek yöntemler kullanılabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-149">Additional methods can be used to connect ASP.NET Core framework features to the routing system:</span></span>
- <span data-ttu-id="ff86d-150">[Sayfalar için MapRazorPages Razor](xref:Microsoft.AspNetCore.Builder.RazorPagesEndpointRouteBuilderExtensions.MapRazorPages*)</span><span class="sxs-lookup"><span data-stu-id="ff86d-150">[MapRazorPages for Razor Pages](xref:Microsoft.AspNetCore.Builder.RazorPagesEndpointRouteBuilderExtensions.MapRazorPages*)</span></span>
- [<span data-ttu-id="ff86d-151">Denetleyiciler için MapControllers</span><span class="sxs-lookup"><span data-stu-id="ff86d-151">MapControllers for controllers</span></span>](xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllers*)
- <span data-ttu-id="ff86d-152">[MapHub \<THub>SignalR](xref:Microsoft.AspNetCore.SignalR.HubRouteBuilder.MapHub*)</span><span class="sxs-lookup"><span data-stu-id="ff86d-152">[MapHub\<THub> for SignalR](xref:Microsoft.AspNetCore.SignalR.HubRouteBuilder.MapHub*)</span></span> 
- [<span data-ttu-id="ff86d-153">GRPC için MapGrpcService \<TService></span><span class="sxs-lookup"><span data-stu-id="ff86d-153">MapGrpcService\<TService> for gRPC</span></span>](xref:grpc/aspnetcore)

<span data-ttu-id="ff86d-154">Aşağıdaki örnekte, daha karmaşık bir yol şablonuyla yönlendirme gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-154">The following example shows routing with a more sophisticated route template:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/RouteTemplateStartup.cs?name=snippet)]

<span data-ttu-id="ff86d-155">Dize `/hello/{name:alpha}` bir **yol şablonudur**.</span><span class="sxs-lookup"><span data-stu-id="ff86d-155">The string `/hello/{name:alpha}` is a **route template**.</span></span> <span data-ttu-id="ff86d-156">Uç noktanın nasıl eşleştirileceği yapılandırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-156">It is used to configure how the endpoint is matched.</span></span> <span data-ttu-id="ff86d-157">Bu durumda, şablon eşleşir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-157">In this case, the template matches:</span></span>

* <span data-ttu-id="ff86d-158">Şunun gibi bir URL`/hello/Ryan`</span><span class="sxs-lookup"><span data-stu-id="ff86d-158">A URL like `/hello/Ryan`</span></span>
* <span data-ttu-id="ff86d-159">`/hello/`Arkasından bir alfabetik karakter dizisi ile başlayan herhangi BIR URL yolu.</span><span class="sxs-lookup"><span data-stu-id="ff86d-159">Any URL path that begins with `/hello/` followed by a sequence of alphabetic characters.</span></span>  <span data-ttu-id="ff86d-160">`:alpha`yalnızca alfabetik karakterlerle eşleşen bir rota kısıtlaması uygular.</span><span class="sxs-lookup"><span data-stu-id="ff86d-160">`:alpha` applies a route constraint that matches only alphabetic characters.</span></span> <span data-ttu-id="ff86d-161">[Yol kısıtlamaları](#route-constraint-reference) bu belgenin ilerleyen kısımlarında açıklanmıştır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-161">[Route constraints](#route-constraint-reference) are explained later in this document.</span></span>

<span data-ttu-id="ff86d-162">URL yolunun ikinci segmenti `{name:alpha}` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-162">The second segment of the URL path, `{name:alpha}`:</span></span>

* <span data-ttu-id="ff86d-163">`name`Parametreye bağlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-163">Is bound to the `name` parameter.</span></span>
* <span data-ttu-id="ff86d-164">Yakalanır ve [HttpRequest. RouteValues](xref:Microsoft.AspNetCore.Http.HttpRequest.RouteValues*)'da depolanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-164">Is captured and stored in [HttpRequest.RouteValues](xref:Microsoft.AspNetCore.Http.HttpRequest.RouteValues*).</span></span>

<span data-ttu-id="ff86d-165">Bu belgede açıklanan uç nokta yönlendirme sistemi ASP.NET Core 3,0 itibariyle yenidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-165">The endpoint routing system described in this document is new as of ASP.NET Core 3.0.</span></span> <span data-ttu-id="ff86d-166">Ancak, ASP.NET Core tüm sürümleri aynı yol şablonu özellikleri ve yol kısıtlamaları kümesini destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-166">However, all versions of ASP.NET Core support the same set of route template features and route constraints.</span></span>

<span data-ttu-id="ff86d-167">Aşağıdaki örnek, [durum denetimleri](xref:host-and-deploy/health-checks) ve yetkilendirmeyle yönlendirmeyi gösterir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-167">The following example shows routing with [health checks](xref:host-and-deploy/health-checks) and authorization:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/AuthorizationStartup.cs?name=snippet)]

[!INCLUDE[request localized comments](~/includes/code-comments-loc.md)]

<span data-ttu-id="ff86d-168">Yukarıdaki örnekte nasıl yapılacağı gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-168">The preceding example demonstrates how:</span></span>

* <span data-ttu-id="ff86d-169">Yetkilendirme ara yazılımı yönlendirmeyle birlikte kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-169">The authorization middleware can be used with routing.</span></span>
* <span data-ttu-id="ff86d-170">Uç noktalar, yetkilendirme davranışını yapılandırmak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-170">Endpoints can be used to configure authorization behavior.</span></span>

<span data-ttu-id="ff86d-171"><xref:Microsoft.AspNetCore.Builder.HealthCheckEndpointRouteBuilderExtensions.MapHealthChecks*>Çağrı bir sistem durumu denetim uç noktası ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-171">The <xref:Microsoft.AspNetCore.Builder.HealthCheckEndpointRouteBuilderExtensions.MapHealthChecks*> call adds a health check endpoint.</span></span> <span data-ttu-id="ff86d-172"><xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*>Bu çağrıda zincir, bir yetkilendirme ilkesini uç noktaya iliştirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-172">Chaining <xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*> on to this call attaches an authorization policy to the endpoint.</span></span>

<span data-ttu-id="ff86d-173"><xref:Microsoft.AspNetCore.Builder.AuthAppBuilderExtensions.UseAuthentication*> <xref:Microsoft.AspNetCore.Builder.AuthorizationAppBuilderExtensions.UseAuthorization*> Kimlik doğrulama ve yetkilendirme ara yazılımını çağırarak ve ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-173">Calling <xref:Microsoft.AspNetCore.Builder.AuthAppBuilderExtensions.UseAuthentication*> and <xref:Microsoft.AspNetCore.Builder.AuthorizationAppBuilderExtensions.UseAuthorization*> adds the authentication and authorization middleware.</span></span> <span data-ttu-id="ff86d-174">Bu ara yazılım, ile arasında yerleştirilir <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> ve `UseEndpoints` şunları yapabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-174">These middleware are placed between <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> and `UseEndpoints` so that they can:</span></span>

* <span data-ttu-id="ff86d-175">Hangi uç noktanın seçili olduğunu görün `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-175">See which endpoint was selected by `UseRouting`.</span></span>
* <span data-ttu-id="ff86d-176">Uç noktaya göndermeden önce bir yetkilendirme ilkesi uygulayın <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-176">Apply an authorization policy before <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> dispatches to the endpoint.</span></span>

<a name="metadata"></a>

### <a name="endpoint-metadata"></a><span data-ttu-id="ff86d-177">Uç nokta meta verileri</span><span class="sxs-lookup"><span data-stu-id="ff86d-177">Endpoint metadata</span></span>

<span data-ttu-id="ff86d-178">Yukarıdaki örnekte, iki uç nokta bulunur, ancak yalnızca sistem durumu denetimi uç noktasına bağlı bir yetkilendirme ilkesi vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-178">In the preceding example, there are two endpoints, but only the health check endpoint has an authorization policy attached.</span></span> <span data-ttu-id="ff86d-179">İstek sistem durumu denetimi uç noktasıyla eşleşiyorsa, `/healthz` bir yetkilendirme denetimi gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-179">If the request matches the health check endpoint, `/healthz`, an authorization check is performed.</span></span> <span data-ttu-id="ff86d-180">Bu, uç noktalara eklenen ek verilere sahip olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-180">This demonstrates that endpoints can have extra data attached to them.</span></span> <span data-ttu-id="ff86d-181">Bu ek verilere uç nokta **meta verileri**denir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-181">This extra data is called endpoint **metadata**:</span></span>

* <span data-ttu-id="ff86d-182">Meta veriler, yönlendirme kullanan ara yazılım tarafından işlenebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-182">The metadata can be processed by routing-aware middleware.</span></span>
* <span data-ttu-id="ff86d-183">Meta veriler herhangi bir .NET türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-183">The metadata can be of any .NET type.</span></span>

## <a name="routing-concepts"></a><span data-ttu-id="ff86d-184">Yönlendirme kavramları</span><span class="sxs-lookup"><span data-stu-id="ff86d-184">Routing concepts</span></span>

<span data-ttu-id="ff86d-185">Yönlendirme sistemi güçlü **uç nokta** kavramı ekleyerek, ara yazılım ardışık düzeninin üst kısmında oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-185">The routing system builds on top of the middleware pipeline by adding the powerful **endpoint** concept.</span></span> <span data-ttu-id="ff86d-186">Uç noktalar, yönlendirme, yetkilendirme ve herhangi bir sayıda ASP.NET Core sistemi açısından birbirinden farklı olan uygulama işlevselliğinin birimlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-186">Endpoints represent units of the app's functionality that are distinct from each other in terms of routing, authorization, and any number of ASP.NET Core's systems.</span></span>

<a name="endpoint"></a>

### <a name="aspnet-core-endpoint-definition"></a><span data-ttu-id="ff86d-187">ASP.NET Core uç noktası tanımı</span><span class="sxs-lookup"><span data-stu-id="ff86d-187">ASP.NET Core endpoint definition</span></span>

<span data-ttu-id="ff86d-188">ASP.NET Core uç noktası:</span><span class="sxs-lookup"><span data-stu-id="ff86d-188">An ASP.NET Core endpoint is:</span></span>

* <span data-ttu-id="ff86d-189">Yürütülebilir dosya: öğesine sahip <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-189">Executable: Has a <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate>.</span></span>
* <span data-ttu-id="ff86d-190">Genişletilebilir: [meta veri](xref:Microsoft.AspNetCore.Http.Endpoint.Metadata*) koleksiyonu vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-190">Extensible: Has a [Metadata](xref:Microsoft.AspNetCore.Http.Endpoint.Metadata*) collection.</span></span>
* <span data-ttu-id="ff86d-191">Seçilebilir: Isteğe bağlı olarak [yönlendirme bilgileri](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern*)vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-191">Selectable: Optionally, has [routing information](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern*).</span></span>
* <span data-ttu-id="ff86d-192">Numaralandırılabilir: bitiş noktaları koleksiyonu, kimden kaynağından alma yoluyla listelenebilir <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> . [DI](xref:fundamentals/dependency-injection)</span><span class="sxs-lookup"><span data-stu-id="ff86d-192">Enumerable: The collection of endpoints can be listed by retrieving the <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> from [DI](xref:fundamentals/dependency-injection).</span></span>

<span data-ttu-id="ff86d-193">Aşağıdaki kod, geçerli istekle eşleşen uç noktanın nasıl alınacağını ve inceleneceği gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-193">The following code shows how to retrieve and inspect the endpoint matching the current request:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/EndpointInspectorStartup.cs?name=snippet)]

<span data-ttu-id="ff86d-194">Seçildiğinde, uç nokta öğesinden alınabilir `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-194">The endpoint, if selected, can be retrieved from the `HttpContext`.</span></span> <span data-ttu-id="ff86d-195">Özellikleri incelenebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-195">Its properties can be inspected.</span></span> <span data-ttu-id="ff86d-196">Uç nokta nesneleri sabittir ve oluşturulduktan sonra değiştirilemez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-196">Endpoint objects are immutable and cannot be modified after creation.</span></span> <span data-ttu-id="ff86d-197">Uç noktanın en yaygın türü bir <xref:Microsoft.AspNetCore.Routing.RouteEndpoint> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-197">The most common type of endpoint is a <xref:Microsoft.AspNetCore.Routing.RouteEndpoint>.</span></span> <span data-ttu-id="ff86d-198">`RouteEndpoint`, yönlendirme sistemi tarafından seçilme olanağı sağlayan bilgiler içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-198">`RouteEndpoint` includes information that allows it to be to selected by the routing system.</span></span>

<span data-ttu-id="ff86d-199">Yukarıdaki kod, [uygulama. Kullanım](xref:Microsoft.AspNetCore.Builder.UseExtensions.Use*) , çevrimiçi bir [Ara yazılım](xref:fundamentals/middleware/index)yapılandırır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-199">In the preceding code, [app.Use](xref:Microsoft.AspNetCore.Builder.UseExtensions.Use*) configures an in-line [middleware](xref:fundamentals/middleware/index).</span></span>

<a name="mt"></a>

<span data-ttu-id="ff86d-200">Aşağıdaki kod, işlem hattında nereye `app.Use` çağrdığına bağlı olarak bir uç nokta olmayabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-200">The following code shows that, depending on where `app.Use` is called in the pipeline, there may not be an endpoint:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/MiddlewareFlowStartup.cs?name=snippet)]

<span data-ttu-id="ff86d-201">Bu önceki örnek `Console.WriteLine` , bir uç noktanın seçili olup olmadığını gösteren deyimler ekliyor.</span><span class="sxs-lookup"><span data-stu-id="ff86d-201">This preceding sample adds `Console.WriteLine` statements that display whether or not an endpoint has been selected.</span></span> <span data-ttu-id="ff86d-202">Netlik açısından örnek, belirtilen uç noktaya bir görünen ad atar `/` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-202">For clarity, the sample assigns a display name to the provided `/` endpoint.</span></span>

<span data-ttu-id="ff86d-203">Bu kodu, ekran URL 'SI ile çalıştırma `/` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-203">Running this code with a URL of `/` displays:</span></span>

```txt
1. Endpoint: (null)
2. Endpoint: Hello
3. Endpoint: Hello
```

<span data-ttu-id="ff86d-204">Bu kodu başka bir URL ile çalıştırmak şunları görüntüler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-204">Running this code with any other URL displays:</span></span>

```txt
1. Endpoint: (null)
2. Endpoint: (null)
4. Endpoint: (null)
```

<span data-ttu-id="ff86d-205">Bu çıkış şunları gösterir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-205">This output demonstrates that:</span></span>

* <span data-ttu-id="ff86d-206">Bitiş noktası, çağrılmadan önce her zaman null olur `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-206">The endpoint is always null before `UseRouting` is called.</span></span>
* <span data-ttu-id="ff86d-207">Bir eşleşme bulunursa, uç nokta ve arasında boş değildir `UseRouting` <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-207">If a match is found, the endpoint is non-null between `UseRouting` and <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span>
* <span data-ttu-id="ff86d-208">`UseEndpoints`Bir eşleşme bulunduğunda ara yazılım **terminaldir** .</span><span class="sxs-lookup"><span data-stu-id="ff86d-208">The `UseEndpoints` middleware is **terminal** when a match is found.</span></span> <span data-ttu-id="ff86d-209">[Terminal ara yazılımı](#tm) bu belgede daha sonra tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-209">[Terminal middleware](#tm) is defined later in this document.</span></span>
* <span data-ttu-id="ff86d-210">`UseEndpoints`Yalnızca eşleşme bulunamadığında yürütme sonrasında ara yazılım.</span><span class="sxs-lookup"><span data-stu-id="ff86d-210">The middleware after `UseEndpoints` execute only when no match is found.</span></span>

<span data-ttu-id="ff86d-211">`UseRouting`Ara yazılım, uç noktayı geçerli bağlama eklemek Için [setendpoint](xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.SetEndpoint*) yöntemini kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-211">The `UseRouting` middleware uses the [SetEndpoint](xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.SetEndpoint*) method to attach the endpoint to the current context.</span></span> <span data-ttu-id="ff86d-212">`UseRouting`Ara yazılımı özel mantık ile değiştirmek ve uç noktaları kullanmanın avantajlarını almaya devam etmek mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-212">It's possible to replace the `UseRouting` middleware with custom logic and still get the benefits of using endpoints.</span></span> <span data-ttu-id="ff86d-213">Uç noktalar, ara yazılım gibi alt düzey bir temel 'tür ve yönlendirme uygulamasıyla birlikte aktarılmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-213">Endpoints are a low-level primitive like middleware, and aren't coupled to the routing implementation.</span></span> <span data-ttu-id="ff86d-214">Çoğu uygulamanın `UseRouting` özel mantık ile değiştirme gereksinimi yoktur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-214">Most apps don't need to replace `UseRouting` with custom logic.</span></span>

<span data-ttu-id="ff86d-215">`UseEndpoints`Ara yazılım, ara yazılım ile birlikte kullanılmak üzere tasarlanmıştır `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-215">The `UseEndpoints` middleware is designed to be used in tandem with the `UseRouting` middleware.</span></span> <span data-ttu-id="ff86d-216">Bir uç noktayı yürütmek için çekirdek mantık karmaşık değildir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-216">The core logic to execute an endpoint isn't complicated.</span></span> <span data-ttu-id="ff86d-217"><xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*>Uç noktayı almak için kullanın ve ardından <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate> özelliğini çağırın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-217">Use <xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*> to retrieve the endpoint, and then invoke its <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate> property.</span></span>

<span data-ttu-id="ff86d-218">Aşağıdaki kod, ara yazılımların yönlendirmeyi nasıl etkileyebileceğini veya tepki vermesini gösterir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-218">The following code demonstrates how middleware can influence or react to routing:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/IntegratedMiddlewareStartup.cs?name=snippet)]

<span data-ttu-id="ff86d-219">Yukarıdaki örnekte iki önemli kavram gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-219">The preceding example demonstrates two important concepts:</span></span>

* <span data-ttu-id="ff86d-220">Ara yazılım, yönlendirmenin çalıştığı verileri değiştirmeden önce çalışabilir `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-220">Middleware can run before `UseRouting` to modify the data that routing operates upon.</span></span>
    * <span data-ttu-id="ff86d-221">Genellikle, yönlendirme öncesinde görünen ara yazılım, veya gibi, isteğin bazı özelliğini değiştirir <xref:Microsoft.AspNetCore.Builder.RewriteBuilderExtensions.UseRewriter*> <xref:Microsoft.AspNetCore.Builder.HttpMethodOverrideExtensions.UseHttpMethodOverride*> <xref:Microsoft.AspNetCore.Builder.UsePathBaseExtensions.UsePathBase*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-221">Usually middleware that appears before routing modifies some property of the request, such as <xref:Microsoft.AspNetCore.Builder.RewriteBuilderExtensions.UseRewriter*>, <xref:Microsoft.AspNetCore.Builder.HttpMethodOverrideExtensions.UseHttpMethodOverride*>, or <xref:Microsoft.AspNetCore.Builder.UsePathBaseExtensions.UsePathBase*>.</span></span>
* <span data-ttu-id="ff86d-222">Ara yazılım, ve arasında çalıştırılabilir `UseRouting` ve <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> uç nokta yürütülmeden önce yönlendirmenin sonuçlarını işleyebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-222">Middleware can run between `UseRouting` and <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> to process the results of routing before the endpoint is executed.</span></span>
    * <span data-ttu-id="ff86d-223">Ve arasında çalışan ara `UseRouting` yazılımlar `UseEndpoints` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-223">Middleware that runs between `UseRouting` and `UseEndpoints`:</span></span>
      * <span data-ttu-id="ff86d-224">Genellikle uç noktaları anlamak için meta verileri inceler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-224">Usually inspects metadata to understand the endpoints.</span></span>
      * <span data-ttu-id="ff86d-225">, Ve tarafından yapıldığı gibi genellikle güvenlik kararları `UseAuthorization` verir `UseCors` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-225">Often makes security decisions, as done by `UseAuthorization` and `UseCors`.</span></span>
    * <span data-ttu-id="ff86d-226">Ara yazılım ve meta verilerin birleşimi, ilkelerin uç nokta başına yapılandırılmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-226">The combination of middleware and metadata allows configuring policies per-endpoint.</span></span>

<span data-ttu-id="ff86d-227">Yukarıdaki kodda, uç nokta başına ilkeleri destekleyen özel bir ara yazılım örneği gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-227">The preceding code shows an example of a custom middleware that supports per-endpoint policies.</span></span> <span data-ttu-id="ff86d-228">Ara yazılım, gizli verilere erişim *denetim günlüğünü* konsola yazar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-228">The middleware writes an *audit log* of access to sensitive data to the console.</span></span> <span data-ttu-id="ff86d-229">Ara yazılım, meta verileri olan bir uç noktayı *denetlemek* üzere yapılandırılabilir `AuditPolicyAttribute` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-229">The middleware can be configured to *audit* an endpoint with the `AuditPolicyAttribute` metadata.</span></span> <span data-ttu-id="ff86d-230">Bu örnek, yalnızca hassas olarak işaretlenen bitiş noktalarının denetlendiği bir *katılım* modelini gösterir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-230">This sample demonstrates an *opt-in* pattern where only endpoints that are marked as sensitive are audited.</span></span> <span data-ttu-id="ff86d-231">Örneğin, güvenli olarak işaretlenmemiş her şeyi denetlemek için bu mantığı ters olarak tanımlamak mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-231">It's possible to define this logic in reverse, auditing everything that isn't marked as safe, for example.</span></span> <span data-ttu-id="ff86d-232">Uç nokta meta veri sistemi esnektir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-232">The endpoint metadata system is flexible.</span></span> <span data-ttu-id="ff86d-233">Bu mantık, kullanım örneğine uygun herhangi bir şekilde tasarlanabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-233">This logic could be designed in whatever way suits the use case.</span></span>

<span data-ttu-id="ff86d-234">Önceki örnek kod, uç noktaların temel kavramlarını göstermek için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-234">The preceding sample code is intended to demonstrate the basic concepts of endpoints.</span></span> <span data-ttu-id="ff86d-235">**Örnek, üretim kullanımı için tasarlanmamıştır**.</span><span class="sxs-lookup"><span data-stu-id="ff86d-235">**The sample is not intended for production use**.</span></span> <span data-ttu-id="ff86d-236">*Denetim günlüğü* ara yazılımı 'nın daha kapsamlı bir sürümü şöyle olacaktır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-236">A more complete version of an *audit log* middleware would:</span></span>

* <span data-ttu-id="ff86d-237">Bir dosya veya veritabanında oturum açın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-237">Log to a file or database.</span></span>
* <span data-ttu-id="ff86d-238">Kullanıcı, IP adresi, hassas bitiş noktasının adı ve daha fazlası gibi ayrıntıları dahil edin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-238">Include details such as the user, IP address, name of the sensitive endpoint, and more.</span></span>

<span data-ttu-id="ff86d-239">Denetim ilkesi meta verileri, `AuditPolicyAttribute` `Attribute` denetleyiciler ve gibi sınıf tabanlı çerçeveler ile daha kolay kullanılmak üzere tanımlanır SignalR .</span><span class="sxs-lookup"><span data-stu-id="ff86d-239">The audit policy metadata `AuditPolicyAttribute` is defined as an `Attribute` for easier use with class-based frameworks such as controllers and SignalR.</span></span> <span data-ttu-id="ff86d-240">*Kod yolu*kullanırken:</span><span class="sxs-lookup"><span data-stu-id="ff86d-240">When using *route to code*:</span></span>

* <span data-ttu-id="ff86d-241">Meta veriler bir Oluşturucu API 'siyle birlikte eklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-241">Metadata is attached with a builder API.</span></span>
* <span data-ttu-id="ff86d-242">Sınıf tabanlı çerçeveler, uç noktalar oluştururken karşılık gelen Yöntem ve sınıftaki tüm öznitelikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-242">Class-based frameworks include all attributes on the corresponding method and class when creating endpoints.</span></span>

<span data-ttu-id="ff86d-243">Meta veri türleri için en iyi uygulamalar, bunları arabirimler ya da öznitelikler olarak tanımlamaktır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-243">The best practices for metadata types are to define them either as interfaces or attributes.</span></span> <span data-ttu-id="ff86d-244">Arabirimler ve öznitelikler kod yeniden kullanılmasına izin verir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-244">Interfaces and attributes allow code reuse.</span></span> <span data-ttu-id="ff86d-245">Meta veri sistemi esnektir ve herhangi bir kısıtlama vermez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-245">The metadata system is flexible and doesn't impose any limitations.</span></span>

<a name="tm"></a>

### <a name="comparing-a-terminal-middleware-and-routing"></a><span data-ttu-id="ff86d-246">Terminal ara yazılımını ve yönlendirmeyi karşılaştırma</span><span class="sxs-lookup"><span data-stu-id="ff86d-246">Comparing a terminal middleware and routing</span></span>

<span data-ttu-id="ff86d-247">Aşağıdaki kod örneği, yönlendirmeyi kullanarak ara yazılım kullanarak karşıtlıkları:</span><span class="sxs-lookup"><span data-stu-id="ff86d-247">The following code sample contrasts using middleware with using routing:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/TerminalMiddlewareStartup.cs?name=snippet)]

<span data-ttu-id="ff86d-248">İle gösterilen ara yazılım stili, `Approach 1:` **Terminal ara yazılımı**.</span><span class="sxs-lookup"><span data-stu-id="ff86d-248">The style of middleware shown with `Approach 1:` is **terminal middleware**.</span></span> <span data-ttu-id="ff86d-249">Bu, eşleşen bir işlem yaptığı için Terminal ara yazılımı olarak adlandırılır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-249">It's called terminal middleware because it does a matching operation:</span></span>

* <span data-ttu-id="ff86d-250">Yukarıdaki örnekteki eşleştirme işlemi, `Path == "/"` Ara yazılım ve `Path == "/Movie"` yönlendirme içindir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-250">The matching operation in the preceding sample is `Path == "/"` for the middleware and `Path == "/Movie"` for routing.</span></span>
* <span data-ttu-id="ff86d-251">Bir eşleşme başarılı olduğunda, ara yazılımı çağırmak yerine bazı işlevleri yürütür ve döndürür `next` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-251">When a match is successful, it executes some functionality and returns, rather than invoking the `next` middleware.</span></span>

<span data-ttu-id="ff86d-252">Bu, aramayı sonlandırdığı, bazı işlevleri yürüttüğünde ve sonra döndürdüğünden, Terminal ara yazılımı olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-252">It's called terminal middleware because it terminates the search, executes some functionality, and then returns.</span></span>

<span data-ttu-id="ff86d-253">Terminal ara yazılımını ve yönlendirmeyi karşılaştırma:</span><span class="sxs-lookup"><span data-stu-id="ff86d-253">Comparing a terminal middleware and routing:</span></span>
* <span data-ttu-id="ff86d-254">Her iki yaklaşım da işleme ardışık düzeninin sonlandırılmasını sağlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-254">Both approaches allow terminating the processing pipeline:</span></span>
    * <span data-ttu-id="ff86d-255">Ara yazılım, komut zinciri çağırmak yerine dönerek işlem hattını sonlandırır `next` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-255">Middleware terminates the pipeline by returning rather than invoking `next`.</span></span>
    * <span data-ttu-id="ff86d-256">Uç noktalar her zaman terminaldir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-256">Endpoints are always terminal.</span></span>
* <span data-ttu-id="ff86d-257">Terminal ara yazılımı, işlem hattındaki ara yazılımı rastgele bir yerde konumlandırmayı sağlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-257">Terminal middleware allows positioning the middleware at an arbitrary place in the pipeline:</span></span>
    * <span data-ttu-id="ff86d-258">Uç noktalar, konumunda yürütülür <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-258">Endpoints execute at the position of <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span>
* <span data-ttu-id="ff86d-259">Terminal ara yazılımı, rastgele kodun, ara yazılımın ne zaman eşleştiğini belirlemesine izin verir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-259">Terminal middleware allows arbitrary code to determine when the middleware matches:</span></span>
    * <span data-ttu-id="ff86d-260">Özel yol eşleştirme kodu ayrıntılı ve doğru yazılması zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-260">Custom route matching code can be verbose and difficult to write correctly.</span></span>
    * <span data-ttu-id="ff86d-261">Yönlendirme, tipik uygulamalar için doğrudan çözümler sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-261">Routing provides straightforward solutions for typical apps.</span></span> <span data-ttu-id="ff86d-262">Çoğu uygulama özel yol eşleştirme kodu gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-262">Most apps don't require custom route matching code.</span></span>
* <span data-ttu-id="ff86d-263">Ve gibi ara yazılım içeren uç noktalar arabirimi `UseAuthorization` `UseCors` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-263">Endpoints interface with middleware such as `UseAuthorization` and `UseCors`.</span></span>
    * <span data-ttu-id="ff86d-264">Veya ile Terminal ara yazılımı `UseAuthorization` kullanmak `UseCors` , yetkilendirme sistemiyle el ile arabirim oluşturma gerektirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-264">Using a terminal middleware with `UseAuthorization` or `UseCors` requires manual interfacing with the authorization system.</span></span>

<span data-ttu-id="ff86d-265">Bir [uç nokta](#endpoint) her ikisini de tanımlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-265">An [endpoint](#endpoint) defines both:</span></span>

* <span data-ttu-id="ff86d-266">İstekleri işlemek için bir temsilci.</span><span class="sxs-lookup"><span data-stu-id="ff86d-266">A delegate to process requests.</span></span>
* <span data-ttu-id="ff86d-267">Rastgele meta veri koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="ff86d-267">A collection of arbitrary metadata.</span></span> <span data-ttu-id="ff86d-268">Meta veriler, her uç noktaya eklenen ilkelere ve yapılandırmaya göre çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-268">The metadata is used to implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span>

<span data-ttu-id="ff86d-269">Terminal ara yazılımı etkin bir araç olabilir, ancak şunları gerektirebilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-269">Terminal middleware can be an effective tool, but can require:</span></span>

* <span data-ttu-id="ff86d-270">Önemli miktarda kodlama ve test.</span><span class="sxs-lookup"><span data-stu-id="ff86d-270">A significant amount of coding and testing.</span></span>
* <span data-ttu-id="ff86d-271">İstenen esneklik düzeyine ulaşmak için diğer sistemlerle el ile tümleştirme.</span><span class="sxs-lookup"><span data-stu-id="ff86d-271">Manual integration with other systems to achieve the desired level of flexibility.</span></span>

<span data-ttu-id="ff86d-272">Terminal ara yazılımı yazmadan önce yönlendirme ile tümleştirmeyi düşünün.</span><span class="sxs-lookup"><span data-stu-id="ff86d-272">Consider integrating with routing before writing a terminal middleware.</span></span>

<span data-ttu-id="ff86d-273">[Eşleme](xref:fundamentals/middleware/index#branch-the-middleware-pipeline) ile tümleştirilen mevcut Terminal ara yazılımı <xref:Microsoft.AspNetCore.Builder.MapWhenExtensions.MapWhen*> , genellikle bir yönlendirme duyarlı uç noktaya açılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-273">Existing terminal middleware that integrates with [Map](xref:fundamentals/middleware/index#branch-the-middleware-pipeline) or <xref:Microsoft.AspNetCore.Builder.MapWhenExtensions.MapWhen*> can usually be turned into a routing aware endpoint.</span></span> <span data-ttu-id="ff86d-274">[Maphealthdenetimleri](https://github.com/aspnet/AspNetCore/blob/master/src/Middleware/HealthChecks/src/Builder/HealthCheckEndpointRouteBuilderExtensions.cs#L16) , yönlendirici-Ware için bir model gösterir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-274">[MapHealthChecks](https://github.com/aspnet/AspNetCore/blob/master/src/Middleware/HealthChecks/src/Builder/HealthCheckEndpointRouteBuilderExtensions.cs#L16) demonstrates the pattern for router-ware:</span></span>
* <span data-ttu-id="ff86d-275">Üzerine bir genişletme yöntemi yazın <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-275">Write an extension method on <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder>.</span></span>
* <span data-ttu-id="ff86d-276">Kullanarak iç içe geçmiş bir ara yazılım işlem hattı oluşturun <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder.CreateApplicationBuilder*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-276">Create a nested middleware pipeline using <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder.CreateApplicationBuilder*>.</span></span>
* <span data-ttu-id="ff86d-277">Yeni işlem hattına ara yazılım ekleyin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-277">Attach the middleware to the new pipeline.</span></span> <span data-ttu-id="ff86d-278">Bu durumda, <xref:Microsoft.AspNetCore.Builder.HealthCheckApplicationBuilderExtensions.UseHealthChecks*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-278">In this case, <xref:Microsoft.AspNetCore.Builder.HealthCheckApplicationBuilderExtensions.UseHealthChecks*>.</span></span>
* <span data-ttu-id="ff86d-279"><xref:Microsoft.AspNetCore.Builder.IApplicationBuilder.Build*>içinde bir ara yazılım ardışık düzeni <xref:Microsoft.AspNetCore.Http.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-279"><xref:Microsoft.AspNetCore.Builder.IApplicationBuilder.Build*> the middleware pipeline into a <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span>
* <span data-ttu-id="ff86d-280">`Map`' İ çağırın ve yeni ara yazılım ardışık düzenini sağlayın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-280">Call `Map` and provide the new middleware pipeline.</span></span>
* <span data-ttu-id="ff86d-281">Uzantı yönteminden tarafından sunulan Oluşturucu nesnesini döndürün `Map` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-281">Return the builder object provided by `Map` from the extension method.</span></span>

<span data-ttu-id="ff86d-282">Aşağıdaki kod, [Maphealthdenetimlerinin](xref:host-and-deploy/health-checks)kullanımını gösterir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-282">The following code shows use of [MapHealthChecks](xref:host-and-deploy/health-checks):</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/AuthorizationStartup.cs?name=snippet)]

<span data-ttu-id="ff86d-283">Yukarıdaki örnek, Oluşturucu nesnesinin neden önemli olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-283">The preceding sample shows why returning the builder object is important.</span></span> <span data-ttu-id="ff86d-284">Oluşturucu nesnesini döndürmek, uygulama geliştiricisinin uç nokta için yetkilendirme gibi ilkeleri yapılandırmasına izin verir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-284">Returning the builder object allows the app developer to configure policies such as authorization for the endpoint.</span></span> <span data-ttu-id="ff86d-285">Bu örnekte, sistem durumu denetimleri ara yazılımı yetkilendirme sistemiyle doğrudan tümleştirme gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-285">In this example, the health checks middleware has no direct integration with the authorization system.</span></span>

<span data-ttu-id="ff86d-286">Meta veri sistemi, Terminal ara yazılımı kullanan genişletilebilirlik yazarları tarafından karşılaşılan sorunlara yanıt olarak oluşturulmuştur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-286">The metadata system was created in response to the problems encountered by extensibility authors using terminal middleware.</span></span> <span data-ttu-id="ff86d-287">Her bir ara yazılım, yetkilendirme sistemiyle kendi tümleştirmesini uygulamak için sorunlu olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-287">It's problematic for each middleware to implement its own integration with the authorization system.</span></span>

<a name="urlm"></a>

### <a name="url-matching"></a><span data-ttu-id="ff86d-288">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-288">URL matching</span></span>

* <span data-ttu-id="ff86d-289">, Yönlendirmenin bir [uç noktaya](#endpoint)gelen istekle eşleştiği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-289">Is the process by which routing matches an incoming request to an [endpoint](#endpoint).</span></span>
* <span data-ttu-id="ff86d-290">URL yolu ve üst bilgilerindeki verileri temel alır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-290">Is based on data in the URL path and headers.</span></span>
* <span data-ttu-id="ff86d-291">İstekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-291">Can be extended to consider any data in the request.</span></span>

<span data-ttu-id="ff86d-292">Bir yönlendirme ara yazılımı yürütüldüğünde, `Endpoint` geçerli istekten ' de bir [istek özelliğine](xref:fundamentals/request-features) bir ve rota değeri ayarlar <xref:Microsoft.AspNetCore.Http.HttpContext> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-292">When a routing middleware executes, it sets an `Endpoint` and route values to a [request feature](xref:fundamentals/request-features) on the <xref:Microsoft.AspNetCore.Http.HttpContext> from the current request:</span></span>

* <span data-ttu-id="ff86d-293">[HttpContext. GetEndpoint](<xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*>) çağrısı uç noktasını alır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-293">Calling [HttpContext.GetEndpoint](<xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*>) gets the endpoint.</span></span>
* <span data-ttu-id="ff86d-294">`HttpRequest.RouteValues`yol değerlerinin koleksiyonunu alır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-294">`HttpRequest.RouteValues` gets the collection of route values.</span></span>

<span data-ttu-id="ff86d-295">Yönlendirme ara yazılımı [, çalışan ara yazılım uç](xref:fundamentals/middleware/index) noktasını inceleyebilir ve işlem yapabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-295">[Middleware](xref:fundamentals/middleware/index) running after the routing middleware can inspect the endpoint and take action.</span></span> <span data-ttu-id="ff86d-296">Örneğin, bir yetkilendirme ara yazılımı, bir yetkilendirme ilkesi için bitiş noktasının meta veri koleksiyonunu sorgulanamıyor.</span><span class="sxs-lookup"><span data-stu-id="ff86d-296">For example, an authorization middleware can interrogate the endpoint's metadata collection for an authorization policy.</span></span> <span data-ttu-id="ff86d-297">İstek işleme ardışık düzeninde bulunan tüm ara yazılım yürütüldükten sonra, seçilen uç noktanın temsilcisi çağrılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-297">After all of the middleware in the request processing pipeline is executed, the selected endpoint's delegate is invoked.</span></span>

<span data-ttu-id="ff86d-298">Uç nokta yönlendirmesinde yönlendirme sistemi tüm gönderme kararlarından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-298">The routing system in endpoint routing is responsible for all dispatching decisions.</span></span> <span data-ttu-id="ff86d-299">Ara yazılım, seçili uç noktaya göre ilkeler uyguladığı için şu önemlidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-299">Because the middleware applies policies based on the selected endpoint, it's important that:</span></span>

* <span data-ttu-id="ff86d-300">Güvenlik ilkelerinin dağıtımını etkileyebilecek herhangi bir karar, yönlendirme sisteminin içinde yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-300">Any decision that can affect dispatching or the application of security policies is made inside the routing system.</span></span>

> [!WARNING]
> <span data-ttu-id="ff86d-301">Geriye dönük uyumluluk için, bir denetleyici veya Razor sayfa uç noktası temsilcisi yürütüldüğünde, [routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özellikleri, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-301">For backwards-compatibility, when a Controller or Razor Pages endpoint delegate is executed, the properties of [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) are set to appropriate values based on the request processing performed thus far.</span></span>
>
> <span data-ttu-id="ff86d-302">`RouteContext`Daha sonraki bir sürümde tür kullanılmıyor olarak işaretlenir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-302">The `RouteContext` type will be marked obsolete in a future release:</span></span>
>
> * <span data-ttu-id="ff86d-303">`RouteData.Values`Öğesine geçirin `HttpRequest.RouteValues` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-303">Migrate `RouteData.Values` to `HttpRequest.RouteValues`.</span></span>
> * <span data-ttu-id="ff86d-304">`RouteData.DataTokens`Uç nokta meta verilerinden [ıdatatokensmetadata](xref:Microsoft.AspNetCore.Routing.IDataTokensMetadata) almak için geçiş yapın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-304">Migrate `RouteData.DataTokens` to retrieve [IDataTokensMetadata](xref:Microsoft.AspNetCore.Routing.IDataTokensMetadata) from the endpoint metadata.</span></span>

<span data-ttu-id="ff86d-305">URL eşleştirme, yapılandırılabilir bir aşamalar kümesinde çalışır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-305">URL matching operates in a configurable set of phases.</span></span> <span data-ttu-id="ff86d-306">Her aşamada, çıktı bir eşleşme kümesidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-306">In each phase, the output is a set of matches.</span></span> <span data-ttu-id="ff86d-307">Eşleşmeler kümesi bir sonraki aşamadan sonra daha dar olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-307">The set of matches can be narrowed down further by the next phase.</span></span> <span data-ttu-id="ff86d-308">Yönlendirme uygulama, eşleşen uç noktalar için bir işlem sırası garantisi vermez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-308">The routing implementation does not guarantee a processing order for matching endpoints.</span></span> <span data-ttu-id="ff86d-309">**Tüm** olası eşleşmeler aynı anda işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-309">**All** possible matches are processed at once.</span></span> <span data-ttu-id="ff86d-310">URL ile eşleşen aşamalar aşağıdaki sırada oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-310">The URL matching phases occur in the following order.</span></span> <span data-ttu-id="ff86d-311">ASP.NET Core:</span><span class="sxs-lookup"><span data-stu-id="ff86d-311">ASP.NET Core:</span></span>

1. <span data-ttu-id="ff86d-312">URL yolunu uç noktalar kümesine ve bunların yol şablonlarına göre işler ve **Tüm** eşleşmeleri toplar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-312">Processes the URL path against the set of endpoints and their route templates, collecting **all** of the matches.</span></span>
1. <span data-ttu-id="ff86d-313">Önceki listeyi alır ve yol kısıtlamalarında başarısız olan eşleşmeleri kaldırır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-313">Takes the preceding list and removes matches that fail with route constraints applied.</span></span>
1. <span data-ttu-id="ff86d-314">Önceki listeyi alır ve [Matcherpolicy](xref:Microsoft.AspNetCore.Routing.MatcherPolicy) örnekleri kümesi başarısız olan eşleşmeleri kaldırır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-314">Takes the preceding list and removes matches that fail the set of [MatcherPolicy](xref:Microsoft.AspNetCore.Routing.MatcherPolicy) instances.</span></span>
1. <span data-ttu-id="ff86d-315">Önceki listeden son bir karar vermek için [Endpointselector](xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector) kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-315">Uses the [EndpointSelector](xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector) to make a final decision from the preceding list.</span></span>

<span data-ttu-id="ff86d-316">Uç noktaların listesi şunlara göre önceliklendirilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-316">The list of endpoints is prioritized according to:</span></span>

* <span data-ttu-id="ff86d-317">[Routeendpoint. Order](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order*)</span><span class="sxs-lookup"><span data-stu-id="ff86d-317">The [RouteEndpoint.Order](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order*)</span></span>
* <span data-ttu-id="ff86d-318">[Yol şablonu önceliği](#rtp)</span><span class="sxs-lookup"><span data-stu-id="ff86d-318">The [route template precedence](#rtp)</span></span>

<span data-ttu-id="ff86d-319">Tüm eşleşen uç noktalar, sonuna kadar her aşamada işlenir <xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-319">All matching endpoints are processed in each phase until the <xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector> is reached.</span></span> <span data-ttu-id="ff86d-320">`EndpointSelector`Son aşamadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-320">The `EndpointSelector` is the final phase.</span></span> <span data-ttu-id="ff86d-321">En iyi eşleşme ile eşleştirmelerle en yüksek öncelikli uç noktayı seçer.</span><span class="sxs-lookup"><span data-stu-id="ff86d-321">It chooses the highest priority endpoint from the matches as the best match.</span></span> <span data-ttu-id="ff86d-322">En iyi eşleşme ile aynı önceliğe sahip başka eşleşmeler varsa, belirsiz eşleşme özel durumu oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-322">If there are other matches with the same priority as the best match, an ambiguous match exception is thrown.</span></span>

<span data-ttu-id="ff86d-323">Yol önceliği, daha yüksek öncelikli olarak verilen **daha belirli** bir yol şablonuna göre hesaplanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-323">The route precedence is computed based on a **more specific** route template being given a higher priority.</span></span> <span data-ttu-id="ff86d-324">Örneğin, şablonları ve şunları göz önünde bulundurun `/hello` `/{message}` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-324">For example, consider the templates `/hello` and `/{message}`:</span></span>

* <span data-ttu-id="ff86d-325">Her ikisi de URL yoluyla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-325">Both match the URL path `/hello`.</span></span>
* <span data-ttu-id="ff86d-326">`/hello`daha özeldir ve bu nedenle daha yüksek önceliğe sahiptir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-326">`/hello`  is more specific and therefore higher priority.</span></span>

<span data-ttu-id="ff86d-327">Genel olarak, rota önceliği, uygulamada kullanılan URL şemaları türleri için en iyi eşleşmeyi seçme konusunda iyi bir iş olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-327">In general, route precedence does a good job of choosing the best match for the kinds of URL schemes used in practice.</span></span> <span data-ttu-id="ff86d-328"><xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order>Belirsizliği önlemek için yalnızca gerekli olduğunda kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-328">Use <xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order> only when necessary to avoid an ambiguity.</span></span>

<span data-ttu-id="ff86d-329">Yönlendirme tarafından sunulan genişletilebilirlik türleri nedeniyle, yönlendirme sisteminin belirsiz yolların önünde işlem yapmak mümkün değildir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-329">Due to the kinds of extensibility provided by routing, it isn't possible for the routing system to compute ahead of time the ambiguous routes.</span></span> <span data-ttu-id="ff86d-330">Yol şablonları gibi bir örnek düşünün `/{message:alpha}` `/{message:int}` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-330">Consider an example such as the route templates `/{message:alpha}` and `/{message:int}`:</span></span>

* <span data-ttu-id="ff86d-331">`alpha`Kısıtlama yalnızca alfabetik karakterlerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-331">The `alpha` constraint matches only alphabetic characters.</span></span>
* <span data-ttu-id="ff86d-332">`int`Kısıtlama yalnızca sayılarla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-332">The `int` constraint matches only numbers.</span></span>
* <span data-ttu-id="ff86d-333">Bu şablonlar aynı rota önceliğine sahiptir, ancak her iki eşleşen tek URL yoktur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-333">These templates have the same route precedence, but there's no single URL they both match.</span></span>
* <span data-ttu-id="ff86d-334">Yönlendirme sistemi başlangıçta bir belirsizlik hatası bildirse, bu geçerli kullanım durumunu engeller.</span><span class="sxs-lookup"><span data-stu-id="ff86d-334">If the routing system reported an ambiguity error at startup, it would block this valid use case.</span></span>

> [!WARNING]
>
> <span data-ttu-id="ff86d-335">İçindeki işlemlerin sırası <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> , yönlendirme davranışını tek bir özel durumla etkilemez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-335">The order of operations inside <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> doesn't influence the behavior of routing, with one exception.</span></span> <span data-ttu-id="ff86d-336"><xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*>ve, <xref:Microsoft.AspNetCore.Builder.MvcAreaRouteBuilderExtensions.MapAreaRoute*> çağrıldığı sıraya göre uç noktalarına otomatik olarak bir sipariş değeri atar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-336"><xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*> and <xref:Microsoft.AspNetCore.Builder.MvcAreaRouteBuilderExtensions.MapAreaRoute*> automatically assign an order value to their endpoints based on the order they are invoked.</span></span> <span data-ttu-id="ff86d-337">Bu, yönlendirme sistemi olmadan, eski yönlendirme uygulamalarıyla aynı garantilere sahip olmayan denetleyicilerin uzun süreli davranışlarına benzetir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-337">This simulates long-time behavior of controllers without the routing system providing the same guarantees as older routing implementations.</span></span>
>
> <span data-ttu-id="ff86d-338">Yönlendirmenin eski uygulamasında, yolların işlendiği sıraya bağımlılığı olan yönlendirme genişletilebilirliği uygulamak mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-338">In the legacy implementation of routing, it's possible to implement routing extensibility that has a dependency on the order in which routes are processed.</span></span> <span data-ttu-id="ff86d-339">ASP.NET Core 3,0 ve üzeri için uç nokta yönlendirme:</span><span class="sxs-lookup"><span data-stu-id="ff86d-339">Endpoint routing in ASP.NET Core 3.0 and later:</span></span>
> 
> * <span data-ttu-id="ff86d-340">Bir yol kavramı yoktur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-340">Doesn't have a concept of routes.</span></span>
> * <span data-ttu-id="ff86d-341">Sıralama garantisi sağlamaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-341">Doesn't provide ordering guarantees.</span></span> <span data-ttu-id="ff86d-342">Tüm uç noktalar aynı anda işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-342">All endpoints are processed at once.</span></span>
>
> <span data-ttu-id="ff86d-343">Bu, eski yönlendirme sistemini kullanarak çıkdıysanız, [Yardım için bir GitHub sorunu açın](https://github.com/dotnet/aspnetcore/issues).</span><span class="sxs-lookup"><span data-stu-id="ff86d-343">If this means you're stuck using the legacy routing system, [open a GitHub issue for assistance](https://github.com/dotnet/aspnetcore/issues).</span></span>

<a name="rtp"></a>

### <a name="route-template-precedence-and-endpoint-selection-order"></a><span data-ttu-id="ff86d-344">Yol şablonu önceliği ve uç nokta seçim sırası</span><span class="sxs-lookup"><span data-stu-id="ff86d-344">Route template precedence and endpoint selection order</span></span>

<span data-ttu-id="ff86d-345">[Yol şablonu önceliği](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L16) , her yol şablonuna, ne kadar belirli bir değere göre bir değer atayan bir sistemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-345">[Route template precedence](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L16) is a system that assigns each route template a value based on how specific it is.</span></span> <span data-ttu-id="ff86d-346">Yol şablonu önceliği:</span><span class="sxs-lookup"><span data-stu-id="ff86d-346">Route template precedence:</span></span>

* <span data-ttu-id="ff86d-347">Ortak durumlarda uç noktaların sırasını ayarlama ihtiyacını önler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-347">Avoids the need to adjust the order of endpoints in common cases.</span></span>
* <span data-ttu-id="ff86d-348">Yönlendirme davranışının ortak Sense beklentilerini eşleştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-348">Attempts to match the common-sense expectations of routing behavior.</span></span>

<span data-ttu-id="ff86d-349">Örneğin, şablonları ve öğesini `/Products/List` düşünün `/Products/{id}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-349">For example, consider templates `/Products/List` and `/Products/{id}`.</span></span> <span data-ttu-id="ff86d-350">`/Products/List`URL yolundan daha iyi bir eşleşme olduğunu varsaymak mantıklı olacaktır `/Products/{id}` `/Products/List` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-350">It would be reasonable to assume that `/Products/List` is a better match than `/Products/{id}` for the URL path `/Products/List`.</span></span> <span data-ttu-id="ff86d-351">, Değişmez değer segmentinin `/List` parametre segmentinden daha iyi önceliğe sahip olduğu kabul edildiği için geçerlidir `/{id}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-351">The works because the literal segment `/List` is considered to have better precedence than the parameter segment `/{id}`.</span></span>

<span data-ttu-id="ff86d-352">Önceliğin nasıl kullanılacağına ilişkin ayrıntılar, yönlendirme şablonlarının nasıl tanımlandığınıza bağlıdır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-352">The details of how precedence works are coupled to how route templates are defined:</span></span>

* <span data-ttu-id="ff86d-353">Daha fazla kesimli şablonlar daha belirgin olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-353">Templates with more segments are considered more specific.</span></span>
* <span data-ttu-id="ff86d-354">Değişmez değerli bir kesim, bir parametre segmentinden daha belirgin olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-354">A segment with literal text is considered more specific than a parameter segment.</span></span>
* <span data-ttu-id="ff86d-355">Kısıtlaması olan bir parametre segmenti, olmadan birden fazla olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-355">A parameter segment with a constraint is considered more specific than one without.</span></span>
* <span data-ttu-id="ff86d-356">Karmaşık bir kesim, kısıtlama içeren bir parametre segmenti olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-356">A complex segment is considered as specific as a parameter segment with a constraint.</span></span>
* <span data-ttu-id="ff86d-357">Catch-all parametreleri en az özgüdür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-357">Catch-all parameters are the least specific.</span></span> <span data-ttu-id="ff86d-358">Catch **-All** rotaları hakkında önemli bilgiler için bkz. [route Template başvurusu](#rtr) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-358">See **catch-all** in the [Route template reference](#rtr) for important information on catch-all routes.</span></span>

<span data-ttu-id="ff86d-359">Tam değerler başvurusu için [GitHub 'da kaynak koda](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L189) bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-359">See the [source code on GitHub](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L189) for a reference of exact values.</span></span>

<a name="lg"></a>

### <a name="url-generation-concepts"></a><span data-ttu-id="ff86d-360">URL oluşturma kavramları</span><span class="sxs-lookup"><span data-stu-id="ff86d-360">URL generation concepts</span></span>

<span data-ttu-id="ff86d-361">URL oluşturma:</span><span class="sxs-lookup"><span data-stu-id="ff86d-361">URL generation:</span></span>

* <span data-ttu-id="ff86d-362">, Yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturmalarına yönelik işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-362">Is the process by which routing can create a URL path based on a set of route values.</span></span>
* <span data-ttu-id="ff86d-363">Uç noktalar ve bunlara erişen URL 'Ler arasında bir mantıksal ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-363">Allows for a logical separation between endpoints and the URLs that access them.</span></span>

<span data-ttu-id="ff86d-364">Endpoint Routing <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'yi içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-364">Endpoint routing includes the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API.</span></span> <span data-ttu-id="ff86d-365">`LinkGenerator`, [dı](xref:fundamentals/dependency-injection)tarafından kullanılabilen bir tek hizmettir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-365">`LinkGenerator` is a singleton service available from [DI](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="ff86d-366">`LinkGenerator`API, yürütülen bir istek bağlamı dışında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-366">The `LinkGenerator` API can be used outside of the context of an executing request.</span></span> <span data-ttu-id="ff86d-367">Etiket Yardımcıları, HTML Yardımcıları ve eylem sonuçları gibi ' i kullanan [Mvc. ıurlhelper](xref:Microsoft.AspNetCore.Mvc.IUrlHelper) ve senaryoları <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> , [Action Results](xref:mvc/controllers/actions) [Tag Helpers](xref:mvc/views/tag-helpers/intro) `LinkGenerator` bağlantı oluşturma yetenekleri sağlamak için API 'yi dahili olarak kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-367">[Mvc.IUrlHelper](xref:Microsoft.AspNetCore.Mvc.IUrlHelper) and scenarios that rely on <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, such as [Tag Helpers](xref:mvc/views/tag-helpers/intro), HTML Helpers, and [Action Results](xref:mvc/controllers/actions), use the `LinkGenerator` API internally to provide link generating capabilities.</span></span>

<span data-ttu-id="ff86d-368">Bağlantı Oluşturucu, bir **Adres** ve **Adres şemaları**kavramıyla desteklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-368">The link generator is backed by the concept of an **address** and **address schemes**.</span></span> <span data-ttu-id="ff86d-369">Adres şeması, bağlantı oluşturma için göz önünde bulundurmanız gereken uç noktaları belirlemenin bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-369">An address scheme is a way of determining the endpoints that should be considered for link generation.</span></span> <span data-ttu-id="ff86d-370">Örneğin, yol adı ve yol değerleri senaryoları birçok kullanıcı, denetleyicilerden ve Razor sayfalardan bir adres düzeni olarak uygulanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-370">For example, the route name and route values scenarios many users are familiar with from controllers and Razor Pages are implemented as an address scheme.</span></span>

<span data-ttu-id="ff86d-371">Bağlantı Oluşturucu Razor aşağıdaki genişletme yöntemleri aracılığıyla denetleyicilere ve sayfalara bağlanabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-371">The link generator can link to controllers and Razor Pages via the following extension methods:</span></span>

* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetPathByAction*>
* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetUriByAction*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetPathByPage*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetUriByPage*>

<span data-ttu-id="ff86d-372">Bu yöntemlerin aşırı yüklemeleri, içeren bağımsız değişkenleri kabul eder `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-372">Overloads of these methods accept arguments that include the `HttpContext`.</span></span> <span data-ttu-id="ff86d-373">Bu yöntemler, [URL. Action](xref:System.Web.Mvc.UrlHelper.Action*) ve [URL. Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*)ile işlevsel olarak eşdeğerdir, ancak ek esneklik ve seçenekler sunar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-373">These methods are functionally equivalent to [Url.Action](xref:System.Web.Mvc.UrlHelper.Action*) and [Url.Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*), but offer additional flexibility and options.</span></span>

<span data-ttu-id="ff86d-374">`GetPath*`Yöntemleri `Url.Action` `Url.Page` , ve ' de, mutlak bir yol içeren bir URI oluşturmak için en çok benzerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-374">The `GetPath*` methods are most similar to `Url.Action` and `Url.Page`, in that they generate a URI containing an absolute path.</span></span> <span data-ttu-id="ff86d-375">`GetUri*`Yöntemler her zaman bir düzen ve konak içeren mutlak BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-375">The `GetUri*` methods always generate an absolute URI containing a scheme and host.</span></span> <span data-ttu-id="ff86d-376">Bir öğesini kabul eden yöntemler, `HttpContext` yürütülmekte olan istek bağlamında BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-376">The methods that accept an `HttpContext` generate a URI in the context of the executing request.</span></span> <span data-ttu-id="ff86d-377">[Ortam](#ambient) yolu DEĞERLERI, URL taban yolu, şeması ve yürütülen istekten ana bilgisayar, geçersiz kılınmadıkça kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-377">The [ambient](#ambient) route values, URL base path, scheme, and host from the executing request are used unless overridden.</span></span>

<span data-ttu-id="ff86d-378"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>bir adresle çağırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-378"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is called with an address.</span></span> <span data-ttu-id="ff86d-379">URI oluşturma iki adımda gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-379">Generating a URI occurs in two steps:</span></span>

1. <span data-ttu-id="ff86d-380">Adres, adresle eşleşen bir uç nokta listesine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-380">An address is bound to a list of endpoints that match the address.</span></span>
1. <span data-ttu-id="ff86d-381">Her uç nokta <xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern> , sağlanan değerlerle eşleşen bir yol deseninin bulunana kadar değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-381">Each endpoint's <xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern> is evaluated until a route pattern that matches the supplied values is found.</span></span> <span data-ttu-id="ff86d-382">Elde edilen çıktı, bağlantı oluşturucuya sağlanan diğer URI parçalarıyla birleştirilir ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-382">The resulting output is combined with the other URI parts supplied to the link generator and returned.</span></span>

<span data-ttu-id="ff86d-383">Tarafından sunulan yöntemler, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> herhangi bir adres türü için standart bağlantı oluşturma yeteneklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-383">The methods provided by <xref:Microsoft.AspNetCore.Routing.LinkGenerator> support standard link generation capabilities for any type of address.</span></span> <span data-ttu-id="ff86d-384">Bağlantı oluşturucuyu kullanmanın en kolay yolu, belirli bir adres türü için işlem gerçekleştiren genişletme yöntemlerine yöneliktir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-384">The most convenient way to use the link generator is through extension methods that perform operations for a specific address type:</span></span>

| <span data-ttu-id="ff86d-385">Genişletme yöntemi</span><span class="sxs-lookup"><span data-stu-id="ff86d-385">Extension Method</span></span> | <span data-ttu-id="ff86d-386">Açıklama</span><span class="sxs-lookup"><span data-stu-id="ff86d-386">Description</span></span> |
| ---------------- | ----------- |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*> | <span data-ttu-id="ff86d-387">Belirtilen değerleri temel alarak mutlak bir yola sahip bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-387">Generates a URI with an absolute path based on the provided values.</span></span> |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetUriByAddress*> | <span data-ttu-id="ff86d-388">Belirtilen değerleri temel alarak mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-388">Generates an absolute URI based on the provided values.</span></span>             |

> [!WARNING]
> <span data-ttu-id="ff86d-389">Çağırma yöntemlerinin aşağıdaki etkilerine dikkat edin <xref:Microsoft.AspNetCore.Routing.LinkGenerator> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-389">Pay attention to the following implications of calling <xref:Microsoft.AspNetCore.Routing.LinkGenerator> methods:</span></span>
>
> * <span data-ttu-id="ff86d-390">`GetUri*`Gelen isteklerin üstbilgisini doğrulayan bir uygulama yapılandırmasında uzantı yöntemlerini dikkatle kullanın `Host` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-390">Use `GetUri*` extension methods with caution in an app configuration that doesn't validate the `Host` header of incoming requests.</span></span> <span data-ttu-id="ff86d-391">`Host`Gelen isteklerin üstbilgisi doğrulanmaz, güvenilir olmayan istek girişi bir görünüm veya sayfadaki URI 'ler içinde istemciye geri gönderilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-391">If the `Host` header of incoming requests isn't validated, untrusted request input can be sent back to the client in URIs in a view or page.</span></span> <span data-ttu-id="ff86d-392">Tüm üretim uygulamalarının, `Host` üst bilgisini bilinen geçerli değerlere karşı doğrulamak için kendi sunucusunu yapılandırmasını öneririz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-392">We recommend that all production apps configure their server to validate the `Host` header against known valid values.</span></span>
>
> * <span data-ttu-id="ff86d-393"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>Veya ile birlikte ara yazılım içinde dikkatli kullanın `Map` `MapWhen` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-393">Use <xref:Microsoft.AspNetCore.Routing.LinkGenerator> with caution in middleware in combination with `Map` or `MapWhen`.</span></span> <span data-ttu-id="ff86d-394">`Map*`yürütülen isteğin temel yolunu değiştirir ve bu da bağlantı oluşturma çıktısını etkiler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-394">`Map*` changes the base path of the executing request, which affects the output of link generation.</span></span> <span data-ttu-id="ff86d-395">Tüm <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'ler temel yol belirtilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-395">All of the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> APIs allow specifying a base path.</span></span> <span data-ttu-id="ff86d-396">Bağlantı oluşturma etkisini geri almak için boş bir temel yol belirtin `Map*` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-396">Specify an empty base path to undo the `Map*` affect on link generation.</span></span>

### <a name="middleware-example"></a><span data-ttu-id="ff86d-397">Ara yazılım örneği</span><span class="sxs-lookup"><span data-stu-id="ff86d-397">Middleware example</span></span>

<span data-ttu-id="ff86d-398">Aşağıdaki örnekte, bir ara yazılım, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> Mağaza ürünlerini listeleyen bir eylem yöntemine bağlantı oluşturmak için API 'yi kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-398">In the following example, a middleware uses the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API to create a link to an action method that lists store products.</span></span> <span data-ttu-id="ff86d-399">Ekleme tarafından bir sınıfa ve çağırarak bir `GenerateLink` uygulamadaki herhangi bir sınıf için kullanılabilir olan bağlantı oluşturucuyu kullanma:</span><span class="sxs-lookup"><span data-stu-id="ff86d-399">Using the link generator by injecting it into a class and calling `GenerateLink` is available to any class in an app:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Middleware/ProductsLinkMiddleware.cs?name=snippet)]

<a name="rtr"></a>

## <a name="route-template-reference"></a><span data-ttu-id="ff86d-400">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-400">Route template reference</span></span>

<span data-ttu-id="ff86d-401">İçindeki belirteçler `{}` , yol eşleştirildiği takdirde bağlanan rota parametrelerini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-401">Tokens within `{}` define route parameters that are bound if the route is matched.</span></span> <span data-ttu-id="ff86d-402">Yol segmentinde birden fazla yol parametresi tanımlanabilir, ancak yol parametreleri bir sabit değer ile ayrılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-402">More than one route parameter can be defined in a route segment, but route parameters  must be separated by a literal value.</span></span> <span data-ttu-id="ff86d-403">Örneğin, `{controller=Home}{action=Index}` ve arasında değişmez değer olmadığından geçerli bir yol değil `{controller}` `{action}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-403">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span>  <span data-ttu-id="ff86d-404">Rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-404">Route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="ff86d-405">Yol parametrelerinden (örneğin, `{id}` ) ve yol ayırıcısından farklı bir metin, `/` URL içindeki metinle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-405">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="ff86d-406">Metin eşleştirme, büyük/küçük harfe duyarsız ve URL 'nin yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-406">Text matching is case-insensitive and based on the decoded representation of the URL's path.</span></span> <span data-ttu-id="ff86d-407">Sabit bir yol parametresi sınırlayıcısından eşlemek `{` veya `}` karakteri tekrarlayarak sınırlayıcıdan kaçış.</span><span class="sxs-lookup"><span data-stu-id="ff86d-407">To match a literal route parameter delimiter `{` or `}`, escape the delimiter by repeating the character.</span></span> <span data-ttu-id="ff86d-408">Örneğin `{{` veya `}}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-408">For example `{{` or `}}`.</span></span>

<span data-ttu-id="ff86d-409">Yıldız işareti `*` veya çift yıldız işareti `**` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-409">Asterisk `*` or double asterisk `**`:</span></span>

* <span data-ttu-id="ff86d-410">URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-410">Can be used as a prefix to a route parameter to bind to the rest of the URI.</span></span>
* <span data-ttu-id="ff86d-411">, **Catch-all** parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-411">Are called a **catch-all** parameters.</span></span> <span data-ttu-id="ff86d-412">Örneğin `blog/{**slug}` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-412">For example, `blog/{**slug}`:</span></span>
  * <span data-ttu-id="ff86d-413">İle başlayan `/blog` ve bundan sonraki bir değere sahip tüm URI ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-413">Matches any URI that starts with `/blog` and has any value following it.</span></span>
  * <span data-ttu-id="ff86d-414">Aşağıdaki değer, `/blog` [başlık](https://developer.mozilla.org/docs/Glossary/Slug) yolu değerine atanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-414">The value following `/blog` is assigned to the [slug](https://developer.mozilla.org/docs/Glossary/Slug) route value.</span></span>

[!INCLUDE[](~/includes/catchall.md)]

<span data-ttu-id="ff86d-415">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-415">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="ff86d-416">Catch-all parametresi, yol ayırıcı karakterleri de dahil olmak üzere bir URL oluşturmak için kullanıldığında uygun karakterlerin bir kaçar `/` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-416">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator `/` characters.</span></span> <span data-ttu-id="ff86d-417">Örneğin, `foo/{*path}` yol değerlerini içeren yol `{ path = "my/path" }` oluşturulur `foo/my%2Fpath` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-417">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="ff86d-418">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-418">Note the escaped forward slash.</span></span> <span data-ttu-id="ff86d-419">Yol ayırıcı karakterlerini yuvarlaklaştırmak için `**` Rota parametresi önekini kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-419">To round-trip path separator characters, use the `**` route parameter prefix.</span></span> <span data-ttu-id="ff86d-420">İle rota `foo/{**path}` `{ path = "my/path" }` oluşturulur `foo/my/path` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-420">The route `foo/{**path}` with `{ path = "my/path" }` generates `foo/my/path`.</span></span>

<span data-ttu-id="ff86d-421">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-421">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="ff86d-422">Örneğin, şablonu göz önünde bulundurun `files/{filename}.{ext?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-422">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="ff86d-423">Hem hem de için değerler olduğunda `filename` `ext` her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-423">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="ff86d-424">URL 'de yalnızca bir değeri `filename` varsa, sondaki değer isteğe bağlı olduğundan yol eşleşir `.` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-424">If only a value for `filename` exists in the URL, the route matches because the trailing `.` is  optional.</span></span> <span data-ttu-id="ff86d-425">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="ff86d-425">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="ff86d-426">Yol parametreleri, parametre adından sonra bir eşittir işaretiyle () ayırarak varsayılan değer belirtilerek belirlenmiş **varsayılan değerlere** sahip olabilir `=` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-426">Route parameters may have **default values** designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="ff86d-427">Örneğin, `{controller=Home}` `Home` için varsayılan değer olarak tanımlar `controller` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-427">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="ff86d-428">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-428">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="ff86d-429">Yol parametreleri, parametre adının sonuna bir soru işareti () eklenerek isteğe bağlı olarak yapılır `?` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-429">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name.</span></span> <span data-ttu-id="ff86d-430">Örneğin, `id?`.</span><span class="sxs-lookup"><span data-stu-id="ff86d-430">For example, `id?`.</span></span> <span data-ttu-id="ff86d-431">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark şudur:</span><span class="sxs-lookup"><span data-stu-id="ff86d-431">The difference between optional values and default route parameters is:</span></span>

* <span data-ttu-id="ff86d-432">Varsayılan değere sahip bir yol parametresi her zaman bir değer üretir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-432">A route parameter with a default value always produces a value.</span></span>
* <span data-ttu-id="ff86d-433">İsteğe bağlı bir parametre yalnızca istek URL 'SI tarafından bir değer sağlandığında bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-433">An optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="ff86d-434">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-434">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="ff86d-435">`:`Yol parametre adından sonra ekleme ve kısıtlama adı, bir rota parametresinde bir satır içi kısıtlamayı belirtir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-435">Adding `:` and constraint name after the route parameter name specifies an inline constraint on a route parameter.</span></span> <span data-ttu-id="ff86d-436">Kısıtlama bağımsız değişkenler gerektiriyorsa, kısıtlama adından sonra parantez içine alınır `(...)` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-436">If the constraint requires arguments, they're enclosed in parentheses `(...)` after the constraint name.</span></span> <span data-ttu-id="ff86d-437">Birden çok *satır içi kısıtlama* , başka bir `:` kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-437">Multiple *inline constraints* can be specified by appending another `:` and constraint name.</span></span>

<span data-ttu-id="ff86d-438">Kısıtlama adı ve bağımsız değişkenler, <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> URL işlemede kullanılmak üzere bir örneği oluşturmak için hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-438">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="ff86d-439">Örneğin, yol şablonu `blog/{article:minlength(10)}` `minlength` bağımsız değişkenle bir kısıtlama belirtir `10` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-439">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="ff86d-440">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-440">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="ff86d-441">Rota parametrelerinin ayrıca parametre dönüştürücüler de olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-441">Route parameters may also have parameter transformers.</span></span> <span data-ttu-id="ff86d-442">Parametre dönüştürücüler, URL 'Ler için bağlantılar ve eşleşen eylemler ve sayfalar oluştururken parametrenin değerini dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-442">Parameter transformers transform a parameter's value when generating links and matching actions and pages to URLs.</span></span> <span data-ttu-id="ff86d-443">Benzer kısıtlamalar, `:` yol parametre adından sonra bir ve transformatör adı eklenerek, parametre dönüştürücüleri bir rota parametresine satır içi eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-443">Like constraints, parameter transformers can be added inline to a route parameter by adding a `:` and transformer name after the route parameter name.</span></span> <span data-ttu-id="ff86d-444">Örneğin, yol şablonu `blog/{article:slugify}` bir `slugify` transformatör belirtir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-444">For example, the route template `blog/{article:slugify}` specifies a `slugify` transformer.</span></span> <span data-ttu-id="ff86d-445">Parametre dönüştürücüler hakkında daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="ff86d-445">For more information on parameter transformers, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

<span data-ttu-id="ff86d-446">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-446">The following table demonstrates example route templates and their behavior:</span></span>

| <span data-ttu-id="ff86d-447">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="ff86d-447">Route Template</span></span>                           | <span data-ttu-id="ff86d-448">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="ff86d-448">Example Matching URI</span></span>    | <span data-ttu-id="ff86d-449">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="ff86d-449">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="ff86d-450">Yalnızca tek bir yolla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-450">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="ff86d-451">İle eşleşir ve `Page` ayarlanır `Home` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-451">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="ff86d-452">İle eşleşir ve `Page` ayarlanır `Contact` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-452">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="ff86d-453">`Products`Denetleyiciye ve `List` eyleme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-453">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="ff86d-454">`Products` `Details` 123 olarak ayarlanan denetleyiciye ve eyleme eşlenir `id` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-454">Maps to the `Products` controller and  `Details` action with`id` set to 123.</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="ff86d-455">`Home`Denetleyiciye ve `Index` yönteme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-455">Maps to the `Home` controller and `Index` method.</span></span> <span data-ttu-id="ff86d-456">`id` yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-456">`id` is ignored.</span></span>        |
| `{controller=Home}/{action=Index}/{id?}` | `/Products`         | <span data-ttu-id="ff86d-457">`Products`Denetleyiciye ve `Index` yönteme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-457">Maps to the `Products` controller and `Index` method.</span></span> <span data-ttu-id="ff86d-458">`id` yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-458">`id` is ignored.</span></span>        |

<span data-ttu-id="ff86d-459">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-459">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="ff86d-460">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-460">Constraints and defaults can also be specified outside the route template.</span></span>

### <a name="complex-segments"></a><span data-ttu-id="ff86d-461">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="ff86d-461">Complex segments</span></span>

<span data-ttu-id="ff86d-462">Karmaşık segmentler, en çok [greksuz olmayan](#greedy) bir şekilde, sabit değer sınırlayıcılarını sağdan sola eşleştirerek işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-462">Complex segments are processed by matching up literal delimiters from right to left in a [non-greedy](#greedy) way.</span></span> <span data-ttu-id="ff86d-463">Örneğin, `[Route("/a{b}c{d}")]` karmaşık bir kesimdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-463">For example, `[Route("/a{b}c{d}")]` is a complex segment.</span></span>
<span data-ttu-id="ff86d-464">Karmaşık segmentler, onları başarılı bir şekilde kullanmak için anlaşılması gereken belirli bir şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-464">Complex segments work in a particular way that must be understood to use them successfully.</span></span> <span data-ttu-id="ff86d-465">Bu bölümdeki örnek, sınırlayıcı metnin parametre değerleri içinde görünmemesinin neden karmaşık segmentlerin gerçekten iyi şekilde çalıştığını gösterir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-465">The example in this section demonstrates why complex segments only really work well when the delimiter text doesn't appear inside the parameter values.</span></span> <span data-ttu-id="ff86d-466">Bir [Regex](/dotnet/standard/base-types/regular-expressions) kullanarak ve daha karmaşık durumlar için değerleri el ile ayıklamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-466">Using a [regex](/dotnet/standard/base-types/regular-expressions) and then manually extracting the values is needed for more complex cases.</span></span>

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="ff86d-467">Bu, yönlendirmenin şablon ve URL yolu ile gerçekleştirdiği adımların bir özetidir `/a{b}c{d}` `/abcd` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-467">This is a summary of the steps that routing performs with the template `/a{b}c{d}` and the URL path `/abcd`.</span></span> <span data-ttu-id="ff86d-468">, `|` Algoritmanın nasıl çalıştığını görselleştirmeye yardımcı olmak için kullanılır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-468">The `|` is used to help visualize how the algorithm works:</span></span>

* <span data-ttu-id="ff86d-469">İlk sabit değer, sağdan sola `c` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-469">The first literal, right to left, is `c`.</span></span> <span data-ttu-id="ff86d-470">Bu nedenle `/abcd` , doğru bir şekilde aranır ve bulur `/ab|c|d` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-470">So `/abcd` is searched from right and finds `/ab|c|d`.</span></span>
* <span data-ttu-id="ff86d-471">Right () öğesine ait her şey `d` artık Route parametresiyle eşleştirildi `{d}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-471">Everything to the right (`d`) is now matched to the route parameter `{d}`.</span></span>
* <span data-ttu-id="ff86d-472">Sonraki değişmez değer, sağdan sola `a` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-472">The next literal, right to left, is `a`.</span></span> <span data-ttu-id="ff86d-473">Bu nedenle `/ab|c|d` , ayrıldığımız yerden başlayarak arama yapılır `a` `/|a|b|c|d` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-473">So `/ab|c|d` is searched starting where we left off, then `a` is found `/|a|b|c|d`.</span></span>
* <span data-ttu-id="ff86d-474">Right ( `b` ) değeri artık Route parametresiyle eşleştirildi `{b}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-474">The value to the right (`b`) is now matched to the route parameter `{b}`.</span></span>
* <span data-ttu-id="ff86d-475">Kalan bir metin yok ve kalan yol şablonu yok, bu nedenle bu bir eşleşmedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-475">There is no remaining text and no remaining route template, so this is a match.</span></span>

<span data-ttu-id="ff86d-476">Aynı şablonu ve URL yolunu kullanarak negatif bir durum örneği aşağıda verilmiştir `/a{b}c{d}` `/aabcd` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-476">Here's an example of a negative case using the same template `/a{b}c{d}` and the URL path `/aabcd`.</span></span> <span data-ttu-id="ff86d-477">, `|` Algoritmanın nasıl çalıştığını görselleştirmeye yardımcı olmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-477">The `|` is used to help visualize how the algorithm works.</span></span> <span data-ttu-id="ff86d-478">Bu durum, aynı algoritma tarafından açıklanan bir eşleşme değildir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-478">This case isn't a match, which is explained by the same algorithm:</span></span>
* <span data-ttu-id="ff86d-479">İlk sabit değer, sağdan sola `c` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-479">The first literal, right to left, is `c`.</span></span> <span data-ttu-id="ff86d-480">Bu nedenle `/aabcd` , doğru bir şekilde aranır ve bulur `/aab|c|d` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-480">So `/aabcd` is searched from right and finds `/aab|c|d`.</span></span>
* <span data-ttu-id="ff86d-481">Right () öğesine ait her şey `d` artık Route parametresiyle eşleştirildi `{d}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-481">Everything to the right (`d`) is now matched to the route parameter `{d}`.</span></span>
* <span data-ttu-id="ff86d-482">Sonraki değişmez değer, sağdan sola `a` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-482">The next literal, right to left, is `a`.</span></span> <span data-ttu-id="ff86d-483">Bu nedenle `/aab|c|d` , ayrıldığımız yerden başlayarak arama yapılır `a` `/a|a|b|c|d` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-483">So `/aab|c|d` is searched starting where we left off, then `a` is found `/a|a|b|c|d`.</span></span>
* <span data-ttu-id="ff86d-484">Right ( `b` ) değeri artık Route parametresiyle eşleştirildi `{b}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-484">The value to the right (`b`) is now matched to the route parameter `{b}`.</span></span>
* <span data-ttu-id="ff86d-485">Bu noktada, kalan metin vardır `a` ancak algoritmanın ayrıştırılacak yol şablonu kalmadı, bu nedenle bu bir eşleşme değildir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-485">At this point there is remaining text `a`, but the algorithm has run out of route template to parse, so this is not a match.</span></span>

<span data-ttu-id="ff86d-486">Eşleşen algoritma [greyumsuz](#greedy)olmadığından:</span><span class="sxs-lookup"><span data-stu-id="ff86d-486">Since the matching algorithm is [non-greedy](#greedy):</span></span>

* <span data-ttu-id="ff86d-487">Her adımda mümkün olan en küçük metin miktarıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-487">It matches the smallest amount of text possible in each step.</span></span>
* <span data-ttu-id="ff86d-488">Sınırlayıcı değerin parametre değerleri içinde göründüğü herhangi bir durum, eşleşmemelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-488">Any case where the delimiter value appears inside the parameter values results in not matching.</span></span>

<span data-ttu-id="ff86d-489">Normal ifadeler, eşleşen davranışları üzerinde çok daha fazla denetim sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-489">Regular expressions provide much more control over their matching behavior.</span></span>

<a name="greedy"></a>

<span data-ttu-id="ff86d-490">Aynı zamanda [yavaş eşleşme](https://wikipedia.org/wiki/Regular_expression#Lazy_matching)olarak da bilinen Greedy eşleştirmesi, olası en büyük dizeyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-490">Greedy matching, also know as [lazy matching](https://wikipedia.org/wiki/Regular_expression#Lazy_matching), matches the largest possible string.</span></span> <span data-ttu-id="ff86d-491">Doyumsuz olmayan dize, olası en küçük dizeyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-491">Non-greedy matches the smallest possible string.</span></span>

## <a name="route-constraint-reference"></a><span data-ttu-id="ff86d-492">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-492">Route constraint reference</span></span>

<span data-ttu-id="ff86d-493">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-493">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="ff86d-494">Yol kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda doğru veya yanlış bir karar vermez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-494">Route constraints generally inspect the route value associated via the route template and make a true or false decision about whether the value is acceptable.</span></span> <span data-ttu-id="ff86d-495">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-495">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="ff86d-496">Örneğin, <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> bir ISTEğI http fiiline bağlı olarak kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-496">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="ff86d-497">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-497">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="ff86d-498">Giriş doğrulaması için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-498">Don't use constraints for input validation.</span></span> <span data-ttu-id="ff86d-499">Giriş doğrulaması için kısıtlamalar kullanılıyorsa, geçersiz giriş, bulunamayan bir Yanıt ile sonuçlanır `404` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-499">If constraints are used for input validation, invalid input results in a `404` Not Found response.</span></span> <span data-ttu-id="ff86d-500">Geçersiz giriş `400` , uygun bir hata iletisiyle hatalı bir istek üretmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-500">Invalid input should produce a `400` Bad Request with an appropriate error message.</span></span> <span data-ttu-id="ff86d-501">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların belirsizliğini ortadan kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-501">Route constraints are used to disambiguate similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="ff86d-502">Aşağıdaki tabloda örnek yol kısıtlamaları ve beklenen davranışlar gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-502">The following table demonstrates example route constraints and their expected behavior:</span></span>

| <span data-ttu-id="ff86d-503">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="ff86d-503">constraint</span></span> | <span data-ttu-id="ff86d-504">Örnek</span><span class="sxs-lookup"><span data-stu-id="ff86d-504">Example</span></span> | <span data-ttu-id="ff86d-505">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="ff86d-505">Example Matches</span></span> | <span data-ttu-id="ff86d-506">Notlar</span><span class="sxs-lookup"><span data-stu-id="ff86d-506">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="ff86d-507">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="ff86d-507">`123456789`, `-123456789`</span></span> | <span data-ttu-id="ff86d-508">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="ff86d-508">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="ff86d-509">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="ff86d-509">`true`, `FALSE`</span></span> | <span data-ttu-id="ff86d-510">Veya ile eşleşir `true` `false` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-510">Matches `true` or `false`.</span></span> <span data-ttu-id="ff86d-511">Büyük/küçük harf duyarsız</span><span class="sxs-lookup"><span data-stu-id="ff86d-511">Case-insensitive</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="ff86d-512">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="ff86d-512">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="ff86d-513">`DateTime`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-513">Matches a valid `DateTime` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-514">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-514">See preceding warning.</span></span> |
| `decimal` | `{price:decimal}` | <span data-ttu-id="ff86d-515">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="ff86d-515">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="ff86d-516">`decimal`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-516">Matches a valid `decimal` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-517">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-517">See preceding warning.</span></span>|
| `double` | `{weight:double}` | <span data-ttu-id="ff86d-518">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="ff86d-518">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="ff86d-519">`double`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-519">Matches a valid `double` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-520">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-520">See preceding warning.</span></span>|
| `float` | `{weight:float}` | <span data-ttu-id="ff86d-521">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="ff86d-521">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="ff86d-522">`float`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-522">Matches a valid `float` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-523">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-523">See preceding warning.</span></span>|
| `guid` | `{id:guid}` | `CD2C1638-1638-72D5-1638-DEADBEEF1638` | <span data-ttu-id="ff86d-524">Geçerli bir `Guid` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="ff86d-524">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="ff86d-525">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="ff86d-525">`123456789`, `-123456789`</span></span> | <span data-ttu-id="ff86d-526">Geçerli bir `long` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="ff86d-526">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="ff86d-527">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-527">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `MyFile` | <span data-ttu-id="ff86d-528">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-528">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="ff86d-529">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-529">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="ff86d-530">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-530">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="ff86d-531">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-531">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="ff86d-532">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-532">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="ff86d-533">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-533">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="ff86d-534">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır `a` - `z` ve büyük/küçük harfe duyarsız olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-534">String must consist of one or more alphabetical characters, `a`-`z` and case-insensitive.</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="ff86d-535">Dize, normal ifadeyle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-535">String must match the regular expression.</span></span> <span data-ttu-id="ff86d-536">Normal ifade tanımlama hakkında ipuçlarına bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-536">See tips about defining a regular expression.</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="ff86d-537">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="ff86d-537">Used to enforce that a non-parameter value is present during URL generation</span></span> |

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="ff86d-538">Birden çok, iki nokta üst üste sınırlı kısıtlama, tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-538">Multiple, colon delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="ff86d-539">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-539">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="ff86d-540">URL 'YI doğrulayan ve CLR türüne dönüştürülen yol kısıtlamaları her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-540">Route constraints that verify the URL and are converted to a CLR type always use the invariant culture.</span></span> <span data-ttu-id="ff86d-541">Örneğin, CLR türüne dönüştürme `int` veya `DateTime` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-541">For example, conversion to the CLR type `int` or `DateTime`.</span></span> <span data-ttu-id="ff86d-542">Bu kısıtlamalar, URL 'nin yerelleştirilebilir olmadığını varsayar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-542">These constraints assume that the URL is not localizable.</span></span> <span data-ttu-id="ff86d-543">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-543">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="ff86d-544">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-544">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="ff86d-545">Örneğin, `float` kısıtlama yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-545">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

### <a name="regular-expressions-in-constraints"></a><span data-ttu-id="ff86d-546">Kısıtlamalarda normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="ff86d-546">Regular expressions in constraints</span></span>

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="ff86d-547">Normal ifadeler, Route kısıtlaması kullanılarak satır içi kısıtlamalar olarak belirtilebilir `regex(...)` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-547">Regular expressions can be specified as inline constraints using the `regex(...)` route constraint.</span></span> <span data-ttu-id="ff86d-548"><xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*>Ailesindeki Yöntemler ayrıca Kısıtlamaların bir nesne sabit değerini de kabul eder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-548">Methods in the <xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*> family also accept an object literal of constraints.</span></span> <span data-ttu-id="ff86d-549">Bu form kullanılıyorsa, dize değerleri normal ifadeler olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-549">If that form is used, string values are interpreted as regular expressions.</span></span>

<span data-ttu-id="ff86d-550">Aşağıdaki kod, bir satır içi Regex kısıtlaması kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-550">The following code uses an inline regex constraint:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupRegex.cs?name=snippet)]

<span data-ttu-id="ff86d-551">Aşağıdaki kod, bir Regex kısıtlaması belirtmek için bir nesne değişmez değeri kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-551">The following code uses an object literal to specify a regex constraint:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupRegex2.cs?name=snippet)]

<span data-ttu-id="ff86d-552">ASP.NET Core Framework, `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` normal ifade oluşturucusuna ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-552">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="ff86d-553"><xref:System.Text.RegularExpressions.RegexOptions>Bu üyelerin açıklaması için bkz..</span><span class="sxs-lookup"><span data-stu-id="ff86d-553">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="ff86d-554">Normal ifadeler, Yönlendirme ve C# dili tarafından kullanılanlarla benzerlik olan sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-554">Regular expressions use delimiters and tokens similar to those used by routing and the C# language.</span></span> <span data-ttu-id="ff86d-555">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-555">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="ff86d-556">Normal ifadeyi `^\d{3}-\d{2}-\d{4}$` bir satır içi kısıtlamada kullanmak için aşağıdakilerden birini kullanın:</span><span class="sxs-lookup"><span data-stu-id="ff86d-556">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in an inline constraint, use one of the following:</span></span>

* <span data-ttu-id="ff86d-557">Dizede `\` belirtilen karakterleri, `\\` dize kaçış karakterinden kaçınmak Için C# kaynak dosyasındaki karakter olarak değiştirin `\` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-557">Replace `\` characters provided in the string as `\\` characters in the C# source file in order to escape the `\` string escape character.</span></span>
* <span data-ttu-id="ff86d-558">Tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string).</span><span class="sxs-lookup"><span data-stu-id="ff86d-558">[Verbatim string literals](/dotnet/csharp/language-reference/keywords/string).</span></span>

<span data-ttu-id="ff86d-559">Yönlendirme parametresi sınırlayıcı karakterlerini `{` ,,, `}` , `[` `]` ifadedeki karakterleri çift kaçış, örneğin `{{` `}}` `[[` `]]` ,,,,.</span><span class="sxs-lookup"><span data-stu-id="ff86d-559">To escape routing parameter delimiter characters `{`, `}`, `[`, `]`, double the characters in the expression, for example, `{{`, `}}`, `[[`, `]]`.</span></span> <span data-ttu-id="ff86d-560">Aşağıdaki tabloda, bir normal ifade ve bunun kaçış sürümü gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-560">The following table shows a regular expression and its escaped version:</span></span>

| <span data-ttu-id="ff86d-561">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="ff86d-561">Regular expression</span></span>    | <span data-ttu-id="ff86d-562">Kaçan normal ifade</span><span class="sxs-lookup"><span data-stu-id="ff86d-562">Escaped regular expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="ff86d-563">Yönlendirmelerde kullanılan normal ifadeler, genellikle karakteriyle başlar `^` ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-563">Regular expressions used in routing often start with the `^` character and match the starting position of the string.</span></span> <span data-ttu-id="ff86d-564">İfadeler genellikle `$` karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-564">The expressions often end with the `$` character and match the end of the string.</span></span> <span data-ttu-id="ff86d-565">`^`Ve `$` karakterleri, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-565">The `^` and `$` characters ensure that the regular expression matches the entire route parameter value.</span></span> <span data-ttu-id="ff86d-566">`^`Ve karakterleri olmadan `$` normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-566">Without the `^` and `$` characters, the regular expression matches any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="ff86d-567">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-567">The following table provides examples and explains why they match or fail to match:</span></span>

| <span data-ttu-id="ff86d-568">İfade</span><span class="sxs-lookup"><span data-stu-id="ff86d-568">Expression</span></span>   | <span data-ttu-id="ff86d-569">Dize</span><span class="sxs-lookup"><span data-stu-id="ff86d-569">String</span></span>    | <span data-ttu-id="ff86d-570">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-570">Match</span></span> | <span data-ttu-id="ff86d-571">Yorum</span><span class="sxs-lookup"><span data-stu-id="ff86d-571">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-572">hello</span><span class="sxs-lookup"><span data-stu-id="ff86d-572">hello</span></span>     | <span data-ttu-id="ff86d-573">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-573">Yes</span></span>   | <span data-ttu-id="ff86d-574">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-574">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-575">123abc456</span><span class="sxs-lookup"><span data-stu-id="ff86d-575">123abc456</span></span> | <span data-ttu-id="ff86d-576">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-576">Yes</span></span>   | <span data-ttu-id="ff86d-577">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-577">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-578">MZ</span><span class="sxs-lookup"><span data-stu-id="ff86d-578">mz</span></span>        | <span data-ttu-id="ff86d-579">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-579">Yes</span></span>   | <span data-ttu-id="ff86d-580">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="ff86d-580">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-581">MZ</span><span class="sxs-lookup"><span data-stu-id="ff86d-581">MZ</span></span>        | <span data-ttu-id="ff86d-582">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-582">Yes</span></span>   | <span data-ttu-id="ff86d-583">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="ff86d-583">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="ff86d-584">hello</span><span class="sxs-lookup"><span data-stu-id="ff86d-584">hello</span></span>     | <span data-ttu-id="ff86d-585">Hayır</span><span class="sxs-lookup"><span data-stu-id="ff86d-585">No</span></span>    | <span data-ttu-id="ff86d-586">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="ff86d-586">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="ff86d-587">123abc456</span><span class="sxs-lookup"><span data-stu-id="ff86d-587">123abc456</span></span> | <span data-ttu-id="ff86d-588">Hayır</span><span class="sxs-lookup"><span data-stu-id="ff86d-588">No</span></span>    | <span data-ttu-id="ff86d-589">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="ff86d-589">See `^` and `$` above</span></span> |

<span data-ttu-id="ff86d-590">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="ff86d-590">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="ff86d-591">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-591">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="ff86d-592">Örneğin, `{action:regex(^(list|get|create)$)}` yalnızca `action` Rota değeri `list` ,, veya ile eşleşir `get` `create` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-592">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="ff86d-593">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-593">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="ff86d-594">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğünde geçirilen kısıtlamalar, normal ifadeler olarak da değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-594">Constraints that are passed in the constraints dictionary that don't match one of the known constraints are also treated as regular expressions.</span></span> <span data-ttu-id="ff86d-595">Bilinen kısıtlamaların biriyle eşleşmeyen bir şablon içinde geçirilen kısıtlamalar normal ifadeler olarak değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-595">Constraints that are passed  within a template that don't match one of the known constraints are not treated as regular expressions.</span></span>

### <a name="custom-route-constraints"></a><span data-ttu-id="ff86d-596">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="ff86d-596">Custom route constraints</span></span>

<span data-ttu-id="ff86d-597">Özel yol kısıtlamaları arabirimi uygulayarak oluşturulabilir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-597">Custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="ff86d-598">`IRouteConstraint`Arabirimi, <xref:System.Web.Routing.IRouteConstraint.Match*> `true` kısıtlama karşılanıp Aksi takdirde döndüren ' i içerir `false` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-598">The `IRouteConstraint` interface contains <xref:System.Web.Routing.IRouteConstraint.Match*>, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="ff86d-599">Özel yol kısıtlamaları nadiren gereklidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-599">Custom route constraints are rarely needed.</span></span> <span data-ttu-id="ff86d-600">Özel bir yol kısıtlaması uygulamadan önce, model bağlama gibi alternatifleri göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-600">Before implementing a custom route constraint, consider alternatives, such as model binding.</span></span>

<span data-ttu-id="ff86d-601">ASP.NET Core [kısıtlamalar](https://github.com/dotnet/aspnetcore/tree/master/src/Http/Routing/src/Constraints) klasörü, bir kısıtlama oluşturmaya yönelik iyi örnekler sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-601">The ASP.NET Core [Constraints](https://github.com/dotnet/aspnetcore/tree/master/src/Http/Routing/src/Constraints) folder provides good examples of creating a constraints.</span></span> <span data-ttu-id="ff86d-602">Örneğin, [Guidrouteconstraint](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Constraints/GuidRouteConstraint.cs#L18).</span><span class="sxs-lookup"><span data-stu-id="ff86d-602">For example, [GuidRouteConstraint](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Constraints/GuidRouteConstraint.cs#L18).</span></span>

<span data-ttu-id="ff86d-603">Özel bir kullanmak için `IRouteConstraint` yol kısıtlama türü, uygulamanın hizmet kapsayıcısında kayıtlı olması gerekir <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-603">To use a custom `IRouteConstraint`, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the service container.</span></span> <span data-ttu-id="ff86d-604">`ConstraintMap`, Yol kısıtlama anahtarlarını `IRouteConstraint` Bu kısıtlamaları doğrulayan uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-604">A `ConstraintMap` is a dictionary that maps route constraint keys to `IRouteConstraint` implementations that validate those constraints.</span></span> <span data-ttu-id="ff86d-605">Bir uygulama `ConstraintMap` , `Startup.ConfigureServices` hizmetlerin bir parçası olarak ' de güncelleştirilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan ile yapılandırma `services.Configure<RouteOptions>` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-605">An app's `ConstraintMap` can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="ff86d-606">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="ff86d-606">For example:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint.cs?name=snippet)]

<span data-ttu-id="ff86d-607">Önceki kısıtlama aşağıdaki kodda uygulanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-607">The preceding constraint is applied in the following code:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/TestController.cs?name=snippet&highlight=6,13)]

[!INCLUDE[](~/includes/MyDisplayRouteInfo.md)]

<span data-ttu-id="ff86d-608">Uygulamasının uygulanması, `MyCustomConstraint` `0` bir rota parametresine uygulanmasını engeller:</span><span class="sxs-lookup"><span data-stu-id="ff86d-608">The implementation of `MyCustomConstraint` prevents `0` being applied to a route parameter:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint.cs?name=snippet2)]

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="ff86d-609">Yukarıdaki kod:</span><span class="sxs-lookup"><span data-stu-id="ff86d-609">The preceding code:</span></span>

* <span data-ttu-id="ff86d-610">`0` `{id}` Yolun segmentinde engeller.</span><span class="sxs-lookup"><span data-stu-id="ff86d-610">Prevents `0` in the `{id}` segment of the route.</span></span>
* <span data-ttu-id="ff86d-611">, Özel bir kısıtlamayı uygulamaya yönelik temel bir örnek sağlamak için gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-611">Is shown to provide a basic example of implementing a custom constraint.</span></span> <span data-ttu-id="ff86d-612">Bu, bir üretim uygulamasında kullanılmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-612">It should not be used in a production app.</span></span>

<span data-ttu-id="ff86d-613">Aşağıdaki kod, içeren bir öğenin işlenmesini önlemek için daha iyi bir yaklaşımdır `id` `0` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-613">The following code is a better approach to preventing an `id` containing a `0` from being processed:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/TestController.cs?name=snippet2)]

<span data-ttu-id="ff86d-614">Yukarıdaki kodda yaklaşım üzerinde aşağıdaki avantajlar bulunur `MyCustomConstraint` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-614">The preceding code has the following advantages over the `MyCustomConstraint` approach:</span></span>

* <span data-ttu-id="ff86d-615">Özel bir kısıtlama gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-615">It doesn't require a custom constraint.</span></span>
* <span data-ttu-id="ff86d-616">Yol parametresi içerdiğinde daha açıklayıcı bir hata döndürür `0` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-616">It returns a more descriptive error when the route parameter includes `0`.</span></span>

## <a name="parameter-transformer-reference"></a><span data-ttu-id="ff86d-617">Parametre transformatörü başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-617">Parameter transformer reference</span></span>

<span data-ttu-id="ff86d-618">Parametre dönüştürücüler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-618">Parameter transformers:</span></span>

* <span data-ttu-id="ff86d-619">Kullanarak bağlantı oluştururken yürütün <xref:Microsoft.AspNetCore.Routing.LinkGenerator> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-619">Execute when generating a link using <xref:Microsoft.AspNetCore.Routing.LinkGenerator>.</span></span>
* <span data-ttu-id="ff86d-620">Uygulayın <xref:Microsoft.AspNetCore.Routing.IOutboundParameterTransformer?displayProperty=fullName> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-620">Implement <xref:Microsoft.AspNetCore.Routing.IOutboundParameterTransformer?displayProperty=fullName>.</span></span>
* <span data-ttu-id="ff86d-621">Kullanılarak yapılandırılır <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-621">Are configured using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>.</span></span>
* <span data-ttu-id="ff86d-622">Parametrenin yol değerini alın ve yeni bir dize değerine dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="ff86d-622">Take the parameter's route value and transform it to a new string value.</span></span>
* <span data-ttu-id="ff86d-623">Oluşturulan bağlantıda Dönüştürülmüş değerin kullanılmasına neden olacak.</span><span class="sxs-lookup"><span data-stu-id="ff86d-623">Result in using the transformed value in the generated link.</span></span>

<span data-ttu-id="ff86d-624">Örneğin, yol düzeninde bir özel `slugify` parametre transformatörü `blog\{article:slugify}` `Url.Action(new { article = "MyTestArticle" })` oluşturur `blog\my-test-article` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-624">For example, a custom `slugify` parameter transformer in route pattern `blog\{article:slugify}` with `Url.Action(new { article = "MyTestArticle" })` generates `blog\my-test-article`.</span></span>

<span data-ttu-id="ff86d-625">Aşağıdaki uygulamayı göz önünde bulundurun `IOutboundParameterTransformer` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-625">Consider the following `IOutboundParameterTransformer` implementation:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint2.cs?name=snippet2)]

<span data-ttu-id="ff86d-626">Bir parametre transformatörü bir yol düzeninde kullanmak için, şunu içinde kullanarak yapılandırın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-626">To use a parameter transformer in a route pattern, configure it using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint2.cs?name=snippet)]

<span data-ttu-id="ff86d-627">ASP.NET Core Framework, bir uç noktanın çözümlediği URI 'yi dönüştürmek için parametre dönüştürücüler kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-627">The ASP.NET Core framework uses parameter transformers to transform the URI where an endpoint resolves.</span></span> <span data-ttu-id="ff86d-628">Örneğin, parametre dönüştürücüler,,, ve ile eşleştirmek için kullanılan rota değerlerini dönüştürür `area` `controller` `action` `page` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-628">For example, parameter transformers transform the route values used to match an `area`, `controller`, `action`, and `page`.</span></span>

```csharp
routes.MapControllerRoute(
    name: "default",
    template: "{controller:slugify=Home}/{action:slugify=Index}/{id?}");
```

<span data-ttu-id="ff86d-629">Önceki yol şablonuyla, eylem `SubscriptionManagementController.GetAll` URI ile eşleştirilir `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-629">With the preceding route template, the action `SubscriptionManagementController.GetAll` is matched with the URI `/subscription-management/get-all`.</span></span> <span data-ttu-id="ff86d-630">Bir parametre transformatörü bir bağlantı oluşturmak için kullanılan rota değerlerini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-630">A parameter transformer doesn't change the route values used to generate a link.</span></span> <span data-ttu-id="ff86d-631">Örneğin, `Url.Action("GetAll", "SubscriptionManagement")` çıktılar `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-631">For example, `Url.Action("GetAll", "SubscriptionManagement")` outputs `/subscription-management/get-all`.</span></span>

<span data-ttu-id="ff86d-632">ASP.NET Core, oluşturulan yollarla parametre dönüştürücüler kullanmak için API kuralları sağlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-632">ASP.NET Core provides API conventions for using parameter transformers with generated routes:</span></span>

* <span data-ttu-id="ff86d-633"><xref:Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention?displayProperty=fullName>MVC kuralı, uygulamadaki tüm öznitelik yollarına belirtilen bir parametre transformatörü uygular.</span><span class="sxs-lookup"><span data-stu-id="ff86d-633">The <xref:Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention?displayProperty=fullName> MVC convention applies a specified parameter transformer to all attribute routes in the app.</span></span> <span data-ttu-id="ff86d-634">Parametre transformatörü, öznitelik yol belirteçlerini değiştirildiklerinde dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-634">The parameter transformer transforms attribute route tokens as they are replaced.</span></span> <span data-ttu-id="ff86d-635">Daha fazla bilgi için bkz. [belirteç değişimini özelleştirmek için bir parametre transformatörü kullanma](xref:mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span><span class="sxs-lookup"><span data-stu-id="ff86d-635">For more information, see [Use a parameter transformer to customize token replacement](xref:mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span></span>
* Razor<span data-ttu-id="ff86d-636">Sayfalar <xref:Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention> API kuralını kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-636"> Pages uses the <xref:Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention> API convention.</span></span> <span data-ttu-id="ff86d-637">Bu kural, belirtilen bir parametre transformatörü otomatik olarak bulunan tüm Razor sayfalara uygular.</span><span class="sxs-lookup"><span data-stu-id="ff86d-637">This convention applies a specified parameter transformer to all automatically discovered Razor Pages.</span></span> <span data-ttu-id="ff86d-638">Parametre transformatörü, sayfa yollarının klasör ve dosya adı segmentlerini dönüştürür Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-638">The parameter transformer transforms the folder and file name segments of Razor Pages routes.</span></span> <span data-ttu-id="ff86d-639">Daha fazla bilgi için bkz. [sayfa yollarını özelleştirmek için bir parametre transformatörü kullanma](xref:razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span><span class="sxs-lookup"><span data-stu-id="ff86d-639">For more information, see [Use a parameter transformer to customize page routes](xref:razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span></span>

<a name="ugr"></a>

## <a name="url-generation-reference"></a><span data-ttu-id="ff86d-640">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-640">URL generation reference</span></span>

<span data-ttu-id="ff86d-641">Bu bölüm, URL oluşturma tarafından uygulanan algoritmanın başvurusunu içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-641">This section contains a reference for the algorithm implemented by URL generation.</span></span> <span data-ttu-id="ff86d-642">Uygulamada, URL oluşturma 'nın çoğu karmaşık örneği denetleyicileri veya sayfaları kullanır Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-642">In practice, most complex examples of URL generation use controllers or Razor Pages.</span></span> <span data-ttu-id="ff86d-643">Daha fazla bilgi için bkz. [denetleyicilerde yönlendirme](xref:mvc/controllers/routing) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-643">See  [routing in controllers](xref:mvc/controllers/routing) for additional information.</span></span>

<span data-ttu-id="ff86d-644">URL oluşturma işlemi [Linkgenerator. GetPathByAddress](xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*) veya benzer bir yöntem çağrısıyla başlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-644">The URL generation process begins with a call to [LinkGenerator.GetPathByAddress](xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*) or a similar method.</span></span> <span data-ttu-id="ff86d-645">Yöntemi bir adres, bir rota değerleri kümesi ve isteğe bağlı olarak geçerli istek hakkında bilgi ile birlikte sağlanır `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-645">The method is provided with an address, a set of route values, and optionally information about the current request from `HttpContext`.</span></span>

<span data-ttu-id="ff86d-646">İlk adım, adresin türüyle eşleşen bir aday uç nokta kümesini çözümlemek için adresi kullanmaktır [`IEndpointAddressScheme<TAddress>`](xref:Microsoft.AspNetCore.Routing.IEndpointAddressScheme`1) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-646">The first step is to use the address to resolve a set of candidate endpoints using an [`IEndpointAddressScheme<TAddress>`](xref:Microsoft.AspNetCore.Routing.IEndpointAddressScheme`1) that matches the address's type.</span></span>

<span data-ttu-id="ff86d-647">Adres kümesi tarafından bir aday kümesini bulduktan sonra, uç noktalar bir URL oluşturma işlemi başarılı olana kadar sıralanır ve işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-647">Once of set of candidates is found by the address scheme, the endpoints are ordered and processed iteratively until a URL generation operation succeeds.</span></span> <span data-ttu-id="ff86d-648">URL oluşturma belirsizlikleri için Denetim **yapmaz** , döndürülen ilk sonuç son sonuçdur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-648">URL generation does **not** check for ambiguities, the first result returned is the final result.</span></span>

### <a name="troubleshooting-url-generation-with-logging"></a><span data-ttu-id="ff86d-649">Günlüğe kaydetme ile URL oluşturma sorunlarını giderme</span><span class="sxs-lookup"><span data-stu-id="ff86d-649">Troubleshooting URL generation with logging</span></span>

<span data-ttu-id="ff86d-650">URL oluşturma sorunlarını gidermeye yönelik ilk adım, günlük düzeyini `Microsoft.AspNetCore.Routing` olarak ayarlıyor `TRACE` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-650">The first step in troubleshooting URL generation is setting the logging level of `Microsoft.AspNetCore.Routing` to `TRACE`.</span></span> <span data-ttu-id="ff86d-651">`LinkGenerator`işleme hakkında birçok ayrıntıyı günlüğe kaydeder ve bu da sorunları gidermek için yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-651">`LinkGenerator` logs many details about its processing which can be useful to troubleshoot problems.</span></span>

<span data-ttu-id="ff86d-652">URL oluşturma hakkında ayrıntılı bilgi için bkz. [URL oluşturma başvurusu](#ugr) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-652">See [URL generation reference](#ugr) for details on URL generation.</span></span>

### <a name="addresses"></a><span data-ttu-id="ff86d-653">Adresleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-653">Addresses</span></span>

<span data-ttu-id="ff86d-654">Adresler, bağlantı oluşturucuya bir çağrıyı bir aday uç noktası kümesine bağlamak için kullanılan URL oluşturma kavramıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-654">Addresses are the concept in URL generation used to bind a call into the link generator to a set of candidate endpoints.</span></span>

<span data-ttu-id="ff86d-655">Adresler, varsayılan olarak iki uygulama ile birlikte gelen Genişletilebilir bir kavramdır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-655">Addresses are an extensible concept that come with two implementations by default:</span></span>

* <span data-ttu-id="ff86d-656">Şu adres olarak *uç nokta adı* ( `string` ) kullanılıyor:</span><span class="sxs-lookup"><span data-stu-id="ff86d-656">Using *endpoint name* (`string`) as the address:</span></span>
    * <span data-ttu-id="ff86d-657">MVC 'nin yol adına benzer işlevsellik sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-657">Provides similar functionality to MVC's route name.</span></span>
    * <span data-ttu-id="ff86d-658"><xref:Microsoft.AspNetCore.Routing.IEndpointNameMetadata>Meta veri türünü kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-658">Uses the <xref:Microsoft.AspNetCore.Routing.IEndpointNameMetadata> metadata type.</span></span>
    * <span data-ttu-id="ff86d-659">Belirtilen dizeyi tüm kayıtlı uç noktaların meta verilerinde çözer.</span><span class="sxs-lookup"><span data-stu-id="ff86d-659">Resolves the provided string against the metadata of all registered endpoints.</span></span>
    * <span data-ttu-id="ff86d-660">Birden fazla uç nokta aynı adı kullanıyorsa, başlangıçta bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-660">Throws an exception on startup if multiple endpoints use the same name.</span></span>
    * <span data-ttu-id="ff86d-661">Denetleyicilerin ve sayfaların dışında genel amaçlı kullanım için önerilir Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-661">Recommended for general-purpose use outside of controllers and Razor Pages.</span></span>
* <span data-ttu-id="ff86d-662">Adres olarak *yol değerlerini* ( <xref:Microsoft.AspNetCore.Routing.RouteValuesAddress> ) kullanma:</span><span class="sxs-lookup"><span data-stu-id="ff86d-662">Using *route values* (<xref:Microsoft.AspNetCore.Routing.RouteValuesAddress>) as the address:</span></span>
    * <span data-ttu-id="ff86d-663">Denetleyiciler ve Razor sayfalar için eskı URL oluşturmaya benzer işlevler sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-663">Provides similar functionality to controllers and Razor Pages legacy URL generation.</span></span>
    * <span data-ttu-id="ff86d-664">Genişletmek ve hata ayıklamak için çok karmaşık.</span><span class="sxs-lookup"><span data-stu-id="ff86d-664">Very complex to extend and debug.</span></span>
    * <span data-ttu-id="ff86d-665">`IUrlHelper`, Etiket yardımcıları, HTML Yardımcıları, eylem sonuçları vb. tarafından kullanılan uygulamayı sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-665">Provides the implementation used by `IUrlHelper`, Tag Helpers, HTML Helpers, Action Results, etc.</span></span>

<span data-ttu-id="ff86d-666">Adres şemasının rolü, rastgele ölçütlere göre adres ve eşleşen uç noktalar arasındaki ilişkiyi oluşturmak için kullanılır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-666">The role of the address scheme is to make the association between the address and matching endpoints by arbitrary criteria:</span></span>

* <span data-ttu-id="ff86d-667">Uç nokta adı şeması, temel bir sözlük araması gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-667">The endpoint name scheme performs a basic dictionary lookup.</span></span>
* <span data-ttu-id="ff86d-668">Yol değerleri şeması, set algoritmasının karmaşık bir en iyi alt kümesine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-668">The route values scheme has a complex best subset of set algorithm.</span></span>

<a name="ambient"></a>

### <a name="ambient-values-and-explicit-values"></a><span data-ttu-id="ff86d-669">Çevresel değerler ve açık değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-669">Ambient values and explicit values</span></span>

<span data-ttu-id="ff86d-670">Yönlendirme geçerli istekten, geçerli isteğin rota değerlerine erişir `HttpContext.Request.RouteValues` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-670">From the current request, routing accesses the route values of the current request `HttpContext.Request.RouteValues`.</span></span> <span data-ttu-id="ff86d-671">Geçerli istekle ilişkili değerler **çevresel değerler**olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-671">The values associated with the current request are referred to as the **ambient values**.</span></span> <span data-ttu-id="ff86d-672">Netlik amacı doğrultusunda, belgeler yöntemlere geçirilen yol değerlerini **açık değerler**olarak ifade eder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-672">For the purpose of clarity, the documentation refers to the route values passed in to methods as **explicit values**.</span></span>

<span data-ttu-id="ff86d-673">Aşağıdaki örnek, ortam değerlerini ve açık değerleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-673">The following example shows ambient values and explicit values.</span></span> <span data-ttu-id="ff86d-674">Geçerli istekten ve açık değerlerden çevresel değerler sağlar: `{ id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-674">It provides ambient values from the current request and explicit values: `{ id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/WidgetController.cs?name=snippet)]

<span data-ttu-id="ff86d-675">Yukarıdaki kod:</span><span class="sxs-lookup"><span data-stu-id="ff86d-675">The preceding code:</span></span>

* <span data-ttu-id="ff86d-676">Döndürdüğü`/Widget/Index/17`</span><span class="sxs-lookup"><span data-stu-id="ff86d-676">Returns `/Widget/Index/17`</span></span>
* <span data-ttu-id="ff86d-677"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> [Dı](xref:fundamentals/dependency-injection)aracılığıyla alır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-677">Gets <xref:Microsoft.AspNetCore.Routing.LinkGenerator> via [DI](xref:fundamentals/dependency-injection).</span></span>

<span data-ttu-id="ff86d-678">Aşağıdaki kod hiçbir çevresel değer ve açık değer sağlar: `{ controller = "Home", action = "Subscribe", id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-678">The following code provides no ambient values and explicit values: `{ controller = "Home", action = "Subscribe", id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/WidgetController.cs?name=snippet2)]

<span data-ttu-id="ff86d-679">Önceki yöntem şunu döndürür`/Home/Subscribe/17`</span><span class="sxs-lookup"><span data-stu-id="ff86d-679">The preceding  method returns `/Home/Subscribe/17`</span></span>

<span data-ttu-id="ff86d-680">Aşağıdaki kod `WidgetController` döndürmektedir `/Widget/Subscribe/17` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-680">The following code in the `WidgetController` returns `/Widget/Subscribe/17`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/WidgetController.cs?name=snippet3)]

<span data-ttu-id="ff86d-681">Aşağıdaki kod, denetleyiciyi geçerli istekteki çevresel değerlerden ve açık değerlerde sağlar: `{ action = "Edit", id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-681">The following code provides the controller from ambient values in the current request and explicit values: `{ action = "Edit", id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/GadgetController.cs?name=snippet)]

<span data-ttu-id="ff86d-682">Yukarıdaki kodda:</span><span class="sxs-lookup"><span data-stu-id="ff86d-682">In the preceding code:</span></span>

* <span data-ttu-id="ff86d-683">`/Gadget/Edit/17`döndürülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-683">`/Gadget/Edit/17` is returned.</span></span>
* <span data-ttu-id="ff86d-684"><xref:Microsoft.AspNetCore.Mvc.ControllerBase.Url>alır <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-684"><xref:Microsoft.AspNetCore.Mvc.ControllerBase.Url> gets the <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>.</span></span>
* <xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Action*>   
<span data-ttu-id="ff86d-685">bir eylem yöntemi için mutlak bir yol içeren bir URL oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-685">generates a URL with an absolute path for an action method.</span></span> <span data-ttu-id="ff86d-686">URL, belirtilen `action` adı ve değerleri içerir `route` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-686">The URL contains the specified `action` name and `route` values.</span></span>

<span data-ttu-id="ff86d-687">Aşağıdaki kod, geçerli istekten ve açık değerlerden çevresel değerler sağlar: `{ page = "./Edit, id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-687">The following code provides ambient values from the current request and explicit values: `{ page = "./Edit, id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Pages/Index.cshtml.cs?name=snippet)]

<span data-ttu-id="ff86d-688">Önceki kod, `url` `/Edit/17` düzenleme Razor sayfası aşağıdaki sayfa yönergesini içerdiğinde olarak ayarlanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-688">The preceding code sets `url` to  `/Edit/17` when the Edit Razor Page contains the following page directive:</span></span>

 `@page "{id:int}"`

<span data-ttu-id="ff86d-689">Düzenleme sayfasında `"{id:int}"` yol şablonu yoksa, `url` olur `/Edit?id=17` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-689">If the Edit page doesn't contain the `"{id:int}"` route template, `url` is `/Edit?id=17`.</span></span>

<span data-ttu-id="ff86d-690">MVC 'nin davranışı, <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> burada açıklanan kurallara ek olarak bir karmaşıklık katmanı ekler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-690">The behavior of MVC's <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> adds a layer of complexity in addition to the rules described here:</span></span>

* <span data-ttu-id="ff86d-691">`IUrlHelper`her zaman geçerli istekten çevresel değerler olarak yol değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-691">`IUrlHelper` always provides the route values from the current request as ambient values.</span></span>
* <span data-ttu-id="ff86d-692">[Iurlhelper. Action](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Action*) , `action` `controller` Geliştirici tarafından geçersiz kılınmadığı sürece her zaman geçerli ve yol değerlerini açık değerler olarak kopyalar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-692">[IUrlHelper.Action](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Action*) always copies the current `action` and `controller` route values as explicit values unless overridden by the developer.</span></span>
* <span data-ttu-id="ff86d-693">[Iurlhelper. Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*) , `page` geçersiz kılınmadığı sürece geçerli yol değerini açık bir değer olarak kopyalar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-693">[IUrlHelper.Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*) always copies the current `page` route value as an explicit value unless overridden.</span></span> <!--by the user-->
* <span data-ttu-id="ff86d-694">`IUrlHelper.Page`her zaman geçersiz `handler` `null` kılınmadığı sürece açık değerler olarak geçerli yol değerini geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-694">`IUrlHelper.Page` always overrides the current `handler` route value with `null` as an explicit values unless overridden.</span></span>

<span data-ttu-id="ff86d-695">MVC kendi kurallarını takip ettiğinden, kullanıcılar genellikle çevresel değerlerinin davranış ayrıntılarına göre görünür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-695">Users are often surprised by the behavioral details of ambient values, because MVC doesn't seem to follow its own rules.</span></span> <span data-ttu-id="ff86d-696">Geçmiş ve uyumluluk nedenleriyle,,, ve gibi belirli rota `action` değerlerinin `controller` `page` `handler` kendi özel durum davranışları vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-696">For historical and compatibility reasons, certain route values such as `action`, `controller`, `page`, and `handler` have their own special-case behavior.</span></span>

<span data-ttu-id="ff86d-697">Tarafından sunulan eşdeğer işlevsellik `LinkGenerator.GetPathByAction` ve `LinkGenerator.GetPathByPage` Bu bozuklukları `IUrlHelper` uyumluluk açısından çoğaltır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-697">The equivalent functionality provided by `LinkGenerator.GetPathByAction` and `LinkGenerator.GetPathByPage` duplicates these anomalies of `IUrlHelper` for compatibility.</span></span>

### <a name="url-generation-process"></a><span data-ttu-id="ff86d-698">URL oluşturma işlemi</span><span class="sxs-lookup"><span data-stu-id="ff86d-698">URL generation process</span></span>

<span data-ttu-id="ff86d-699">Aday uç noktalar kümesi bulunduğunda, URL oluşturma algoritması:</span><span class="sxs-lookup"><span data-stu-id="ff86d-699">Once the set of candidate endpoints are found, the URL generation algorithm:</span></span>

* <span data-ttu-id="ff86d-700">Son noktaları yinelemeli olarak işler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-700">Processes the endpoints iteratively.</span></span>
* <span data-ttu-id="ff86d-701">İlk başarılı sonucu döndürür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-701">Returns the first successful result.</span></span>

<span data-ttu-id="ff86d-702">Bu işlemdeki ilk adıma **Rota değeri ınvalidation**adı verilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-702">The first step in this process is called **route value invalidation**.</span></span>  <span data-ttu-id="ff86d-703">Rota değeri geçersiz kılma, yönlendirmenin çevresel değerlerden hangi rota değerlerinin kullanılması gerektiğini ve göz ardı edileceğine yönelik bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-703">Route value invalidation is the process by which routing decides which route values from the ambient values should be used and which should be ignored.</span></span> <span data-ttu-id="ff86d-704">Her ortam değeri, açık değerlerle birlikte değerlendirilir ve yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-704">Each ambient value is considered and either combined with the explicit values, or ignored.</span></span>

<span data-ttu-id="ff86d-705">Ortam değerlerinin rolünü düşünmenin en iyi yolu, bazı yaygın durumlarda, yazma uygulama geliştiricilerinin kaydedilmesini denedikleri değerlerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-705">The best way to think about the role of ambient values is that they attempt to save application developers typing, in some common cases.</span></span> <span data-ttu-id="ff86d-706">Geleneksel olarak, ortam değerlerinin yararlı olduğu senaryolar MVC ile ilgilidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-706">Traditionally, the scenarios where ambient values are helpful are related to MVC:</span></span>

* <span data-ttu-id="ff86d-707">Aynı denetleyicideki başka bir eyleme bağlantı sırasında, denetleyicinin adının belirtilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-707">When linking to another action in the same controller, the controller name doesn't need to be specified.</span></span>
* <span data-ttu-id="ff86d-708">Aynı alandaki başka bir denetleyiciye bağlanırken, alan adının belirtilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-708">When linking to another controller in the same area, the area name doesn't need to be specified.</span></span>
* <span data-ttu-id="ff86d-709">Aynı eylem yöntemine bağlanırken, rota değerlerinin belirtilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-709">When linking to the same action method, route values don't need to be specified.</span></span>
* <span data-ttu-id="ff86d-710">Uygulamanın başka bir bölümüne bağlanırken, uygulamanın o bölümünde anlamı olmayan rota değerlerini yürütmek istemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-710">When linking to another part of the app, you don't want to carry over route values that have no meaning in that part of the app.</span></span>

<span data-ttu-id="ff86d-711">Ya da `LinkGenerator` `IUrlHelper` Bu dönüşe yapılan çağrılar `null` genellikle yol değeri doğrulaması anlaşılmadığında oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-711">Calls to `LinkGenerator` or `IUrlHelper` that return `null` are usually caused by not understanding route value invalidation.</span></span> <span data-ttu-id="ff86d-712">Yolun sorunu çözüp çözmediğini görmek için daha fazla yol değeri belirtip yol değerlerini geçersiz kılma sorunlarını giderin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-712">Troubleshoot route value invalidation by explicitly specifying more of the route values to see if that solves the problem.</span></span>

<span data-ttu-id="ff86d-713">Rota değeri geçersiz kılma, uygulamanın URL şeması, soldan sağa doğru oluşturulmuş bir hiyerarşi ile hiyerarşik olduğu varsayımıyla birlikte çalışıyor.</span><span class="sxs-lookup"><span data-stu-id="ff86d-713">Route value invalidation works on the assumption that the app's URL scheme is hierarchical, with a hierarchy formed from left-to-right.</span></span> <span data-ttu-id="ff86d-714">`{controller}/{action}/{id?}`Bu uygulamada nasıl çalıştığını daha kolay bir şekilde öğrenmek için temel denetleyici yönlendirme şablonunu göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-714">Consider the basic controller route template `{controller}/{action}/{id?}` to get an intuitive sense of how this works in practice.</span></span> <span data-ttu-id="ff86d-715">Bir değer **değişikliği** , sağda görünen tüm rota değerlerini **geçersiz kılar** .</span><span class="sxs-lookup"><span data-stu-id="ff86d-715">A **change** to a value **invalidates** all of the route values that appear to the right.</span></span> <span data-ttu-id="ff86d-716">Bu, hiyerarşiyle ilgili varsayımını yansıtır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-716">This reflects the assumption about hierarchy.</span></span> <span data-ttu-id="ff86d-717">Uygulamanın için bir ortam değeri varsa `id` ve işlem için farklı bir değer belirtiyorsa `controller` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-717">If the app has an ambient value for `id`, and the operation specifies a different value for the `controller`:</span></span>

* <span data-ttu-id="ff86d-718">`id``{controller}`, öğesinin solunda olduğu için yeniden kullanılamaz `{id?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-718">`id` won't be reused because `{controller}` is to the left of `{id?}`.</span></span>

<span data-ttu-id="ff86d-719">Bu ilkeyi gösteren bazı örnekler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-719">Some examples demonstrating this principle:</span></span>

* <span data-ttu-id="ff86d-720">Açık değerler için bir değer içeriyorsa `id` , ortam değeri `id` yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-720">If the explicit values contain a value for `id`, the ambient value for `id` is ignored.</span></span> <span data-ttu-id="ff86d-721">Ve için çevresel değerler `controller` `action` kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-721">The ambient values for `controller` and `action` can be used.</span></span>
* <span data-ttu-id="ff86d-722">Açık değerler için bir değer içeriyorsa `action` , için herhangi bir ortam değeri `action` yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-722">If the explicit values contain a value for `action`, any ambient value for `action` is ignored.</span></span> <span data-ttu-id="ff86d-723">İçin çevresel değerler `controller` kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-723">The ambient values for `controller` can be used.</span></span> <span data-ttu-id="ff86d-724">İçin açık değeri, `action` için çevresel değerden farklıysa `action` , `id` değer kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-724">If the explicit value for `action` is different from the ambient value for `action`, the `id` value won't be used.</span></span>  <span data-ttu-id="ff86d-725">İçin açık değeri, `action` için çevresel değer ile aynıysa `action` , `id` değer kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-725">If the explicit value for `action` is the same as the ambient value for `action`, the `id` value can be used.</span></span>
* <span data-ttu-id="ff86d-726">Açık değerler için bir değer içeriyorsa `controller` , için herhangi bir ortam değeri `controller` yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-726">If the explicit values contain a value for `controller`, any ambient value for `controller` is ignored.</span></span> <span data-ttu-id="ff86d-727">İçin açık değeri, `controller` için çevresel değerden farklıysa `controller` , `action` ve `id` değerleri kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-727">If the explicit value for `controller` is different from the ambient value for `controller`, the `action` and `id` values won't be used.</span></span> <span data-ttu-id="ff86d-728">İçin açık değeri, `controller` için çevresel değer ile aynıysa `controller` , `action` ve `id` değerleri kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-728">If the explicit value for `controller` is the same as the ambient value for `controller`, the `action` and `id` values can be used.</span></span>

<span data-ttu-id="ff86d-729">Bu işlem, öznitelik yollarının ve adanmış geleneksel yolların varlığı tarafından daha karmaşıktır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-729">This process is further complicated by the existence of attribute routes and dedicated conventional routes.</span></span> <span data-ttu-id="ff86d-730">`{controller}/{action}/{id?}`Yönlendirme parametrelerini kullanarak bir hiyerarşiyi belirtmek gibi denetleyici geleneksel yolları.</span><span class="sxs-lookup"><span data-stu-id="ff86d-730">Controller conventional routes such as `{controller}/{action}/{id?}` specify a hierarchy using route parameters.</span></span> <span data-ttu-id="ff86d-731">[Özel geleneksel yollar](xref:mvc/controllers/routing#dcr) ve denetleyicilere ve sayfalara yönelik [öznitelik yolları](xref:mvc/controllers/routing#ar) için Razor :</span><span class="sxs-lookup"><span data-stu-id="ff86d-731">For [dedicated conventional routes](xref:mvc/controllers/routing#dcr) and [attribute routes](xref:mvc/controllers/routing#ar) to controllers and Razor Pages:</span></span>

* <span data-ttu-id="ff86d-732">Yol değerlerinin bir hiyerarşisi vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-732">There is a hierarchy of route values.</span></span>
* <span data-ttu-id="ff86d-733">Bunlar şablonda görünmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-733">They don't appear in the template.</span></span>

<span data-ttu-id="ff86d-734">Bu gibi durumlarda, URL oluşturma **gerekli değerler** kavramını tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-734">For these cases, URL generation defines the **required values** concept.</span></span> <span data-ttu-id="ff86d-735">Denetleyiciler ve sayfalar tarafından oluşturulan uç noktalar, Razor Rota değeri geçersiz kılma çalışmasına izin veren gerekli değerlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-735">Endpoints created by controllers and Razor Pages have required values specified that allow route value invalidation to work.</span></span>

<span data-ttu-id="ff86d-736">Ayrıntı olarak yol değeri geçersiz kılma algoritması:</span><span class="sxs-lookup"><span data-stu-id="ff86d-736">The route value invalidation algorithm in detail:</span></span>

* <span data-ttu-id="ff86d-737">Gerekli değer adları, yol parametreleriyle birleştirilir ve soldan sağa işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-737">The required value names are combined with the route parameters, then processed from left-to-right.</span></span>
* <span data-ttu-id="ff86d-738">Her parametre için çevresel değer ve açık değer karşılaştırılır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-738">For each parameter, the ambient value and explicit value are compared:</span></span>
    * <span data-ttu-id="ff86d-739">Çevresel değer ve açık değer aynıysa işlem devam eder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-739">If the ambient value and explicit value are the same, the process continues.</span></span>
    * <span data-ttu-id="ff86d-740">Çevresel değer varsa ve açık değer değilse, URL oluşturulurken çevresel değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-740">If the ambient value is present and the explicit value isn't, the ambient value is used when generating the URL.</span></span>
    * <span data-ttu-id="ff86d-741">Çevresel değer yoksa ve açık değer ise, ortam değerini ve sonraki tüm ortam değerlerini reddedin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-741">If the ambient value isn't present and the explicit value is, reject the ambient value and all subsequent ambient values.</span></span>
    * <span data-ttu-id="ff86d-742">Çevresel değer ve açık değer varsa ve iki değer farklıysa, ortam değerini ve sonraki tüm ortam değerlerini reddedin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-742">If the ambient value and the explicit value are present, and the two values are different, reject the ambient value and all subsequent ambient values.</span></span>

<span data-ttu-id="ff86d-743">Bu noktada, URL oluşturma işlemi yol kısıtlamalarını değerlendirmek için hazırlayın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-743">At this point, the URL generation operation is ready to evaluate route constraints.</span></span> <span data-ttu-id="ff86d-744">Kabul edilen değerler kümesi, kısıtlamalara sunulan parametre varsayılan değerleriyle birleştirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-744">The set of accepted values is combined with the parameter default values, which is provided to constraints.</span></span> <span data-ttu-id="ff86d-745">Kısıtlamaların tümü başarılı olursa işlem devam eder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-745">If the constraints all pass, the operation continues.</span></span>

<span data-ttu-id="ff86d-746">Ardından, **kabul edilen değerler** yol şablonunu genişletmek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-746">Next, the **accepted values** can be used to expand the route template.</span></span> <span data-ttu-id="ff86d-747">Yol şablonu işlenir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-747">The route template is processed:</span></span>

* <span data-ttu-id="ff86d-748">Soldan sağa.</span><span class="sxs-lookup"><span data-stu-id="ff86d-748">From left-to-right.</span></span>
* <span data-ttu-id="ff86d-749">Her parametrenin kabul edilen değeri değiştirildi.</span><span class="sxs-lookup"><span data-stu-id="ff86d-749">Each parameter has its accepted value substituted.</span></span>
* <span data-ttu-id="ff86d-750">Aşağıdaki özel durumlar ile:</span><span class="sxs-lookup"><span data-stu-id="ff86d-750">With the following special cases:</span></span>
  * <span data-ttu-id="ff86d-751">Kabul edilen değerlerde bir değer eksikse ve parametrenin varsayılan değeri varsa, varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-751">If the accepted values is missing a value and the parameter has a default value, the default value is used.</span></span>
  * <span data-ttu-id="ff86d-752">Kabul edilen değerlerde bir değer eksikse ve parametresi isteğe bağlıdır, işleme devam eder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-752">If the accepted values is missing a value and the parameter is optional, processing continues.</span></span>
  * <span data-ttu-id="ff86d-753">Eksik bir isteğe bağlı parametrenin sağında herhangi bir rota parametresinin değeri varsa, işlem başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-753">If any route parameter to the right of a missing optional parameter has a value, the operation fails.</span></span>
  * <!-- review default-valued parameters optional parameters --> <span data-ttu-id="ff86d-754">Ardışık varsayılan değerli parametreler ve isteğe bağlı parametreler mümkün olduğunda daraltılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-754">Contiguous default-valued parameters and optional parameters are collapsed where possible.</span></span>

<span data-ttu-id="ff86d-755">Yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-755">Values explicitly provided that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="ff86d-756">Aşağıdaki tabloda, yol şablonu kullanılırken sonuç gösterilmektedir `{controller}/{action}/{id?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-756">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="ff86d-757">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-757">Ambient Values</span></span>                     | <span data-ttu-id="ff86d-758">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-758">Explicit Values</span></span>                        | <span data-ttu-id="ff86d-759">Sonuç</span><span class="sxs-lookup"><span data-stu-id="ff86d-759">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="ff86d-760">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-760">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-761">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-761">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="ff86d-762">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-762">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-763">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-763">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="ff86d-764">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="ff86d-764">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="ff86d-765">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-765">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="ff86d-766">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-766">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-767">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="ff86d-767">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

### <a name="problems-with-route-value-invalidation"></a><span data-ttu-id="ff86d-768">Rota değeri geçersiz kılma sorunları</span><span class="sxs-lookup"><span data-stu-id="ff86d-768">Problems with route value invalidation</span></span>

<span data-ttu-id="ff86d-769">ASP.NET Core 3,0 itibariyle, önceki ASP.NET Core sürümlerde kullanılan bazı URL oluşturma şemaları, URL oluşturma ile iyi çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-769">As of ASP.NET Core 3.0, some URL generation schemes used in earlier ASP.NET Core versions don't work well with URL generation.</span></span> <span data-ttu-id="ff86d-770">ASP.NET Core takım, gelecekteki sürümlerde bu ihtiyaçları karşılamak için özellik eklemeyi planlıyor.</span><span class="sxs-lookup"><span data-stu-id="ff86d-770">The ASP.NET Core team plans to add features to address these needs in a future release.</span></span> <span data-ttu-id="ff86d-771">Şimdilik en iyi çözüm eski yönlendirmeyi kullanmaktır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-771">For now the best solution is to use legacy routing.</span></span>

<span data-ttu-id="ff86d-772">Aşağıdaki kod, yönlendirme tarafından desteklenmeyen bir URL oluşturma şemasına bir örnek gösterir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-772">The following code shows an example of a URL generation scheme that's not supported by routing.</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupUnsupported.cs?name=snippet)]

<span data-ttu-id="ff86d-773">Yukarıdaki kodda, `culture` Yerelleştirme için Route parametresi kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-773">In the preceding code, the `culture` route parameter is used for localization.</span></span> <span data-ttu-id="ff86d-774">Bunun için `culture` parametre her zaman bir çevresel değer olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-774">The desire is to have the `culture` parameter always accepted as an ambient value.</span></span> <span data-ttu-id="ff86d-775">Ancak, `culture` gerekli değerlerin çalışma şekli nedeniyle parametre bir çevresel değer olarak kabul edilmez:</span><span class="sxs-lookup"><span data-stu-id="ff86d-775">However, the `culture` parameter is not accepted as an ambient value because of the way required values work:</span></span>

* <span data-ttu-id="ff86d-776">`"default"`Yol şablonunda, `culture` yol parametresi solunda bulunur `controller` , bu nedenle değişiklikler `controller` geçersiz olmaz `culture` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-776">In the `"default"` route template, the `culture` route parameter is to the left of `controller`, so changes to `controller` won't invalidate `culture`.</span></span>
* <span data-ttu-id="ff86d-777">`"blog"`Yol şablonunda, `culture` yol parametresi, `controller` gerekli değerlerde görüntülenen, öğesinin sağında kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-777">In the `"blog"` route template, the `culture` route parameter is considered to be to the right of `controller`, which appears in the required values.</span></span>

## <a name="configuring-endpoint-metadata"></a><span data-ttu-id="ff86d-778">Uç nokta meta verilerini yapılandırma</span><span class="sxs-lookup"><span data-stu-id="ff86d-778">Configuring endpoint metadata</span></span>

<span data-ttu-id="ff86d-779">Aşağıdaki bağlantılar, uç nokta meta verilerini yapılandırma hakkında bilgi sağlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-779">The following links provide information on configuring endpoint metadata:</span></span>

* [<span data-ttu-id="ff86d-780">Uç nokta yönlendirme ile CORS 'yi etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-780">Enable Cors with endpoint routing</span></span>](xref:security/cors#enable-cors-with-endpoint-routing)
* <span data-ttu-id="ff86d-781">Özel bir öznitelik kullanan [ıauthorizationpolicyprovider örneği](https://github.com/dotnet/AspNetCore/tree/release/3.0/src/Security/samples/CustomPolicyProvider) `[MinimumAgeAuthorize]`</span><span class="sxs-lookup"><span data-stu-id="ff86d-781">[IAuthorizationPolicyProvider sample](https://github.com/dotnet/AspNetCore/tree/release/3.0/src/Security/samples/CustomPolicyProvider) using a custom `[MinimumAgeAuthorize]` attribute</span></span>
* <span data-ttu-id="ff86d-782">[[Yetkilendir] özniteliğiyle test kimlik doğrulaması](xref:security/authentication/identity#test-identity)</span><span class="sxs-lookup"><span data-stu-id="ff86d-782">[Test authentication with the [Authorize] attribute](xref:security/authentication/identity#test-identity)</span></span>
* <xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*>
* <span data-ttu-id="ff86d-783">[[Yetkilendir] özniteliğiyle düzeni seçme](xref:security/authorization/limitingidentitybyscheme#selecting-the-scheme-with-the-authorize-attribute)</span><span class="sxs-lookup"><span data-stu-id="ff86d-783">[Selecting the scheme with the [Authorize] attribute](xref:security/authorization/limitingidentitybyscheme#selecting-the-scheme-with-the-authorize-attribute)</span></span>
* <span data-ttu-id="ff86d-784">[[Yetkilendir] özniteliğini kullanarak ilke uygulama](xref:security/authorization/policies#apply-policies-to-mvc-controllers)</span><span class="sxs-lookup"><span data-stu-id="ff86d-784">[Apply policies using the [Authorize] attribute](xref:security/authorization/policies#apply-policies-to-mvc-controllers)</span></span>
* <xref:security/authorization/roles>

<a name="hostmatch"></a>

## <a name="host-matching-in-routes-with-requirehost"></a><span data-ttu-id="ff86d-785">RequireHost ile yollarla eşleşen ana bilgisayar</span><span class="sxs-lookup"><span data-stu-id="ff86d-785">Host matching in routes with RequireHost</span></span>

<span data-ttu-id="ff86d-786"><xref:Microsoft.AspNetCore.Builder.RoutingEndpointConventionBuilderExtensions.RequireHost*>rotaya belirtilen Konağı gerektiren bir kısıtlama uygular.</span><span class="sxs-lookup"><span data-stu-id="ff86d-786"><xref:Microsoft.AspNetCore.Builder.RoutingEndpointConventionBuilderExtensions.RequireHost*> applies a constraint to the route which requires the specified host.</span></span> <span data-ttu-id="ff86d-787">`RequireHost`Veya [[Host]](xref:Microsoft.AspNetCore.Routing.HostAttribute) parametresi şu olabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-787">The `RequireHost` or [[Host]](xref:Microsoft.AspNetCore.Routing.HostAttribute) parameter can be:</span></span>

* <span data-ttu-id="ff86d-788">Ana bilgisayar: `www.domain.com` , `www.domain.com` herhangi bir bağlantı noktasıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-788">Host: `www.domain.com`, matches `www.domain.com` with any port.</span></span>
* <span data-ttu-id="ff86d-789">Joker karakterle,, `*.domain.com` `www.domain.com` `subdomain.domain.com` veya `www.subdomain.domain.com` herhangi bir bağlantı noktasında eşleşen ana bilgisayar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-789">Host with wildcard: `*.domain.com`, matches `www.domain.com`, `subdomain.domain.com`, or `www.subdomain.domain.com` on any port.</span></span>
* <span data-ttu-id="ff86d-790">Bağlantı noktası: `*:5000` , bağlantı noktası 5000 ile herhangi bir konak ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-790">Port: `*:5000`, matches port 5000 with any host.</span></span>
* <span data-ttu-id="ff86d-791">Konak ve bağlantı noktası: `www.domain.com:5000` veya `*.domain.com:5000` , ana bilgisayar ve bağlantı noktasıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-791">Host and port: `www.domain.com:5000` or `*.domain.com:5000`, matches host and port.</span></span>

<span data-ttu-id="ff86d-792">Veya kullanılarak birden çok parametre belirtilebilir `RequireHost` `[Host]` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-792">Multiple parameters can be specified using `RequireHost` or `[Host]`.</span></span> <span data-ttu-id="ff86d-793">Kısıtlama, parametrelerden herhangi biri için geçerli olan konaklarla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-793">The constraint  matches hosts valid for any of the parameters.</span></span> <span data-ttu-id="ff86d-794">Örneğin,, `[Host("domain.com", "*.domain.com")]` ve ile eşleşir `domain.com` `www.domain.com` `subdomain.domain.com` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-794">For example, `[Host("domain.com", "*.domain.com")]` matches `domain.com`, `www.domain.com`, and `subdomain.domain.com`.</span></span>

<span data-ttu-id="ff86d-795">Aşağıdaki kod, `RequireHost` rotada belirtilen Konağı gerektirmek için kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-795">The following code uses `RequireHost` to require the specified host on the route:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupRequireHost.cs?name=snippet)]

<span data-ttu-id="ff86d-796">Aşağıdaki kod, `[Host]` belirtilen konaklardan herhangi birini gerektirmek için denetleyicideki özniteliğini kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-796">The following code uses the `[Host]` attribute on the controller to require any of the specified hosts:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/ProductController.cs?name=snippet)]

<span data-ttu-id="ff86d-797">`[Host]`Özniteliği hem denetleyici hem de eylem yöntemine uygulandığında:</span><span class="sxs-lookup"><span data-stu-id="ff86d-797">When the `[Host]` attribute is applied to both the controller and action method:</span></span>

* <span data-ttu-id="ff86d-798">Eylem üzerindeki özniteliği kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-798">The attribute on the action is used.</span></span>
* <span data-ttu-id="ff86d-799">Denetleyici özniteliği yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-799">The controller attribute is ignored.</span></span>

## <a name="performance-guidance-for-routing"></a><span data-ttu-id="ff86d-800">Yönlendirme için performans Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="ff86d-800">Performance guidance for routing</span></span>

<span data-ttu-id="ff86d-801">Yönlendirmenin çoğu, performansı artırmak için ASP.NET Core 3,0 ' de güncelleştirildi.</span><span class="sxs-lookup"><span data-stu-id="ff86d-801">Most of routing was updated in ASP.NET Core 3.0 to increase performance.</span></span>

<span data-ttu-id="ff86d-802">Bir uygulamada performans sorunları olduğunda, yönlendirme genellikle sorun olarak şüpheli.</span><span class="sxs-lookup"><span data-stu-id="ff86d-802">When an app has performance problems, routing is often suspected as the problem.</span></span> <span data-ttu-id="ff86d-803">Yönlendirme nedeni, denetleyiciler ve sayfalar gibi çerçevelerin, Razor kendi günlük iletilerinde çerçeve içinde harcanan süreyi raporlamadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-803">The reason routing is suspected is that frameworks like controllers and Razor Pages report the amount of time spent inside the framework in their logging messages.</span></span> <span data-ttu-id="ff86d-804">Denetleyiciler tarafından bildirilen zaman ve isteğin toplam süresi arasında önemli bir fark olduğunda:</span><span class="sxs-lookup"><span data-stu-id="ff86d-804">When there's a significant difference between the time reported by controllers and the total time of the request:</span></span>

* <span data-ttu-id="ff86d-805">Geliştiriciler, uygulamanın kodunu sorunun kaynağı olarak ortadan kaldırır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-805">Developers eliminate their app code as the source of the problem.</span></span>
* <span data-ttu-id="ff86d-806">Yönlendirmenin neden olduğunu varsaymak yaygındır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-806">It's common to assume routing is the cause.</span></span>

<span data-ttu-id="ff86d-807">Yönlendirme, binlerce uç nokta kullanılarak performansa göre test edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-807">Routing is performance tested using thousands of endpoints.</span></span> <span data-ttu-id="ff86d-808">Tipik bir uygulamanın çok büyük bir performans sorunuyla karşılaşmasının pek olası olması normaldir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-808">It's unlikely that a typical app will encounter a performance problem just by being too large.</span></span> <span data-ttu-id="ff86d-809">Yavaş yönlendirme performansının en yaygın temel nedenlerinden biri genellikle hatalı davranan özel bir ara yazılım olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-809">The most common root cause of slow routing performance is usually a badly-behaving custom middleware.</span></span>

<span data-ttu-id="ff86d-810">Aşağıdaki kod örneğinde, gecikme kaynağını daraltmak için temel bir teknik gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-810">This following code sample demonstrates a basic technique for narrowing down the source of delay:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupDelay.cs?name=snippet)]

<span data-ttu-id="ff86d-811">Zaman yönlendirme:</span><span class="sxs-lookup"><span data-stu-id="ff86d-811">To time routing:</span></span>

* <span data-ttu-id="ff86d-812">Yukarıdaki kodda gösterilen zamanlama ara yazılımı kopyasıyla her bir ara yazılımı ayırma.</span><span class="sxs-lookup"><span data-stu-id="ff86d-812">Interleave each middleware with a copy of the timing middleware shown in the preceding code.</span></span>
* <span data-ttu-id="ff86d-813">Zamanlama verilerinin kodla ilişkilendirilmesi için benzersiz bir tanımlayıcı ekleyin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-813">Add a unique identifier to correlate the timing data with the code.</span></span>

<span data-ttu-id="ff86d-814">Bu, örneğin, çok önemli olduğunda gecikmeyi daraltmak için temel bir yoldur `10ms` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-814">This is a basic way to narrow down the delay when it's significant, for example, more than `10ms`.</span></span>  <span data-ttu-id="ff86d-815">`Time 2` `Time 1` Raporlar, ara yazılım içinde harcanan süreyi çıkararak `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-815">Subtracting `Time 2` from `Time 1` reports the time spent inside the `UseRouting` middleware.</span></span>

<span data-ttu-id="ff86d-816">Aşağıdaki kod, önceki zamanlama koduna yönelik daha küçük bir yaklaşım kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-816">The following code uses a more compact approach to the preceding timing code:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupSW.cs?name=snippetSW)]

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupSW.cs?name=snippet)]

### <a name="potentially-expensive-routing-features"></a><span data-ttu-id="ff86d-817">Potansiyel olarak pahalı yönlendirme özellikleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-817">Potentially expensive routing features</span></span>

<span data-ttu-id="ff86d-818">Aşağıdaki listede, temel yol şablonlarıyla karşılaştırıldığında görece maliyetli yönlendirme özellikleri hakkında bazı Öngörüler sunulmaktadır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-818">The following list provides some insight into routing features that are relatively expensive compared with basic route templates:</span></span>

* <span data-ttu-id="ff86d-819">Normal ifadeler: karmaşık ifadeler yazmak veya az miktarda giriş ile uzun süre çalışma süresi olması mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-819">Regular expressions: It's possible to write regular expressions that are complex, or have long running time with a small amount of input.</span></span>

* <span data-ttu-id="ff86d-820">Karmaşık segmentler ( `{x}-{y}-{z}` ):</span><span class="sxs-lookup"><span data-stu-id="ff86d-820">Complex segments (`{x}-{y}-{z}`):</span></span> 
  * <span data-ttu-id="ff86d-821">, Normal bir URL yolu segmentini ayrıştırmaktan önemli ölçüde daha pahalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-821">Are significantly more expensive than parsing a regular URL path segment.</span></span>
  * <span data-ttu-id="ff86d-822">Daha fazla sayıda alt dizenin ayrılması sonucunu elde edin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-822">Result in many more substrings being allocated.</span></span>
  * <span data-ttu-id="ff86d-823">Karmaşık kesim mantığı ASP.NET Core 3,0 yönlendirme performansı güncelleştirmesinde güncelleştirilmedi.</span><span class="sxs-lookup"><span data-stu-id="ff86d-823">The complex segment logic was not updated in ASP.NET Core 3.0 routing performance update.</span></span>

* <span data-ttu-id="ff86d-824">Zaman uyumlu veri erişimi: birçok karmaşık uygulamanın, yönlendirmenin bir parçası olarak veritabanı erişimi vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-824">Synchronous data access: Many complex apps have database access as part of their routing.</span></span> <span data-ttu-id="ff86d-825">ASP.NET Core 2,2 ve önceki yönlendirme, veritabanı erişim yönlendirmesini desteklemek için doğru genişletilebilirlik noktaları sağlamayabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-825">ASP.NET Core 2.2 and earlier routing might not provide the right extensibility points to support database access routing.</span></span> <span data-ttu-id="ff86d-826">Örneğin, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> , ve <xref:Microsoft.AspNetCore.Mvc.ActionConstraints.IActionConstraint> zaman uyumludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-826">For example, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, and <xref:Microsoft.AspNetCore.Mvc.ActionConstraints.IActionConstraint> are synchronous.</span></span> <span data-ttu-id="ff86d-827">Ve gibi genişletilebilirlik noktaları <xref:Microsoft.AspNetCore.Routing.MatcherPolicy> <xref:Microsoft.AspNetCore.Routing.EndpointSelectorContext> zaman uyumsuzdur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-827">Extensibility points such as <xref:Microsoft.AspNetCore.Routing.MatcherPolicy> and <xref:Microsoft.AspNetCore.Routing.EndpointSelectorContext> are asynchronous.</span></span>

## <a name="guidance-for-library-authors"></a><span data-ttu-id="ff86d-828">Kitaplık yazarları için kılavuz</span><span class="sxs-lookup"><span data-stu-id="ff86d-828">Guidance for library authors</span></span>

<span data-ttu-id="ff86d-829">Bu bölüm, yönlendirmenin üzerine binen üst düzey kitaplık yazarları için yönergeler içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-829">This section contains guidance for library authors building on top of routing.</span></span> <span data-ttu-id="ff86d-830">Bu ayrıntılar, uygulama geliştiricilerinin yönlendirmeyi genişleten kitaplıkları ve çerçeveleri kullanarak iyi bir deneyim aldığından emin olmak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-830">These details are intended to ensure that app developers have a good experience using libraries and frameworks that extend routing.</span></span>

### <a name="define-endpoints"></a><span data-ttu-id="ff86d-831">Uç noktaları tanımlama</span><span class="sxs-lookup"><span data-stu-id="ff86d-831">Define endpoints</span></span>

<span data-ttu-id="ff86d-832">URL eşleştirmesi için yönlendirmeyi kullanan bir çerçeve oluşturmak için, en üstünde yapı yapan bir kullanıcı deneyimi tanımlayarak başlayın <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-832">To create a framework that uses routing for URL matching, start by defining a user experience that builds on top of <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span>

<span data-ttu-id="ff86d-833">Üzerine derleme **yapın** <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-833">**DO** build on top of <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder>.</span></span> <span data-ttu-id="ff86d-834">Bu, kullanıcıların kendi çatısını diğer ASP.NET Core özellikleriyle karışıklık olmadan oluşturma olanağı sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-834">This allows users to compose your framework with other ASP.NET Core features without confusion.</span></span> <span data-ttu-id="ff86d-835">Her ASP.NET Core şablonu yönlendirme içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-835">Every ASP.NET Core template includes routing.</span></span> <span data-ttu-id="ff86d-836">Yönlendirmenin mevcut olduğunu ve kullanıcılara tanıdık olduğunu varsayın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-836">Assume routing is present and familiar for users.</span></span>

```csharp
app.UseEndpoints(endpoints =>
{
    // Your framework
    endpoints.MapMyFramework(...);

    endpoints.MapHealthChecks("/healthz");
});
```

<span data-ttu-id="ff86d-837">Bu, uygulayan bir çağrıdan Sealed somut **tür döndürün** `MapMyFramework(...)` <xref:Microsoft.AspNetCore.Builder.IEndpointConventionBuilder> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-837">**DO** return a sealed concrete type from a call to `MapMyFramework(...)` that implements <xref:Microsoft.AspNetCore.Builder.IEndpointConventionBuilder>.</span></span> <span data-ttu-id="ff86d-838">Çoğu Framework `Map...` yöntemi bu kalıbı izler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-838">Most framework `Map...` methods follow this pattern.</span></span> <span data-ttu-id="ff86d-839">`IEndpointConventionBuilder`Arabirim:</span><span class="sxs-lookup"><span data-stu-id="ff86d-839">The `IEndpointConventionBuilder` interface:</span></span>

* <span data-ttu-id="ff86d-840">Meta verilerin bir kısmını bilme olanağı tanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-840">Allows composability of metadata.</span></span>
* <span data-ttu-id="ff86d-841">, Çeşitli uzantı yöntemleriyle hedeflenmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-841">Is targeted by a variety of extension methods.</span></span>

<span data-ttu-id="ff86d-842">Kendi türünü bildirmek, çerçeveye özgü işlevselliği oluşturucuya eklemenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-842">Declaring your own type allows you to add your own framework-specific functionality to the builder.</span></span> <span data-ttu-id="ff86d-843">Çerçeve tarafından tanımlanan bir oluşturucuyu sarmadan ve çağrıları iletecek.</span><span class="sxs-lookup"><span data-stu-id="ff86d-843">It's ok to wrap a framework-declared builder and forward calls to it.</span></span>

```csharp
app.UseEndpoints(endpoints =>
{
    // Your framework
    endpoints.MapMyFramework(...).RequrireAuthorization()
                                 .WithMyFrameworkFeature(awesome: true);

    endpoints.MapHealthChecks("/healthz");
});
```

<span data-ttu-id="ff86d-844">**CONSIDER** Kendinizinkini yazmayı düşünün <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-844">**CONSIDER** writing your own <xref:Microsoft.AspNetCore.Routing.EndpointDataSource>.</span></span> <span data-ttu-id="ff86d-845">`EndpointDataSource`, uç nokta koleksiyonunu bildirmek ve güncelleştirmek için alt düzey temel değer.</span><span class="sxs-lookup"><span data-stu-id="ff86d-845">`EndpointDataSource` is the low-level primitive for declaring and updating a collection of endpoints.</span></span> <span data-ttu-id="ff86d-846">`EndpointDataSource`, denetleyiciler ve sayfalar tarafından kullanılan güçlü bir API 'dir Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-846">`EndpointDataSource` is a powerful API used by controllers and Razor Pages.</span></span>

<span data-ttu-id="ff86d-847">Yönlendirme testlerinin, güncelleştirme olmayan bir veri kaynağına ilişkin [temel bir örneği](https://github.com/aspnet/AspNetCore/blob/master/src/Http/Routing/test/testassets/RoutingSandbox/Framework/FrameworkEndpointDataSource.cs#L17) vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-847">The routing tests have a [basic example](https://github.com/aspnet/AspNetCore/blob/master/src/Http/Routing/test/testassets/RoutingSandbox/Framework/FrameworkEndpointDataSource.cs#L17) of a non-updating data source.</span></span>

<span data-ttu-id="ff86d-848">Varsayılan olarak **kaydetmeyi denemeyin** `EndpointDataSource` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-848">**DO NOT** attempt to register an `EndpointDataSource` by default.</span></span> <span data-ttu-id="ff86d-849">Kullanıcıların çatısını kaydetmesini gerektir <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-849">Require users to register your framework in <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span> <span data-ttu-id="ff86d-850">Yönlendirmenin felseı, varsayılan olarak hiçbir şey dahil değildir ve `UseEndpoints` uç noktaların kaydedileceği yerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-850">The philosophy of routing is that nothing is included by default, and that `UseEndpoints` is the place to register endpoints.</span></span>

### <a name="creating-routing-integrated-middleware"></a><span data-ttu-id="ff86d-851">Yönlendirme ile tümleşik ara yazılım oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-851">Creating routing-integrated middleware</span></span>

<span data-ttu-id="ff86d-852">Meta veri türlerini arabirim olarak tanımlamayı **düşünün** .</span><span class="sxs-lookup"><span data-stu-id="ff86d-852">**CONSIDER** defining metadata types as an interface.</span></span>

<span data-ttu-id="ff86d-853">Meta veri türlerini sınıflar ve yöntemlerde öznitelik olarak kullanmayı **mümkün hale getirir** .</span><span class="sxs-lookup"><span data-stu-id="ff86d-853">**DO** make it possible to use metadata types as an attribute on classes and methods.</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/ICoolMetadata.cs?name=snippet2)]

<span data-ttu-id="ff86d-854">Denetleyiciler ve sayfalar gibi çerçeveler Razor , meta veri özniteliklerinin türlere ve yöntemlere uygulanmasını destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-854">Frameworks like controllers and Razor Pages support applying metadata attributes to types and methods.</span></span> <span data-ttu-id="ff86d-855">Meta veri türleri bildirirseniz:</span><span class="sxs-lookup"><span data-stu-id="ff86d-855">If you declare metadata types:</span></span>

* <span data-ttu-id="ff86d-856">Bunları [öznitelik](/dotnet/csharp/programming-guide/concepts/attributes/)olarak erişilebilir yapın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-856">Make them accessible as [attributes](/dotnet/csharp/programming-guide/concepts/attributes/).</span></span>
* <span data-ttu-id="ff86d-857">Çoğu Kullanıcı öznitelikleri uygulamayla tanıdık gelecektir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-857">Most users are familiar with applying attributes.</span></span>

<span data-ttu-id="ff86d-858">Bir arabirim olarak bir meta veri türü bildirmek, başka bir esneklik katmanı ekler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-858">Declaring a metadata type as an interface adds another layer of flexibility:</span></span>

* <span data-ttu-id="ff86d-859">Arabirimler birleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-859">Interfaces are composable.</span></span>
* <span data-ttu-id="ff86d-860">Geliştiriciler, birden çok ilkeyi birleştiren kendi türlerini bildirebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-860">Developers can declare their own types that combine multiple policies.</span></span>

<span data-ttu-id="ff86d-861">Aşağıdaki örnekte gösterildiği gibi meta verileri **geçersiz kılmak mümkün hale gelir** :</span><span class="sxs-lookup"><span data-stu-id="ff86d-861">**DO** make it possible to override metadata, as shown in the following example:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/ICoolMetadata.cs?name=snippet)]

<span data-ttu-id="ff86d-862">Bu yönergeleri izlemek için en iyi yol, **işaretleyici meta verileri**tanımlamayı önleyemaktır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-862">The best way to follow these guidelines is to avoid defining **marker metadata**:</span></span>

* <span data-ttu-id="ff86d-863">Yalnızca meta veri türünün varolup olmadığını göz atın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-863">Don't just look for the presence of a metadata type.</span></span>
* <span data-ttu-id="ff86d-864">Meta verilerde bir özellik tanımlayın ve özelliği denetleyin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-864">Define a property on the metadata and check the property.</span></span>

<span data-ttu-id="ff86d-865">Meta veri koleksiyonu sıralanmıştır ve önceliğe göre geçersiz kılmayı destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-865">The metadata collection is ordered and supports overriding by priority.</span></span> <span data-ttu-id="ff86d-866">Denetleyiciler söz konusu olduğunda, eylem yöntemindeki meta veriler en özeldir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-866">In the case of controllers, metadata on the action method is most specific.</span></span>

<span data-ttu-id="ff86d-867">Ara yazılımı, yönlendirme olmadan ve ile **yararlı hale getirin** .</span><span class="sxs-lookup"><span data-stu-id="ff86d-867">**DO** make middleware useful with and without routing.</span></span>

```csharp
app.UseRouting();

app.UseAuthorization(new AuthorizationPolicy() { ... });

app.UseEndpoints(endpoints =>
{
    // Your framework
    endpoints.MapMyFramework(...).RequrireAuthorization();
});
```

<span data-ttu-id="ff86d-868">Bu yönergeye örnek olarak, ara yazılımı göz önünde bulundurun `UseAuthorization` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-868">As an example of this guideline, consider the `UseAuthorization` middleware.</span></span> <span data-ttu-id="ff86d-869">Yetkilendirme ara yazılımı, bir geri dönüş ilkesi geçirmenize olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-869">The authorization middleware allows you to pass in a fallback policy.</span></span> <!-- shown where?  (shown here) --> <span data-ttu-id="ff86d-870">Belirtilmişse, geri dönüş ilkesi her ikisi için de geçerlidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-870">The fallback policy, if specified, applies to both:</span></span>

* <span data-ttu-id="ff86d-871">Belirli bir ilke olmadan uç noktalar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-871">Endpoints without a specified policy.</span></span>
* <span data-ttu-id="ff86d-872">Bir uç noktasıyla eşleşmeyen istekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-872">Requests that don't match an endpoint.</span></span>

<span data-ttu-id="ff86d-873">Bu, yetkilendirme ara yazılımını yönlendirme bağlamı dışında yararlı hale getirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-873">This makes the authorization middleware useful outside of the context of routing.</span></span> <span data-ttu-id="ff86d-874">Yetkilendirme ara yazılımı geleneksel ara yazılım programlama için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-874">The authorization middleware can be used for traditional middleware programming.</span></span>

[!INCLUDE[](~/includes/dbg-route.md)]

::: moniker-end

::: moniker range="= aspnetcore-2.2"

<span data-ttu-id="ff86d-875">Yönlendirme, istek URI 'Lerini uç noktalarla eşleştirmekten ve gelen istekleri bu uç noktalara gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-875">Routing is responsible for mapping request URIs to endpoints and dispatching incoming requests to those endpoints.</span></span> <span data-ttu-id="ff86d-876">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-876">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="ff86d-877">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-877">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="ff86d-878">Uygulamadan yönlendirme bilgilerini kullanarak, yönlendirme, uç noktalarıyla eşlenen URL 'Ler de oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-878">Using route information from the app, routing is also able to generate URLs that map to endpoints.</span></span>

<span data-ttu-id="ff86d-879">ASP.NET Core 2,2 ' de en son yönlendirme senaryolarını kullanmak için, ' de MVC Hizmetleri kaydının [Uyumluluk sürümünü](xref:mvc/compatibility-version) belirtin `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-879">To use the latest routing scenarios in ASP.NET Core 2.2, specify the [compatibility version](xref:mvc/compatibility-version) to the MVC services registration in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc()
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

<span data-ttu-id="ff86d-880">Bu <xref:Microsoft.AspNetCore.Mvc.MvcOptions.EnableEndpointRouting> seçenek yönlendirmenin <xref:Microsoft.AspNetCore.Routing.IRouter> ASP.NET Core 2,1 veya önceki bir sürümünün uç nokta tabanlı Logic mi yoksa tabanlı mantığını mı kullanması gerektiğini belirler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-880">The <xref:Microsoft.AspNetCore.Mvc.MvcOptions.EnableEndpointRouting> option determines if routing should internally use endpoint-based logic or the <xref:Microsoft.AspNetCore.Routing.IRouter>-based logic of ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="ff86d-881">Uyumluluk sürümü 2,2 veya üzeri bir değere ayarlandığında, varsayılan değer olur `true` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-881">When the compatibility version is set to 2.2 or later, the default value is `true`.</span></span> <span data-ttu-id="ff86d-882">`false`Önceki yönlendirme mantığını kullanmak için değeri olarak ayarlayın:</span><span class="sxs-lookup"><span data-stu-id="ff86d-882">Set the value to `false` to use the prior routing logic:</span></span>

```csharp
// Use the routing logic of ASP.NET Core 2.1 or earlier:
services.AddMvc(options => options.EnableEndpointRouting = false)
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

<span data-ttu-id="ff86d-883">Tabanlı yönlendirme hakkında daha fazla bilgi için <xref:Microsoft.AspNetCore.Routing.IRouter> [bu konunun ASP.NET Core 2,1 sürümüne](/aspnet/core/fundamentals/routing?view=aspnetcore-2.1)bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-883">For more information on <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, see the [ASP.NET Core 2.1 version of this topic](/aspnet/core/fundamentals/routing?view=aspnetcore-2.1).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="ff86d-884">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-884">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="ff86d-885">ASP.NET Core MVC yönlendirme hakkında bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-885">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="ff86d-886">Sayfalardaki yönlendirme kuralları hakkında daha fazla bilgi için Razor bkz <xref:razor-pages/razor-pages-conventions> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-886">For information on routing conventions in Razor Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="ff86d-887">[Örnek kodu görüntüleme veya indirme](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceği](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="ff86d-887">[View or download sample code](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="ff86d-888">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-888">Routing basics</span></span>

<span data-ttu-id="ff86d-889">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-889">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="ff86d-890">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-890">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="ff86d-891">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-891">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="ff86d-892">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-892">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="ff86d-893">Geliştiriciler, genellikle [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda uygulamanın yüksek trafikte daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-893">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span> <span data-ttu-id="ff86d-894">Özelleştirilmiş durumlar örnekleri, blog ve eticaret uç noktaları içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-894">Specialized situations examples include, blog and ecommerce endpoints.</span></span>

<span data-ttu-id="ff86d-895">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-895">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="ff86d-896">Bu, örneğin, al ve POST gibi birçok işlemin aynı mantıksal kaynakta aynı URL 'YI kullanması anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-896">This means that many operations, for example, GET, and POST, on the same logical resource use the same URL.</span></span> <span data-ttu-id="ff86d-897">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-897">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

Razor<span data-ttu-id="ff86d-898">Sayfalar uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-898"> Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="ff86d-899">Sayfa yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-899">Additional conventions are available that allow you to customize Razor Pages routing behavior.</span></span> <span data-ttu-id="ff86d-900">Daha fazla bilgi için <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-900">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="ff86d-901">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-901">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="ff86d-902">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-902">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="ff86d-903">Yönlendirme *endpoints* `Endpoint` , bir uygulamadaki mantıksal uç noktaları temsil etmek için uç noktaları () kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-903">Routing uses *endpoints* (`Endpoint`) to represent logical endpoints in an app.</span></span>

<span data-ttu-id="ff86d-904">Bir uç nokta, istekleri işlemek için bir temsilci ve rastgele meta veri koleksiyonunu tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-904">An endpoint defines a delegate to process requests and a collection of arbitrary metadata.</span></span> <span data-ttu-id="ff86d-905">Meta veriler, her bir uç noktaya eklenen ilkelere ve yapılandırmaya bağlı olarak çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-905">The metadata is used implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span>

<span data-ttu-id="ff86d-906">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-906">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="ff86d-907">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-907">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="ff86d-908">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-908">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="ff86d-909"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-909"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="ff86d-910">MVC/sayfalar gibi uygulama modelleri, Razor yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm uç noktalarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-910">App models, such as MVC/Razor Pages, register all of their endpoints, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="ff86d-911">Yönlendirme gerçekleştirme, yönlendirme kararlarını, ara yazılım ardışık düzeninde istediğiniz yere getirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-911">The routing implementation makes routing decisions wherever desired in the middleware pipeline.</span></span>
* <span data-ttu-id="ff86d-912">Bir yönlendirme ara yazılımı, belirli bir istek URI 'SI için yönlendirme ara yazılımı uç noktası kararının sonucunu inceleyebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-912">Middleware that appears after a Routing Middleware can inspect the result of the Routing Middleware's endpoint decision for a given request URI.</span></span>
* <span data-ttu-id="ff86d-913">Uygulamadaki tüm uç noktaları, ara yazılım ardışık düzeninde herhangi bir yerde listelemek mümkündür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-913">It's possible to enumerate all of the endpoints in the app anywhere in the middleware pipeline.</span></span>
* <span data-ttu-id="ff86d-914">Bir uygulama, uç nokta bilgilerine göre URL 'Ler oluşturmak için (örneğin, yeniden yönlendirme veya bağlantılar için) yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-914">An app can use routing to generate URLs (for example, for redirection or links) based on endpoint information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="ff86d-915">URL oluşturma, rastgele genişletilebilirliği destekleyen adreslere dayalıdır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-915">URL generation is based on addresses, which support arbitrary extensibility:</span></span>

  * <span data-ttu-id="ff86d-916">Bağlantı Oluşturucu API 'SI ( <xref:Microsoft.AspNetCore.Routing.LinkGenerator> ), URL oluşturmak için [bağımlılık ekleme (dı)](xref:fundamentals/dependency-injection) kullanılarak her yerde çözülebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-916">The Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>) can be resolved anywhere using [dependency injection (DI)](xref:fundamentals/dependency-injection) to generate URLs.</span></span>
  * <span data-ttu-id="ff86d-917">Bağlantı Oluşturucu API 'sinin dı aracılığıyla kullanılamadığı durumlarda <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> URL 'leri derlemek için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-917">Where the Link Generator API isn't available via DI, <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>

> [!NOTE]
> <span data-ttu-id="ff86d-918">ASP.NET Core 2,2 ' de uç nokta yönlendirme yayını ile, uç nokta bağlama MVC/ Razor sayfa eylemleri ve sayfaları ile sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-918">With the release of endpoint routing in ASP.NET Core 2.2, endpoint linking is limited to MVC/Razor Pages actions and pages.</span></span> <span data-ttu-id="ff86d-919">Uç nokta bağlama yeteneklerinin genişletmeleri, gelecek sürümlerde planlanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-919">The expansions of endpoint-linking capabilities is planned for future releases.</span></span>

<span data-ttu-id="ff86d-920">Yönlendirme, sınıfı tarafından bulunan [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-920">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="ff86d-921">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve sayfalar uygulamalarında yönlendirmeyi işler Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-921">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and Razor Pages apps.</span></span> <span data-ttu-id="ff86d-922">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-922">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="ff86d-923">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-923">URL matching</span></span>

<span data-ttu-id="ff86d-924">URL eşleştirme, yönlendirmenin bir *uç noktaya*gelen isteği gönderdiği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-924">URL matching is the process by which routing dispatches an incoming request to an *endpoint*.</span></span> <span data-ttu-id="ff86d-925">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-925">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="ff86d-926">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-926">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="ff86d-927">Uç nokta yönlendirmesinde yönlendirme sistemi tüm gönderme kararlarından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-927">The routing system in endpoint routing is responsible for all dispatching decisions.</span></span> <span data-ttu-id="ff86d-928">Ara yazılım, seçili uç noktaya göre ilkeler uyguladığı için, yönlendirme sisteminde güvenlik ilkelerinin dağıtımını etkileyebilecek veya uygulamanın uygulanmasını etkileyebilecek herhangi bir kararın olması önemlidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-928">Since the middleware applies policies based on the selected endpoint, it's important that any decision that can affect dispatching or the application of security policies is made inside the routing system.</span></span>

<span data-ttu-id="ff86d-929">Uç nokta temsilcisi yürütüldüğünde, [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özellikleri, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-929">When the endpoint delegate is executed, the properties of [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) are set to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="ff86d-930">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-930">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="ff86d-931">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-931">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="ff86d-932">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-932">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="ff86d-933"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*>Her rotayla durum verilerinin ilişkilendirilmesini desteklemek için sağlanır, böylece uygulama hangi yolun eşleştiğini temel alarak kararlar alabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-933"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="ff86d-934">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="ff86d-934">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="ff86d-935">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-935">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="ff86d-936">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-936">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="ff86d-937">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-937">Routes can be nested inside of one another.</span></span> <span data-ttu-id="ff86d-938"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers>Özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-938">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="ff86d-939">Genellikle, içindeki ilk öğe <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-939">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="ff86d-940">İçindeki son öğe, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-940">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation-with-linkgenerator"></a><span data-ttu-id="ff86d-941">LinkGenerator ile URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-941">URL generation with LinkGenerator</span></span>

<span data-ttu-id="ff86d-942">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-942">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="ff86d-943">Bu, uç noktalarınız ve bunlara erişen URL 'Ler arasında mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-943">This allows for a logical separation between your endpoints and the URLs that access them.</span></span>

<span data-ttu-id="ff86d-944">Endpoint Routing, bağlantı Oluşturucu API 'SI ( <xref:Microsoft.AspNetCore.Routing.LinkGenerator> ) içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-944">Endpoint routing includes the Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>).</span></span> <span data-ttu-id="ff86d-945"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>, [dı](xref:fundamentals/dependency-injection)'den alınabilecek bir tek hizmettir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-945"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is a singleton service that can be retrieved from [DI](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="ff86d-946">API, yürütülen bir istek bağlamı dışında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-946">The API can be used outside of the context of an executing request.</span></span> <span data-ttu-id="ff86d-947"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper> <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> [Etiket Yardımcıları](xref:mvc/views/tag-helpers/intro), HTML Yardımcıları ve [eylem sonuçları](xref:mvc/controllers/actions)gibi mvc 'nin ve senaryolarına yönelik senaryolar, bağlantı oluşturma özellikleri sağlamak için bağlantı oluşturucuyu kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-947">MVC's <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> and scenarios that rely on <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, such as [Tag Helpers](xref:mvc/views/tag-helpers/intro), HTML Helpers, and [Action Results](xref:mvc/controllers/actions), use the link generator to provide link generating capabilities.</span></span>

<span data-ttu-id="ff86d-948">Bağlantı Oluşturucu, bir *Adres* ve *Adres şemaları*kavramıyla desteklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-948">The link generator is backed by the concept of an *address* and *address schemes*.</span></span> <span data-ttu-id="ff86d-949">Adres şeması, bağlantı oluşturma için göz önünde bulundurmanız gereken uç noktaları belirlemenin bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-949">An address scheme is a way of determining the endpoints that should be considered for link generation.</span></span> <span data-ttu-id="ff86d-950">Örneğin, çok sayıda kullanıcının yol adı ve yol değerleri senaryoları, MVC/ Razor sayfalardan bir adres düzeni olarak uygulanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-950">For example, the route name and route values scenarios many users are familiar with from MVC/Razor Pages are implemented as an address scheme.</span></span>

<span data-ttu-id="ff86d-951">Bağlantı Oluşturucu, MVC/ Razor Pages eylemlerine ve sayfalarına aşağıdaki genişletme yöntemleri aracılığıyla bağlanabilir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-951">The link generator can link to MVC/Razor Pages actions and pages via the following extension methods:</span></span>

* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetPathByAction*>
* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetUriByAction*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetPathByPage*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetUriByPage*>

<span data-ttu-id="ff86d-952">Bu yöntemlerin aşırı yüklemesi, içeren bağımsız değişkenleri kabul eder `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-952">An overload of these methods accepts arguments that include the `HttpContext`.</span></span> <span data-ttu-id="ff86d-953">Bu yöntemler işlevsel olarak eşdeğerdir, `Url.Action` `Url.Page` ancak ek esneklik ve seçenekler sunar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-953">These methods are functionally equivalent to `Url.Action` and `Url.Page` but offer additional flexibility and options.</span></span>

<span data-ttu-id="ff86d-954">`GetPath*`Yöntemler, `Url.Action` `Url.Page` mutlak bir yol içeren bir URI oluşturabilen ve ' a benzerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-954">The `GetPath*` methods are most similar to `Url.Action` and `Url.Page` in that they generate a URI containing an absolute path.</span></span> <span data-ttu-id="ff86d-955">`GetUri*`Yöntemler her zaman bir düzen ve konak içeren mutlak BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-955">The `GetUri*` methods always generate an absolute URI containing a scheme and host.</span></span> <span data-ttu-id="ff86d-956">Bir öğesini kabul eden yöntemler, `HttpContext` yürütülmekte olan istek bağlamında BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-956">The methods that accept an `HttpContext` generate a URI in the context of the executing request.</span></span> <span data-ttu-id="ff86d-957">Ortam yolu değerleri, URL taban yolu, şeması ve yürütülen istekten ana bilgisayar, geçersiz kılınmadıkça kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-957">The ambient route values, URL base path, scheme, and host from the executing request are used unless overridden.</span></span>

<span data-ttu-id="ff86d-958"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>bir adresle çağırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-958"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is called with an address.</span></span> <span data-ttu-id="ff86d-959">URI oluşturma iki adımda gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-959">Generating a URI occurs in two steps:</span></span>

1. <span data-ttu-id="ff86d-960">Adres, adresle eşleşen bir uç nokta listesine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-960">An address is bound to a list of endpoints that match the address.</span></span>
1. <span data-ttu-id="ff86d-961">Her uç nokta `RoutePattern` , sağlanan değerlerle eşleşen bir yol deseninin bulunana kadar değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-961">Each endpoint's `RoutePattern` is evaluated until a route pattern that matches the supplied values is found.</span></span> <span data-ttu-id="ff86d-962">Elde edilen çıktı, bağlantı oluşturucuya sağlanan diğer URI parçalarıyla birleştirilir ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-962">The resulting output is combined with the other URI parts supplied to the link generator and returned.</span></span>

<span data-ttu-id="ff86d-963">Tarafından sunulan yöntemler, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> herhangi bir adres türü için standart bağlantı oluşturma yeteneklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-963">The methods provided by <xref:Microsoft.AspNetCore.Routing.LinkGenerator> support standard link generation capabilities for any type of address.</span></span> <span data-ttu-id="ff86d-964">Bağlantı oluşturucuyu kullanmanın en kolay yolu, belirli bir adres türü için işlem gerçekleştiren genişletme yöntemlerine yöneliktir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-964">The most convenient way to use the link generator is through extension methods that perform operations for a specific address type.</span></span>

| <span data-ttu-id="ff86d-965">Genişletme yöntemi</span><span class="sxs-lookup"><span data-stu-id="ff86d-965">Extension Method</span></span>   | <span data-ttu-id="ff86d-966">Açıklama</span><span class="sxs-lookup"><span data-stu-id="ff86d-966">Description</span></span>                                                         |
| ------------------ | ------------------------------------------------------------------- |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*> | <span data-ttu-id="ff86d-967">Belirtilen değerleri temel alarak mutlak bir yola sahip bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-967">Generates a URI with an absolute path based on the provided values.</span></span> |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetUriByAddress*> | <span data-ttu-id="ff86d-968">Belirtilen değerleri temel alarak mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-968">Generates an absolute URI based on the provided values.</span></span>             |

> [!WARNING]
> <span data-ttu-id="ff86d-969">Çağırma yöntemlerinin aşağıdaki etkilerine dikkat edin <xref:Microsoft.AspNetCore.Routing.LinkGenerator> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-969">Pay attention to the following implications of calling <xref:Microsoft.AspNetCore.Routing.LinkGenerator> methods:</span></span>
>
> * <span data-ttu-id="ff86d-970">`GetUri*`Gelen isteklerin üstbilgisini doğrulayan bir uygulama yapılandırmasında uzantı yöntemlerini dikkatle kullanın `Host` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-970">Use `GetUri*` extension methods with caution in an app configuration that doesn't validate the `Host` header of incoming requests.</span></span> <span data-ttu-id="ff86d-971">`Host`Gelen isteklerin üstbilgisi doğrulandıktan sonra, güvenilir olmayan istek girişi, bir görünüm/sayfada URI 'ler halinde istemciye geri gönderilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-971">If the `Host` header of incoming requests isn't validated, untrusted request input can be sent back to the client in URIs in a view/page.</span></span> <span data-ttu-id="ff86d-972">Tüm üretim uygulamalarının, `Host` üst bilgisini bilinen geçerli değerlere karşı doğrulamak için kendi sunucusunu yapılandırmasını öneririz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-972">We recommend that all production apps configure their server to validate the `Host` header against known valid values.</span></span>
>
> * <span data-ttu-id="ff86d-973"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>Veya ile birlikte ara yazılım içinde dikkatli kullanın `Map` `MapWhen` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-973">Use <xref:Microsoft.AspNetCore.Routing.LinkGenerator> with caution in middleware in combination with `Map` or `MapWhen`.</span></span> <span data-ttu-id="ff86d-974">`Map*`yürütülen isteğin temel yolunu değiştirir ve bu da bağlantı oluşturma çıktısını etkiler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-974">`Map*` changes the base path of the executing request, which affects the output of link generation.</span></span> <span data-ttu-id="ff86d-975">Tüm <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'ler temel yol belirtilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-975">All of the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> APIs allow specifying a base path.</span></span> <span data-ttu-id="ff86d-976">Bağlantı oluşturma işlemi geri almak için her zaman boş bir temel yol belirtin `Map*` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-976">Always specify an empty base path to undo `Map*`'s affect on link generation.</span></span>

## <a name="differences-from-earlier-versions-of-routing"></a><span data-ttu-id="ff86d-977">Yönlendirmenin önceki sürümlerinden farklılıklar</span><span class="sxs-lookup"><span data-stu-id="ff86d-977">Differences from earlier versions of routing</span></span>

<span data-ttu-id="ff86d-978">ASP.NET Core 2,2 veya üzeri ve daha önceki yönlendirme sürümlerindeki ASP.NET Core uç nokta yönlendirmesi arasında birkaç fark vardır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-978">A few differences exist between endpoint routing in ASP.NET Core 2.2 or later and earlier versions of routing in ASP.NET Core:</span></span>

* <span data-ttu-id="ff86d-979">Uç nokta yönlendirme sistemi <xref:Microsoft.AspNetCore.Routing.IRouter> , devralma dahil olmak üzere tabanlı genişletilebilirliği desteklemez <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-979">The endpoint routing system doesn't support <xref:Microsoft.AspNetCore.Routing.IRouter>-based extensibility, including inheriting from <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>

* <span data-ttu-id="ff86d-980">Uç nokta yönlendirme [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim)desteklemez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-980">Endpoint routing doesn't support [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim).</span></span> <span data-ttu-id="ff86d-981">Uyumluluk Shim 'yi kullanmaya devam etmek için 2,1 [Uyumluluk sürümünü](xref:mvc/compatibility-version) ( `.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)` ) kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-981">Use the 2.1 [compatibility version](xref:mvc/compatibility-version) (`.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)`) to continue using the compatibility shim.</span></span>

* <span data-ttu-id="ff86d-982">Uç nokta yönlendirmesinde, geleneksel yollar kullanılırken oluşturulan URI 'lerin büyük küçük harfleri için farklı davranış vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-982">Endpoint Routing has different behavior for the casing of generated URIs when using conventional routes.</span></span>

  <span data-ttu-id="ff86d-983">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="ff86d-983">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="ff86d-984">Aşağıdaki rotayı kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="ff86d-984">Suppose you generate a link to an action using the following route:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "blog", new { id = 17, });
  ```

  <span data-ttu-id="ff86d-985"><xref:Microsoft.AspNetCore.Routing.IRouter>Tabanlı yönlendirme ile, bu kod, `/blog/ReadPost/17` belirtilen rota değerinin büyük küçük harflerini BELIRTEN bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-985">With <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, this code generates a URI of `/blog/ReadPost/17`, which respects the casing of the provided route value.</span></span> <span data-ttu-id="ff86d-986">ASP.NET Core 2,2 veya üzeri bir nokta yönlendirme `/Blog/ReadPost/17` ("blog" büyük harfli) üretir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-986">Endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` ("Blog" is capitalized).</span></span> <span data-ttu-id="ff86d-987">Endpoint Routing, `IOutboundParameterTransformer` Bu davranışı genel olarak özelleştirmek veya URL eşleme için farklı kurallar uygulamak üzere kullanılabilecek arabirimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-987">Endpoint routing provides the `IOutboundParameterTransformer` interface that can be used to customize this behavior globally or to apply different conventions for mapping URLs.</span></span>

  <span data-ttu-id="ff86d-988">Daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="ff86d-988">For more information, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

* <span data-ttu-id="ff86d-989">Geleneksel yolların bulunduğu MVC/sayfalar tarafından kullanılan bağlantı üretimi, Razor mevcut olmayan bir denetleyiciye/eyleme veya sayfaya bağlantı kurmaya çalışırken farklı şekilde davranır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-989">Link Generation used by MVC/Razor Pages with conventional routes behaves differently when attempting to link to an controller/action or page that doesn't exist.</span></span>

  <span data-ttu-id="ff86d-990">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="ff86d-990">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="ff86d-991">Aşağıdaki ile varsayılan şablonu kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="ff86d-991">Suppose you generate a link to an action using the default template with the following:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "Blog", new { id = 17, });
  ```

  <span data-ttu-id="ff86d-992">`IRouter`Tabanlı yönlendirme ile, `/Blog/ReadPost/17` `BlogController` mevcut olmasa veya bir eylem yöntemine sahip olmasalar bile sonuç her zaman olur `ReadPost` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-992">With `IRouter`-based routing, the result is always `/Blog/ReadPost/17`, even if the `BlogController` doesn't exist or doesn't have a `ReadPost` action method.</span></span> <span data-ttu-id="ff86d-993">Beklendiği gibi, ASP.NET Core 2,2 veya sonraki bir sürümde uç nokta yönlendirme `/Blog/ReadPost/17` eylem yöntemi varsa üretir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-993">As expected, endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` if the action method exists.</span></span> <span data-ttu-id="ff86d-994">*Ancak, uç nokta yönlendirme, eylem yoksa boş bir dize oluşturur.*</span><span class="sxs-lookup"><span data-stu-id="ff86d-994">*However, endpoint routing produces an empty string if the action doesn't exist.*</span></span> <span data-ttu-id="ff86d-995">Kavramsal olarak, uç nokta yönlendirme, eylem mevcut değilse uç noktanın var olduğunu varsaymaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-995">Conceptually, endpoint routing doesn't assume that the endpoint exists if the action doesn't exist.</span></span>

* <span data-ttu-id="ff86d-996">Bağlantı oluşturma *çevresel değeri ınvalidation algoritması* , uç nokta yönlendirme ile kullanıldığında farklı davranır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-996">The link generation *ambient value invalidation algorithm* behaves differently when used with endpoint routing.</span></span>

  <span data-ttu-id="ff86d-997">*Çevresel değer ınvalidation* , şu anda yürütülmekte olan isteğin (çevresel değerler) hangi rota değerlerinin bağlantı oluşturma işlemlerinde kullanılabileceğini belirleyen algoritmadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-997">*Ambient value invalidation* is the algorithm that decides which route values from the currently executing request (the ambient values) can be used in link generation operations.</span></span> <span data-ttu-id="ff86d-998">Geleneksel yönlendirme, farklı bir eyleme bağlanılırken ek yol değerlerini her zaman geçersiz kıldı.</span><span class="sxs-lookup"><span data-stu-id="ff86d-998">Conventional routing always invalidated extra route values when linking to a different action.</span></span> <span data-ttu-id="ff86d-999">ASP.NET Core 2,2 sürümünden önce öznitelik yönlendirmesinde bu davranış yoktu.</span><span class="sxs-lookup"><span data-stu-id="ff86d-999">Attribute routing didn't have this behavior prior to the release of ASP.NET Core 2.2.</span></span> <span data-ttu-id="ff86d-1000">ASP.NET Core 'nin önceki sürümlerinde, aynı yol parametresi adlarını kullanan başka bir eyleme bağlantılar, bağlantı oluşturma hatalarıyla sonuçlandı.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1000">In earlier versions of ASP.NET Core, links to another action that use the same route parameter names resulted in link generation errors.</span></span> <span data-ttu-id="ff86d-1001">ASP.NET Core 2,2 veya sonraki sürümlerde, yönlendirme biçimlerinin her ikisi de başka bir eyleme bağlanırken değerleri geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1001">In ASP.NET Core 2.2 or later, both forms of routing invalidate values when linking to another action.</span></span>

  <span data-ttu-id="ff86d-1002">ASP.NET Core 2,1 veya önceki sürümlerde aşağıdaki örneği göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1002">Consider the following example in ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="ff86d-1003">Başka bir eyleme (veya başka bir sayfaya) bağlanırken, yol değerleri istenmeyen yollarla yeniden kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1003">When linking to another action (or another page), route values can be reused in undesirable ways.</span></span>

  <span data-ttu-id="ff86d-1004">*/Pages/Store/Product.exe*:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1004">In */Pages/Store/Product.cshtml*:</span></span>

  ```cshtml
  @page "{id}"
  @Url.Page("/Login")
  ```

  <span data-ttu-id="ff86d-1005">*/Pages/Login.exe*içinde:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1005">In */Pages/Login.cshtml*:</span></span>

  ```cshtml
  @page "{id?}"
  ```

  <span data-ttu-id="ff86d-1006">URI `/Store/Product/18` ASP.NET Core 2,1 veya daha önceki bir sürümdeyse, tarafından Store/Info sayfasında oluşturulan bağlantı `@Url.Page("/Login")` olur `/Login/18` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1006">If the URI is `/Store/Product/18` in ASP.NET Core 2.1 or earlier, the link generated on the Store/Info page by `@Url.Page("/Login")` is `/Login/18`.</span></span> <span data-ttu-id="ff86d-1007">`id`16 değeri, bağlantı hedefi uygulamanın tamamen farklı bir parçası olsa da yeniden kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1007">The `id` value of 18 is reused, even though the link destination is different part of the app entirely.</span></span> <span data-ttu-id="ff86d-1008">`id`Sayfa bağlamındaki yol değeri `/Login` büyük olasılıkla bir mağaza ürün kimliği değeri değil, BIR kullanıcı kimliği değeridir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1008">The `id` route value in the context of the `/Login` page is probably a user ID value, not a store product ID value.</span></span>

  <span data-ttu-id="ff86d-1009">ASP.NET Core 2,2 veya sonraki bir sürümü ile Endpoint Routing de sonuç olur `/Login` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1009">In endpoint routing with ASP.NET Core 2.2 or later, the result is `/Login`.</span></span> <span data-ttu-id="ff86d-1010">Bağlantılı hedef farklı bir eylem veya sayfa olduğunda çevresel değerler yeniden kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1010">Ambient values aren't reused when the linked destination is a different action or page.</span></span>

* <span data-ttu-id="ff86d-1011">Gidiş dönüşü yol parametresi sözdizimi: çift yıldız ( `**` ) catch-all parametre sözdizimi kullanılırken eğik çizgiler kodlanmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1011">Round-tripping route parameter syntax: Forward slashes aren't encoded when using a double-asterisk (`**`) catch-all parameter syntax.</span></span>

  <span data-ttu-id="ff86d-1012">Bağlantı oluşturma sırasında, yönlendirme sistemi, eğik çizgiler hariç, bir çift yıldız ( `**` ) catch-all parametresinde (örneğin,) yakalanan değeri kodlar `{**myparametername}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1012">During link generation, the routing system encodes the value captured in a double-asterisk (`**`) catch-all parameter (for example, `{**myparametername}`) except the forward slashes.</span></span> <span data-ttu-id="ff86d-1013">Çift yıldız yakalama, `IRouter` ASP.NET Core 2,2 veya üzeri sürümlerde tabanlı yönlendirme ile desteklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1013">The double-asterisk catch-all is supported with `IRouter`-based routing in ASP.NET Core 2.2 or later.</span></span>

  <span data-ttu-id="ff86d-1014">ASP.NET Core () öğesinin önceki sürümlerindeki tek yıldız catch-all parametre sözdizimi `{*myparametername}` desteklenmeye devam eder ve eğik çizgi kodlandı.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1014">The single asterisk catch-all parameter syntax in prior versions of ASP.NET Core (`{*myparametername}`) remains supported, and forward slashes are encoded.</span></span>

  | <span data-ttu-id="ff86d-1015">Yol</span><span class="sxs-lookup"><span data-stu-id="ff86d-1015">Route</span></span>              | <span data-ttu-id="ff86d-1016">İle oluşturulan bağlantı</span><span class="sxs-lookup"><span data-stu-id="ff86d-1016">Link generated with</span></span><br>`Url.Action(new { category = "admin/products" })`&hellip; |
  | ------------------ | --------------------------------------------------------------------- |
  | `/search/{*page}`  | <span data-ttu-id="ff86d-1017">`/search/admin%2Fproducts`(eğik çizgi kodlandı)</span><span class="sxs-lookup"><span data-stu-id="ff86d-1017">`/search/admin%2Fproducts` (the forward slash is encoded)</span></span>             |
  | `/search/{**page}` | `/search/admin/products`                                              |

### <a name="middleware-example"></a><span data-ttu-id="ff86d-1018">Ara yazılım örneği</span><span class="sxs-lookup"><span data-stu-id="ff86d-1018">Middleware example</span></span>

<span data-ttu-id="ff86d-1019">Aşağıdaki örnekte, bir ara yazılım, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> Mağaza ürünlerini listeleyen bir eylem yöntemine bağlantı oluşturmak için API 'yi kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1019">In the following example, a middleware uses the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API to create link to an action method that lists store products.</span></span> <span data-ttu-id="ff86d-1020">Bağlantı Oluşturucuyu bir sınıfa ekleme ve çağırarak bir `GenerateLink` uygulamadaki herhangi bir sınıf için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1020">Using the link generator by injecting it into a class and calling `GenerateLink` is available to any class in an app.</span></span>

```csharp
using Microsoft.AspNetCore.Routing;

public class ProductsLinkMiddleware
{
    private readonly LinkGenerator _linkGenerator;

    public ProductsLinkMiddleware(RequestDelegate next, LinkGenerator linkGenerator)
    {
        _linkGenerator = linkGenerator;
    }

    public async Task InvokeAsync(HttpContext httpContext)
    {
        var url = _linkGenerator.GetPathByAction("ListProducts", "Store");

        httpContext.Response.ContentType = "text/plain";

        await httpContext.Response.WriteAsync($"Go to {url} to see our products.");
    }
}
```

### <a name="create-routes"></a><span data-ttu-id="ff86d-1021">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1021">Create routes</span></span>

<span data-ttu-id="ff86d-1022">Çoğu uygulama, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ' de tanımlanan benzer uzantı yöntemlerinden birini çağırarak veya arayarak yollar oluşturur <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1022">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="ff86d-1023"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder>Uzantı yöntemlerinden herhangi biri bir örneği oluşturur <xref:Microsoft.AspNetCore.Routing.Route> ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1023">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="ff86d-1024"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>yol işleyici parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1024"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="ff86d-1025"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>yalnızca tarafından işlenen rotaları ekler <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1025"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="ff86d-1026">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-1026">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="ff86d-1027">Aşağıdaki kod örneği, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan bir çağrının örneğidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1027">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="ff86d-1028">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1028">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="ff86d-1029">Örneğin, yol `/Products/Details/17` Şu yol değerlerini oluşturur: `{ controller = Products, action = Details, id = 17 }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1029">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="ff86d-1030">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1030">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="ff86d-1031">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1031">Route parameters are named.</span></span> <span data-ttu-id="ff86d-1032">Küme ayraçları içinde parametre adının çevreleme tarafından tanımlanan parametreler `{ ... }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1032">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="ff86d-1033">Yukarıdaki şablon URL yoluyla da eşleştirebilir `/` ve değerleri üretebilir `{ controller = Home, action = Index }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1033">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="ff86d-1034">Bu durum, `{controller}` ve `{action}` Rota parametrelerinin varsayılan değerleri olduğu ve `id` route parametresinin isteğe bağlı olması nedeniyle oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1034">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="ff86d-1035">Bir eşittir işareti ( `=` ), yol parametre adından sonra gelen bir değer, parametre için varsayılan bir değer tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1035">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="ff86d-1036">`?`Yol parametre adından sonra bir soru işareti () isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1036">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="ff86d-1037">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1037">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="ff86d-1038">İsteğe bağlı parametreler, karşılık gelen bir URL yol kesimi yoksa rota değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1038">Optional parameters don't produce a route value if there is no corresponding URL path segment.</span></span> <span data-ttu-id="ff86d-1039">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1039">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="ff86d-1040">Aşağıdaki örnekte, Route parametresi tanımı `{id:int}` yol parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlar `id` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1040">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="ff86d-1041">Bu şablon, gibi bir URL yoluyla eşleşir `/Products/Details/17` `/Products/Details/Apples` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1041">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="ff86d-1042">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> , yol değerlerini uygulayıp onları doğrulamak için inceler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1042">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="ff86d-1043">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1043">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="ff86d-1044">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1044">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="ff86d-1045"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, Ve için değer kabul etmenin ek aşırı yüklemeleri `constraints` `dataTokens` `defaults` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1045">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="ff86d-1046">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1046">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="ff86d-1047">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekler eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1047">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="ff86d-1048">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1048">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="ff86d-1049">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1049">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="ff86d-1050">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1050">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{**article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="ff86d-1051">Önceki şablon, benzer bir URL yoluyla eşleşir `/Blog/All-About-Routing/Introduction` ve değerleri ayıklar `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1051">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="ff86d-1052">Ve için varsayılan yol değerleri `controller` , `action` şablonda karşılık gelen hiçbir yol parametresi olmasa bile rota tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1052">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="ff86d-1053">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1053">Default values can be specified in the route template.</span></span> <span data-ttu-id="ff86d-1054">Route `article` parametresi, yol parametre adından önce bir çift yıldız işareti () görünümüne göre *catch-all* olarak tanımlanır `**` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1054">The `article` route parameter is defined as a *catch-all* by the appearance of an double asterisk (`**`) before the route parameter name.</span></span> <span data-ttu-id="ff86d-1055">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1055">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="ff86d-1056">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1056">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="ff86d-1057">Önceki şablon, benzer bir URL yoluyla eşleşir `/en-US/Products/5` ve değerleri `{ controller = Products, action = Details, id = 5 }` ve veri belirteçlerini ayıklar `{ locale = en-US }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1057">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="ff86d-1059">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1059">Route class URL generation</span></span>

<span data-ttu-id="ff86d-1060">Sınıf, yol <xref:Microsoft.AspNetCore.Routing.Route> değerlerini bir kümesini rota şablonuyla BIRLEŞTIREREK URL oluşturma işlemi de gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1060">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="ff86d-1061">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1061">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="ff86d-1062">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1062">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="ff86d-1063">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="ff86d-1063">What values would be produced?</span></span> <span data-ttu-id="ff86d-1064">Bu, URL oluşturma işlevinin sınıfında nasıl çalıştığına ilişkin kaba bir eştir <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1064">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="ff86d-1065">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1065">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="ff86d-1066">Yol değerleriyle `{ controller = Products, action = List }` , URL `/Products/List` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1066">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="ff86d-1067">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1067">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="ff86d-1068">`id`, İsteğe bağlı bir yol parametresi olduğundan, URL 'si için bir değer olmadan başarıyla oluşturulur `id` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1068">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="ff86d-1069">Yol değerleriyle `{ controller = Home, action = Index }` , URL `/` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1069">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="ff86d-1070">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1070">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="ff86d-1071">Her iki URL de aşağıdaki yol tanımıyla (ve) birlikte gidiş dönüş, `/Home/Index` `/` URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1071">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="ff86d-1072">ASP.NET Core MVC kullanan bir uygulama <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> , doğrudan yönlendirmeye çağırmak yerine URL 'ler oluşturmak için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1072">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="ff86d-1073">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1073">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="ff86d-1074">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1074">Use Routing Middleware</span></span>

<span data-ttu-id="ff86d-1075">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1075">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="ff86d-1076">İçindeki hizmet kapsayıcısına yönlendirme Ekle `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1076">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="ff86d-1077">Yolların yönteminde yapılandırılması gerekir `Startup.Configure` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1077">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="ff86d-1078">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1078">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="ff86d-1079"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Yalnızca HTTP GET isteklerini eşleştirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1079"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="ff86d-1080">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1080">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="ff86d-1081">URI</span><span class="sxs-lookup"><span data-stu-id="ff86d-1081">URI</span></span>                    | <span data-ttu-id="ff86d-1082">Yanıt</span><span class="sxs-lookup"><span data-stu-id="ff86d-1082">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="ff86d-1083">Herkese!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1083">Hello!</span></span> <span data-ttu-id="ff86d-1084">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="ff86d-1084">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="ff86d-1085">Herkese!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1085">Hello!</span></span> <span data-ttu-id="ff86d-1086">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="ff86d-1086">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="ff86d-1087">Herkese!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1087">Hello!</span></span> <span data-ttu-id="ff86d-1088">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="ff86d-1088">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="ff86d-1089">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1089">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="ff86d-1090">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1090">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="ff86d-1091">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1091">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="ff86d-1092">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1092">The request falls through, no match.</span></span>              |

<span data-ttu-id="ff86d-1093">Framework, yollar () oluşturmak için bir genişletme yöntemleri kümesi sağlar <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1093">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="ff86d-1094">`Map[Verb]`Yöntemler, yöntem ADıNDAKI http fiili ile rotayı sınırlandırmak için kısıtlamalar kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1094">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="ff86d-1095">Örneğin, bkz <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> . ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1095">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="ff86d-1096">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1096">Route template reference</span></span>

<span data-ttu-id="ff86d-1097">Küme ayraçları () içindeki belirteçler `{ ... }` , yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1097">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="ff86d-1098">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1098">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="ff86d-1099">Örneğin, `{controller=Home}{action=Index}` ve arasında değişmez değer olmadığından geçerli bir yol değil `{controller}` `{action}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1099">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="ff86d-1100">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1100">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="ff86d-1101">Yol parametrelerinden (örneğin, `{id}` ) ve yol ayırıcısından farklı bir metin, `/` URL içindeki metinle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1101">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="ff86d-1102">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1102">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="ff86d-1103">Bir sabit yol parametresi sınırlayıcısından ( `{` veya) eşleştirmek için `}` , karakteri (veya) tekrarlayarak sınırlayıcıdan kaçış `{{` `}}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1103">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="ff86d-1104">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1104">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="ff86d-1105">Örneğin, şablonu göz önünde bulundurun `files/{filename}.{ext?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1105">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="ff86d-1106">Hem hem de için değerler olduğunda `filename` `ext` her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1106">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="ff86d-1107">URL 'de yalnızca bir değeri `filename` varsa, sondaki nokta ( `.` ) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1107">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="ff86d-1108">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1108">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="ff86d-1109">`*` `**` URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak bir yıldız işareti () veya çift yıldız işareti () kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1109">You can use an asterisk (`*`) or double asterisk (`**`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="ff86d-1110">Bunlara *catch-all* parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1110">These are called a *catch-all* parameters.</span></span> <span data-ttu-id="ff86d-1111">Örneğin, `blog/{**slug}` ile başlayan `/blog` ve bunu izleyen bir değere sahip olan ve yol değerine atanan HERHANGI bir URI ile eşleşir `slug` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1111">For example, `blog/{**slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="ff86d-1112">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1112">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="ff86d-1113">Yol ayırıcısı () karakterleri de dahil olmak üzere bir URL oluşturmak için, catch-all parametresi uygun karakterleri de çıkar `/` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1113">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="ff86d-1114">Örneğin, `foo/{*path}` yol değerlerini içeren yol `{ path = "my/path" }` oluşturulur `foo/my%2Fpath` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1114">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="ff86d-1115">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1115">Note the escaped forward slash.</span></span> <span data-ttu-id="ff86d-1116">Yol ayırıcı karakterlerini yuvarlaklaştırmak için `**` Rota parametresi önekini kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1116">To round-trip path separator characters, use the `**` route parameter prefix.</span></span> <span data-ttu-id="ff86d-1117">İle rota `foo/{**path}` `{ path = "my/path" }` oluşturulur `foo/my/path` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1117">The route `foo/{**path}` with `{ path = "my/path" }` generates `foo/my/path`.</span></span>

<span data-ttu-id="ff86d-1118">Yol parametreleri, parametre adından sonra bir eşittir işaretiyle () ayırarak varsayılan değer belirtilerek belirlenmiş *varsayılan değerlere* sahip olabilir `=` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1118">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="ff86d-1119">Örneğin, `{controller=Home}` `Home` için varsayılan değer olarak tanımlar `controller` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1119">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="ff86d-1120">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1120">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="ff86d-1121">Yol parametreleri `?` , içinde olduğu gibi parametre adının sonuna bir soru işareti () eklenerek isteğe bağlı olarak yapılır `id?` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1121">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="ff86d-1122">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden, &mdash; isteğe bağlı bir parametre yalnızca Istek URL 'si tarafından bir değer sağlandığı zaman bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1122">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="ff86d-1123">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1123">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="ff86d-1124">`:`Yol parametre adından sonra bir iki nokta () ve kısıtlama adı eklemek, bir rota parametresinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1124">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="ff86d-1125">Kısıtlama bağımsız değişkenler gerektiriyorsa, `(...)` kısıtlama adından sonra parantez () içine alınır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1125">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="ff86d-1126">Birden çok satır içi kısıtlama, başka bir iki nokta ( `:` ) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1126">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="ff86d-1127">Kısıtlama adı ve bağımsız değişkenler, <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> URL işlemede kullanılmak üzere bir örneği oluşturmak için hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1127">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="ff86d-1128">Örneğin, yol şablonu `blog/{article:minlength(10)}` `minlength` bağımsız değişkenle bir kısıtlama belirtir `10` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1128">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="ff86d-1129">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1129">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="ff86d-1130">Yol parametrelerinde Ayrıca, bağlantı oluştururken ve URL 'Ler ile eşleşen eylemler ve sayfalar için bir parametrenin değerini dönüştüren parametre dönüştürücüler bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1130">Route parameters may also have parameter transformers, which transform a parameter's value when generating links and matching actions and pages to URLs.</span></span> <span data-ttu-id="ff86d-1131">LIKE kısıtlamaları, yol parametre adından sonra iki nokta üst üste ( `:` ) ve transformatör adı eklenerek, parametre dönüştürücüleri bir rota parametresine eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1131">Like constraints, parameter transformers can be added inline to a route parameter by adding a colon (`:`) and transformer name after the route parameter name.</span></span> <span data-ttu-id="ff86d-1132">Örneğin, yol şablonu `blog/{article:slugify}` bir `slugify` transformatör belirtir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1132">For example, the route template `blog/{article:slugify}` specifies a `slugify` transformer.</span></span> <span data-ttu-id="ff86d-1133">Parametre dönüştürücüler hakkında daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1133">For more information on parameter transformers, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

<span data-ttu-id="ff86d-1134">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1134">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="ff86d-1135">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1135">Route Template</span></span>                           | <span data-ttu-id="ff86d-1136">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="ff86d-1136">Example Matching URI</span></span>    | <span data-ttu-id="ff86d-1137">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="ff86d-1137">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="ff86d-1138">Yalnızca tek bir yolla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1138">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="ff86d-1139">İle eşleşir ve `Page` ayarlanır `Home` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1139">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="ff86d-1140">İle eşleşir ve `Page` ayarlanır `Contact` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1140">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="ff86d-1141">`Products`Denetleyiciye ve `List` eyleme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1141">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="ff86d-1142">`Products`Denetleyici ve `Details` eyleme eşlenir ( `id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1142">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="ff86d-1143">`Home`Denetleyiciye ve `Index` yönteme eşlenir ( `id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1143">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="ff86d-1144">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1144">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="ff86d-1145">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1145">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="ff86d-1146">İstekleri eşleme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1146">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="reserved-routing-names"></a><span data-ttu-id="ff86d-1147">Ayrılmış yönlendirme adları</span><span class="sxs-lookup"><span data-stu-id="ff86d-1147">Reserved routing names</span></span>

<span data-ttu-id="ff86d-1148">Aşağıdaki anahtar sözcükler ayrılmış isimlerdir ve yol adları veya parametreler olarak kullanılamaz:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1148">The following keywords are reserved names and can't be used as route names or parameters:</span></span>

* `action`
* `area`
* `controller`
* `handler`
* `page`

## <a name="route-constraint-reference"></a><span data-ttu-id="ff86d-1149">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1149">Route constraint reference</span></span>

<span data-ttu-id="ff86d-1150">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1150">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="ff86d-1151">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1151">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="ff86d-1152">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1152">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="ff86d-1153">Örneğin, <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> bir ISTEğI http fiiline bağlı olarak kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1153">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="ff86d-1154">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1154">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="ff86d-1155">**Giriş doğrulaması**için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1155">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="ff86d-1156">**Giriş doğrulaması**için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1156">If constraints are used for **input validation**, invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="ff86d-1157">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1157">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="ff86d-1158">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1158">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="ff86d-1159">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="ff86d-1159">constraint</span></span> | <span data-ttu-id="ff86d-1160">Örnek</span><span class="sxs-lookup"><span data-stu-id="ff86d-1160">Example</span></span> | <span data-ttu-id="ff86d-1161">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1161">Example Matches</span></span> | <span data-ttu-id="ff86d-1162">Notlar</span><span class="sxs-lookup"><span data-stu-id="ff86d-1162">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="ff86d-1163">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1163">`123456789`, `-123456789`</span></span> | <span data-ttu-id="ff86d-1164">Herhangi bir tamsayıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1164">Matches any integer.</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="ff86d-1165">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1165">`true`, `FALSE`</span></span> | <span data-ttu-id="ff86d-1166">Eşleşiyor `true` veya ' false.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1166">Matches `true` or \`false.</span></span> <span data-ttu-id="ff86d-1167">Büyük/küçük harf duyarsız.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1167">Case-insensitive.</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="ff86d-1168">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1168">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="ff86d-1169">`DateTime`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1169">Matches a valid `DateTime` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1170">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1170">See  preceding warning.</span></span>|
| `decimal` | `{price:decimal}` | <span data-ttu-id="ff86d-1171">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1171">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="ff86d-1172">`decimal`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1172">Matches a valid `decimal` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1173">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1173">See  preceding warning.</span></span>|
| `double` | `{weight:double}` | <span data-ttu-id="ff86d-1174">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1174">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="ff86d-1175">`double`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1175">Matches a valid `double` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1176">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1176">See  preceding warning.</span></span>|
| `float` | `{weight:float}` | <span data-ttu-id="ff86d-1177">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1177">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="ff86d-1178">`float`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1178">Matches a valid `float` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1179">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1179">See  preceding warning.</span></span>|
| `guid` | `{id:guid}` | <span data-ttu-id="ff86d-1180">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1180">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="ff86d-1181">Geçerli bir `Guid` değerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1181">Matches a valid `Guid` value.</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="ff86d-1182">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1182">`123456789`, `-123456789`</span></span> | <span data-ttu-id="ff86d-1183">Geçerli bir `long` değerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1183">Matches a valid `long` value.</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="ff86d-1184">Dize en az 4 karakter olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1184">String must be at least 4 characters.</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `MyFile` | <span data-ttu-id="ff86d-1185">Dizede en fazla 8 karakter vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1185">String has maximum of 8 characters.</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="ff86d-1186">Dize tam olarak 12 karakter uzunluğunda olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1186">String must be exactly 12 characters long.</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="ff86d-1187">Dize en az 8 olmalı ve en fazla 16 karakter uzunluğunda olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1187">String must be at least 8 and has maximum of 16 characters.</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="ff86d-1188">Tamsayı değeri en az 18 olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1188">Integer value must be at least 18.</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="ff86d-1189">Tamsayı değeri üst sınırı 120.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1189">Integer value maximum of 120.</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="ff86d-1190">Tamsayı değeri en az 18 ve en fazla 120 olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1190">Integer value must be at least 18 and maximum of 120.</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="ff86d-1191">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır `a` - `z` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1191">String must consist of one or more alphabetical characters `a`-`z`.</span></span>  <span data-ttu-id="ff86d-1192">Büyük/küçük harf duyarsız.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1192">Case-insensitive.</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="ff86d-1193">Dize, normal ifadeyle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1193">String must match the regular expression.</span></span> <span data-ttu-id="ff86d-1194">Normal ifade tanımlama hakkında ipuçlarına bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1194">See tips about defining a regular expression.</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="ff86d-1195">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1195">Used to enforce that a non-parameter value is present during URL generation.</span></span> |

<span data-ttu-id="ff86d-1196">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1196">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="ff86d-1197">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1197">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="ff86d-1198">URL 'YI doğrulayan ve bir CLR türüne (veya gibi) dönüştürülen yol kısıtlamaları `int` `DateTime` , her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1198">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="ff86d-1199">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1199">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="ff86d-1200">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1200">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="ff86d-1201">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1201">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="ff86d-1202">Örneğin, `float` kısıtlama yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1202">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="ff86d-1203">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1203">Regular expressions</span></span>

<span data-ttu-id="ff86d-1204">ASP.NET Core Framework, `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` normal ifade oluşturucusuna ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1204">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="ff86d-1205"><xref:System.Text.RegularExpressions.RegexOptions>Bu üyelerin açıklaması için bkz..</span><span class="sxs-lookup"><span data-stu-id="ff86d-1205">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="ff86d-1206">Normal ifadeler, Yönlendirme ve C# dili tarafından kullanılanlarla benzerlik olan sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1206">Regular expressions use delimiters and tokens similar to those used by routing and the C# language.</span></span> <span data-ttu-id="ff86d-1207">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1207">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="ff86d-1208">Yönlendirmenin normal ifadesini kullanmak için `^\d{3}-\d{2}-\d{4}$` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1208">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing:</span></span>

* <span data-ttu-id="ff86d-1209">İfade, `\` dize içinde, kaynak kodunda çift ters eğik çizgi olarak belirtilen tek ters eğik çizgi karakterleri içermelidir `\\` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1209">The expression must have the single backslash `\` characters provided in the string as double backslash `\\` characters in the source code.</span></span>
* <span data-ttu-id="ff86d-1210">Normal ifade, `\\` dize kaçış karakterinden kaçınmak için bizim için olmalıdır `\` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1210">The regular expression must us `\\` in order to escape the `\` string escape character.</span></span>
* <span data-ttu-id="ff86d-1211">Normal ifade, tam `\\` [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanılırken gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1211">The regular expression doesn't require `\\` when using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string).</span></span>

<span data-ttu-id="ff86d-1212">Yönlendirme parametresi sınırlayıcı karakterleri,,,, `{` `}` `[` `]` ifadesindeki karakterleri çift `{{` `}` `[[` `]]` kaçış,,,,,,,,,,,.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1212">To escape routing parameter delimiter characters `{`, `}`, `[`, `]`, double the characters in the expression `{{`, `}`, `[[`, `]]`.</span></span> <span data-ttu-id="ff86d-1213">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1213">The following table shows a regular expression and the escaped version:</span></span>

| <span data-ttu-id="ff86d-1214">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="ff86d-1214">Regular Expression</span></span>    | <span data-ttu-id="ff86d-1215">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="ff86d-1215">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="ff86d-1216">Yönlendirmelerde kullanılan normal ifadeler, genellikle giriş işareti `^` karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1216">Regular expressions used in routing often start with the caret `^` character and match starting position of the string.</span></span> <span data-ttu-id="ff86d-1217">İfadeler, genellikle dolar işareti `$` karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1217">The expressions often end with the dollar sign `$` character and match end of the string.</span></span> <span data-ttu-id="ff86d-1218">`^`Ve `$` karakterleri, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1218">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="ff86d-1219">`^`Ve karakterleri olmadan `$` normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1219">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="ff86d-1220">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1220">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="ff86d-1221">İfade</span><span class="sxs-lookup"><span data-stu-id="ff86d-1221">Expression</span></span>   | <span data-ttu-id="ff86d-1222">Dize</span><span class="sxs-lookup"><span data-stu-id="ff86d-1222">String</span></span>    | <span data-ttu-id="ff86d-1223">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-1223">Match</span></span> | <span data-ttu-id="ff86d-1224">Yorum</span><span class="sxs-lookup"><span data-stu-id="ff86d-1224">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1225">hello</span><span class="sxs-lookup"><span data-stu-id="ff86d-1225">hello</span></span>     | <span data-ttu-id="ff86d-1226">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1226">Yes</span></span>   | <span data-ttu-id="ff86d-1227">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1227">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1228">123abc456</span><span class="sxs-lookup"><span data-stu-id="ff86d-1228">123abc456</span></span> | <span data-ttu-id="ff86d-1229">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1229">Yes</span></span>   | <span data-ttu-id="ff86d-1230">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1230">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1231">MZ</span><span class="sxs-lookup"><span data-stu-id="ff86d-1231">mz</span></span>        | <span data-ttu-id="ff86d-1232">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1232">Yes</span></span>   | <span data-ttu-id="ff86d-1233">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="ff86d-1233">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1234">MZ</span><span class="sxs-lookup"><span data-stu-id="ff86d-1234">MZ</span></span>        | <span data-ttu-id="ff86d-1235">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1235">Yes</span></span>   | <span data-ttu-id="ff86d-1236">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="ff86d-1236">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="ff86d-1237">hello</span><span class="sxs-lookup"><span data-stu-id="ff86d-1237">hello</span></span>     | <span data-ttu-id="ff86d-1238">Hayır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1238">No</span></span>    | <span data-ttu-id="ff86d-1239">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1239">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="ff86d-1240">123abc456</span><span class="sxs-lookup"><span data-stu-id="ff86d-1240">123abc456</span></span> | <span data-ttu-id="ff86d-1241">Hayır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1241">No</span></span>    | <span data-ttu-id="ff86d-1242">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1242">See `^` and `$` above</span></span> |

<span data-ttu-id="ff86d-1243">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1243">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="ff86d-1244">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1244">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="ff86d-1245">Örneğin, `{action:regex(^(list|get|create)$)}` yalnızca `action` Rota değeri `list` ,, veya ile eşleşir `get` `create` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1245">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="ff86d-1246">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1246">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="ff86d-1247">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1247">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="ff86d-1248">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="ff86d-1248">Custom route constraints</span></span>

<span data-ttu-id="ff86d-1249">Yerleşik yol kısıtlamalarına ek olarak, arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1249">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="ff86d-1250"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>Arabirim, `Match` `true` kısıtlama karşılanıp Aksi takdirde döndüren tek bir yöntemi içerir `false` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1250">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="ff86d-1251">Özel bir kullanmak için <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> yol kısıtlama türü, uygulamanın uygulamanın hizmet kapsayıcısında kayıtlı olması gerekir <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1251">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="ff86d-1252"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, Yol kısıtlama anahtarlarını <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> Bu kısıtlamaları doğrulayan uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1252">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="ff86d-1253">Bir uygulama <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> , `Startup.ConfigureServices` hizmetlerin bir parçası olarak ' de güncelleştirilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan ile yapılandırma `services.Configure<RouteOptions>` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1253">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="ff86d-1254">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1254">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="ff86d-1255">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1255">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="ff86d-1256">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1256">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="parameter-transformer-reference"></a><span data-ttu-id="ff86d-1257">Parametre transformatörü başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1257">Parameter transformer reference</span></span>

<span data-ttu-id="ff86d-1258">Parametre dönüştürücüler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1258">Parameter transformers:</span></span>

* <span data-ttu-id="ff86d-1259">Bir bağlantısını oluştururken yürütün <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1259">Execute when generating a link for a <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>
* <span data-ttu-id="ff86d-1260">Uygulayın `Microsoft.AspNetCore.Routing.IOutboundParameterTransformer` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1260">Implement `Microsoft.AspNetCore.Routing.IOutboundParameterTransformer`.</span></span>
* <span data-ttu-id="ff86d-1261">Kullanılarak yapılandırılır <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1261">Are configured using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>.</span></span>
* <span data-ttu-id="ff86d-1262">Parametrenin yol değerini alın ve yeni bir dize değerine dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1262">Take the parameter's route value and transform it to a new string value.</span></span>
* <span data-ttu-id="ff86d-1263">Oluşturulan bağlantıda Dönüştürülmüş değerin kullanılmasına neden olacak.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1263">Result in using the transformed value in the generated link.</span></span>

<span data-ttu-id="ff86d-1264">Örneğin, yol düzeninde bir özel `slugify` parametre transformatörü `blog\{article:slugify}` `Url.Action(new { article = "MyTestArticle" })` oluşturur `blog\my-test-article` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1264">For example, a custom `slugify` parameter transformer in route pattern `blog\{article:slugify}` with `Url.Action(new { article = "MyTestArticle" })` generates `blog\my-test-article`.</span></span>

<span data-ttu-id="ff86d-1265">Bir parametre transformatörü bir yol düzeninde kullanmak için, önce ' de kullanarak bunu <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> yapılandırın `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1265">To use a parameter transformer in a route pattern, configure it first using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddRouting(options =>
{
    // Replace the type and the name used to refer to it with your own
    // IOutboundParameterTransformer implementation
    options.ConstraintMap["slugify"] = typeof(SlugifyParameterTransformer);
});
```

<span data-ttu-id="ff86d-1266">Parametre dönüştürücüler, bir uç noktanın çözümlediği URI 'yi dönüştürmek için çerçevesi tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1266">Parameter transformers are used by the framework to transform the URI where an endpoint resolves.</span></span> <span data-ttu-id="ff86d-1267">Örneğin, ASP.NET Core MVC,,, ve ile eşleşecek şekilde kullanılan rota değerini dönüştürmek için parametre dönüştürücüler kullanır `area` `controller` `action` `page` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1267">For example, ASP.NET Core MVC uses parameter transformers to transform the route value used to match an `area`, `controller`, `action`, and `page`.</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller:slugify=Home}/{action:slugify=Index}/{id?}");
```

<span data-ttu-id="ff86d-1268">Önceki yol ile, eylem `SubscriptionManagementController.GetAll` URI ile eşleştirilir `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1268">With the preceding route, the action `SubscriptionManagementController.GetAll` is matched with the URI `/subscription-management/get-all`.</span></span> <span data-ttu-id="ff86d-1269">Bir parametre transformatörü bir bağlantı oluşturmak için kullanılan rota değerlerini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1269">A parameter transformer doesn't change the route values used to generate a link.</span></span> <span data-ttu-id="ff86d-1270">Örneğin, `Url.Action("GetAll", "SubscriptionManagement")` çıktılar `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1270">For example, `Url.Action("GetAll", "SubscriptionManagement")` outputs `/subscription-management/get-all`.</span></span>

<span data-ttu-id="ff86d-1271">ASP.NET Core, oluşturulan yollarla bir parametre dönüştürücüler kullanmak için API kuralları sağlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1271">ASP.NET Core provides API conventions for using a parameter transformers with generated routes:</span></span>

* <span data-ttu-id="ff86d-1272">ASP.NET Core MVC 'nin `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API kuralı vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1272">ASP.NET Core MVC has the `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API convention.</span></span> <span data-ttu-id="ff86d-1273">Bu kural, uygulamadaki tüm öznitelik yollarına belirtilen bir parametre transformatörü uygular.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1273">This convention applies a specified parameter transformer to all attribute routes in the app.</span></span> <span data-ttu-id="ff86d-1274">Parametre transformatörü, öznitelik yol belirteçlerini değiştirildiklerinde dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1274">The parameter transformer transforms attribute route tokens as they are replaced.</span></span> <span data-ttu-id="ff86d-1275">Daha fazla bilgi için bkz. [belirteç değişimini özelleştirmek için bir parametre transformatörü kullanma](/aspnet/core/mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1275">For more information, see [Use a parameter transformer to customize token replacement](/aspnet/core/mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span></span>
* Razor<span data-ttu-id="ff86d-1276">Sayfaların `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API kuralı vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1276"> Pages has the `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API convention.</span></span> <span data-ttu-id="ff86d-1277">Bu kural, belirtilen bir parametre transformatörü otomatik olarak bulunan tüm Razor sayfalara uygular.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1277">This convention applies a specified parameter transformer to all automatically discovered Razor Pages.</span></span> <span data-ttu-id="ff86d-1278">Parametre transformatörü, sayfa yollarının klasör ve dosya adı segmentlerini dönüştürür Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1278">The parameter transformer transforms the folder and file name segments of Razor Pages routes.</span></span> <span data-ttu-id="ff86d-1279">Daha fazla bilgi için bkz. [sayfa yollarını özelleştirmek için bir parametre transformatörü kullanma](/aspnet/core/razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1279">For more information, see [Use a parameter transformer to customize page routes](/aspnet/core/razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span></span>

## <a name="url-generation-reference"></a><span data-ttu-id="ff86d-1280">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1280">URL generation reference</span></span>

<span data-ttu-id="ff86d-1281">Aşağıdaki örnek, yol değerlerinin ve bir sözlüğünün bir sözlüğüne verilen bir yolun bağlantısının nasıl oluşturulacağını gösterir <xref:Microsoft.AspNetCore.Routing.RouteCollection> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1281">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="ff86d-1282"><xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath>Önceki örnek sonunda oluşturulan `/package/create/123` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1282">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="ff86d-1283">Sözlük, `operation` `id` "paket yolunu izle" şablonunun ve rota değerlerini sağlar `package/{operation}/{id}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1283">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="ff86d-1284">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1284">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="ff86d-1285">Oluşturucunun ikinci parametresi <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> bir *ortam değerleri*koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1285">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="ff86d-1286">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1286">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="ff86d-1287">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1287">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="ff86d-1288">ASP.NET Core MVC uygulamasının `About` öğesinin eyleminde, `HomeController` `Index` &mdash; ortam değeri kullanılan eyleme bağlamak için denetleyici yol değerini belirtmeniz gerekmez `Home` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1288">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="ff86d-1289">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1289">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="ff86d-1290">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1290">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="ff86d-1291">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1291">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="ff86d-1292">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1292">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="ff86d-1293">Aşağıdaki tabloda, yol şablonu kullanılırken sonuç gösterilmektedir `{controller}/{action}/{id?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1293">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="ff86d-1294">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1294">Ambient Values</span></span>                     | <span data-ttu-id="ff86d-1295">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1295">Explicit Values</span></span>                        | <span data-ttu-id="ff86d-1296">Sonuç</span><span class="sxs-lookup"><span data-stu-id="ff86d-1296">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="ff86d-1297">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1297">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-1298">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1298">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="ff86d-1299">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1299">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-1300">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1300">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="ff86d-1301">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1301">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="ff86d-1302">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1302">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="ff86d-1303">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1303">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-1304">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1304">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="ff86d-1305">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1305">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="ff86d-1306">Bağlantı oluşturma yalnızca ve için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur `controller` `action` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1306">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="ff86d-1307">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1307">Complex segments</span></span>

<span data-ttu-id="ff86d-1308">Karmaşık segmentler (örneğin `[Route("/x{token}y")]` ), sabit değerli olmayan değişmez değer ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1308">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="ff86d-1309">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1309">See [this code](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="ff86d-1310">[Kod örneği](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1310">The [code sample](https://github.com/dotnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>
<!-- While that code is no longer used by ASP.NET Core for complex segment matching, it provides a good match to the current algorithm. The [current code](https://github.com/dotnet/AspNetCore/blob/91514c9af7e0f4c44029b51f05a01c6fe4c96e4c/src/Http/Routing/src/Matching/DfaMatcherBuilder.cs#L227-L244) is too abstracted from matching to be useful for understanding complex segment matching.
-->

::: moniker-end

::: moniker range="< aspnetcore-2.2"

<span data-ttu-id="ff86d-1311">Yönlendirme, istek URI 'Lerini rota işleyicileriyle eşleştirmekten ve gelen istekleri gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1311">Routing is responsible for mapping request URIs to route handlers and dispatching an incoming requests.</span></span> <span data-ttu-id="ff86d-1312">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1312">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="ff86d-1313">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1313">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="ff86d-1314">Uygulamadan yapılandırılan yollar kullanıldığında, yönlendirme, yol işleyicileriyle eşlenen URL 'Ler oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1314">Using configured routes from the app, routing is able to generate URLs that map to route handlers.</span></span>

<span data-ttu-id="ff86d-1315">ASP.NET Core 2,1 ' de en son yönlendirme senaryolarını kullanmak için, ' de MVC Hizmetleri kaydının [Uyumluluk sürümünü](xref:mvc/compatibility-version) belirtin `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1315">To use the latest routing scenarios in ASP.NET Core 2.1, specify the [compatibility version](xref:mvc/compatibility-version) to the MVC services registration in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc()
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_1);
```

> [!IMPORTANT]
> <span data-ttu-id="ff86d-1316">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1316">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="ff86d-1317">ASP.NET Core MVC yönlendirme hakkında bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-1317">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="ff86d-1318">Sayfalardaki yönlendirme kuralları hakkında daha fazla bilgi için Razor bkz <xref:razor-pages/razor-pages-conventions> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-1318">For information on routing conventions in Razor Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="ff86d-1319">[Örnek kodu görüntüleme veya indirme](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceği](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="ff86d-1319">[View or download sample code](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="ff86d-1320">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1320">Routing basics</span></span>

<span data-ttu-id="ff86d-1321">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1321">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="ff86d-1322">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1322">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="ff86d-1323">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1323">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="ff86d-1324">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1324">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="ff86d-1325">Geliştiriciler, [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda (örneğin, blog ve e-ticaret uç noktaları) bir uygulamanın yüksek trafik bölümlerine daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1325">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations (for example, blog and ecommerce endpoints) using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span>

<span data-ttu-id="ff86d-1326">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1326">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="ff86d-1327">Bu, aynı mantıksal kaynaktaki birçok işlemin (örneğin, GET, POST) aynı URL 'YI kullanacağı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1327">This means that many operations (for example, GET, POST) on the same logical resource will use the same URL.</span></span> <span data-ttu-id="ff86d-1328">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1328">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

Razor<span data-ttu-id="ff86d-1329">Sayfalar uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1329"> Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="ff86d-1330">Sayfa yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1330">Additional conventions are available that allow you to customize Razor Pages routing behavior.</span></span> <span data-ttu-id="ff86d-1331">Daha fazla bilgi için <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1331">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="ff86d-1332">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1332">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="ff86d-1333">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1333">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="ff86d-1334">Yönlendirme, ' nin rotalar uygulamalarını kullanır <xref:Microsoft.AspNetCore.Routing.IRouter> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1334">Routing uses routes implementations of <xref:Microsoft.AspNetCore.Routing.IRouter> to:</span></span>

* <span data-ttu-id="ff86d-1335">Gelen istekleri *Rota işleyicilerine*eşleyin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1335">Map incoming requests to *route handlers*.</span></span>
* <span data-ttu-id="ff86d-1336">Yanıtlarda kullanılan URL 'Leri oluşturun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1336">Generate the URLs used in responses.</span></span>

<span data-ttu-id="ff86d-1337">Varsayılan olarak, bir uygulama tek bir yollar koleksiyonuna sahiptir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1337">By default, an app has a single collection of routes.</span></span> <span data-ttu-id="ff86d-1338">Bir istek geldiğinde koleksiyondaki yollar, koleksiyonda var olan sırada işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1338">When a request arrives, the routes in the collection are processed in the order that they exist in the collection.</span></span> <span data-ttu-id="ff86d-1339">Çerçeve koleksiyondaki her bir rotada yöntemini çağırarak, bir gelen istek URL 'sini koleksiyondaki bir yola eşleştirmeye çalışır <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1339">The framework attempts to match an incoming request URL to a route in the collection by calling the <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> method on each route in the collection.</span></span> <span data-ttu-id="ff86d-1340">Yanıt, yönlendirme bilgilerine göre URL (örneğin, yeniden yönlendirme veya bağlantılar için) oluşturmak için yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1340">A response can use routing to generate URLs (for example, for redirection or links) based on route information and thus avoid hard-coded URLs, which helps maintainability.</span></span>

<span data-ttu-id="ff86d-1341">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1341">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="ff86d-1342">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1342">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="ff86d-1343">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1343">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="ff86d-1344"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1344"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="ff86d-1345">MVC/sayfalar gibi uygulama modelleri, Razor yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm yollarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1345">App models, such as MVC/Razor Pages, register all of their routes, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="ff86d-1346">Yanıt, yönlendirme bilgilerine göre URL (örneğin, yeniden yönlendirme veya bağlantılar için) oluşturmak için yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1346">A response can use routing to generate URLs (for example, for redirection or links) based on route information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="ff86d-1347">URL oluşturma, rastgele genişletilebilirliği destekleyen yollara dayalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1347">URL generation is based on routes, which support arbitrary extensibility.</span></span> <span data-ttu-id="ff86d-1348"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper>URL 'Ler oluşturmak için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1348"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>
<!-- fix [middleware](xref:fundamentals/middleware/index) -->
<span data-ttu-id="ff86d-1349">Yönlendirme, sınıfı tarafından bulunan [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1349">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="ff86d-1350">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve sayfalar uygulamalarında yönlendirmeyi işler Razor .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1350">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and Razor Pages apps.</span></span> <span data-ttu-id="ff86d-1351">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1351">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="ff86d-1352">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-1352">URL matching</span></span>

<span data-ttu-id="ff86d-1353">URL eşleştirme, yönlendirmenin bir *işleyiciye*gelen istek gönderdiğine yönelik işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1353">URL matching is the process by which routing dispatches an incoming request to a *handler*.</span></span> <span data-ttu-id="ff86d-1354">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1354">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="ff86d-1355">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1355">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="ff86d-1356">Gelen istekler, <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> sırayla her bir yoldaki yöntemi çağıran öğesini girer.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1356">Incoming requests enter the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware>, which calls the <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> method on each route in sequence.</span></span> <span data-ttu-id="ff86d-1357"><xref:Microsoft.AspNetCore.Routing.IRouter>Örnek, [routecontext. Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler*) değerini null olmayan olarak ayarlayarak isteğin *işleneceğini* seçer <xref:Microsoft.AspNetCore.Http.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1357">The <xref:Microsoft.AspNetCore.Routing.IRouter> instance chooses whether to *handle* the request by setting the [RouteContext.Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler*) to a non-null <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span> <span data-ttu-id="ff86d-1358">Bir yol istek için bir işleyici ayarlarsa, yol işleme duraklar ve işleyici isteği işlemek için çağrılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1358">If a route sets a handler for the request, route processing stops, and the handler is invoked to process the request.</span></span> <span data-ttu-id="ff86d-1359">İsteği işlemek için yol işleyicisi bulunmazsa, ara yazılım istek ardışık düzeninde sonraki bir ara yazılım için isteği kapatır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1359">If no route handler is found to process the request, the middleware hands the request off to the next middleware in the request pipeline.</span></span>

<span data-ttu-id="ff86d-1360">' Ye birincil giriş, <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> geçerli istekle Ilişkili [routecontext. HttpContext](xref:Microsoft.AspNetCore.Routing.RouteContext.HttpContext*) ' dir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1360">The primary input to <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> is the [RouteContext.HttpContext](xref:Microsoft.AspNetCore.Routing.RouteContext.HttpContext*) associated with the current request.</span></span> <span data-ttu-id="ff86d-1361">[Routecontext. Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler) ve [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData*) , bir yol eşleştirdikten sonra çıkış olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1361">The [RouteContext.Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler) and [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData*) are outputs set after a route is matched.</span></span>

<span data-ttu-id="ff86d-1362">Çağıran bir eşleşme <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> de [routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özelliklerinin özelliklerini, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1362">A match that calls <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> also sets the properties of the [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="ff86d-1363">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1363">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="ff86d-1364">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1364">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="ff86d-1365">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1365">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="ff86d-1366"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*>Her rotayla durum verilerinin ilişkilendirilmesini desteklemek için sağlanır, böylece uygulama hangi yolun eşleştiğini temel alarak kararlar alabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1366"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="ff86d-1367">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="ff86d-1367">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="ff86d-1368">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1368">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="ff86d-1369">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1369">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="ff86d-1370">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1370">Routes can be nested inside of one another.</span></span> <span data-ttu-id="ff86d-1371"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers>Özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1371">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="ff86d-1372">Genellikle, içindeki ilk öğe <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1372">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="ff86d-1373">İçindeki son öğe, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1373">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation"></a><span data-ttu-id="ff86d-1374">URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1374">URL generation</span></span>

<span data-ttu-id="ff86d-1375">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1375">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="ff86d-1376">Bu, yönlendirme işleyicileri ve bunlara erişen URL 'Ler arasındaki mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1376">This allows for a logical separation between route handlers and the URLs that access them.</span></span>

<span data-ttu-id="ff86d-1377">URL oluşturma benzer bir yinelemeli işlem izler, ancak yol koleksiyonu metoduna çağıran kullanıcı veya çerçeve kodu ile başlar <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1377">URL generation follows a similar iterative process, but it starts with user or framework code calling into the <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> method of the route collection.</span></span> <span data-ttu-id="ff86d-1378">Her *yol* , <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> null olmayan bir değer döndürülene kadar metodu sırayla çağırılır <xref:Microsoft.AspNetCore.Routing.VirtualPathData> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1378">Each *route* has its <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> method called in sequence until a non-null <xref:Microsoft.AspNetCore.Routing.VirtualPathData> is returned.</span></span>

<span data-ttu-id="ff86d-1379">Birincil girdileri <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1379">The primary inputs to <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> are:</span></span>

* [<span data-ttu-id="ff86d-1380">VirtualPathContext. HttpContext</span><span class="sxs-lookup"><span data-stu-id="ff86d-1380">VirtualPathContext.HttpContext</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext)
* [<span data-ttu-id="ff86d-1381">VirtualPathContext. Values</span><span class="sxs-lookup"><span data-stu-id="ff86d-1381">VirtualPathContext.Values</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values)
* [<span data-ttu-id="ff86d-1382">VirtualPathContext. AmbientValues</span><span class="sxs-lookup"><span data-stu-id="ff86d-1382">VirtualPathContext.AmbientValues</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues)

<span data-ttu-id="ff86d-1383">Yollar birincil olarak, tarafından belirtilen yol değerlerini kullanır ve <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> bir URL oluşturma ve hangi değerlerin dahil edileceğini belirlemek için.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1383">Routes primarily use the route values provided by <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> and <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> to decide whether it's possible to generate a URL and what values to include.</span></span> <span data-ttu-id="ff86d-1384">, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> Geçerli istekle eşleştirmeden üretilmiş olan rota değerleri kümesidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1384">The <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> are the set of route values that were produced from matching the current request.</span></span> <span data-ttu-id="ff86d-1385">Buna karşılık, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> geçerli işlem için Istenen URL 'nin nasıl oluşturulacağını belirten rota değerlerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1385">In contrast, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> are the route values that specify how to generate the desired URL for the current operation.</span></span> <span data-ttu-id="ff86d-1386">, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext> Bir yolun geçerli bağlamla ilişkili hizmetleri veya ek verileri alması durumunda sağlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1386">The <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext> is provided in case a route should obtain services or additional data associated with the current context.</span></span>

> [!TIP]
> <span data-ttu-id="ff86d-1387">Virtualpathcontext [. Values](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values*) öğesini [Virtualpathcontext. AmbientValues](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues*)için bir geçersiz kılma kümesi olarak düşünün.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1387">Think of [VirtualPathContext.Values](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values*) as a set of overrides for the [VirtualPathContext.AmbientValues](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues*).</span></span> <span data-ttu-id="ff86d-1388">URL oluşturma, aynı rota veya yol değerlerini kullanan bağlantılar için URL 'Ler oluşturmak üzere geçerli istekten yol değerlerini yeniden kullanmayı dener.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1388">URL generation attempts to reuse route values from the current request to generate URLs for links using the same route or route values.</span></span>

<span data-ttu-id="ff86d-1389">Çıkışı <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> bir <xref:Microsoft.AspNetCore.Routing.VirtualPathData> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1389">The output of <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> is a <xref:Microsoft.AspNetCore.Routing.VirtualPathData>.</span></span> <span data-ttu-id="ff86d-1390"><xref:Microsoft.AspNetCore.Routing.VirtualPathData>, öğesinin bir paraleldir <xref:Microsoft.AspNetCore.Routing.RouteData> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1390"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> is a parallel of <xref:Microsoft.AspNetCore.Routing.RouteData>.</span></span> <span data-ttu-id="ff86d-1391"><xref:Microsoft.AspNetCore.Routing.VirtualPathData><xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath>Çıkış URL 'si için ve yol tarafından ayarlanması gereken bazı ek özellikler içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1391"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> contains the <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> for the output URL and some additional properties that should be set by the route.</span></span>

<span data-ttu-id="ff86d-1392">[VirtualPathData. VirtualPath](xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath*) özelliği, yol tarafından üretilen *sanal yolu* içerir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1392">The [VirtualPathData.VirtualPath](xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath*) property contains the *virtual path* produced by the route.</span></span> <span data-ttu-id="ff86d-1393">Gereksinimlerinize bağlı olarak, yolu daha fazla işlem yapmanız gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1393">Depending on your needs, you may need to process the path further.</span></span> <span data-ttu-id="ff86d-1394">Oluşturulan URL 'YI HTML 'de işlemek istiyorsanız, uygulamanın temel yolunu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1394">If you want to render the generated URL in HTML, prepend the base path of the app.</span></span>

<span data-ttu-id="ff86d-1395">[VirtualPathData. Router](xref:Microsoft.AspNetCore.Routing.VirtualPathData.Router*) , URL 'yi başarıyla oluşturmuş olan yola bir başvurudur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1395">The [VirtualPathData.Router](xref:Microsoft.AspNetCore.Routing.VirtualPathData.Router*) is a reference to the route that successfully generated the URL.</span></span>

<span data-ttu-id="ff86d-1396">[VirtualPathData. DataTokens](xref:Microsoft.AspNetCore.Routing.VirtualPathData.DataTokens*) özellikleri, URL 'yi oluşturan rotayla ilgili ek verilerin bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1396">The [VirtualPathData.DataTokens](xref:Microsoft.AspNetCore.Routing.VirtualPathData.DataTokens*) properties is a dictionary of additional data related to the route that generated the URL.</span></span> <span data-ttu-id="ff86d-1397">Bu, [RouteData. Datatoken](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*)'ların paraleldir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1397">This is the parallel of [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*).</span></span>

### <a name="create-routes"></a><span data-ttu-id="ff86d-1398">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1398">Create routes</span></span>

<span data-ttu-id="ff86d-1399">Yönlendirme <xref:Microsoft.AspNetCore.Routing.Route> sınıfı sınıfının standart uygulamasını sağlar <xref:Microsoft.AspNetCore.Routing.IRouter> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1399">Routing provides the <xref:Microsoft.AspNetCore.Routing.Route> class as the standard implementation of <xref:Microsoft.AspNetCore.Routing.IRouter>.</span></span> <span data-ttu-id="ff86d-1400"><xref:Microsoft.AspNetCore.Routing.Route>çağrıldığında URL yoluyla eşleştirilecek desenleri tanımlamak için *yol şablonu* sözdizimini kullanır <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1400"><xref:Microsoft.AspNetCore.Routing.Route> uses the *route template* syntax to define patterns to match against the URL path when <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> is called.</span></span> <span data-ttu-id="ff86d-1401"><xref:Microsoft.AspNetCore.Routing.Route>çağrıldığında bir URL oluşturmak için aynı rota şablonunu kullanır <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1401"><xref:Microsoft.AspNetCore.Routing.Route> uses the same route template to generate a URL when <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> is called.</span></span>

<span data-ttu-id="ff86d-1402">Çoğu uygulama, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ' de tanımlanan benzer uzantı yöntemlerinden birini çağırarak veya arayarak yollar oluşturur <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1402">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="ff86d-1403"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder>Uzantı yöntemlerinden herhangi biri bir örneği oluşturur <xref:Microsoft.AspNetCore.Routing.Route> ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1403">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="ff86d-1404"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>yol işleyici parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1404"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="ff86d-1405"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>yalnızca tarafından işlenen rotaları ekler <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1405"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="ff86d-1406">Varsayılan işleyici bir ' dir `IRouter` ve işleyici isteği işleyemeyebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1406">The default handler is an `IRouter`, and the handler might not handle the request.</span></span> <span data-ttu-id="ff86d-1407">Örneğin, ASP.NET Core MVC genellikle yalnızca kullanılabilir bir denetleyici ve eylemle eşleşen istekleri işleyen varsayılan bir işleyici olarak yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1407">For example, ASP.NET Core MVC is typically configured as a default handler that only handles requests that match an available controller and action.</span></span> <span data-ttu-id="ff86d-1408">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="ff86d-1408">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="ff86d-1409">Aşağıdaki kod örneği, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan bir çağrının örneğidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1409">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="ff86d-1410">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1410">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="ff86d-1411">Örneğin, yol `/Products/Details/17` Şu yol değerlerini oluşturur: `{ controller = Products, action = Details, id = 17 }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1411">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="ff86d-1412">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1412">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="ff86d-1413">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1413">Route parameters are named.</span></span> <span data-ttu-id="ff86d-1414">Küme ayraçları içinde parametre adının çevreleme tarafından tanımlanan parametreler `{ ... }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1414">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="ff86d-1415">Yukarıdaki şablon URL yoluyla da eşleştirebilir `/` ve değerleri üretebilir `{ controller = Home, action = Index }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1415">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="ff86d-1416">Bu durum, `{controller}` ve `{action}` Rota parametrelerinin varsayılan değerleri olduğu ve `id` route parametresinin isteğe bağlı olması nedeniyle oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1416">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="ff86d-1417">Bir eşittir işareti ( `=` ), yol parametre adından sonra gelen bir değer, parametre için varsayılan bir değer tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1417">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="ff86d-1418">`?`Yol parametre adından sonra bir soru işareti () isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1418">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="ff86d-1419">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1419">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="ff86d-1420">İsteğe bağlı parametreler, karşılık gelen bir URL yol kesimi yoksa rota değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1420">Optional parameters don't produce a route value if there is no corresponding URL path segment.</span></span> <span data-ttu-id="ff86d-1421">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1421">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="ff86d-1422">Aşağıdaki örnekte, Route parametresi tanımı `{id:int}` yol parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlar `id` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1422">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="ff86d-1423">Bu şablon, gibi bir URL yoluyla eşleşir `/Products/Details/17` `/Products/Details/Apples` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1423">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="ff86d-1424">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> , yol değerlerini uygulayıp onları doğrulamak için inceler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1424">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="ff86d-1425">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1425">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="ff86d-1426">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1426">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="ff86d-1427"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, Ve için değer kabul etmenin ek aşırı yüklemeleri `constraints` `dataTokens` `defaults` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1427">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="ff86d-1428">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1428">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="ff86d-1429">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekler eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1429">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="ff86d-1430">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1430">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="ff86d-1431">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1431">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="ff86d-1432">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1432">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{*article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="ff86d-1433">Önceki şablon, benzer bir URL yoluyla eşleşir `/Blog/All-About-Routing/Introduction` ve değerleri ayıklar `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1433">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="ff86d-1434">Ve için varsayılan yol değerleri `controller` , `action` şablonda karşılık gelen hiçbir yol parametresi olmasa bile rota tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1434">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="ff86d-1435">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1435">Default values can be specified in the route template.</span></span> <span data-ttu-id="ff86d-1436">`article`Route parametresi, yol parametre adından önce bir yıldız işareti () görünümüne göre *catch-all* olarak tanımlanır `*` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1436">The `article` route parameter is defined as a *catch-all* by the appearance of an asterisk (`*`) before the route parameter name.</span></span> <span data-ttu-id="ff86d-1437">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1437">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="ff86d-1438">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1438">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="ff86d-1439">Önceki şablon, benzer bir URL yoluyla eşleşir `/en-US/Products/5` ve değerleri `{ controller = Products, action = Details, id = 5 }` ve veri belirteçlerini ayıklar `{ locale = en-US }` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1439">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="ff86d-1441">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1441">Route class URL generation</span></span>

<span data-ttu-id="ff86d-1442">Sınıf, yol <xref:Microsoft.AspNetCore.Routing.Route> değerlerini bir kümesini rota şablonuyla BIRLEŞTIREREK URL oluşturma işlemi de gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1442">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="ff86d-1443">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1443">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="ff86d-1444">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1444">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="ff86d-1445">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="ff86d-1445">What values would be produced?</span></span> <span data-ttu-id="ff86d-1446">Bu, URL oluşturma işlevinin sınıfında nasıl çalıştığına ilişkin kaba bir eştir <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1446">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="ff86d-1447">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1447">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="ff86d-1448">Yol değerleriyle `{ controller = Products, action = List }` , URL `/Products/List` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1448">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="ff86d-1449">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1449">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="ff86d-1450">`id`, İsteğe bağlı bir yol parametresi olduğundan, URL 'si için bir değer olmadan başarıyla oluşturulur `id` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1450">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="ff86d-1451">Yol değerleriyle `{ controller = Home, action = Index }` , URL `/` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1451">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="ff86d-1452">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1452">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="ff86d-1453">Her iki URL de aşağıdaki yol tanımıyla (ve) birlikte gidiş dönüş, `/Home/Index` `/` URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1453">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="ff86d-1454">ASP.NET Core MVC kullanan bir uygulama <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> , doğrudan yönlendirmeye çağırmak yerine URL 'ler oluşturmak için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1454">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="ff86d-1455">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1455">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="ff86d-1456">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="ff86d-1456">Use routing middleware</span></span>

<span data-ttu-id="ff86d-1457">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1457">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="ff86d-1458">İçindeki hizmet kapsayıcısına yönlendirme Ekle `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1458">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="ff86d-1459">Yolların yönteminde yapılandırılması gerekir `Startup.Configure` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1459">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="ff86d-1460">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1460">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="ff86d-1461"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Yalnızca HTTP GET isteklerini eşleştirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1461"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="ff86d-1462">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1462">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="ff86d-1463">URI</span><span class="sxs-lookup"><span data-stu-id="ff86d-1463">URI</span></span>                    | <span data-ttu-id="ff86d-1464">Yanıt</span><span class="sxs-lookup"><span data-stu-id="ff86d-1464">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="ff86d-1465">Herkese!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1465">Hello!</span></span> <span data-ttu-id="ff86d-1466">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="ff86d-1466">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="ff86d-1467">Herkese!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1467">Hello!</span></span> <span data-ttu-id="ff86d-1468">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="ff86d-1468">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="ff86d-1469">Herkese!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1469">Hello!</span></span> <span data-ttu-id="ff86d-1470">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="ff86d-1470">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="ff86d-1471">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1471">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="ff86d-1472">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="ff86d-1472">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="ff86d-1473">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1473">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="ff86d-1474">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1474">The request falls through, no match.</span></span>              |

<span data-ttu-id="ff86d-1475">Tek bir yol yapılandırıyorsanız, <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*> bir örneği geçirme çağrısı yapın `IRouter` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1475">If you're configuring a single route, call <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*> passing in an `IRouter` instance.</span></span> <span data-ttu-id="ff86d-1476">Kullanmanız gerekmez <xref:Microsoft.AspNetCore.Routing.RouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1476">You won't need to use <xref:Microsoft.AspNetCore.Routing.RouteBuilder>.</span></span>

<span data-ttu-id="ff86d-1477">Framework, yollar () oluşturmak için bir genişletme yöntemleri kümesi sağlar <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions> :</span><span class="sxs-lookup"><span data-stu-id="ff86d-1477">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="ff86d-1478">Gibi listelenen yöntemlerin bazıları için <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> gerekir <xref:Microsoft.AspNetCore.Http.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1478">Some of listed methods, such as <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>, require a <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span> <span data-ttu-id="ff86d-1479"><xref:Microsoft.AspNetCore.Http.RequestDelegate>Yol eşleştiğinde *yol işleyicisi* olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1479">The <xref:Microsoft.AspNetCore.Http.RequestDelegate> is used as the *route handler* when the route matches.</span></span> <span data-ttu-id="ff86d-1480">Bu ailedeki diğer yöntemler, yönlendirme işleyicisi olarak kullanılmak üzere bir ara yazılım ardışık düzeni yapılandırmaya olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1480">Other methods in this family allow configuring a middleware pipeline for use as the route handler.</span></span> <span data-ttu-id="ff86d-1481">Yöntemi gibi `Map*` bir işleyiciyi kabul etmez, <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*> öğesini kullanır <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1481">If the `Map*` method doesn't accept a handler, such as <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>, it uses the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span>

<span data-ttu-id="ff86d-1482">`Map[Verb]`Yöntemler, yöntem ADıNDAKI http fiili ile rotayı sınırlandırmak için kısıtlamalar kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1482">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="ff86d-1483">Örneğin, bkz <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> . ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1483">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="ff86d-1484">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1484">Route template reference</span></span>

<span data-ttu-id="ff86d-1485">Küme ayraçları () içindeki belirteçler `{ ... }` , yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1485">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="ff86d-1486">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1486">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="ff86d-1487">Örneğin, `{controller=Home}{action=Index}` ve arasında değişmez değer olmadığından geçerli bir yol değil `{controller}` `{action}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1487">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="ff86d-1488">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1488">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="ff86d-1489">Yol parametrelerinden (örneğin, `{id}` ) ve yol ayırıcısından farklı bir metin, `/` URL içindeki metinle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1489">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="ff86d-1490">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1490">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="ff86d-1491">Bir sabit yol parametresi sınırlayıcısından ( `{` veya) eşleştirmek için `}` , karakteri (veya) tekrarlayarak sınırlayıcıdan kaçış `{{` `}}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1491">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="ff86d-1492">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1492">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="ff86d-1493">Örneğin, şablonu göz önünde bulundurun `files/{filename}.{ext?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1493">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="ff86d-1494">Hem hem de için değerler olduğunda `filename` `ext` her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1494">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="ff86d-1495">URL 'de yalnızca bir değeri `filename` varsa, sondaki nokta ( `.` ) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1495">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="ff86d-1496">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1496">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="ff86d-1497">`*`URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak yıldız işareti () kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1497">You can use the asterisk (`*`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="ff86d-1498">Buna *catch-all* parametresi denir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1498">This is called a *catch-all* parameter.</span></span> <span data-ttu-id="ff86d-1499">Örneğin, `blog/{*slug}` ile başlayan `/blog` ve bunu izleyen bir değere sahip olan ve yol değerine atanan HERHANGI bir URI ile eşleşir `slug` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1499">For example, `blog/{*slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="ff86d-1500">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1500">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="ff86d-1501">Yol ayırıcısı () karakterleri de dahil olmak üzere bir URL oluşturmak için, catch-all parametresi uygun karakterleri de çıkar `/` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1501">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="ff86d-1502">Örneğin, `foo/{*path}` yol değerlerini içeren yol `{ path = "my/path" }` oluşturulur `foo/my%2Fpath` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1502">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="ff86d-1503">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1503">Note the escaped forward slash.</span></span>

<span data-ttu-id="ff86d-1504">Yol parametreleri, parametre adından sonra bir eşittir işaretiyle () ayırarak varsayılan değer belirtilerek belirlenmiş *varsayılan değerlere* sahip olabilir `=` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1504">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="ff86d-1505">Örneğin, `{controller=Home}` `Home` için varsayılan değer olarak tanımlar `controller` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1505">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="ff86d-1506">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1506">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="ff86d-1507">Yol parametreleri `?` , içinde olduğu gibi parametre adının sonuna bir soru işareti () eklenerek isteğe bağlı olarak yapılır `id?` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1507">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="ff86d-1508">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden, &mdash; isteğe bağlı bir parametre yalnızca Istek URL 'si tarafından bir değer sağlandığı zaman bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1508">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="ff86d-1509">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1509">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="ff86d-1510">`:`Yol parametre adından sonra bir iki nokta () ve kısıtlama adı eklemek, bir rota parametresinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1510">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="ff86d-1511">Kısıtlama bağımsız değişkenler gerektiriyorsa, `(...)` kısıtlama adından sonra parantez () içine alınır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1511">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="ff86d-1512">Birden çok satır içi kısıtlama, başka bir iki nokta ( `:` ) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1512">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="ff86d-1513">Kısıtlama adı ve bağımsız değişkenler, <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> URL işlemede kullanılmak üzere bir örneği oluşturmak için hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1513">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="ff86d-1514">Örneğin, yol şablonu `blog/{article:minlength(10)}` `minlength` bağımsız değişkenle bir kısıtlama belirtir `10` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1514">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="ff86d-1515">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1515">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="ff86d-1516">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1516">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="ff86d-1517">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1517">Route Template</span></span>                           | <span data-ttu-id="ff86d-1518">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="ff86d-1518">Example Matching URI</span></span>    | <span data-ttu-id="ff86d-1519">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="ff86d-1519">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="ff86d-1520">Yalnızca tek bir yolla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1520">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="ff86d-1521">İle eşleşir ve `Page` ayarlanır `Home` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1521">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="ff86d-1522">İle eşleşir ve `Page` ayarlanır `Contact` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1522">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="ff86d-1523">`Products`Denetleyiciye ve `List` eyleme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1523">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="ff86d-1524">`Products`Denetleyici ve `Details` eyleme eşlenir ( `id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1524">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="ff86d-1525">`Home`Denetleyiciye ve `Index` yönteme eşlenir ( `id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1525">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="ff86d-1526">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1526">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="ff86d-1527">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1527">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="ff86d-1528">İstekleri eşleme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1528">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="route-constraint-reference"></a><span data-ttu-id="ff86d-1529">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1529">Route constraint reference</span></span>

<span data-ttu-id="ff86d-1530">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1530">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="ff86d-1531">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1531">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="ff86d-1532">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1532">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="ff86d-1533">Örneğin, <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> bir ISTEğI http fiiline bağlı olarak kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1533">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="ff86d-1534">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1534">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="ff86d-1535">**Giriş doğrulaması**için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1535">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="ff86d-1536">**Giriş doğrulaması**için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1536">If constraints are used for **input validation**, invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="ff86d-1537">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1537">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="ff86d-1538">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1538">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="ff86d-1539">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="ff86d-1539">constraint</span></span> | <span data-ttu-id="ff86d-1540">Örnek</span><span class="sxs-lookup"><span data-stu-id="ff86d-1540">Example</span></span> | <span data-ttu-id="ff86d-1541">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1541">Example Matches</span></span> | <span data-ttu-id="ff86d-1542">Notlar</span><span class="sxs-lookup"><span data-stu-id="ff86d-1542">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="ff86d-1543">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1543">`123456789`, `-123456789`</span></span> | <span data-ttu-id="ff86d-1544">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="ff86d-1544">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="ff86d-1545">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1545">`true`, `FALSE`</span></span> | <span data-ttu-id="ff86d-1546">Eşleşiyor `true` veya `false` (büyük/küçük harf duyarsız)</span><span class="sxs-lookup"><span data-stu-id="ff86d-1546">Matches `true` or `false` (case-insensitive)</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="ff86d-1547">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1547">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="ff86d-1548">`DateTime`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1548">Matches a valid `DateTime` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1549">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1549">See  preceding warning.</span></span>|
| `decimal` | `{price:decimal}` | <span data-ttu-id="ff86d-1550">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1550">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="ff86d-1551">`decimal`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1551">Matches a valid `decimal` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1552">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1552">See  preceding warning.</span></span>|
| `double` | `{weight:double}` | <span data-ttu-id="ff86d-1553">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1553">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="ff86d-1554">`double`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1554">Matches a valid `double` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1555">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1555">See  preceding warning.</span></span>|
| `float` | `{weight:float}` | <span data-ttu-id="ff86d-1556">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1556">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="ff86d-1557">`float`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1557">Matches a valid `float` value in the invariant culture.</span></span> <span data-ttu-id="ff86d-1558">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1558">See  preceding warning.</span></span>|
| `guid` | `{id:guid}` | <span data-ttu-id="ff86d-1559">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1559">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="ff86d-1560">Geçerli bir `Guid` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="ff86d-1560">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="ff86d-1561">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="ff86d-1561">`123456789`, `-123456789`</span></span> | <span data-ttu-id="ff86d-1562">Geçerli bir `long` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="ff86d-1562">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="ff86d-1563">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1563">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `Richard` | <span data-ttu-id="ff86d-1564">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1564">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="ff86d-1565">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1565">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="ff86d-1566">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1566">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="ff86d-1567">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1567">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="ff86d-1568">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1568">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="ff86d-1569">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1569">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="ff86d-1570">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır ( `a` - `z` büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="ff86d-1570">String must consist of one or more alphabetical characters (`a`-`z`, case-insensitive)</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="ff86d-1571">Dize, normal ifadeyle eşleşmelidir (normal ifade tanımlama hakkında ipuçlarına bakın)</span><span class="sxs-lookup"><span data-stu-id="ff86d-1571">String must match the regular expression (see tips about defining a regular expression)</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="ff86d-1572">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1572">Used to enforce that a non-parameter value is present during URL generation</span></span> |

<span data-ttu-id="ff86d-1573">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1573">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="ff86d-1574">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1574">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="ff86d-1575">URL 'YI doğrulayan ve bir CLR türüne (veya gibi) dönüştürülen yol kısıtlamaları `int` `DateTime` , her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1575">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="ff86d-1576">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1576">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="ff86d-1577">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1577">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="ff86d-1578">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1578">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="ff86d-1579">Örneğin, `float` kısıtlama yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1579">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="ff86d-1580">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1580">Regular expressions</span></span>

<span data-ttu-id="ff86d-1581">ASP.NET Core Framework, `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` normal ifade oluşturucusuna ekler.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1581">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="ff86d-1582"><xref:System.Text.RegularExpressions.RegexOptions>Bu üyelerin açıklaması için bkz..</span><span class="sxs-lookup"><span data-stu-id="ff86d-1582">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="ff86d-1583">Normal ifadeler, Yönlendirme ve C# dili tarafından kullanılanlarla benzerlik olan sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1583">Regular expressions use delimiters and tokens similar to those used by Routing and the C# language.</span></span> <span data-ttu-id="ff86d-1584">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1584">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="ff86d-1585">Yönlendirmenin normal ifadesini kullanmak için `^\d{3}-\d{2}-\d{4}$` , ifadenin `\` `\\` `\` dize kaçış karakterini (tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanmadıkça) atlamak için C# kaynak dosyasındaki dize (çift ters eğik çizgi) karakterleriyle belirtilen (tek ters eğik çizgi) karakterlere sahip olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1585">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing, the expression must have the `\` (single backslash) characters provided in the string as `\\` (double backslash) characters in the C# source file in order to escape the `\` string escape character (unless using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string)).</span></span> <span data-ttu-id="ff86d-1586">Yönlendirme parametresi sınırlayıcı karakterlerini ( `{` ,,, `}` ) atlamak için `[` `]` , ifadedeki karakterleri (,,, `{{` ) çift `}` `[[` `]]` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1586">To escape routing parameter delimiter characters (`{`, `}`, `[`, `]`), double the characters in the expression (`{{`, `}`, `[[`, `]]`).</span></span> <span data-ttu-id="ff86d-1587">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1587">The following table shows a regular expression and the escaped version.</span></span>

| <span data-ttu-id="ff86d-1588">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="ff86d-1588">Regular Expression</span></span>    | <span data-ttu-id="ff86d-1589">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="ff86d-1589">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="ff86d-1590">Yönlendirmelerde kullanılan normal ifadeler, genellikle şapka işareti ( `^` ) karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1590">Regular expressions used in routing often start with the caret (`^`) character and match starting position of the string.</span></span> <span data-ttu-id="ff86d-1591">İfadeler, genellikle dolar işareti ( `$` ) karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1591">The expressions often end with the dollar sign (`$`) character and match end of the string.</span></span> <span data-ttu-id="ff86d-1592">`^`Ve `$` karakterleri, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1592">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="ff86d-1593">`^`Ve karakterleri olmadan `$` normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1593">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="ff86d-1594">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1594">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="ff86d-1595">İfade</span><span class="sxs-lookup"><span data-stu-id="ff86d-1595">Expression</span></span>   | <span data-ttu-id="ff86d-1596">Dize</span><span class="sxs-lookup"><span data-stu-id="ff86d-1596">String</span></span>    | <span data-ttu-id="ff86d-1597">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="ff86d-1597">Match</span></span> | <span data-ttu-id="ff86d-1598">Yorum</span><span class="sxs-lookup"><span data-stu-id="ff86d-1598">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1599">hello</span><span class="sxs-lookup"><span data-stu-id="ff86d-1599">hello</span></span>     | <span data-ttu-id="ff86d-1600">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1600">Yes</span></span>   | <span data-ttu-id="ff86d-1601">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1601">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1602">123abc456</span><span class="sxs-lookup"><span data-stu-id="ff86d-1602">123abc456</span></span> | <span data-ttu-id="ff86d-1603">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1603">Yes</span></span>   | <span data-ttu-id="ff86d-1604">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1604">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1605">MZ</span><span class="sxs-lookup"><span data-stu-id="ff86d-1605">mz</span></span>        | <span data-ttu-id="ff86d-1606">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1606">Yes</span></span>   | <span data-ttu-id="ff86d-1607">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="ff86d-1607">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="ff86d-1608">MZ</span><span class="sxs-lookup"><span data-stu-id="ff86d-1608">MZ</span></span>        | <span data-ttu-id="ff86d-1609">Yes</span><span class="sxs-lookup"><span data-stu-id="ff86d-1609">Yes</span></span>   | <span data-ttu-id="ff86d-1610">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="ff86d-1610">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="ff86d-1611">hello</span><span class="sxs-lookup"><span data-stu-id="ff86d-1611">hello</span></span>     | <span data-ttu-id="ff86d-1612">Hayır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1612">No</span></span>    | <span data-ttu-id="ff86d-1613">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1613">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="ff86d-1614">123abc456</span><span class="sxs-lookup"><span data-stu-id="ff86d-1614">123abc456</span></span> | <span data-ttu-id="ff86d-1615">Hayır</span><span class="sxs-lookup"><span data-stu-id="ff86d-1615">No</span></span>    | <span data-ttu-id="ff86d-1616">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="ff86d-1616">See `^` and `$` above</span></span> |

<span data-ttu-id="ff86d-1617">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="ff86d-1617">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="ff86d-1618">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1618">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="ff86d-1619">Örneğin, `{action:regex(^(list|get|create)$)}` yalnızca `action` Rota değeri `list` ,, veya ile eşleşir `get` `create` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1619">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="ff86d-1620">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1620">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="ff86d-1621">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1621">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="ff86d-1622">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="ff86d-1622">Custom Route Constraints</span></span>

<span data-ttu-id="ff86d-1623">Yerleşik yol kısıtlamalarına ek olarak, arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1623">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="ff86d-1624"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>Arabirim, `Match` `true` kısıtlama karşılanıp Aksi takdirde döndüren tek bir yöntemi içerir `false` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1624">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="ff86d-1625">Özel bir kullanmak için <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> yol kısıtlama türü, uygulamanın uygulamanın hizmet kapsayıcısında kayıtlı olması gerekir <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1625">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="ff86d-1626"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, Yol kısıtlama anahtarlarını <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> Bu kısıtlamaları doğrulayan uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1626">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="ff86d-1627">Bir uygulama <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> , `Startup.ConfigureServices` hizmetlerin bir parçası olarak ' de güncelleştirilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan ile yapılandırma `services.Configure<RouteOptions>` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1627">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="ff86d-1628">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1628">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="ff86d-1629">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1629">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="ff86d-1630">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1630">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="url-generation-reference"></a><span data-ttu-id="ff86d-1631">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="ff86d-1631">URL generation reference</span></span>

<span data-ttu-id="ff86d-1632">Aşağıdaki örnek, yol değerlerinin ve bir sözlüğünün bir sözlüğüne verilen bir yolun bağlantısının nasıl oluşturulacağını gösterir <xref:Microsoft.AspNetCore.Routing.RouteCollection> .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1632">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="ff86d-1633"><xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath>Önceki örnek sonunda oluşturulan `/package/create/123` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1633">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="ff86d-1634">Sözlük, `operation` `id` "paket yolunu izle" şablonunun ve rota değerlerini sağlar `package/{operation}/{id}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1634">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="ff86d-1635">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1635">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="ff86d-1636">Oluşturucunun ikinci parametresi <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> bir *ortam değerleri*koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1636">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="ff86d-1637">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1637">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="ff86d-1638">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1638">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="ff86d-1639">ASP.NET Core MVC uygulamasının `About` öğesinin eyleminde, `HomeController` `Index` &mdash; ortam değeri kullanılan eyleme bağlamak için denetleyici yol değerini belirtmeniz gerekmez `Home` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1639">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="ff86d-1640">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1640">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="ff86d-1641">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1641">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="ff86d-1642">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1642">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="ff86d-1643">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1643">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="ff86d-1644">Aşağıdaki tabloda, yol şablonu kullanılırken sonuç gösterilmektedir `{controller}/{action}/{id?}` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1644">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="ff86d-1645">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1645">Ambient Values</span></span>                     | <span data-ttu-id="ff86d-1646">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1646">Explicit Values</span></span>                        | <span data-ttu-id="ff86d-1647">Sonuç</span><span class="sxs-lookup"><span data-stu-id="ff86d-1647">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="ff86d-1648">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1648">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-1649">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1649">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="ff86d-1650">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1650">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-1651">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1651">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="ff86d-1652">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1652">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="ff86d-1653">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1653">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="ff86d-1654">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1654">controller = "Home"</span></span>                | <span data-ttu-id="ff86d-1655">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="ff86d-1655">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="ff86d-1656">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="ff86d-1656">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="ff86d-1657">Bağlantı oluşturma yalnızca ve için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur `controller` `action` .</span><span class="sxs-lookup"><span data-stu-id="ff86d-1657">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="ff86d-1658">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="ff86d-1658">Complex segments</span></span>

<span data-ttu-id="ff86d-1659">Karmaşık segmentler (örneğin `[Route("/x{token}y")]` ), sabit değerli olmayan değişmez değer ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1659">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="ff86d-1660">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/aspnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1660">See [this code](https://github.com/aspnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="ff86d-1661">[Kod örneği](https://github.com/aspnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="ff86d-1661">The [code sample](https://github.com/aspnet/AspNetCore/blob/release/2.2/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>

::: moniker-end
