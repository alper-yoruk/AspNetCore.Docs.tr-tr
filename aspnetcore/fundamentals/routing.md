---
title: ASP.NET Core yönlendirme
author: rick-anderson
description: HTTP isteklerinden eşleşen ve yürütülebilir uç noktalara gönderen ASP.NET Core yönlendirmenin nasıl sorumlu olduğunu öğrenin.
monikerRange: '>= aspnetcore-2.1'
ms.author: riande
ms.custom: mvc
ms.date: 4/1/2020
no-loc:
- ':::no-loc(appsettings.json):::'
- ':::no-loc(ASP.NET Core Identity):::'
- ':::no-loc(cookie):::'
- ':::no-loc(Cookie):::'
- ':::no-loc(Blazor):::'
- ':::no-loc(Blazor Server):::'
- ':::no-loc(Blazor WebAssembly):::'
- ':::no-loc(Identity):::'
- ":::no-loc(Let's Encrypt):::"
- ':::no-loc(Razor):::'
- ':::no-loc(SignalR):::'
uid: fundamentals/routing
ms.openlocfilehash: e134832ad00b10bb01239afa06acc74d86707af1
ms.sourcegitcommit: 91e14f1e2a25c98a57c2217fe91b172e0ff2958c
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 11/10/2020
ms.locfileid: "94422567"
---
# <a name="routing-in-aspnet-core"></a><span data-ttu-id="dddbb-103">ASP.NET Core yönlendirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-103">Routing in ASP.NET Core</span></span>

<span data-ttu-id="dddbb-104">[Ryan şimdi ak](https://github.com/rynowak), [Kirk Larkabağı](https://twitter.com/serpent5)ve [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="dddbb-104">By [Ryan Nowak](https://github.com/rynowak), [Kirk Larkin](https://twitter.com/serpent5), and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

::: moniker range=">= aspnetcore-3.0"

<span data-ttu-id="dddbb-105">Yönlendirme, gelen HTTP isteklerini eşleştirmekten ve bu istekleri uygulamanın yürütülebilir uç noktalarına gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-105">Routing is responsible for matching incoming HTTP requests and dispatching those requests to the app's executable endpoints.</span></span> <span data-ttu-id="dddbb-106">[Uç noktalar](#endpoint) , uygulamanın yürütülebilir istek işleme kodu birimleridir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-106">[Endpoints](#endpoint) are the app's units of executable request-handling code.</span></span> <span data-ttu-id="dddbb-107">Uç noktalar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-107">Endpoints are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="dddbb-108">Uç nokta eşleştirme işlemi, isteğin URL 'sindeki değerleri çıkarabilir ve bu değerleri istek işleme için sağlayabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-108">The endpoint matching process can extract values from the request's URL and provide those values for request processing.</span></span> <span data-ttu-id="dddbb-109">Uygulamanın uç nokta bilgilerini kullanarak, yönlendirme, uç noktalarıyla eşlenen URL 'Ler de oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-109">Using endpoint information from the app, routing is also able to generate URLs that map to endpoints.</span></span>

<span data-ttu-id="dddbb-110">Uygulamalar, kullanarak yönlendirmeyi yapılandırabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-110">Apps can configure routing using:</span></span>

- <span data-ttu-id="dddbb-111">Denetleyiciler</span><span class="sxs-lookup"><span data-stu-id="dddbb-111">Controllers</span></span>
- <span data-ttu-id="dddbb-112">:::no-loc(Razor)::: Sayfaları</span><span class="sxs-lookup"><span data-stu-id="dddbb-112">:::no-loc(Razor)::: Pages</span></span>
- :::no-loc(SignalR):::
- <span data-ttu-id="dddbb-113">gRPC Hizmetleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-113">gRPC Services</span></span>
- <span data-ttu-id="dddbb-114">[Sistem durumu denetimleri](xref:host-and-deploy/health-checks)gibi uç nokta özellikli [Ara yazılım](xref:fundamentals/middleware/index) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-114">Endpoint-enabled [middleware](xref:fundamentals/middleware/index) such as [Health Checks](xref:host-and-deploy/health-checks).</span></span>
- <span data-ttu-id="dddbb-115">Yönlendirme ile kayıtlı temsilciler ve Lambdalar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-115">Delegates and lambdas registered with routing.</span></span>

<span data-ttu-id="dddbb-116">Bu belgede ASP.NET Core yönlendirmenin alt düzey ayrıntıları ele alınmaktadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-116">This document covers low-level details of ASP.NET Core routing.</span></span> <span data-ttu-id="dddbb-117">Yönlendirmeyi yapılandırma hakkında bilgi için:</span><span class="sxs-lookup"><span data-stu-id="dddbb-117">For information on configuring routing:</span></span>

* <span data-ttu-id="dddbb-118">Denetleyiciler için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-118">For controllers, see <xref:mvc/controllers/routing>.</span></span>
* <span data-ttu-id="dddbb-119">:::no-loc(Razor):::Sayfa kuralları için bkz <xref:razor-pages/razor-pages-conventions> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-119">For :::no-loc(Razor)::: Pages conventions, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="dddbb-120">Bu belgede açıklanan uç nokta yönlendirme sistemi, ASP.NET Core 3,0 ve üzeri için geçerlidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-120">The endpoint routing system described in this document applies to ASP.NET Core 3.0 and later.</span></span> <span data-ttu-id="dddbb-121">Önceki yönlendirme sistemiyle ilgili daha fazla bilgi için <xref:Microsoft.AspNetCore.Routing.IRouter> , aşağıdaki yaklaşımlardan birini kullanarak ASP.NET Core 2,1 sürümünü seçin:</span><span class="sxs-lookup"><span data-stu-id="dddbb-121">For information on the previous routing system based on <xref:Microsoft.AspNetCore.Routing.IRouter>, select the ASP.NET Core 2.1 version using one of the following approaches:</span></span>

* <span data-ttu-id="dddbb-122">Önceki sürümün sürüm Seçicisi.</span><span class="sxs-lookup"><span data-stu-id="dddbb-122">The version selector for a previous version.</span></span>
* <span data-ttu-id="dddbb-123">[ASP.NET Core 2,1 yönlendirme](?view=aspnetcore-2.1)öğesini seçin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-123">Select [ASP.NET Core 2.1 routing](?view=aspnetcore-2.1).</span></span>

<span data-ttu-id="dddbb-124">[Örnek kodu görüntüleme veya indirme](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples/3.x) ([nasıl indirileceği](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="dddbb-124">[View or download sample code](https://github.com/aspnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples/3.x) ([how to download](xref:index#how-to-download-a-sample))</span></span>

<span data-ttu-id="dddbb-125">Bu belgenin karşıdan yükleme örnekleri belirli bir sınıf tarafından etkinleştirilir `Startup` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-125">The download samples for this document are enabled by a specific `Startup` class.</span></span> <span data-ttu-id="dddbb-126">Belirli bir örneği çalıştırmak için, *program.cs* öğesini istenen sınıfı çağırmak üzere değiştirin `Startup` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-126">To run a specific sample, modify *Program.cs* to call the desired `Startup` class.</span></span>

## <a name="routing-basics"></a><span data-ttu-id="dddbb-127">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-127">Routing basics</span></span>

<span data-ttu-id="dddbb-128">Tüm ASP.NET Core şablonları oluşturulan koda yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-128">All ASP.NET Core templates include routing in the generated code.</span></span> <span data-ttu-id="dddbb-129">Yönlendirme, içindeki [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine kaydedilir `Startup.Configure` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-129">Routing is registered in the [middleware](xref:fundamentals/middleware/index) pipeline in `Startup.Configure`.</span></span>

<span data-ttu-id="dddbb-130">Aşağıdaki kod, yönlendirmenin temel bir örneğini göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-130">The following code shows a basic example of routing:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Startup.cs?name=snippet&highlight=8,10)]

<span data-ttu-id="dddbb-131">Yönlendirme, ve tarafından kaydedilen bir dizi ara yazılımı <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> kullanır <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-131">Routing uses a pair of middleware, registered by <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> and <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>:</span></span>

* <span data-ttu-id="dddbb-132">`UseRouting` ara yazılım ardışık düzenine eşleşen rota ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-132">`UseRouting` adds route matching to the middleware pipeline.</span></span> <span data-ttu-id="dddbb-133">Bu ara yazılım, uygulamada tanımlanan uç nokta kümesine bakar ve isteğe bağlı olarak [en iyi eşleşmeyi](#urlm) seçer.</span><span class="sxs-lookup"><span data-stu-id="dddbb-133">This middleware looks at the set of endpoints defined in the app, and selects the [best match](#urlm) based on the request.</span></span>
* <span data-ttu-id="dddbb-134">`UseEndpoints` uç nokta yürütmeyi ara yazılım ardışık düzenine ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-134">`UseEndpoints` adds endpoint execution to the middleware pipeline.</span></span> <span data-ttu-id="dddbb-135">Seçili uç noktayla ilişkili temsilciyi çalıştırır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-135">It runs the delegate associated with the selected endpoint.</span></span>

<span data-ttu-id="dddbb-136">Önceki örnekte, [Mapget](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*) yöntemi kullanılarak kod uç noktasına tek bir *yol* dahildir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-136">The preceding example includes a single *route to code* endpoint using the [MapGet](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*) method:</span></span>

* <span data-ttu-id="dddbb-137">`GET`Kök URL 'ye BIR http isteği gönderildiğinde `/` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-137">When an HTTP `GET` request is sent to the root URL `/`:</span></span>
  * <span data-ttu-id="dddbb-138">Gösterilen istek temsilcisi yürütülüyor.</span><span class="sxs-lookup"><span data-stu-id="dddbb-138">The request delegate shown executes.</span></span>
  * <span data-ttu-id="dddbb-139">`Hello World!` HTTP yanıtına yazılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-139">`Hello World!` is written to the HTTP response.</span></span> <span data-ttu-id="dddbb-140">Varsayılan olarak, kök URL 'sidir `/` `https://localhost:5001/` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-140">By default, the root URL `/` is `https://localhost:5001/`.</span></span>
* <span data-ttu-id="dddbb-141">İstek yöntemi yoksa `GET` veya kök URL 'si değilse `/` , hiçbir yol eşleşmesi ve bir HTTP 404 döndürülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-141">If the request method is not `GET` or the root URL is not `/`, no route matches and an HTTP 404 is returned.</span></span>

### <a name="endpoint"></a><span data-ttu-id="dddbb-142">Uç Nokta</span><span class="sxs-lookup"><span data-stu-id="dddbb-142">Endpoint</span></span>

<a name="endpoint"></a>

<span data-ttu-id="dddbb-143">`MapGet`Yöntemi, bir **uç noktayı** tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-143">The `MapGet` method is used to define an **endpoint**.</span></span> <span data-ttu-id="dddbb-144">Uç nokta şöyle olabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-144">An endpoint is something that can be:</span></span>

* <span data-ttu-id="dddbb-145">URL ve HTTP yöntemiyle eşleştirerek seçilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-145">Selected, by matching the URL and HTTP method.</span></span>
* <span data-ttu-id="dddbb-146">, Temsilcisi çalıştırılarak yürütülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-146">Executed, by running the delegate.</span></span>

<span data-ttu-id="dddbb-147">Uygulama tarafından eşleştirilecek ve çalıştırılabilen uç noktalar ' de yapılandırılır `UseEndpoints` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-147">Endpoints that can be matched and executed by the app are configured in `UseEndpoints`.</span></span> <span data-ttu-id="dddbb-148">Örneğin,, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*> <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*> ve [benzer yöntemler](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions) , istek temsilcilerinizi yönlendirme sistemine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-148">For example, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapGet*>, <xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions.MapPost*>, and [similar methods](xref:Microsoft.AspNetCore.Builder.EndpointRouteBuilderExtensions) connect request delegates to the routing system.</span></span>
<span data-ttu-id="dddbb-149">ASP.NET Core Framework özelliklerini yönlendirme sistemine bağlamak için ek yöntemler kullanılabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-149">Additional methods can be used to connect ASP.NET Core framework features to the routing system:</span></span>
- <span data-ttu-id="dddbb-150">[Sayfalar :::no-loc(Razor)::: için harita sayfaları :::no-loc(Razor):::](xref:Microsoft.AspNetCore.Builder.:::no-loc(Razor):::PagesEndpointRouteBuilderExtensions.Map:::no-loc(Razor):::Pages*)</span><span class="sxs-lookup"><span data-stu-id="dddbb-150">[Map:::no-loc(Razor):::Pages for :::no-loc(Razor)::: Pages](xref:Microsoft.AspNetCore.Builder.:::no-loc(Razor):::PagesEndpointRouteBuilderExtensions.Map:::no-loc(Razor):::Pages*)</span></span>
- [<span data-ttu-id="dddbb-151">Denetleyiciler için MapControllers</span><span class="sxs-lookup"><span data-stu-id="dddbb-151">MapControllers for controllers</span></span>](xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllers*)
- <span data-ttu-id="dddbb-152">[MapHub \<THub>:::no-loc(SignalR):::](xref:Microsoft.AspNetCore.:::no-loc(SignalR):::.HubRouteBuilder.MapHub*)</span><span class="sxs-lookup"><span data-stu-id="dddbb-152">[MapHub\<THub> for :::no-loc(SignalR):::](xref:Microsoft.AspNetCore.:::no-loc(SignalR):::.HubRouteBuilder.MapHub*)</span></span> 
- [<span data-ttu-id="dddbb-153">GRPC için MapGrpcService \<TService></span><span class="sxs-lookup"><span data-stu-id="dddbb-153">MapGrpcService\<TService> for gRPC</span></span>](xref:grpc/aspnetcore)

<span data-ttu-id="dddbb-154">Aşağıdaki örnekte, daha karmaşık bir yol şablonuyla yönlendirme gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-154">The following example shows routing with a more sophisticated route template:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/RouteTemplateStartup.cs?name=snippet)]

<span data-ttu-id="dddbb-155">Dize `/hello/{name:alpha}` bir **yol şablonudur**.</span><span class="sxs-lookup"><span data-stu-id="dddbb-155">The string `/hello/{name:alpha}` is a **route template**.</span></span> <span data-ttu-id="dddbb-156">Uç noktanın nasıl eşleştirileceği yapılandırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-156">It is used to configure how the endpoint is matched.</span></span> <span data-ttu-id="dddbb-157">Bu durumda, şablon eşleşir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-157">In this case, the template matches:</span></span>

* <span data-ttu-id="dddbb-158">Şunun gibi bir URL `/hello/Ryan`</span><span class="sxs-lookup"><span data-stu-id="dddbb-158">A URL like `/hello/Ryan`</span></span>
* <span data-ttu-id="dddbb-159">`/hello/`Arkasından bir alfabetik karakter dizisi ile başlayan herhangi BIR URL yolu.</span><span class="sxs-lookup"><span data-stu-id="dddbb-159">Any URL path that begins with `/hello/` followed by a sequence of alphabetic characters.</span></span>  <span data-ttu-id="dddbb-160">`:alpha` yalnızca alfabetik karakterlerle eşleşen bir rota kısıtlaması uygular.</span><span class="sxs-lookup"><span data-stu-id="dddbb-160">`:alpha` applies a route constraint that matches only alphabetic characters.</span></span> <span data-ttu-id="dddbb-161">[Yol kısıtlamaları](#route-constraint-reference) bu belgenin ilerleyen kısımlarında açıklanmıştır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-161">[Route constraints](#route-constraint-reference) are explained later in this document.</span></span>

<span data-ttu-id="dddbb-162">URL yolunun ikinci segmenti `{name:alpha}` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-162">The second segment of the URL path, `{name:alpha}`:</span></span>

* <span data-ttu-id="dddbb-163">`name`Parametreye bağlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-163">Is bound to the `name` parameter.</span></span>
* <span data-ttu-id="dddbb-164">Yakalanır ve [HttpRequest. RouteValues](xref:Microsoft.AspNetCore.Http.HttpRequest.RouteValues*)'da depolanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-164">Is captured and stored in [HttpRequest.RouteValues](xref:Microsoft.AspNetCore.Http.HttpRequest.RouteValues*).</span></span>

<span data-ttu-id="dddbb-165">Bu belgede açıklanan uç nokta yönlendirme sistemi ASP.NET Core 3,0 itibariyle yenidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-165">The endpoint routing system described in this document is new as of ASP.NET Core 3.0.</span></span> <span data-ttu-id="dddbb-166">Ancak, ASP.NET Core tüm sürümleri aynı yol şablonu özellikleri ve yol kısıtlamaları kümesini destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-166">However, all versions of ASP.NET Core support the same set of route template features and route constraints.</span></span>

<span data-ttu-id="dddbb-167">Aşağıdaki örnek, [durum denetimleri](xref:host-and-deploy/health-checks) ve yetkilendirmeyle yönlendirmeyi gösterir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-167">The following example shows routing with [health checks](xref:host-and-deploy/health-checks) and authorization:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/AuthorizationStartup.cs?name=snippet)]

[!INCLUDE[request localized comments](~/includes/code-comments-loc.md)]

<span data-ttu-id="dddbb-168">Yukarıdaki örnekte nasıl yapılacağı gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-168">The preceding example demonstrates how:</span></span>

* <span data-ttu-id="dddbb-169">Yetkilendirme ara yazılımı yönlendirmeyle birlikte kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-169">The authorization middleware can be used with routing.</span></span>
* <span data-ttu-id="dddbb-170">Uç noktalar, yetkilendirme davranışını yapılandırmak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-170">Endpoints can be used to configure authorization behavior.</span></span>

<span data-ttu-id="dddbb-171"><xref:Microsoft.AspNetCore.Builder.HealthCheckEndpointRouteBuilderExtensions.MapHealthChecks*>Çağrı bir sistem durumu denetim uç noktası ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-171">The <xref:Microsoft.AspNetCore.Builder.HealthCheckEndpointRouteBuilderExtensions.MapHealthChecks*> call adds a health check endpoint.</span></span> <span data-ttu-id="dddbb-172"><xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*>Bu çağrıda zincir, bir yetkilendirme ilkesini uç noktaya iliştirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-172">Chaining <xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*> on to this call attaches an authorization policy to the endpoint.</span></span>

<span data-ttu-id="dddbb-173"><xref:Microsoft.AspNetCore.Builder.AuthAppBuilderExtensions.UseAuthentication*> <xref:Microsoft.AspNetCore.Builder.AuthorizationAppBuilderExtensions.UseAuthorization*> Kimlik doğrulama ve yetkilendirme ara yazılımını çağırarak ve ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-173">Calling <xref:Microsoft.AspNetCore.Builder.AuthAppBuilderExtensions.UseAuthentication*> and <xref:Microsoft.AspNetCore.Builder.AuthorizationAppBuilderExtensions.UseAuthorization*> adds the authentication and authorization middleware.</span></span> <span data-ttu-id="dddbb-174">Bu ara yazılım, ile arasında yerleştirilir <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> ve `UseEndpoints` şunları yapabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-174">These middleware are placed between <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseRouting*> and `UseEndpoints` so that they can:</span></span>

* <span data-ttu-id="dddbb-175">Hangi uç noktanın seçili olduğunu görün `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-175">See which endpoint was selected by `UseRouting`.</span></span>
* <span data-ttu-id="dddbb-176">Uç noktaya göndermeden önce bir yetkilendirme ilkesi uygulayın <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-176">Apply an authorization policy before <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> dispatches to the endpoint.</span></span>

<a name="metadata"></a>

### <a name="endpoint-metadata"></a><span data-ttu-id="dddbb-177">Uç nokta meta verileri</span><span class="sxs-lookup"><span data-stu-id="dddbb-177">Endpoint metadata</span></span>

<span data-ttu-id="dddbb-178">Yukarıdaki örnekte, iki uç nokta bulunur, ancak yalnızca sistem durumu denetimi uç noktasına bağlı bir yetkilendirme ilkesi vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-178">In the preceding example, there are two endpoints, but only the health check endpoint has an authorization policy attached.</span></span> <span data-ttu-id="dddbb-179">İstek sistem durumu denetimi uç noktasıyla eşleşiyorsa, `/healthz` bir yetkilendirme denetimi gerçekleştirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-179">If the request matches the health check endpoint, `/healthz`, an authorization check is performed.</span></span> <span data-ttu-id="dddbb-180">Bu, uç noktalara eklenen ek verilere sahip olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-180">This demonstrates that endpoints can have extra data attached to them.</span></span> <span data-ttu-id="dddbb-181">Bu ek verilere uç nokta **meta verileri** denir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-181">This extra data is called endpoint **metadata** :</span></span>

* <span data-ttu-id="dddbb-182">Meta veriler, yönlendirme kullanan ara yazılım tarafından işlenebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-182">The metadata can be processed by routing-aware middleware.</span></span>
* <span data-ttu-id="dddbb-183">Meta veriler herhangi bir .NET türü olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-183">The metadata can be of any .NET type.</span></span>

## <a name="routing-concepts"></a><span data-ttu-id="dddbb-184">Yönlendirme kavramları</span><span class="sxs-lookup"><span data-stu-id="dddbb-184">Routing concepts</span></span>

<span data-ttu-id="dddbb-185">Yönlendirme sistemi güçlü **uç nokta** kavramı ekleyerek, ara yazılım ardışık düzeninin üst kısmında oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-185">The routing system builds on top of the middleware pipeline by adding the powerful **endpoint** concept.</span></span> <span data-ttu-id="dddbb-186">Uç noktalar, yönlendirme, yetkilendirme ve herhangi bir sayıda ASP.NET Core sistemi açısından birbirinden farklı olan uygulama işlevselliğinin birimlerini temsil eder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-186">Endpoints represent units of the app's functionality that are distinct from each other in terms of routing, authorization, and any number of ASP.NET Core's systems.</span></span>

<a name="endpoint"></a>

### <a name="aspnet-core-endpoint-definition"></a><span data-ttu-id="dddbb-187">ASP.NET Core uç noktası tanımı</span><span class="sxs-lookup"><span data-stu-id="dddbb-187">ASP.NET Core endpoint definition</span></span>

<span data-ttu-id="dddbb-188">ASP.NET Core uç noktası:</span><span class="sxs-lookup"><span data-stu-id="dddbb-188">An ASP.NET Core endpoint is:</span></span>

* <span data-ttu-id="dddbb-189">Yürütülebilir dosya: öğesine sahip <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-189">Executable: Has a <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate>.</span></span>
* <span data-ttu-id="dddbb-190">Genişletilebilir: [meta veri](xref:Microsoft.AspNetCore.Http.Endpoint.Metadata*) koleksiyonu vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-190">Extensible: Has a [Metadata](xref:Microsoft.AspNetCore.Http.Endpoint.Metadata*) collection.</span></span>
* <span data-ttu-id="dddbb-191">Seçilebilir: Isteğe bağlı olarak [yönlendirme bilgileri](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern*)vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-191">Selectable: Optionally, has [routing information](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern*).</span></span>
* <span data-ttu-id="dddbb-192">Numaralandırılabilir: bitiş noktaları koleksiyonu, kimden kaynağından alma yoluyla listelenebilir <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> . [DI](xref:fundamentals/dependency-injection)</span><span class="sxs-lookup"><span data-stu-id="dddbb-192">Enumerable: The collection of endpoints can be listed by retrieving the <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> from [DI](xref:fundamentals/dependency-injection).</span></span>

<span data-ttu-id="dddbb-193">Aşağıdaki kod, geçerli istekle eşleşen uç noktanın nasıl alınacağını ve inceleneceği gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-193">The following code shows how to retrieve and inspect the endpoint matching the current request:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/EndpointInspectorStartup.cs?name=snippet)]

<span data-ttu-id="dddbb-194">Seçildiğinde, uç nokta öğesinden alınabilir `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-194">The endpoint, if selected, can be retrieved from the `HttpContext`.</span></span> <span data-ttu-id="dddbb-195">Özellikleri incelenebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-195">Its properties can be inspected.</span></span> <span data-ttu-id="dddbb-196">Uç nokta nesneleri sabittir ve oluşturulduktan sonra değiştirilemez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-196">Endpoint objects are immutable and cannot be modified after creation.</span></span> <span data-ttu-id="dddbb-197">Uç noktanın en yaygın türü bir <xref:Microsoft.AspNetCore.Routing.RouteEndpoint> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-197">The most common type of endpoint is a <xref:Microsoft.AspNetCore.Routing.RouteEndpoint>.</span></span> <span data-ttu-id="dddbb-198">`RouteEndpoint` , yönlendirme sistemi tarafından seçilme olanağı sağlayan bilgiler içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-198">`RouteEndpoint` includes information that allows it to be to selected by the routing system.</span></span>

<span data-ttu-id="dddbb-199">Yukarıdaki kod, [uygulama. Kullanım](xref:Microsoft.AspNetCore.Builder.UseExtensions.Use*) , çevrimiçi bir [Ara yazılım](xref:fundamentals/middleware/index)yapılandırır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-199">In the preceding code, [app.Use](xref:Microsoft.AspNetCore.Builder.UseExtensions.Use*) configures an in-line [middleware](xref:fundamentals/middleware/index).</span></span>

<a name="mt"></a>

<span data-ttu-id="dddbb-200">Aşağıdaki kod, işlem hattında nereye `app.Use` çağrdığına bağlı olarak bir uç nokta olmayabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-200">The following code shows that, depending on where `app.Use` is called in the pipeline, there may not be an endpoint:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/MiddlewareFlowStartup.cs?name=snippet)]

<span data-ttu-id="dddbb-201">Bu önceki örnek `Console.WriteLine` , bir uç noktanın seçili olup olmadığını gösteren deyimler ekliyor.</span><span class="sxs-lookup"><span data-stu-id="dddbb-201">This preceding sample adds `Console.WriteLine` statements that display whether or not an endpoint has been selected.</span></span> <span data-ttu-id="dddbb-202">Netlik açısından örnek, belirtilen uç noktaya bir görünen ad atar `/` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-202">For clarity, the sample assigns a display name to the provided `/` endpoint.</span></span>

<span data-ttu-id="dddbb-203">Bu kodu, ekran URL 'SI ile çalıştırma `/` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-203">Running this code with a URL of `/` displays:</span></span>

```txt
1. Endpoint: (null)
2. Endpoint: Hello
3. Endpoint: Hello
```

<span data-ttu-id="dddbb-204">Bu kodu başka bir URL ile çalıştırmak şunları görüntüler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-204">Running this code with any other URL displays:</span></span>

```txt
1. Endpoint: (null)
2. Endpoint: (null)
4. Endpoint: (null)
```

<span data-ttu-id="dddbb-205">Bu çıkış şunları gösterir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-205">This output demonstrates that:</span></span>

* <span data-ttu-id="dddbb-206">Bitiş noktası, çağrılmadan önce her zaman null olur `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-206">The endpoint is always null before `UseRouting` is called.</span></span>
* <span data-ttu-id="dddbb-207">Bir eşleşme bulunursa, uç nokta ve arasında boş değildir `UseRouting` <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-207">If a match is found, the endpoint is non-null between `UseRouting` and <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span>
* <span data-ttu-id="dddbb-208">`UseEndpoints`Bir eşleşme bulunduğunda ara yazılım **terminaldir** .</span><span class="sxs-lookup"><span data-stu-id="dddbb-208">The `UseEndpoints` middleware is **terminal** when a match is found.</span></span> <span data-ttu-id="dddbb-209">[Terminal ara yazılımı](#tm) bu belgede daha sonra tanımlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-209">[Terminal middleware](#tm) is defined later in this document.</span></span>
* <span data-ttu-id="dddbb-210">`UseEndpoints`Yalnızca eşleşme bulunamadığında yürütme sonrasında ara yazılım.</span><span class="sxs-lookup"><span data-stu-id="dddbb-210">The middleware after `UseEndpoints` execute only when no match is found.</span></span>

<span data-ttu-id="dddbb-211">`UseRouting`Ara yazılım, uç noktayı geçerli bağlama eklemek Için [setendpoint](xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.SetEndpoint*) yöntemini kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-211">The `UseRouting` middleware uses the [SetEndpoint](xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.SetEndpoint*) method to attach the endpoint to the current context.</span></span> <span data-ttu-id="dddbb-212">`UseRouting`Ara yazılımı özel mantık ile değiştirmek ve uç noktaları kullanmanın avantajlarını almaya devam etmek mümkündür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-212">It's possible to replace the `UseRouting` middleware with custom logic and still get the benefits of using endpoints.</span></span> <span data-ttu-id="dddbb-213">Uç noktalar, ara yazılım gibi alt düzey bir temel 'tür ve yönlendirme uygulamasıyla birlikte aktarılmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-213">Endpoints are a low-level primitive like middleware, and aren't coupled to the routing implementation.</span></span> <span data-ttu-id="dddbb-214">Çoğu uygulamanın `UseRouting` özel mantık ile değiştirme gereksinimi yoktur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-214">Most apps don't need to replace `UseRouting` with custom logic.</span></span>

<span data-ttu-id="dddbb-215">`UseEndpoints`Ara yazılım, ara yazılım ile birlikte kullanılmak üzere tasarlanmıştır `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-215">The `UseEndpoints` middleware is designed to be used in tandem with the `UseRouting` middleware.</span></span> <span data-ttu-id="dddbb-216">Bir uç noktayı yürütmek için çekirdek mantık karmaşık değildir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-216">The core logic to execute an endpoint isn't complicated.</span></span> <span data-ttu-id="dddbb-217"><xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*>Uç noktayı almak için kullanın ve ardından <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate> özelliğini çağırın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-217">Use <xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*> to retrieve the endpoint, and then invoke its <xref:Microsoft.AspNetCore.Http.Endpoint.RequestDelegate> property.</span></span>

<span data-ttu-id="dddbb-218">Aşağıdaki kod, ara yazılımların yönlendirmeyi nasıl etkileyebileceğini veya tepki vermesini gösterir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-218">The following code demonstrates how middleware can influence or react to routing:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/IntegratedMiddlewareStartup.cs?name=snippet)]

<span data-ttu-id="dddbb-219">Yukarıdaki örnekte iki önemli kavram gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-219">The preceding example demonstrates two important concepts:</span></span>

* <span data-ttu-id="dddbb-220">Ara yazılım, yönlendirmenin çalıştığı verileri değiştirmeden önce çalışabilir `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-220">Middleware can run before `UseRouting` to modify the data that routing operates upon.</span></span>
    * <span data-ttu-id="dddbb-221">Genellikle, yönlendirme öncesinde görünen ara yazılım, veya gibi, isteğin bazı özelliğini değiştirir <xref:Microsoft.AspNetCore.Builder.RewriteBuilderExtensions.UseRewriter*> <xref:Microsoft.AspNetCore.Builder.HttpMethodOverrideExtensions.UseHttpMethodOverride*> <xref:Microsoft.AspNetCore.Builder.UsePathBaseExtensions.UsePathBase*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-221">Usually middleware that appears before routing modifies some property of the request, such as <xref:Microsoft.AspNetCore.Builder.RewriteBuilderExtensions.UseRewriter*>, <xref:Microsoft.AspNetCore.Builder.HttpMethodOverrideExtensions.UseHttpMethodOverride*>, or <xref:Microsoft.AspNetCore.Builder.UsePathBaseExtensions.UsePathBase*>.</span></span>
* <span data-ttu-id="dddbb-222">Ara yazılım, ve arasında çalıştırılabilir `UseRouting` ve <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> uç nokta yürütülmeden önce yönlendirmenin sonuçlarını işleyebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-222">Middleware can run between `UseRouting` and <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> to process the results of routing before the endpoint is executed.</span></span>
    * <span data-ttu-id="dddbb-223">Ve arasında çalışan ara `UseRouting` yazılımlar `UseEndpoints` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-223">Middleware that runs between `UseRouting` and `UseEndpoints`:</span></span>
      * <span data-ttu-id="dddbb-224">Genellikle uç noktaları anlamak için meta verileri inceler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-224">Usually inspects metadata to understand the endpoints.</span></span>
      * <span data-ttu-id="dddbb-225">, Ve tarafından yapıldığı gibi genellikle güvenlik kararları `UseAuthorization` verir `UseCors` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-225">Often makes security decisions, as done by `UseAuthorization` and `UseCors`.</span></span>
    * <span data-ttu-id="dddbb-226">Ara yazılım ve meta verilerin birleşimi, ilkelerin uç nokta başına yapılandırılmasını sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-226">The combination of middleware and metadata allows configuring policies per-endpoint.</span></span>

<span data-ttu-id="dddbb-227">Yukarıdaki kodda, uç nokta başına ilkeleri destekleyen özel bir ara yazılım örneği gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-227">The preceding code shows an example of a custom middleware that supports per-endpoint policies.</span></span> <span data-ttu-id="dddbb-228">Ara yazılım, gizli verilere erişim *denetim günlüğünü* konsola yazar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-228">The middleware writes an *audit log* of access to sensitive data to the console.</span></span> <span data-ttu-id="dddbb-229">Ara yazılım, meta verileri olan bir uç noktayı *denetlemek* üzere yapılandırılabilir `AuditPolicyAttribute` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-229">The middleware can be configured to *audit* an endpoint with the `AuditPolicyAttribute` metadata.</span></span> <span data-ttu-id="dddbb-230">Bu örnek, yalnızca hassas olarak işaretlenen bitiş noktalarının denetlendiği bir *katılım* modelini gösterir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-230">This sample demonstrates an *opt-in* pattern where only endpoints that are marked as sensitive are audited.</span></span> <span data-ttu-id="dddbb-231">Örneğin, güvenli olarak işaretlenmemiş her şeyi denetlemek için bu mantığı ters olarak tanımlamak mümkündür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-231">It's possible to define this logic in reverse, auditing everything that isn't marked as safe, for example.</span></span> <span data-ttu-id="dddbb-232">Uç nokta meta veri sistemi esnektir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-232">The endpoint metadata system is flexible.</span></span> <span data-ttu-id="dddbb-233">Bu mantık, kullanım örneğine uygun herhangi bir şekilde tasarlanabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-233">This logic could be designed in whatever way suits the use case.</span></span>

<span data-ttu-id="dddbb-234">Önceki örnek kod, uç noktaların temel kavramlarını göstermek için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-234">The preceding sample code is intended to demonstrate the basic concepts of endpoints.</span></span> <span data-ttu-id="dddbb-235">**Örnek, üretim kullanımı için tasarlanmamıştır**.</span><span class="sxs-lookup"><span data-stu-id="dddbb-235">**The sample is not intended for production use**.</span></span> <span data-ttu-id="dddbb-236">*Denetim günlüğü* ara yazılımı 'nın daha kapsamlı bir sürümü şöyle olacaktır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-236">A more complete version of an *audit log* middleware would:</span></span>

* <span data-ttu-id="dddbb-237">Bir dosya veya veritabanında oturum açın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-237">Log to a file or database.</span></span>
* <span data-ttu-id="dddbb-238">Kullanıcı, IP adresi, hassas bitiş noktasının adı ve daha fazlası gibi ayrıntıları dahil edin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-238">Include details such as the user, IP address, name of the sensitive endpoint, and more.</span></span>

<span data-ttu-id="dddbb-239">Denetim ilkesi meta verileri, `AuditPolicyAttribute` `Attribute` denetleyiciler ve gibi sınıf tabanlı çerçeveler ile daha kolay kullanılmak üzere tanımlanır :::no-loc(SignalR)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-239">The audit policy metadata `AuditPolicyAttribute` is defined as an `Attribute` for easier use with class-based frameworks such as controllers and :::no-loc(SignalR):::.</span></span> <span data-ttu-id="dddbb-240">*Kod yolu* kullanırken:</span><span class="sxs-lookup"><span data-stu-id="dddbb-240">When using *route to code* :</span></span>

* <span data-ttu-id="dddbb-241">Meta veriler bir Oluşturucu API 'siyle birlikte eklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-241">Metadata is attached with a builder API.</span></span>
* <span data-ttu-id="dddbb-242">Sınıf tabanlı çerçeveler, uç noktalar oluştururken karşılık gelen Yöntem ve sınıftaki tüm öznitelikleri içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-242">Class-based frameworks include all attributes on the corresponding method and class when creating endpoints.</span></span>

<span data-ttu-id="dddbb-243">Meta veri türleri için en iyi uygulamalar, bunları arabirimler ya da öznitelikler olarak tanımlamaktır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-243">The best practices for metadata types are to define them either as interfaces or attributes.</span></span> <span data-ttu-id="dddbb-244">Arabirimler ve öznitelikler kod yeniden kullanılmasına izin verir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-244">Interfaces and attributes allow code reuse.</span></span> <span data-ttu-id="dddbb-245">Meta veri sistemi esnektir ve herhangi bir kısıtlama vermez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-245">The metadata system is flexible and doesn't impose any limitations.</span></span>

<a name="tm"></a>

### <a name="comparing-a-terminal-middleware-and-routing"></a><span data-ttu-id="dddbb-246">Terminal ara yazılımını ve yönlendirmeyi karşılaştırma</span><span class="sxs-lookup"><span data-stu-id="dddbb-246">Comparing a terminal middleware and routing</span></span>

<span data-ttu-id="dddbb-247">Aşağıdaki kod örneği, yönlendirmeyi kullanarak ara yazılım kullanarak karşıtlıkları:</span><span class="sxs-lookup"><span data-stu-id="dddbb-247">The following code sample contrasts using middleware with using routing:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/TerminalMiddlewareStartup.cs?name=snippet)]

<span data-ttu-id="dddbb-248">İle gösterilen ara yazılım stili, `Approach 1:` **Terminal ara yazılımı**.</span><span class="sxs-lookup"><span data-stu-id="dddbb-248">The style of middleware shown with `Approach 1:` is **terminal middleware**.</span></span> <span data-ttu-id="dddbb-249">Bu, eşleşen bir işlem yaptığı için Terminal ara yazılımı olarak adlandırılır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-249">It's called terminal middleware because it does a matching operation:</span></span>

* <span data-ttu-id="dddbb-250">Yukarıdaki örnekteki eşleştirme işlemi, `Path == "/"` Ara yazılım ve `Path == "/Movie"` yönlendirme içindir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-250">The matching operation in the preceding sample is `Path == "/"` for the middleware and `Path == "/Movie"` for routing.</span></span>
* <span data-ttu-id="dddbb-251">Bir eşleşme başarılı olduğunda, ara yazılımı çağırmak yerine bazı işlevleri yürütür ve döndürür `next` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-251">When a match is successful, it executes some functionality and returns, rather than invoking the `next` middleware.</span></span>

<span data-ttu-id="dddbb-252">Bu, aramayı sonlandırdığı, bazı işlevleri yürüttüğünde ve sonra döndürdüğünden, Terminal ara yazılımı olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-252">It's called terminal middleware because it terminates the search, executes some functionality, and then returns.</span></span>

<span data-ttu-id="dddbb-253">Terminal ara yazılımını ve yönlendirmeyi karşılaştırma:</span><span class="sxs-lookup"><span data-stu-id="dddbb-253">Comparing a terminal middleware and routing:</span></span>
* <span data-ttu-id="dddbb-254">Her iki yaklaşım da işleme ardışık düzeninin sonlandırılmasını sağlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-254">Both approaches allow terminating the processing pipeline:</span></span>
    * <span data-ttu-id="dddbb-255">Ara yazılım, komut zinciri çağırmak yerine dönerek işlem hattını sonlandırır `next` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-255">Middleware terminates the pipeline by returning rather than invoking `next`.</span></span>
    * <span data-ttu-id="dddbb-256">Uç noktalar her zaman terminaldir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-256">Endpoints are always terminal.</span></span>
* <span data-ttu-id="dddbb-257">Terminal ara yazılımı, işlem hattındaki ara yazılımı rastgele bir yerde konumlandırmayı sağlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-257">Terminal middleware allows positioning the middleware at an arbitrary place in the pipeline:</span></span>
    * <span data-ttu-id="dddbb-258">Uç noktalar, konumunda yürütülür <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-258">Endpoints execute at the position of <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span>
* <span data-ttu-id="dddbb-259">Terminal ara yazılımı, rastgele kodun, ara yazılımın ne zaman eşleştiğini belirlemesine izin verir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-259">Terminal middleware allows arbitrary code to determine when the middleware matches:</span></span>
    * <span data-ttu-id="dddbb-260">Özel yol eşleştirme kodu ayrıntılı ve doğru yazılması zor olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-260">Custom route matching code can be verbose and difficult to write correctly.</span></span>
    * <span data-ttu-id="dddbb-261">Yönlendirme, tipik uygulamalar için doğrudan çözümler sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-261">Routing provides straightforward solutions for typical apps.</span></span> <span data-ttu-id="dddbb-262">Çoğu uygulama özel yol eşleştirme kodu gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-262">Most apps don't require custom route matching code.</span></span>
* <span data-ttu-id="dddbb-263">Ve gibi ara yazılım içeren uç noktalar arabirimi `UseAuthorization` `UseCors` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-263">Endpoints interface with middleware such as `UseAuthorization` and `UseCors`.</span></span>
    * <span data-ttu-id="dddbb-264">Veya ile Terminal ara yazılımı `UseAuthorization` kullanmak `UseCors` , yetkilendirme sistemiyle el ile arabirim oluşturma gerektirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-264">Using a terminal middleware with `UseAuthorization` or `UseCors` requires manual interfacing with the authorization system.</span></span>

<span data-ttu-id="dddbb-265">Bir [uç nokta](#endpoint) her ikisini de tanımlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-265">An [endpoint](#endpoint) defines both:</span></span>

* <span data-ttu-id="dddbb-266">İstekleri işlemek için bir temsilci.</span><span class="sxs-lookup"><span data-stu-id="dddbb-266">A delegate to process requests.</span></span>
* <span data-ttu-id="dddbb-267">Rastgele meta veri koleksiyonu.</span><span class="sxs-lookup"><span data-stu-id="dddbb-267">A collection of arbitrary metadata.</span></span> <span data-ttu-id="dddbb-268">Meta veriler, her uç noktaya eklenen ilkelere ve yapılandırmaya göre çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-268">The metadata is used to implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span>

<span data-ttu-id="dddbb-269">Terminal ara yazılımı etkin bir araç olabilir, ancak şunları gerektirebilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-269">Terminal middleware can be an effective tool, but can require:</span></span>

* <span data-ttu-id="dddbb-270">Önemli miktarda kodlama ve test.</span><span class="sxs-lookup"><span data-stu-id="dddbb-270">A significant amount of coding and testing.</span></span>
* <span data-ttu-id="dddbb-271">İstenen esneklik düzeyine ulaşmak için diğer sistemlerle el ile tümleştirme.</span><span class="sxs-lookup"><span data-stu-id="dddbb-271">Manual integration with other systems to achieve the desired level of flexibility.</span></span>

<span data-ttu-id="dddbb-272">Terminal ara yazılımı yazmadan önce yönlendirme ile tümleştirmeyi düşünün.</span><span class="sxs-lookup"><span data-stu-id="dddbb-272">Consider integrating with routing before writing a terminal middleware.</span></span>

<span data-ttu-id="dddbb-273">[Eşleme](xref:fundamentals/middleware/index#branch-the-middleware-pipeline) ile tümleştirilen mevcut Terminal ara yazılımı <xref:Microsoft.AspNetCore.Builder.MapWhenExtensions.MapWhen*> , genellikle bir yönlendirme duyarlı uç noktaya açılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-273">Existing terminal middleware that integrates with [Map](xref:fundamentals/middleware/index#branch-the-middleware-pipeline) or <xref:Microsoft.AspNetCore.Builder.MapWhenExtensions.MapWhen*> can usually be turned into a routing aware endpoint.</span></span> <span data-ttu-id="dddbb-274">[Maphealthdenetimleri](https://github.com/aspnet/AspNetCore/blob/master/src/Middleware/HealthChecks/src/Builder/HealthCheckEndpointRouteBuilderExtensions.cs#L16) , yönlendirici-Ware için bir model gösterir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-274">[MapHealthChecks](https://github.com/aspnet/AspNetCore/blob/master/src/Middleware/HealthChecks/src/Builder/HealthCheckEndpointRouteBuilderExtensions.cs#L16) demonstrates the pattern for router-ware:</span></span>
* <span data-ttu-id="dddbb-275">Üzerine bir genişletme yöntemi yazın <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-275">Write an extension method on <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder>.</span></span>
* <span data-ttu-id="dddbb-276">Kullanarak iç içe geçmiş bir ara yazılım işlem hattı oluşturun <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder.CreateApplicationBuilder*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-276">Create a nested middleware pipeline using <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder.CreateApplicationBuilder*>.</span></span>
* <span data-ttu-id="dddbb-277">Yeni işlem hattına ara yazılım ekleyin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-277">Attach the middleware to the new pipeline.</span></span> <span data-ttu-id="dddbb-278">Bu durumda, <xref:Microsoft.AspNetCore.Builder.HealthCheckApplicationBuilderExtensions.UseHealthChecks*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-278">In this case, <xref:Microsoft.AspNetCore.Builder.HealthCheckApplicationBuilderExtensions.UseHealthChecks*>.</span></span>
* <span data-ttu-id="dddbb-279"><xref:Microsoft.AspNetCore.Builder.IApplicationBuilder.Build*> içinde bir ara yazılım ardışık düzeni <xref:Microsoft.AspNetCore.Http.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-279"><xref:Microsoft.AspNetCore.Builder.IApplicationBuilder.Build*> the middleware pipeline into a <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span>
* <span data-ttu-id="dddbb-280">`Map`' İ çağırın ve yeni ara yazılım ardışık düzenini sağlayın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-280">Call `Map` and provide the new middleware pipeline.</span></span>
* <span data-ttu-id="dddbb-281">Uzantı yönteminden tarafından sunulan Oluşturucu nesnesini döndürün `Map` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-281">Return the builder object provided by `Map` from the extension method.</span></span>

<span data-ttu-id="dddbb-282">Aşağıdaki kod, [Maphealthdenetimlerinin](xref:host-and-deploy/health-checks)kullanımını gösterir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-282">The following code shows use of [MapHealthChecks](xref:host-and-deploy/health-checks):</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/AuthorizationStartup.cs?name=snippet)]

<span data-ttu-id="dddbb-283">Yukarıdaki örnek, Oluşturucu nesnesinin neden önemli olduğunu gösterir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-283">The preceding sample shows why returning the builder object is important.</span></span> <span data-ttu-id="dddbb-284">Oluşturucu nesnesini döndürmek, uygulama geliştiricisinin uç nokta için yetkilendirme gibi ilkeleri yapılandırmasına izin verir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-284">Returning the builder object allows the app developer to configure policies such as authorization for the endpoint.</span></span> <span data-ttu-id="dddbb-285">Bu örnekte, sistem durumu denetimleri ara yazılımı yetkilendirme sistemiyle doğrudan tümleştirme gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-285">In this example, the health checks middleware has no direct integration with the authorization system.</span></span>

<span data-ttu-id="dddbb-286">Meta veri sistemi, Terminal ara yazılımı kullanan genişletilebilirlik yazarları tarafından karşılaşılan sorunlara yanıt olarak oluşturulmuştur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-286">The metadata system was created in response to the problems encountered by extensibility authors using terminal middleware.</span></span> <span data-ttu-id="dddbb-287">Her bir ara yazılım, yetkilendirme sistemiyle kendi tümleştirmesini uygulamak için sorunlu olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-287">It's problematic for each middleware to implement its own integration with the authorization system.</span></span>

<a name="urlm"></a>

### <a name="url-matching"></a><span data-ttu-id="dddbb-288">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-288">URL matching</span></span>

* <span data-ttu-id="dddbb-289">, Yönlendirmenin bir [uç noktaya](#endpoint)gelen istekle eşleştiği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-289">Is the process by which routing matches an incoming request to an [endpoint](#endpoint).</span></span>
* <span data-ttu-id="dddbb-290">URL yolu ve üst bilgilerindeki verileri temel alır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-290">Is based on data in the URL path and headers.</span></span>
* <span data-ttu-id="dddbb-291">İstekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-291">Can be extended to consider any data in the request.</span></span>

<span data-ttu-id="dddbb-292">Bir yönlendirme ara yazılımı yürütüldüğünde, `Endpoint` geçerli istekten ' de bir [istek özelliğine](xref:fundamentals/request-features) bir ve rota değeri ayarlar <xref:Microsoft.AspNetCore.Http.HttpContext> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-292">When a routing middleware executes, it sets an `Endpoint` and route values to a [request feature](xref:fundamentals/request-features) on the <xref:Microsoft.AspNetCore.Http.HttpContext> from the current request:</span></span>

* <span data-ttu-id="dddbb-293">[HttpContext. GetEndpoint](<xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*>) çağrısı uç noktasını alır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-293">Calling [HttpContext.GetEndpoint](<xref:Microsoft.AspNetCore.Http.EndpointHttpContextExtensions.GetEndpoint*>) gets the endpoint.</span></span>
* <span data-ttu-id="dddbb-294">`HttpRequest.RouteValues` yol değerlerinin koleksiyonunu alır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-294">`HttpRequest.RouteValues` gets the collection of route values.</span></span>

<span data-ttu-id="dddbb-295">Yönlendirme ara yazılımı [, çalışan ara yazılım uç](xref:fundamentals/middleware/index) noktasını inceleyebilir ve işlem yapabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-295">[Middleware](xref:fundamentals/middleware/index) running after the routing middleware can inspect the endpoint and take action.</span></span> <span data-ttu-id="dddbb-296">Örneğin, bir yetkilendirme ara yazılımı, bir yetkilendirme ilkesi için bitiş noktasının meta veri koleksiyonunu sorgulanamıyor.</span><span class="sxs-lookup"><span data-stu-id="dddbb-296">For example, an authorization middleware can interrogate the endpoint's metadata collection for an authorization policy.</span></span> <span data-ttu-id="dddbb-297">İstek işleme ardışık düzeninde bulunan tüm ara yazılım yürütüldükten sonra, seçilen uç noktanın temsilcisi çağrılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-297">After all of the middleware in the request processing pipeline is executed, the selected endpoint's delegate is invoked.</span></span>

<span data-ttu-id="dddbb-298">Uç nokta yönlendirmesinde yönlendirme sistemi tüm gönderme kararlarından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-298">The routing system in endpoint routing is responsible for all dispatching decisions.</span></span> <span data-ttu-id="dddbb-299">Ara yazılım, seçili uç noktaya göre ilkeler uyguladığı için şu önemlidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-299">Because the middleware applies policies based on the selected endpoint, it's important that:</span></span>

* <span data-ttu-id="dddbb-300">Güvenlik ilkelerinin dağıtımını etkileyebilecek herhangi bir karar, yönlendirme sisteminin içinde yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-300">Any decision that can affect dispatching or the application of security policies is made inside the routing system.</span></span>

> [!WARNING]
> <span data-ttu-id="dddbb-301">Geriye dönük uyumluluk için, bir denetleyici veya :::no-loc(Razor)::: sayfa uç noktası temsilcisi yürütüldüğünde, [routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özellikleri, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-301">For backwards-compatibility, when a Controller or :::no-loc(Razor)::: Pages endpoint delegate is executed, the properties of [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) are set to appropriate values based on the request processing performed thus far.</span></span>
>
> <span data-ttu-id="dddbb-302">`RouteContext`Daha sonraki bir sürümde tür kullanılmıyor olarak işaretlenir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-302">The `RouteContext` type will be marked obsolete in a future release:</span></span>
>
> * <span data-ttu-id="dddbb-303">`RouteData.Values`Öğesine geçirin `HttpRequest.RouteValues` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-303">Migrate `RouteData.Values` to `HttpRequest.RouteValues`.</span></span>
> * <span data-ttu-id="dddbb-304">`RouteData.DataTokens`Uç nokta meta verilerinden [ıdatatokensmetadata](xref:Microsoft.AspNetCore.Routing.IDataTokensMetadata) almak için geçiş yapın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-304">Migrate `RouteData.DataTokens` to retrieve [IDataTokensMetadata](xref:Microsoft.AspNetCore.Routing.IDataTokensMetadata) from the endpoint metadata.</span></span>

<span data-ttu-id="dddbb-305">URL eşleştirme, yapılandırılabilir bir aşamalar kümesinde çalışır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-305">URL matching operates in a configurable set of phases.</span></span> <span data-ttu-id="dddbb-306">Her aşamada, çıktı bir eşleşme kümesidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-306">In each phase, the output is a set of matches.</span></span> <span data-ttu-id="dddbb-307">Eşleşmeler kümesi bir sonraki aşamadan sonra daha dar olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-307">The set of matches can be narrowed down further by the next phase.</span></span> <span data-ttu-id="dddbb-308">Yönlendirme uygulama, eşleşen uç noktalar için bir işlem sırası garantisi vermez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-308">The routing implementation does not guarantee a processing order for matching endpoints.</span></span> <span data-ttu-id="dddbb-309">**Tüm** olası eşleşmeler aynı anda işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-309">**All** possible matches are processed at once.</span></span> <span data-ttu-id="dddbb-310">URL ile eşleşen aşamalar aşağıdaki sırada oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-310">The URL matching phases occur in the following order.</span></span> <span data-ttu-id="dddbb-311">ASP.NET Core:</span><span class="sxs-lookup"><span data-stu-id="dddbb-311">ASP.NET Core:</span></span>

1. <span data-ttu-id="dddbb-312">URL yolunu uç noktalar kümesine ve bunların yol şablonlarına göre işler ve **Tüm** eşleşmeleri toplar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-312">Processes the URL path against the set of endpoints and their route templates, collecting **all** of the matches.</span></span>
1. <span data-ttu-id="dddbb-313">Önceki listeyi alır ve yol kısıtlamalarında başarısız olan eşleşmeleri kaldırır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-313">Takes the preceding list and removes matches that fail with route constraints applied.</span></span>
1. <span data-ttu-id="dddbb-314">Önceki listeyi alır ve [Matcherpolicy](xref:Microsoft.AspNetCore.Routing.MatcherPolicy) örnekleri kümesi başarısız olan eşleşmeleri kaldırır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-314">Takes the preceding list and removes matches that fail the set of [MatcherPolicy](xref:Microsoft.AspNetCore.Routing.MatcherPolicy) instances.</span></span>
1. <span data-ttu-id="dddbb-315">Önceki listeden son bir karar vermek için [Endpointselector](xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector) kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-315">Uses the [EndpointSelector](xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector) to make a final decision from the preceding list.</span></span>

<span data-ttu-id="dddbb-316">Uç noktaların listesi şunlara göre önceliklendirilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-316">The list of endpoints is prioritized according to:</span></span>

* <span data-ttu-id="dddbb-317">[Routeendpoint. Order](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order*)</span><span class="sxs-lookup"><span data-stu-id="dddbb-317">The [RouteEndpoint.Order](xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order*)</span></span>
* <span data-ttu-id="dddbb-318">[Yol şablonu önceliği](#rtp)</span><span class="sxs-lookup"><span data-stu-id="dddbb-318">The [route template precedence](#rtp)</span></span>

<span data-ttu-id="dddbb-319">Tüm eşleşen uç noktalar, sonuna kadar her aşamada işlenir <xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-319">All matching endpoints are processed in each phase until the <xref:Microsoft.AspNetCore.Routing.Matching.EndpointSelector> is reached.</span></span> <span data-ttu-id="dddbb-320">`EndpointSelector`Son aşamadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-320">The `EndpointSelector` is the final phase.</span></span> <span data-ttu-id="dddbb-321">En iyi eşleşme ile eşleştirmelerle en yüksek öncelikli uç noktayı seçer.</span><span class="sxs-lookup"><span data-stu-id="dddbb-321">It chooses the highest priority endpoint from the matches as the best match.</span></span> <span data-ttu-id="dddbb-322">En iyi eşleşme ile aynı önceliğe sahip başka eşleşmeler varsa, belirsiz eşleşme özel durumu oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-322">If there are other matches with the same priority as the best match, an ambiguous match exception is thrown.</span></span>

<span data-ttu-id="dddbb-323">Yol önceliği, daha yüksek öncelikli olarak verilen **daha belirli** bir yol şablonuna göre hesaplanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-323">The route precedence is computed based on a **more specific** route template being given a higher priority.</span></span> <span data-ttu-id="dddbb-324">Örneğin, şablonları ve şunları göz önünde bulundurun `/hello` `/{message}` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-324">For example, consider the templates `/hello` and `/{message}`:</span></span>

* <span data-ttu-id="dddbb-325">Her ikisi de URL yoluyla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-325">Both match the URL path `/hello`.</span></span>
* <span data-ttu-id="dddbb-326">`/hello`  daha özeldir ve bu nedenle daha yüksek önceliğe sahiptir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-326">`/hello`  is more specific and therefore higher priority.</span></span>

<span data-ttu-id="dddbb-327">Genel olarak, rota önceliği, uygulamada kullanılan URL şemaları türleri için en iyi eşleşmeyi seçme konusunda iyi bir iş olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-327">In general, route precedence does a good job of choosing the best match for the kinds of URL schemes used in practice.</span></span> <span data-ttu-id="dddbb-328"><xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order>Belirsizliği önlemek için yalnızca gerekli olduğunda kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-328">Use <xref:Microsoft.AspNetCore.Routing.RouteEndpoint.Order> only when necessary to avoid an ambiguity.</span></span>

<span data-ttu-id="dddbb-329">Yönlendirme tarafından sunulan genişletilebilirlik türleri nedeniyle, yönlendirme sisteminin belirsiz yolların önünde işlem yapmak mümkün değildir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-329">Due to the kinds of extensibility provided by routing, it isn't possible for the routing system to compute ahead of time the ambiguous routes.</span></span> <span data-ttu-id="dddbb-330">Yol şablonları gibi bir örnek düşünün `/{message:alpha}` `/{message:int}` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-330">Consider an example such as the route templates `/{message:alpha}` and `/{message:int}`:</span></span>

* <span data-ttu-id="dddbb-331">`alpha`Kısıtlama yalnızca alfabetik karakterlerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-331">The `alpha` constraint matches only alphabetic characters.</span></span>
* <span data-ttu-id="dddbb-332">`int`Kısıtlama yalnızca sayılarla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-332">The `int` constraint matches only numbers.</span></span>
* <span data-ttu-id="dddbb-333">Bu şablonlar aynı rota önceliğine sahiptir, ancak her iki eşleşen tek URL yoktur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-333">These templates have the same route precedence, but there's no single URL they both match.</span></span>
* <span data-ttu-id="dddbb-334">Yönlendirme sistemi başlangıçta bir belirsizlik hatası bildirse, bu geçerli kullanım durumunu engeller.</span><span class="sxs-lookup"><span data-stu-id="dddbb-334">If the routing system reported an ambiguity error at startup, it would block this valid use case.</span></span>

> [!WARNING]
>
> <span data-ttu-id="dddbb-335">İçindeki işlemlerin sırası <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> , yönlendirme davranışını tek bir özel durumla etkilemez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-335">The order of operations inside <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> doesn't influence the behavior of routing, with one exception.</span></span> <span data-ttu-id="dddbb-336"><xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*> ve, <xref:Microsoft.AspNetCore.Builder.MvcAreaRouteBuilderExtensions.MapAreaRoute*> çağrıldığı sıraya göre uç noktalarına otomatik olarak bir sipariş değeri atar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-336"><xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*> and <xref:Microsoft.AspNetCore.Builder.MvcAreaRouteBuilderExtensions.MapAreaRoute*> automatically assign an order value to their endpoints based on the order they are invoked.</span></span> <span data-ttu-id="dddbb-337">Bu, yönlendirme sistemi olmadan, eski yönlendirme uygulamalarıyla aynı garantilere sahip olmayan denetleyicilerin uzun süreli davranışlarına benzetir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-337">This simulates long-time behavior of controllers without the routing system providing the same guarantees as older routing implementations.</span></span>
>
> <span data-ttu-id="dddbb-338">Yönlendirmenin eski uygulamasında, yolların işlendiği sıraya bağımlılığı olan yönlendirme genişletilebilirliği uygulamak mümkündür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-338">In the legacy implementation of routing, it's possible to implement routing extensibility that has a dependency on the order in which routes are processed.</span></span> <span data-ttu-id="dddbb-339">ASP.NET Core 3,0 ve üzeri için uç nokta yönlendirme:</span><span class="sxs-lookup"><span data-stu-id="dddbb-339">Endpoint routing in ASP.NET Core 3.0 and later:</span></span>
> 
> * <span data-ttu-id="dddbb-340">Bir yol kavramı yoktur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-340">Doesn't have a concept of routes.</span></span>
> * <span data-ttu-id="dddbb-341">Sıralama garantisi sağlamaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-341">Doesn't provide ordering guarantees.</span></span> <span data-ttu-id="dddbb-342">Tüm uç noktalar aynı anda işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-342">All endpoints are processed at once.</span></span>

<a name="rtp"></a>

### <a name="route-template-precedence-and-endpoint-selection-order"></a><span data-ttu-id="dddbb-343">Yol şablonu önceliği ve uç nokta seçim sırası</span><span class="sxs-lookup"><span data-stu-id="dddbb-343">Route template precedence and endpoint selection order</span></span>

<span data-ttu-id="dddbb-344">[Yol şablonu önceliği](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L16) , her yol şablonuna, ne kadar belirli bir değere göre bir değer atayan bir sistemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-344">[Route template precedence](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L16) is a system that assigns each route template a value based on how specific it is.</span></span> <span data-ttu-id="dddbb-345">Yol şablonu önceliği:</span><span class="sxs-lookup"><span data-stu-id="dddbb-345">Route template precedence:</span></span>

* <span data-ttu-id="dddbb-346">Ortak durumlarda uç noktaların sırasını ayarlama ihtiyacını önler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-346">Avoids the need to adjust the order of endpoints in common cases.</span></span>
* <span data-ttu-id="dddbb-347">Yönlendirme davranışının ortak Sense beklentilerini eşleştirmeye çalışır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-347">Attempts to match the common-sense expectations of routing behavior.</span></span>

<span data-ttu-id="dddbb-348">Örneğin, şablonları ve öğesini `/Products/List` düşünün `/Products/{id}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-348">For example, consider templates `/Products/List` and `/Products/{id}`.</span></span> <span data-ttu-id="dddbb-349">`/Products/List`URL yolundan daha iyi bir eşleşme olduğunu varsaymak mantıklı olacaktır `/Products/{id}` `/Products/List` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-349">It would be reasonable to assume that `/Products/List` is a better match than `/Products/{id}` for the URL path `/Products/List`.</span></span> <span data-ttu-id="dddbb-350">Bu, değişmez değer segmentinin `/List` parametre segmentinden daha iyi önceliğe sahip olduğu kabul edildiği için geçerlidir `/{id}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-350">This works because the literal segment `/List` is considered to have better precedence than the parameter segment `/{id}`.</span></span>

<span data-ttu-id="dddbb-351">Önceliğin nasıl kullanılacağına ilişkin ayrıntılar, yönlendirme şablonlarının nasıl tanımlandığınıza bağlıdır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-351">The details of how precedence works are coupled to how route templates are defined:</span></span>

* <span data-ttu-id="dddbb-352">Daha fazla kesimli şablonlar daha belirgin olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-352">Templates with more segments are considered more specific.</span></span>
* <span data-ttu-id="dddbb-353">Değişmez değerli bir kesim, bir parametre segmentinden daha belirgin olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-353">A segment with literal text is considered more specific than a parameter segment.</span></span>
* <span data-ttu-id="dddbb-354">Kısıtlaması olan bir parametre segmenti, olmadan birden fazla olarak değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-354">A parameter segment with a constraint is considered more specific than one without.</span></span>
* <span data-ttu-id="dddbb-355">Karmaşık bir kesim, kısıtlama içeren bir parametre segmenti olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-355">A complex segment is considered as specific as a parameter segment with a constraint.</span></span>
* <span data-ttu-id="dddbb-356">Catch-all parametreleri en az özgüdür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-356">Catch-all parameters are the least specific.</span></span> <span data-ttu-id="dddbb-357">Catch **-All** rotaları hakkında önemli bilgiler için bkz. [route Template başvurusu](#rtr) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-357">See **catch-all** in the [Route template reference](#rtr) for important information on catch-all routes.</span></span>

<span data-ttu-id="dddbb-358">Tam değerler başvurusu için [GitHub 'da kaynak koda](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L189) bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-358">See the [source code on GitHub](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Template/RoutePrecedence.cs#L189) for a reference of exact values.</span></span>

<a name="lg"></a>

### <a name="url-generation-concepts"></a><span data-ttu-id="dddbb-359">URL oluşturma kavramları</span><span class="sxs-lookup"><span data-stu-id="dddbb-359">URL generation concepts</span></span>

<span data-ttu-id="dddbb-360">URL oluşturma:</span><span class="sxs-lookup"><span data-stu-id="dddbb-360">URL generation:</span></span>

* <span data-ttu-id="dddbb-361">, Yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturmalarına yönelik işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-361">Is the process by which routing can create a URL path based on a set of route values.</span></span>
* <span data-ttu-id="dddbb-362">Uç noktalar ve bunlara erişen URL 'Ler arasında bir mantıksal ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-362">Allows for a logical separation between endpoints and the URLs that access them.</span></span>

<span data-ttu-id="dddbb-363">Endpoint Routing <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'yi içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-363">Endpoint routing includes the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API.</span></span> <span data-ttu-id="dddbb-364">`LinkGenerator` , [dı](xref:fundamentals/dependency-injection)tarafından kullanılabilen bir tek hizmettir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-364">`LinkGenerator` is a singleton service available from [DI](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="dddbb-365">`LinkGenerator`API, yürütülen bir istek bağlamı dışında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-365">The `LinkGenerator` API can be used outside of the context of an executing request.</span></span> <span data-ttu-id="dddbb-366">Etiket Yardımcıları, HTML Yardımcıları ve eylem sonuçları gibi ' i kullanan [Mvc. ıurlhelper](xref:Microsoft.AspNetCore.Mvc.IUrlHelper) ve senaryoları <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> , [Action Results](xref:mvc/controllers/actions) [Tag Helpers](xref:mvc/views/tag-helpers/intro) `LinkGenerator` bağlantı oluşturma yetenekleri sağlamak için API 'yi dahili olarak kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-366">[Mvc.IUrlHelper](xref:Microsoft.AspNetCore.Mvc.IUrlHelper) and scenarios that rely on <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, such as [Tag Helpers](xref:mvc/views/tag-helpers/intro), HTML Helpers, and [Action Results](xref:mvc/controllers/actions), use the `LinkGenerator` API internally to provide link generating capabilities.</span></span>

<span data-ttu-id="dddbb-367">Bağlantı Oluşturucu, bir **Adres** ve **Adres şemaları** kavramıyla desteklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-367">The link generator is backed by the concept of an **address** and **address schemes**.</span></span> <span data-ttu-id="dddbb-368">Adres şeması, bağlantı oluşturma için göz önünde bulundurmanız gereken uç noktaları belirlemenin bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-368">An address scheme is a way of determining the endpoints that should be considered for link generation.</span></span> <span data-ttu-id="dddbb-369">Örneğin, yol adı ve yol değerleri senaryoları birçok kullanıcı, denetleyicilerden ve :::no-loc(Razor)::: sayfalardan bir adres düzeni olarak uygulanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-369">For example, the route name and route values scenarios many users are familiar with from controllers and :::no-loc(Razor)::: Pages are implemented as an address scheme.</span></span>

<span data-ttu-id="dddbb-370">Bağlantı Oluşturucu :::no-loc(Razor)::: aşağıdaki genişletme yöntemleri aracılığıyla denetleyicilere ve sayfalara bağlanabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-370">The link generator can link to controllers and :::no-loc(Razor)::: Pages via the following extension methods:</span></span>

* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetPathByAction*>
* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetUriByAction*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetPathByPage*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetUriByPage*>

<span data-ttu-id="dddbb-371">Bu yöntemlerin aşırı yüklemeleri, içeren bağımsız değişkenleri kabul eder `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-371">Overloads of these methods accept arguments that include the `HttpContext`.</span></span> <span data-ttu-id="dddbb-372">Bu yöntemler, [URL. Action](xref:System.Web.Mvc.UrlHelper.Action*) ve [URL. Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*)ile işlevsel olarak eşdeğerdir, ancak ek esneklik ve seçenekler sunar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-372">These methods are functionally equivalent to [Url.Action](xref:System.Web.Mvc.UrlHelper.Action*) and [Url.Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*), but offer additional flexibility and options.</span></span>

<span data-ttu-id="dddbb-373">`GetPath*`Yöntemleri `Url.Action` `Url.Page` , ve ' de, mutlak bir yol içeren bir URI oluşturmak için en çok benzerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-373">The `GetPath*` methods are most similar to `Url.Action` and `Url.Page`, in that they generate a URI containing an absolute path.</span></span> <span data-ttu-id="dddbb-374">`GetUri*`Yöntemler her zaman bir düzen ve konak içeren mutlak BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-374">The `GetUri*` methods always generate an absolute URI containing a scheme and host.</span></span> <span data-ttu-id="dddbb-375">Bir öğesini kabul eden yöntemler, `HttpContext` yürütülmekte olan istek bağlamında BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-375">The methods that accept an `HttpContext` generate a URI in the context of the executing request.</span></span> <span data-ttu-id="dddbb-376">[Ortam](#ambient) yolu DEĞERLERI, URL taban yolu, şeması ve yürütülen istekten ana bilgisayar, geçersiz kılınmadıkça kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-376">The [ambient](#ambient) route values, URL base path, scheme, and host from the executing request are used unless overridden.</span></span>

<span data-ttu-id="dddbb-377"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> bir adresle çağırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-377"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is called with an address.</span></span> <span data-ttu-id="dddbb-378">URI oluşturma iki adımda gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-378">Generating a URI occurs in two steps:</span></span>

1. <span data-ttu-id="dddbb-379">Adres, adresle eşleşen bir uç nokta listesine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-379">An address is bound to a list of endpoints that match the address.</span></span>
1. <span data-ttu-id="dddbb-380">Her uç nokta <xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern> , sağlanan değerlerle eşleşen bir yol deseninin bulunana kadar değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-380">Each endpoint's <xref:Microsoft.AspNetCore.Routing.RouteEndpoint.RoutePattern> is evaluated until a route pattern that matches the supplied values is found.</span></span> <span data-ttu-id="dddbb-381">Elde edilen çıktı, bağlantı oluşturucuya sağlanan diğer URI parçalarıyla birleştirilir ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-381">The resulting output is combined with the other URI parts supplied to the link generator and returned.</span></span>

<span data-ttu-id="dddbb-382">Tarafından sunulan yöntemler, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> herhangi bir adres türü için standart bağlantı oluşturma yeteneklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-382">The methods provided by <xref:Microsoft.AspNetCore.Routing.LinkGenerator> support standard link generation capabilities for any type of address.</span></span> <span data-ttu-id="dddbb-383">Bağlantı oluşturucuyu kullanmanın en kolay yolu, belirli bir adres türü için işlem gerçekleştiren genişletme yöntemlerine yöneliktir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-383">The most convenient way to use the link generator is through extension methods that perform operations for a specific address type:</span></span>

| <span data-ttu-id="dddbb-384">Genişletme yöntemi</span><span class="sxs-lookup"><span data-stu-id="dddbb-384">Extension Method</span></span> | <span data-ttu-id="dddbb-385">Açıklama</span><span class="sxs-lookup"><span data-stu-id="dddbb-385">Description</span></span> |
| ---------------- | ----------- |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*> | <span data-ttu-id="dddbb-386">Belirtilen değerleri temel alarak mutlak bir yola sahip bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-386">Generates a URI with an absolute path based on the provided values.</span></span> |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetUriByAddress*> | <span data-ttu-id="dddbb-387">Belirtilen değerleri temel alarak mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-387">Generates an absolute URI based on the provided values.</span></span>             |

> [!WARNING]
> <span data-ttu-id="dddbb-388">Çağırma yöntemlerinin aşağıdaki etkilerine dikkat edin <xref:Microsoft.AspNetCore.Routing.LinkGenerator> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-388">Pay attention to the following implications of calling <xref:Microsoft.AspNetCore.Routing.LinkGenerator> methods:</span></span>
>
> * <span data-ttu-id="dddbb-389">`GetUri*`Gelen isteklerin üstbilgisini doğrulayan bir uygulama yapılandırmasında uzantı yöntemlerini dikkatle kullanın `Host` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-389">Use `GetUri*` extension methods with caution in an app configuration that doesn't validate the `Host` header of incoming requests.</span></span> <span data-ttu-id="dddbb-390">`Host`Gelen isteklerin üstbilgisi doğrulanmaz, güvenilir olmayan istek girişi bir görünüm veya sayfadaki URI 'ler içinde istemciye geri gönderilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-390">If the `Host` header of incoming requests isn't validated, untrusted request input can be sent back to the client in URIs in a view or page.</span></span> <span data-ttu-id="dddbb-391">Tüm üretim uygulamalarının, `Host` üst bilgisini bilinen geçerli değerlere karşı doğrulamak için kendi sunucusunu yapılandırmasını öneririz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-391">We recommend that all production apps configure their server to validate the `Host` header against known valid values.</span></span>
>
> * <span data-ttu-id="dddbb-392"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>Veya ile birlikte ara yazılım içinde dikkatli kullanın `Map` `MapWhen` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-392">Use <xref:Microsoft.AspNetCore.Routing.LinkGenerator> with caution in middleware in combination with `Map` or `MapWhen`.</span></span> <span data-ttu-id="dddbb-393">`Map*` yürütülen isteğin temel yolunu değiştirir ve bu da bağlantı oluşturma çıktısını etkiler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-393">`Map*` changes the base path of the executing request, which affects the output of link generation.</span></span> <span data-ttu-id="dddbb-394">Tüm <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'ler temel yol belirtilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-394">All of the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> APIs allow specifying a base path.</span></span> <span data-ttu-id="dddbb-395">Bağlantı oluşturma etkisini geri almak için boş bir temel yol belirtin `Map*` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-395">Specify an empty base path to undo the `Map*` affect on link generation.</span></span>

### <a name="middleware-example"></a><span data-ttu-id="dddbb-396">Ara yazılım örneği</span><span class="sxs-lookup"><span data-stu-id="dddbb-396">Middleware example</span></span>

<span data-ttu-id="dddbb-397">Aşağıdaki örnekte, bir ara yazılım, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> Mağaza ürünlerini listeleyen bir eylem yöntemine bağlantı oluşturmak için API 'yi kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-397">In the following example, a middleware uses the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API to create a link to an action method that lists store products.</span></span> <span data-ttu-id="dddbb-398">Ekleme tarafından bir sınıfa ve çağırarak bir `GenerateLink` uygulamadaki herhangi bir sınıf için kullanılabilir olan bağlantı oluşturucuyu kullanma:</span><span class="sxs-lookup"><span data-stu-id="dddbb-398">Using the link generator by injecting it into a class and calling `GenerateLink` is available to any class in an app:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Middleware/ProductsLinkMiddleware.cs?name=snippet)]

<a name="rtr"></a>

## <a name="route-template-reference"></a><span data-ttu-id="dddbb-399">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-399">Route template reference</span></span>

<span data-ttu-id="dddbb-400">İçindeki belirteçler `{}` , yol eşleştirildiği takdirde bağlanan rota parametrelerini tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-400">Tokens within `{}` define route parameters that are bound if the route is matched.</span></span> <span data-ttu-id="dddbb-401">Yol segmentinde birden fazla yol parametresi tanımlanabilir, ancak yol parametreleri bir sabit değer ile ayrılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-401">More than one route parameter can be defined in a route segment, but route parameters  must be separated by a literal value.</span></span> <span data-ttu-id="dddbb-402">Örneğin, `{controller=Home}{action=Index}` ve arasında değişmez değer olmadığından geçerli bir yol değil `{controller}` `{action}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-402">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span>  <span data-ttu-id="dddbb-403">Rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-403">Route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="dddbb-404">Yol parametrelerinden (örneğin, `{id}` ) ve yol ayırıcısından farklı bir metin, `/` URL içindeki metinle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-404">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="dddbb-405">Metin eşleştirme, büyük/küçük harfe duyarsız ve URL 'nin yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-405">Text matching is case-insensitive and based on the decoded representation of the URL's path.</span></span> <span data-ttu-id="dddbb-406">Sabit bir yol parametresi sınırlayıcısından eşlemek `{` veya `}` karakteri tekrarlayarak sınırlayıcıdan kaçış.</span><span class="sxs-lookup"><span data-stu-id="dddbb-406">To match a literal route parameter delimiter `{` or `}`, escape the delimiter by repeating the character.</span></span> <span data-ttu-id="dddbb-407">Örneğin `{{` veya `}}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-407">For example `{{` or `}}`.</span></span>

<span data-ttu-id="dddbb-408">Yıldız işareti `*` veya çift yıldız işareti `**` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-408">Asterisk `*` or double asterisk `**`:</span></span>

* <span data-ttu-id="dddbb-409">URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-409">Can be used as a prefix to a route parameter to bind to the rest of the URI.</span></span>
* <span data-ttu-id="dddbb-410">, **Catch-all** parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-410">Are called a **catch-all** parameters.</span></span> <span data-ttu-id="dddbb-411">Örneğin `blog/{**slug}` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-411">For example, `blog/{**slug}`:</span></span>
  * <span data-ttu-id="dddbb-412">İle başlayan `/blog` ve bundan sonraki bir değere sahip tüm URI ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-412">Matches any URI that starts with `/blog` and has any value following it.</span></span>
  * <span data-ttu-id="dddbb-413">Aşağıdaki değer, `/blog` [başlık](https://developer.mozilla.org/docs/Glossary/Slug) yolu değerine atanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-413">The value following `/blog` is assigned to the [slug](https://developer.mozilla.org/docs/Glossary/Slug) route value.</span></span>

[!INCLUDE[](~/includes/catchall.md)]

<span data-ttu-id="dddbb-414">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-414">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="dddbb-415">Catch-all parametresi, yol ayırıcı karakterleri de dahil olmak üzere bir URL oluşturmak için kullanıldığında uygun karakterlerin bir kaçar `/` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-415">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator `/` characters.</span></span> <span data-ttu-id="dddbb-416">Örneğin, `foo/{*path}` yol değerlerini içeren yol `{ path = "my/path" }` oluşturulur `foo/my%2Fpath` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-416">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="dddbb-417">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-417">Note the escaped forward slash.</span></span> <span data-ttu-id="dddbb-418">Yol ayırıcı karakterlerini yuvarlaklaştırmak için `**` Rota parametresi önekini kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-418">To round-trip path separator characters, use the `**` route parameter prefix.</span></span> <span data-ttu-id="dddbb-419">İle rota `foo/{**path}` `{ path = "my/path" }` oluşturulur `foo/my/path` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-419">The route `foo/{**path}` with `{ path = "my/path" }` generates `foo/my/path`.</span></span>

<span data-ttu-id="dddbb-420">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-420">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="dddbb-421">Örneğin, şablonu göz önünde bulundurun `files/{filename}.{ext?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-421">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="dddbb-422">Hem hem de için değerler olduğunda `filename` `ext` her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-422">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="dddbb-423">URL 'de yalnızca bir değeri `filename` varsa, sondaki değer isteğe bağlı olduğundan yol eşleşir `.` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-423">If only a value for `filename` exists in the URL, the route matches because the trailing `.` is  optional.</span></span> <span data-ttu-id="dddbb-424">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="dddbb-424">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="dddbb-425">Yol parametreleri, parametre adından sonra bir eşittir işaretiyle () ayırarak varsayılan değer belirtilerek belirlenmiş **varsayılan değerlere** sahip olabilir `=` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-425">Route parameters may have **default values** designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="dddbb-426">Örneğin, `{controller=Home}` `Home` için varsayılan değer olarak tanımlar `controller` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-426">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="dddbb-427">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-427">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="dddbb-428">Yol parametreleri, parametre adının sonuna bir soru işareti () eklenerek isteğe bağlı olarak yapılır `?` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-428">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name.</span></span> <span data-ttu-id="dddbb-429">Örneğin, `id?`.</span><span class="sxs-lookup"><span data-stu-id="dddbb-429">For example, `id?`.</span></span> <span data-ttu-id="dddbb-430">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark şudur:</span><span class="sxs-lookup"><span data-stu-id="dddbb-430">The difference between optional values and default route parameters is:</span></span>

* <span data-ttu-id="dddbb-431">Varsayılan değere sahip bir yol parametresi her zaman bir değer üretir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-431">A route parameter with a default value always produces a value.</span></span>
* <span data-ttu-id="dddbb-432">İsteğe bağlı bir parametre yalnızca istek URL 'SI tarafından bir değer sağlandığında bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-432">An optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="dddbb-433">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-433">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="dddbb-434">`:`Yol parametre adından sonra ekleme ve kısıtlama adı, bir rota parametresinde bir satır içi kısıtlamayı belirtir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-434">Adding `:` and constraint name after the route parameter name specifies an inline constraint on a route parameter.</span></span> <span data-ttu-id="dddbb-435">Kısıtlama bağımsız değişkenler gerektiriyorsa, kısıtlama adından sonra parantez içine alınır `(...)` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-435">If the constraint requires arguments, they're enclosed in parentheses `(...)` after the constraint name.</span></span> <span data-ttu-id="dddbb-436">Birden çok *satır içi kısıtlama* , başka bir `:` kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-436">Multiple *inline constraints* can be specified by appending another `:` and constraint name.</span></span>

<span data-ttu-id="dddbb-437">Kısıtlama adı ve bağımsız değişkenler, <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> URL işlemede kullanılmak üzere bir örneği oluşturmak için hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-437">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="dddbb-438">Örneğin, yol şablonu `blog/{article:minlength(10)}` `minlength` bağımsız değişkenle bir kısıtlama belirtir `10` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-438">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="dddbb-439">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-439">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="dddbb-440">Rota parametrelerinin ayrıca parametre dönüştürücüler de olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-440">Route parameters may also have parameter transformers.</span></span> <span data-ttu-id="dddbb-441">Parametre dönüştürücüler, URL 'Ler için bağlantılar ve eşleşen eylemler ve sayfalar oluştururken parametrenin değerini dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-441">Parameter transformers transform a parameter's value when generating links and matching actions and pages to URLs.</span></span> <span data-ttu-id="dddbb-442">Benzer kısıtlamalar, `:` yol parametre adından sonra bir ve transformatör adı eklenerek, parametre dönüştürücüleri bir rota parametresine satır içi eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-442">Like constraints, parameter transformers can be added inline to a route parameter by adding a `:` and transformer name after the route parameter name.</span></span> <span data-ttu-id="dddbb-443">Örneğin, yol şablonu `blog/{article:slugify}` bir `slugify` transformatör belirtir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-443">For example, the route template `blog/{article:slugify}` specifies a `slugify` transformer.</span></span> <span data-ttu-id="dddbb-444">Parametre dönüştürücüler hakkında daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="dddbb-444">For more information on parameter transformers, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

<span data-ttu-id="dddbb-445">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-445">The following table demonstrates example route templates and their behavior:</span></span>

| <span data-ttu-id="dddbb-446">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="dddbb-446">Route Template</span></span>                           | <span data-ttu-id="dddbb-447">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="dddbb-447">Example Matching URI</span></span>    | <span data-ttu-id="dddbb-448">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="dddbb-448">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="dddbb-449">Yalnızca tek bir yolla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-449">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="dddbb-450">İle eşleşir ve `Page` ayarlanır `Home` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-450">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="dddbb-451">İle eşleşir ve `Page` ayarlanır `Contact` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-451">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="dddbb-452">`Products`Denetleyiciye ve `List` eyleme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-452">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="dddbb-453">`Products` `Details` 123 olarak ayarlanan denetleyiciye ve eyleme eşlenir `id` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-453">Maps to the `Products` controller and  `Details` action with`id` set to 123.</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="dddbb-454">`Home`Denetleyiciye ve `Index` yönteme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-454">Maps to the `Home` controller and `Index` method.</span></span> <span data-ttu-id="dddbb-455">`id` yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-455">`id` is ignored.</span></span>        |
| `{controller=Home}/{action=Index}/{id?}` | `/Products`         | <span data-ttu-id="dddbb-456">`Products`Denetleyiciye ve `Index` yönteme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-456">Maps to the `Products` controller and `Index` method.</span></span> <span data-ttu-id="dddbb-457">`id` yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-457">`id` is ignored.</span></span>        |

<span data-ttu-id="dddbb-458">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-458">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="dddbb-459">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-459">Constraints and defaults can also be specified outside the route template.</span></span>

### <a name="complex-segments"></a><span data-ttu-id="dddbb-460">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="dddbb-460">Complex segments</span></span>

<span data-ttu-id="dddbb-461">Karmaşık segmentler, en çok [greksuz olmayan](#greedy) bir şekilde, sabit değer sınırlayıcılarını sağdan sola eşleştirerek işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-461">Complex segments are processed by matching up literal delimiters from right to left in a [non-greedy](#greedy) way.</span></span> <span data-ttu-id="dddbb-462">Örneğin, `[Route("/a{b}c{d}")]` karmaşık bir kesimdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-462">For example, `[Route("/a{b}c{d}")]` is a complex segment.</span></span>
<span data-ttu-id="dddbb-463">Karmaşık segmentler, onları başarılı bir şekilde kullanmak için anlaşılması gereken belirli bir şekilde çalışır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-463">Complex segments work in a particular way that must be understood to use them successfully.</span></span> <span data-ttu-id="dddbb-464">Bu bölümdeki örnek, sınırlayıcı metnin parametre değerleri içinde görünmemesinin neden karmaşık segmentlerin gerçekten iyi şekilde çalıştığını gösterir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-464">The example in this section demonstrates why complex segments only really work well when the delimiter text doesn't appear inside the parameter values.</span></span> <span data-ttu-id="dddbb-465">Bir [Regex](/dotnet/standard/base-types/regular-expressions) kullanarak ve daha karmaşık durumlar için değerleri el ile ayıklamanız gerekir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-465">Using a [regex](/dotnet/standard/base-types/regular-expressions) and then manually extracting the values is needed for more complex cases.</span></span>

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="dddbb-466">Bu, yönlendirmenin şablon ve URL yolu ile gerçekleştirdiği adımların bir özetidir `/a{b}c{d}` `/abcd` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-466">This is a summary of the steps that routing performs with the template `/a{b}c{d}` and the URL path `/abcd`.</span></span> <span data-ttu-id="dddbb-467">, `|` Algoritmanın nasıl çalıştığını görselleştirmeye yardımcı olmak için kullanılır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-467">The `|` is used to help visualize how the algorithm works:</span></span>

* <span data-ttu-id="dddbb-468">İlk sabit değer, sağdan sola `c` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-468">The first literal, right to left, is `c`.</span></span> <span data-ttu-id="dddbb-469">Bu nedenle `/abcd` , doğru bir şekilde aranır ve bulur `/ab|c|d` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-469">So `/abcd` is searched from right and finds `/ab|c|d`.</span></span>
* <span data-ttu-id="dddbb-470">Right () öğesine ait her şey `d` artık Route parametresiyle eşleştirildi `{d}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-470">Everything to the right (`d`) is now matched to the route parameter `{d}`.</span></span>
* <span data-ttu-id="dddbb-471">Sonraki değişmez değer, sağdan sola `a` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-471">The next literal, right to left, is `a`.</span></span> <span data-ttu-id="dddbb-472">Bu nedenle `/ab|c|d` , ayrıldığımız yerden başlayarak arama yapılır `a` `/|a|b|c|d` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-472">So `/ab|c|d` is searched starting where we left off, then `a` is found `/|a|b|c|d`.</span></span>
* <span data-ttu-id="dddbb-473">Right ( `b` ) değeri artık Route parametresiyle eşleştirildi `{b}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-473">The value to the right (`b`) is now matched to the route parameter `{b}`.</span></span>
* <span data-ttu-id="dddbb-474">Kalan bir metin yok ve kalan yol şablonu yok, bu nedenle bu bir eşleşmedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-474">There is no remaining text and no remaining route template, so this is a match.</span></span>

<span data-ttu-id="dddbb-475">Aynı şablonu ve URL yolunu kullanarak negatif bir durum örneği aşağıda verilmiştir `/a{b}c{d}` `/aabcd` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-475">Here's an example of a negative case using the same template `/a{b}c{d}` and the URL path `/aabcd`.</span></span> <span data-ttu-id="dddbb-476">, `|` Algoritmanın nasıl çalıştığını görselleştirmeye yardımcı olmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-476">The `|` is used to help visualize how the algorithm works.</span></span> <span data-ttu-id="dddbb-477">Bu durum, aynı algoritma tarafından açıklanan bir eşleşme değildir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-477">This case isn't a match, which is explained by the same algorithm:</span></span>
* <span data-ttu-id="dddbb-478">İlk sabit değer, sağdan sola `c` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-478">The first literal, right to left, is `c`.</span></span> <span data-ttu-id="dddbb-479">Bu nedenle `/aabcd` , doğru bir şekilde aranır ve bulur `/aab|c|d` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-479">So `/aabcd` is searched from right and finds `/aab|c|d`.</span></span>
* <span data-ttu-id="dddbb-480">Right () öğesine ait her şey `d` artık Route parametresiyle eşleştirildi `{d}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-480">Everything to the right (`d`) is now matched to the route parameter `{d}`.</span></span>
* <span data-ttu-id="dddbb-481">Sonraki değişmez değer, sağdan sola `a` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-481">The next literal, right to left, is `a`.</span></span> <span data-ttu-id="dddbb-482">Bu nedenle `/aab|c|d` , ayrıldığımız yerden başlayarak arama yapılır `a` `/a|a|b|c|d` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-482">So `/aab|c|d` is searched starting where we left off, then `a` is found `/a|a|b|c|d`.</span></span>
* <span data-ttu-id="dddbb-483">Right ( `b` ) değeri artık Route parametresiyle eşleştirildi `{b}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-483">The value to the right (`b`) is now matched to the route parameter `{b}`.</span></span>
* <span data-ttu-id="dddbb-484">Bu noktada, kalan metin vardır `a` ancak algoritmanın ayrıştırılacak yol şablonu kalmadı, bu nedenle bu bir eşleşme değildir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-484">At this point there is remaining text `a`, but the algorithm has run out of route template to parse, so this is not a match.</span></span>

<span data-ttu-id="dddbb-485">Eşleşen algoritma [greyumsuz](#greedy)olmadığından:</span><span class="sxs-lookup"><span data-stu-id="dddbb-485">Since the matching algorithm is [non-greedy](#greedy):</span></span>

* <span data-ttu-id="dddbb-486">Her adımda mümkün olan en küçük metin miktarıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-486">It matches the smallest amount of text possible in each step.</span></span>
* <span data-ttu-id="dddbb-487">Sınırlayıcı değerin parametre değerleri içinde göründüğü herhangi bir durum, eşleşmemelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-487">Any case where the delimiter value appears inside the parameter values results in not matching.</span></span>

<span data-ttu-id="dddbb-488">Normal ifadeler, eşleşen davranışları üzerinde çok daha fazla denetim sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-488">Regular expressions provide much more control over their matching behavior.</span></span>

<a name="greedy"></a>

<span data-ttu-id="dddbb-489">Aynı zamanda [yavaş eşleşme](https://wikipedia.org/wiki/Regular_expression#Lazy_matching)olarak da bilinen Greedy eşleştirmesi, olası en büyük dizeyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-489">Greedy matching, also know as [lazy matching](https://wikipedia.org/wiki/Regular_expression#Lazy_matching), matches the largest possible string.</span></span> <span data-ttu-id="dddbb-490">Doyumsuz olmayan dize, olası en küçük dizeyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-490">Non-greedy matches the smallest possible string.</span></span>

## <a name="route-constraint-reference"></a><span data-ttu-id="dddbb-491">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-491">Route constraint reference</span></span>

<span data-ttu-id="dddbb-492">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-492">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="dddbb-493">Yol kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda doğru veya yanlış bir karar vermez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-493">Route constraints generally inspect the route value associated via the route template and make a true or false decision about whether the value is acceptable.</span></span> <span data-ttu-id="dddbb-494">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-494">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="dddbb-495">Örneğin, <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> bir ISTEğI http fiiline bağlı olarak kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-495">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="dddbb-496">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-496">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="dddbb-497">Giriş doğrulaması için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-497">Don't use constraints for input validation.</span></span> <span data-ttu-id="dddbb-498">Giriş doğrulaması için kısıtlamalar kullanılıyorsa, geçersiz giriş, bulunamayan bir Yanıt ile sonuçlanır `404` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-498">If constraints are used for input validation, invalid input results in a `404` Not Found response.</span></span> <span data-ttu-id="dddbb-499">Geçersiz giriş `400` , uygun bir hata iletisiyle hatalı bir istek üretmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-499">Invalid input should produce a `400` Bad Request with an appropriate error message.</span></span> <span data-ttu-id="dddbb-500">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların belirsizliğini ortadan kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-500">Route constraints are used to disambiguate similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="dddbb-501">Aşağıdaki tabloda örnek yol kısıtlamaları ve beklenen davranışlar gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-501">The following table demonstrates example route constraints and their expected behavior:</span></span>

| <span data-ttu-id="dddbb-502">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="dddbb-502">constraint</span></span> | <span data-ttu-id="dddbb-503">Örnek</span><span class="sxs-lookup"><span data-stu-id="dddbb-503">Example</span></span> | <span data-ttu-id="dddbb-504">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="dddbb-504">Example Matches</span></span> | <span data-ttu-id="dddbb-505">Notlar</span><span class="sxs-lookup"><span data-stu-id="dddbb-505">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="dddbb-506">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="dddbb-506">`123456789`, `-123456789`</span></span> | <span data-ttu-id="dddbb-507">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="dddbb-507">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="dddbb-508">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="dddbb-508">`true`, `FALSE`</span></span> | <span data-ttu-id="dddbb-509">Veya ile eşleşir `true` `false` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-509">Matches `true` or `false`.</span></span> <span data-ttu-id="dddbb-510">Büyük/küçük harf duyarsız</span><span class="sxs-lookup"><span data-stu-id="dddbb-510">Case-insensitive</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="dddbb-511">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="dddbb-511">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="dddbb-512">`DateTime`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-512">Matches a valid `DateTime` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-513">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-513">See preceding warning.</span></span> |
| `decimal` | `{price:decimal}` | <span data-ttu-id="dddbb-514">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="dddbb-514">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="dddbb-515">`decimal`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-515">Matches a valid `decimal` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-516">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-516">See preceding warning.</span></span>|
| `double` | `{weight:double}` | <span data-ttu-id="dddbb-517">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="dddbb-517">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="dddbb-518">`double`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-518">Matches a valid `double` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-519">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-519">See preceding warning.</span></span>|
| `float` | `{weight:float}` | <span data-ttu-id="dddbb-520">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="dddbb-520">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="dddbb-521">`float`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-521">Matches a valid `float` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-522">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-522">See preceding warning.</span></span>|
| `guid` | `{id:guid}` | `CD2C1638-1638-72D5-1638-DEADBEEF1638` | <span data-ttu-id="dddbb-523">Geçerli bir `Guid` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="dddbb-523">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="dddbb-524">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="dddbb-524">`123456789`, `-123456789`</span></span> | <span data-ttu-id="dddbb-525">Geçerli bir `long` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="dddbb-525">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="dddbb-526">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-526">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `MyFile` | <span data-ttu-id="dddbb-527">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-527">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="dddbb-528">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-528">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="dddbb-529">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-529">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="dddbb-530">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-530">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="dddbb-531">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-531">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="dddbb-532">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-532">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="dddbb-533">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır `a` - `z` ve büyük/küçük harfe duyarsız olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-533">String must consist of one or more alphabetical characters, `a`-`z` and case-insensitive.</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="dddbb-534">Dize, normal ifadeyle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-534">String must match the regular expression.</span></span> <span data-ttu-id="dddbb-535">Normal ifade tanımlama hakkında ipuçlarına bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-535">See tips about defining a regular expression.</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="dddbb-536">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="dddbb-536">Used to enforce that a non-parameter value is present during URL generation</span></span> |

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="dddbb-537">Birden çok, iki nokta üst üste sınırlı kısıtlama, tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-537">Multiple, colon delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="dddbb-538">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-538">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="dddbb-539">URL 'YI doğrulayan ve CLR türüne dönüştürülen yol kısıtlamaları her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-539">Route constraints that verify the URL and are converted to a CLR type always use the invariant culture.</span></span> <span data-ttu-id="dddbb-540">Örneğin, CLR türüne dönüştürme `int` veya `DateTime` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-540">For example, conversion to the CLR type `int` or `DateTime`.</span></span> <span data-ttu-id="dddbb-541">Bu kısıtlamalar, URL 'nin yerelleştirilebilir olmadığını varsayar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-541">These constraints assume that the URL is not localizable.</span></span> <span data-ttu-id="dddbb-542">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-542">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="dddbb-543">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-543">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="dddbb-544">Örneğin, `float` kısıtlama yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-544">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

### <a name="regular-expressions-in-constraints"></a><span data-ttu-id="dddbb-545">Kısıtlamalarda normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="dddbb-545">Regular expressions in constraints</span></span>

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="dddbb-546">Normal ifadeler, Route kısıtlaması kullanılarak satır içi kısıtlamalar olarak belirtilebilir `regex(...)` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-546">Regular expressions can be specified as inline constraints using the `regex(...)` route constraint.</span></span> <span data-ttu-id="dddbb-547"><xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*>Ailesindeki Yöntemler ayrıca Kısıtlamaların bir nesne sabit değerini de kabul eder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-547">Methods in the <xref:Microsoft.AspNetCore.Builder.ControllerEndpointRouteBuilderExtensions.MapControllerRoute*> family also accept an object literal of constraints.</span></span> <span data-ttu-id="dddbb-548">Bu form kullanılıyorsa, dize değerleri normal ifadeler olarak yorumlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-548">If that form is used, string values are interpreted as regular expressions.</span></span>

<span data-ttu-id="dddbb-549">Aşağıdaki kod, bir satır içi Regex kısıtlaması kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-549">The following code uses an inline regex constraint:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupRegex.cs?name=snippet)]

<span data-ttu-id="dddbb-550">Aşağıdaki kod, bir Regex kısıtlaması belirtmek için bir nesne değişmez değeri kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-550">The following code uses an object literal to specify a regex constraint:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupRegex2.cs?name=snippet)]

<span data-ttu-id="dddbb-551">ASP.NET Core Framework, `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` normal ifade oluşturucusuna ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-551">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="dddbb-552"><xref:System.Text.RegularExpressions.RegexOptions>Bu üyelerin açıklaması için bkz..</span><span class="sxs-lookup"><span data-stu-id="dddbb-552">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="dddbb-553">Normal ifadeler, Yönlendirme ve C# dili tarafından kullanılanlarla benzerlik olan sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-553">Regular expressions use delimiters and tokens similar to those used by routing and the C# language.</span></span> <span data-ttu-id="dddbb-554">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-554">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="dddbb-555">Normal ifadeyi `^\d{3}-\d{2}-\d{4}$` bir satır içi kısıtlamada kullanmak için aşağıdakilerden birini kullanın:</span><span class="sxs-lookup"><span data-stu-id="dddbb-555">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in an inline constraint, use one of the following:</span></span>

* <span data-ttu-id="dddbb-556">Dizede `\` belirtilen karakterleri, `\\` dize kaçış karakterinden kaçınmak Için C# kaynak dosyasındaki karakter olarak değiştirin `\` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-556">Replace `\` characters provided in the string as `\\` characters in the C# source file in order to escape the `\` string escape character.</span></span>
* <span data-ttu-id="dddbb-557">Tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string).</span><span class="sxs-lookup"><span data-stu-id="dddbb-557">[Verbatim string literals](/dotnet/csharp/language-reference/keywords/string).</span></span>

<span data-ttu-id="dddbb-558">Yönlendirme parametresi sınırlayıcı karakterlerini `{` ,,, `}` , `[` `]` ifadedeki karakterleri çift kaçış, örneğin `{{` `}}` `[[` `]]` ,,,,.</span><span class="sxs-lookup"><span data-stu-id="dddbb-558">To escape routing parameter delimiter characters `{`, `}`, `[`, `]`, double the characters in the expression, for example, `{{`, `}}`, `[[`, `]]`.</span></span> <span data-ttu-id="dddbb-559">Aşağıdaki tabloda, bir normal ifade ve bunun kaçış sürümü gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-559">The following table shows a regular expression and its escaped version:</span></span>

| <span data-ttu-id="dddbb-560">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="dddbb-560">Regular expression</span></span>    | <span data-ttu-id="dddbb-561">Kaçan normal ifade</span><span class="sxs-lookup"><span data-stu-id="dddbb-561">Escaped regular expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="dddbb-562">Yönlendirmelerde kullanılan normal ifadeler, genellikle karakteriyle başlar `^` ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-562">Regular expressions used in routing often start with the `^` character and match the starting position of the string.</span></span> <span data-ttu-id="dddbb-563">İfadeler genellikle `$` karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-563">The expressions often end with the `$` character and match the end of the string.</span></span> <span data-ttu-id="dddbb-564">`^`Ve `$` karakterleri, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-564">The `^` and `$` characters ensure that the regular expression matches the entire route parameter value.</span></span> <span data-ttu-id="dddbb-565">`^`Ve karakterleri olmadan `$` normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-565">Without the `^` and `$` characters, the regular expression matches any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="dddbb-566">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-566">The following table provides examples and explains why they match or fail to match:</span></span>

| <span data-ttu-id="dddbb-567">Expression</span><span class="sxs-lookup"><span data-stu-id="dddbb-567">Expression</span></span>   | <span data-ttu-id="dddbb-568">Dize</span><span class="sxs-lookup"><span data-stu-id="dddbb-568">String</span></span>    | <span data-ttu-id="dddbb-569">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-569">Match</span></span> | <span data-ttu-id="dddbb-570">Yorum</span><span class="sxs-lookup"><span data-stu-id="dddbb-570">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-571">hello</span><span class="sxs-lookup"><span data-stu-id="dddbb-571">hello</span></span>     | <span data-ttu-id="dddbb-572">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-572">Yes</span></span>   | <span data-ttu-id="dddbb-573">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-573">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-574">123abc456</span><span class="sxs-lookup"><span data-stu-id="dddbb-574">123abc456</span></span> | <span data-ttu-id="dddbb-575">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-575">Yes</span></span>   | <span data-ttu-id="dddbb-576">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-576">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-577">MZ</span><span class="sxs-lookup"><span data-stu-id="dddbb-577">mz</span></span>        | <span data-ttu-id="dddbb-578">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-578">Yes</span></span>   | <span data-ttu-id="dddbb-579">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="dddbb-579">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-580">MZ</span><span class="sxs-lookup"><span data-stu-id="dddbb-580">MZ</span></span>        | <span data-ttu-id="dddbb-581">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-581">Yes</span></span>   | <span data-ttu-id="dddbb-582">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="dddbb-582">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="dddbb-583">hello</span><span class="sxs-lookup"><span data-stu-id="dddbb-583">hello</span></span>     | <span data-ttu-id="dddbb-584">Hayır</span><span class="sxs-lookup"><span data-stu-id="dddbb-584">No</span></span>    | <span data-ttu-id="dddbb-585">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="dddbb-585">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="dddbb-586">123abc456</span><span class="sxs-lookup"><span data-stu-id="dddbb-586">123abc456</span></span> | <span data-ttu-id="dddbb-587">Hayır</span><span class="sxs-lookup"><span data-stu-id="dddbb-587">No</span></span>    | <span data-ttu-id="dddbb-588">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="dddbb-588">See `^` and `$` above</span></span> |

<span data-ttu-id="dddbb-589">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="dddbb-589">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="dddbb-590">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-590">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="dddbb-591">Örneğin, `{action:regex(^(list|get|create)$)}` yalnızca `action` Rota değeri `list` ,, veya ile eşleşir `get` `create` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-591">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="dddbb-592">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-592">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="dddbb-593">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğünde geçirilen kısıtlamalar, normal ifadeler olarak da değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-593">Constraints that are passed in the constraints dictionary that don't match one of the known constraints are also treated as regular expressions.</span></span> <span data-ttu-id="dddbb-594">Bilinen kısıtlamaların biriyle eşleşmeyen bir şablon içinde geçirilen kısıtlamalar normal ifadeler olarak değerlendirilmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-594">Constraints that are passed  within a template that don't match one of the known constraints are not treated as regular expressions.</span></span>

### <a name="custom-route-constraints"></a><span data-ttu-id="dddbb-595">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="dddbb-595">Custom route constraints</span></span>

<span data-ttu-id="dddbb-596">Özel yol kısıtlamaları arabirimi uygulayarak oluşturulabilir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-596">Custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="dddbb-597">`IRouteConstraint`Arabirimi, <xref:System.Web.Routing.IRouteConstraint.Match*> `true` kısıtlama karşılanıp Aksi takdirde döndüren ' i içerir `false` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-597">The `IRouteConstraint` interface contains <xref:System.Web.Routing.IRouteConstraint.Match*>, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="dddbb-598">Özel yol kısıtlamaları nadiren gereklidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-598">Custom route constraints are rarely needed.</span></span> <span data-ttu-id="dddbb-599">Özel bir yol kısıtlaması uygulamadan önce, model bağlama gibi alternatifleri göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-599">Before implementing a custom route constraint, consider alternatives, such as model binding.</span></span>

<span data-ttu-id="dddbb-600">ASP.NET Core [kısıtlamalar](https://github.com/dotnet/aspnetcore/tree/master/src/Http/Routing/src/Constraints) klasörü, bir kısıtlama oluşturmaya yönelik iyi örnekler sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-600">The ASP.NET Core [Constraints](https://github.com/dotnet/aspnetcore/tree/master/src/Http/Routing/src/Constraints) folder provides good examples of creating a constraints.</span></span> <span data-ttu-id="dddbb-601">Örneğin, [Guidrouteconstraint](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Constraints/GuidRouteConstraint.cs#L18).</span><span class="sxs-lookup"><span data-stu-id="dddbb-601">For example, [GuidRouteConstraint](https://github.com/dotnet/aspnetcore/blob/master/src/Http/Routing/src/Constraints/GuidRouteConstraint.cs#L18).</span></span>

<span data-ttu-id="dddbb-602">Özel bir kullanmak için `IRouteConstraint` yol kısıtlama türü, uygulamanın hizmet kapsayıcısında kayıtlı olması gerekir <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-602">To use a custom `IRouteConstraint`, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the service container.</span></span> <span data-ttu-id="dddbb-603">`ConstraintMap`, Yol kısıtlama anahtarlarını `IRouteConstraint` Bu kısıtlamaları doğrulayan uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-603">A `ConstraintMap` is a dictionary that maps route constraint keys to `IRouteConstraint` implementations that validate those constraints.</span></span> <span data-ttu-id="dddbb-604">Bir uygulama `ConstraintMap` , `Startup.ConfigureServices` hizmetlerin bir parçası olarak ' de güncelleştirilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan ile yapılandırma `services.Configure<RouteOptions>` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-604">An app's `ConstraintMap` can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="dddbb-605">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="dddbb-605">For example:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint.cs?name=snippet)]

<span data-ttu-id="dddbb-606">Önceki kısıtlama aşağıdaki kodda uygulanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-606">The preceding constraint is applied in the following code:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/TestController.cs?name=snippet&highlight=6,13)]

[!INCLUDE[](~/includes/MyDisplayRouteInfo.md)]

<span data-ttu-id="dddbb-607">Uygulamasının uygulanması, `MyCustomConstraint` `0` bir rota parametresine uygulanmasını engeller:</span><span class="sxs-lookup"><span data-stu-id="dddbb-607">The implementation of `MyCustomConstraint` prevents `0` being applied to a route parameter:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint.cs?name=snippet2)]

[!INCLUDE[](~/includes/regex.md)]

<span data-ttu-id="dddbb-608">Yukarıdaki kod:</span><span class="sxs-lookup"><span data-stu-id="dddbb-608">The preceding code:</span></span>

* <span data-ttu-id="dddbb-609">`0` `{id}` Yolun segmentinde engeller.</span><span class="sxs-lookup"><span data-stu-id="dddbb-609">Prevents `0` in the `{id}` segment of the route.</span></span>
* <span data-ttu-id="dddbb-610">, Özel bir kısıtlamayı uygulamaya yönelik temel bir örnek sağlamak için gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-610">Is shown to provide a basic example of implementing a custom constraint.</span></span> <span data-ttu-id="dddbb-611">Bu, bir üretim uygulamasında kullanılmamalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-611">It should not be used in a production app.</span></span>

<span data-ttu-id="dddbb-612">Aşağıdaki kod, içeren bir öğenin işlenmesini önlemek için daha iyi bir yaklaşımdır `id` `0` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-612">The following code is a better approach to preventing an `id` containing a `0` from being processed:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/TestController.cs?name=snippet2)]

<span data-ttu-id="dddbb-613">Yukarıdaki kodda yaklaşım üzerinde aşağıdaki avantajlar bulunur `MyCustomConstraint` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-613">The preceding code has the following advantages over the `MyCustomConstraint` approach:</span></span>

* <span data-ttu-id="dddbb-614">Özel bir kısıtlama gerektirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-614">It doesn't require a custom constraint.</span></span>
* <span data-ttu-id="dddbb-615">Yol parametresi içerdiğinde daha açıklayıcı bir hata döndürür `0` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-615">It returns a more descriptive error when the route parameter includes `0`.</span></span>

## <a name="parameter-transformer-reference"></a><span data-ttu-id="dddbb-616">Parametre transformatörü başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-616">Parameter transformer reference</span></span>

<span data-ttu-id="dddbb-617">Parametre dönüştürücüler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-617">Parameter transformers:</span></span>

* <span data-ttu-id="dddbb-618">Kullanarak bağlantı oluştururken yürütün <xref:Microsoft.AspNetCore.Routing.LinkGenerator> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-618">Execute when generating a link using <xref:Microsoft.AspNetCore.Routing.LinkGenerator>.</span></span>
* <span data-ttu-id="dddbb-619">Uygulayın <xref:Microsoft.AspNetCore.Routing.IOutboundParameterTransformer?displayProperty=fullName> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-619">Implement <xref:Microsoft.AspNetCore.Routing.IOutboundParameterTransformer?displayProperty=fullName>.</span></span>
* <span data-ttu-id="dddbb-620">Kullanılarak yapılandırılır <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-620">Are configured using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>.</span></span>
* <span data-ttu-id="dddbb-621">Parametrenin yol değerini alın ve yeni bir dize değerine dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="dddbb-621">Take the parameter's route value and transform it to a new string value.</span></span>
* <span data-ttu-id="dddbb-622">Oluşturulan bağlantıda Dönüştürülmüş değerin kullanılmasına neden olacak.</span><span class="sxs-lookup"><span data-stu-id="dddbb-622">Result in using the transformed value in the generated link.</span></span>

<span data-ttu-id="dddbb-623">Örneğin, yol düzeninde bir özel `slugify` parametre transformatörü `blog\{article:slugify}` `Url.Action(new { article = "MyTestArticle" })` oluşturur `blog\my-test-article` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-623">For example, a custom `slugify` parameter transformer in route pattern `blog\{article:slugify}` with `Url.Action(new { article = "MyTestArticle" })` generates `blog\my-test-article`.</span></span>

<span data-ttu-id="dddbb-624">Aşağıdaki uygulamayı göz önünde bulundurun `IOutboundParameterTransformer` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-624">Consider the following `IOutboundParameterTransformer` implementation:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint2.cs?name=snippet2)]

<span data-ttu-id="dddbb-625">Bir parametre transformatörü bir yol düzeninde kullanmak için, şunu içinde kullanarak yapılandırın <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-625">To use a parameter transformer in a route pattern, configure it using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupConstraint2.cs?name=snippet)]

<span data-ttu-id="dddbb-626">ASP.NET Core Framework, bir uç noktanın çözümlediği URI 'yi dönüştürmek için parametre dönüştürücüler kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-626">The ASP.NET Core framework uses parameter transformers to transform the URI where an endpoint resolves.</span></span> <span data-ttu-id="dddbb-627">Örneğin, parametre dönüştürücüler,,, ve ile eşleştirmek için kullanılan rota değerlerini dönüştürür `area` `controller` `action` `page` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-627">For example, parameter transformers transform the route values used to match an `area`, `controller`, `action`, and `page`.</span></span>

```csharp
routes.MapControllerRoute(
    name: "default",
    template: "{controller:slugify=Home}/{action:slugify=Index}/{id?}");
```

<span data-ttu-id="dddbb-628">Önceki yol şablonuyla, eylem `SubscriptionManagementController.GetAll` URI ile eşleştirilir `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-628">With the preceding route template, the action `SubscriptionManagementController.GetAll` is matched with the URI `/subscription-management/get-all`.</span></span> <span data-ttu-id="dddbb-629">Bir parametre transformatörü bir bağlantı oluşturmak için kullanılan rota değerlerini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-629">A parameter transformer doesn't change the route values used to generate a link.</span></span> <span data-ttu-id="dddbb-630">Örneğin, `Url.Action("GetAll", "SubscriptionManagement")` çıktılar `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-630">For example, `Url.Action("GetAll", "SubscriptionManagement")` outputs `/subscription-management/get-all`.</span></span>

<span data-ttu-id="dddbb-631">ASP.NET Core, oluşturulan yollarla parametre dönüştürücüler kullanmak için API kuralları sağlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-631">ASP.NET Core provides API conventions for using parameter transformers with generated routes:</span></span>

* <span data-ttu-id="dddbb-632"><xref:Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention?displayProperty=fullName>MVC kuralı, uygulamadaki tüm öznitelik yollarına belirtilen bir parametre transformatörü uygular.</span><span class="sxs-lookup"><span data-stu-id="dddbb-632">The <xref:Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention?displayProperty=fullName> MVC convention applies a specified parameter transformer to all attribute routes in the app.</span></span> <span data-ttu-id="dddbb-633">Parametre transformatörü, öznitelik yol belirteçlerini değiştirildiklerinde dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-633">The parameter transformer transforms attribute route tokens as they are replaced.</span></span> <span data-ttu-id="dddbb-634">Daha fazla bilgi için bkz. [belirteç değişimini özelleştirmek için bir parametre transformatörü kullanma](xref:mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span><span class="sxs-lookup"><span data-stu-id="dddbb-634">For more information, see [Use a parameter transformer to customize token replacement](xref:mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span></span>
* <span data-ttu-id="dddbb-635">:::no-loc(Razor)::: Sayfalar <xref:Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention> API kuralını kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-635">:::no-loc(Razor)::: Pages uses the <xref:Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention> API convention.</span></span> <span data-ttu-id="dddbb-636">Bu kural, belirtilen bir parametre transformatörü otomatik olarak bulunan tüm :::no-loc(Razor)::: sayfalara uygular.</span><span class="sxs-lookup"><span data-stu-id="dddbb-636">This convention applies a specified parameter transformer to all automatically discovered :::no-loc(Razor)::: Pages.</span></span> <span data-ttu-id="dddbb-637">Parametre transformatörü, sayfa yollarının klasör ve dosya adı segmentlerini dönüştürür :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-637">The parameter transformer transforms the folder and file name segments of :::no-loc(Razor)::: Pages routes.</span></span> <span data-ttu-id="dddbb-638">Daha fazla bilgi için bkz. [sayfa yollarını özelleştirmek için bir parametre transformatörü kullanma](xref:razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span><span class="sxs-lookup"><span data-stu-id="dddbb-638">For more information, see [Use a parameter transformer to customize page routes](xref:razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span></span>

<a name="ugr"></a>

## <a name="url-generation-reference"></a><span data-ttu-id="dddbb-639">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-639">URL generation reference</span></span>

<span data-ttu-id="dddbb-640">Bu bölüm, URL oluşturma tarafından uygulanan algoritmanın başvurusunu içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-640">This section contains a reference for the algorithm implemented by URL generation.</span></span> <span data-ttu-id="dddbb-641">Uygulamada, URL oluşturma 'nın çoğu karmaşık örneği denetleyicileri veya sayfaları kullanır :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-641">In practice, most complex examples of URL generation use controllers or :::no-loc(Razor)::: Pages.</span></span> <span data-ttu-id="dddbb-642">Daha fazla bilgi için bkz.  [denetleyicilerde yönlendirme](xref:mvc/controllers/routing) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-642">See  [routing in controllers](xref:mvc/controllers/routing) for additional information.</span></span>

<span data-ttu-id="dddbb-643">URL oluşturma işlemi [Linkgenerator. GetPathByAddress](xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*) veya benzer bir yöntem çağrısıyla başlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-643">The URL generation process begins with a call to [LinkGenerator.GetPathByAddress](xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*) or a similar method.</span></span> <span data-ttu-id="dddbb-644">Yöntemi bir adres, bir rota değerleri kümesi ve isteğe bağlı olarak geçerli istek hakkında bilgi ile birlikte sağlanır `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-644">The method is provided with an address, a set of route values, and optionally information about the current request from `HttpContext`.</span></span>

<span data-ttu-id="dddbb-645">İlk adım, adresin türüyle eşleşen bir aday uç nokta kümesini çözümlemek için adresi kullanmaktır [`IEndpointAddressScheme<TAddress>`](xref:Microsoft.AspNetCore.Routing.IEndpointAddressScheme`1) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-645">The first step is to use the address to resolve a set of candidate endpoints using an [`IEndpointAddressScheme<TAddress>`](xref:Microsoft.AspNetCore.Routing.IEndpointAddressScheme`1) that matches the address's type.</span></span>

<span data-ttu-id="dddbb-646">Adres kümesi tarafından bir aday kümesini bulduktan sonra, uç noktalar bir URL oluşturma işlemi başarılı olana kadar sıralanır ve işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-646">Once of set of candidates is found by the address scheme, the endpoints are ordered and processed iteratively until a URL generation operation succeeds.</span></span> <span data-ttu-id="dddbb-647">URL oluşturma belirsizlikleri için Denetim **yapmaz** , döndürülen ilk sonuç son sonuçdur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-647">URL generation does **not** check for ambiguities, the first result returned is the final result.</span></span>

### <a name="troubleshooting-url-generation-with-logging"></a><span data-ttu-id="dddbb-648">Günlüğe kaydetme ile URL oluşturma sorunlarını giderme</span><span class="sxs-lookup"><span data-stu-id="dddbb-648">Troubleshooting URL generation with logging</span></span>

<span data-ttu-id="dddbb-649">URL oluşturma sorunlarını gidermeye yönelik ilk adım, günlük düzeyini `Microsoft.AspNetCore.Routing` olarak ayarlıyor `TRACE` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-649">The first step in troubleshooting URL generation is setting the logging level of `Microsoft.AspNetCore.Routing` to `TRACE`.</span></span> <span data-ttu-id="dddbb-650">`LinkGenerator` işleme hakkında birçok ayrıntıyı günlüğe kaydeder ve bu da sorunları gidermek için yararlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-650">`LinkGenerator` logs many details about its processing which can be useful to troubleshoot problems.</span></span>

<span data-ttu-id="dddbb-651">URL oluşturma hakkında ayrıntılı bilgi için bkz. [URL oluşturma başvurusu](#ugr) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-651">See [URL generation reference](#ugr) for details on URL generation.</span></span>

### <a name="addresses"></a><span data-ttu-id="dddbb-652">Adresler</span><span class="sxs-lookup"><span data-stu-id="dddbb-652">Addresses</span></span>

<span data-ttu-id="dddbb-653">Adresler, bağlantı oluşturucuya bir çağrıyı bir aday uç noktası kümesine bağlamak için kullanılan URL oluşturma kavramıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-653">Addresses are the concept in URL generation used to bind a call into the link generator to a set of candidate endpoints.</span></span>

<span data-ttu-id="dddbb-654">Adresler, varsayılan olarak iki uygulama ile birlikte gelen Genişletilebilir bir kavramdır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-654">Addresses are an extensible concept that come with two implementations by default:</span></span>

* <span data-ttu-id="dddbb-655">Şu adres olarak *uç nokta adı* ( `string` ) kullanılıyor:</span><span class="sxs-lookup"><span data-stu-id="dddbb-655">Using *endpoint name* (`string`) as the address:</span></span>
    * <span data-ttu-id="dddbb-656">MVC 'nin yol adına benzer işlevsellik sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-656">Provides similar functionality to MVC's route name.</span></span>
    * <span data-ttu-id="dddbb-657"><xref:Microsoft.AspNetCore.Routing.IEndpointNameMetadata>Meta veri türünü kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-657">Uses the <xref:Microsoft.AspNetCore.Routing.IEndpointNameMetadata> metadata type.</span></span>
    * <span data-ttu-id="dddbb-658">Belirtilen dizeyi tüm kayıtlı uç noktaların meta verilerinde çözer.</span><span class="sxs-lookup"><span data-stu-id="dddbb-658">Resolves the provided string against the metadata of all registered endpoints.</span></span>
    * <span data-ttu-id="dddbb-659">Birden fazla uç nokta aynı adı kullanıyorsa, başlangıçta bir özel durum oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-659">Throws an exception on startup if multiple endpoints use the same name.</span></span>
    * <span data-ttu-id="dddbb-660">Denetleyicilerin ve sayfaların dışında genel amaçlı kullanım için önerilir :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-660">Recommended for general-purpose use outside of controllers and :::no-loc(Razor)::: Pages.</span></span>
* <span data-ttu-id="dddbb-661">Adres olarak *yol değerlerini* ( <xref:Microsoft.AspNetCore.Routing.RouteValuesAddress> ) kullanma:</span><span class="sxs-lookup"><span data-stu-id="dddbb-661">Using *route values* (<xref:Microsoft.AspNetCore.Routing.RouteValuesAddress>) as the address:</span></span>
    * <span data-ttu-id="dddbb-662">Denetleyiciler ve :::no-loc(Razor)::: sayfalar için eskı URL oluşturmaya benzer işlevler sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-662">Provides similar functionality to controllers and :::no-loc(Razor)::: Pages legacy URL generation.</span></span>
    * <span data-ttu-id="dddbb-663">Genişletmek ve hata ayıklamak için çok karmaşık.</span><span class="sxs-lookup"><span data-stu-id="dddbb-663">Very complex to extend and debug.</span></span>
    * <span data-ttu-id="dddbb-664">`IUrlHelper`, Etiket yardımcıları, HTML Yardımcıları, eylem sonuçları vb. tarafından kullanılan uygulamayı sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-664">Provides the implementation used by `IUrlHelper`, Tag Helpers, HTML Helpers, Action Results, etc.</span></span>

<span data-ttu-id="dddbb-665">Adres şemasının rolü, rastgele ölçütlere göre adres ve eşleşen uç noktalar arasındaki ilişkiyi oluşturmak için kullanılır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-665">The role of the address scheme is to make the association between the address and matching endpoints by arbitrary criteria:</span></span>

* <span data-ttu-id="dddbb-666">Uç nokta adı şeması, temel bir sözlük araması gerçekleştirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-666">The endpoint name scheme performs a basic dictionary lookup.</span></span>
* <span data-ttu-id="dddbb-667">Yol değerleri şeması, set algoritmasının karmaşık bir en iyi alt kümesine sahiptir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-667">The route values scheme has a complex best subset of set algorithm.</span></span>

<a name="ambient"></a>

### <a name="ambient-values-and-explicit-values"></a><span data-ttu-id="dddbb-668">Çevresel değerler ve açık değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-668">Ambient values and explicit values</span></span>

<span data-ttu-id="dddbb-669">Yönlendirme geçerli istekten, geçerli isteğin rota değerlerine erişir `HttpContext.Request.RouteValues` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-669">From the current request, routing accesses the route values of the current request `HttpContext.Request.RouteValues`.</span></span> <span data-ttu-id="dddbb-670">Geçerli istekle ilişkili değerler **çevresel değerler** olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-670">The values associated with the current request are referred to as the **ambient values**.</span></span> <span data-ttu-id="dddbb-671">Netlik amacı doğrultusunda, belgeler yöntemlere geçirilen yol değerlerini **açık değerler** olarak ifade eder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-671">For the purpose of clarity, the documentation refers to the route values passed in to methods as **explicit values**.</span></span>

<span data-ttu-id="dddbb-672">Aşağıdaki örnek, ortam değerlerini ve açık değerleri gösterir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-672">The following example shows ambient values and explicit values.</span></span> <span data-ttu-id="dddbb-673">Geçerli istekten ve açık değerlerden çevresel değerler sağlar: `{ id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-673">It provides ambient values from the current request and explicit values: `{ id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/WidgetController.cs?name=snippet)]

<span data-ttu-id="dddbb-674">Yukarıdaki kod:</span><span class="sxs-lookup"><span data-stu-id="dddbb-674">The preceding code:</span></span>

* <span data-ttu-id="dddbb-675">Döndürdüğü `/Widget/Index/17`</span><span class="sxs-lookup"><span data-stu-id="dddbb-675">Returns `/Widget/Index/17`</span></span>
* <span data-ttu-id="dddbb-676"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> [Dı](xref:fundamentals/dependency-injection)aracılığıyla alır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-676">Gets <xref:Microsoft.AspNetCore.Routing.LinkGenerator> via [DI](xref:fundamentals/dependency-injection).</span></span>

<span data-ttu-id="dddbb-677">Aşağıdaki kod hiçbir çevresel değer ve açık değer sağlar: `{ controller = "Home", action = "Subscribe", id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-677">The following code provides no ambient values and explicit values: `{ controller = "Home", action = "Subscribe", id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/WidgetController.cs?name=snippet2)]

<span data-ttu-id="dddbb-678">Önceki yöntem şunu döndürür `/Home/Subscribe/17`</span><span class="sxs-lookup"><span data-stu-id="dddbb-678">The preceding  method returns `/Home/Subscribe/17`</span></span>

<span data-ttu-id="dddbb-679">Aşağıdaki kod `WidgetController` döndürmektedir `/Widget/Subscribe/17` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-679">The following code in the `WidgetController` returns `/Widget/Subscribe/17`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/WidgetController.cs?name=snippet3)]

<span data-ttu-id="dddbb-680">Aşağıdaki kod, denetleyiciyi geçerli istekteki çevresel değerlerden ve açık değerlerde sağlar: `{ action = "Edit", id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-680">The following code provides the controller from ambient values in the current request and explicit values: `{ action = "Edit", id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/GadgetController.cs?name=snippet)]

<span data-ttu-id="dddbb-681">Yukarıdaki kodda:</span><span class="sxs-lookup"><span data-stu-id="dddbb-681">In the preceding code:</span></span>

* <span data-ttu-id="dddbb-682">`/Gadget/Edit/17` döndürülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-682">`/Gadget/Edit/17` is returned.</span></span>
* <span data-ttu-id="dddbb-683"><xref:Microsoft.AspNetCore.Mvc.ControllerBase.Url> alır <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-683"><xref:Microsoft.AspNetCore.Mvc.ControllerBase.Url> gets the <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>.</span></span>
* <xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Action*>   
<span data-ttu-id="dddbb-684">bir eylem yöntemi için mutlak bir yol içeren bir URL oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-684">generates a URL with an absolute path for an action method.</span></span> <span data-ttu-id="dddbb-685">URL, belirtilen `action` adı ve değerleri içerir `route` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-685">The URL contains the specified `action` name and `route` values.</span></span>

<span data-ttu-id="dddbb-686">Aşağıdaki kod, geçerli istekten ve açık değerlerden çevresel değerler sağlar: `{ page = "./Edit, id = 17, }` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-686">The following code provides ambient values from the current request and explicit values: `{ page = "./Edit, id = 17, }`:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Pages/Index.cshtml.cs?name=snippet)]

<span data-ttu-id="dddbb-687">Önceki kod, `url`  `/Edit/17` düzenleme :::no-loc(Razor)::: sayfası aşağıdaki sayfa yönergesini içerdiğinde olarak ayarlanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-687">The preceding code sets `url` to  `/Edit/17` when the Edit :::no-loc(Razor)::: Page contains the following page directive:</span></span>

 `@page "{id:int}"`

<span data-ttu-id="dddbb-688">Düzenleme sayfasında `"{id:int}"` yol şablonu yoksa, `url` olur `/Edit?id=17` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-688">If the Edit page doesn't contain the `"{id:int}"` route template, `url` is `/Edit?id=17`.</span></span>

<span data-ttu-id="dddbb-689">MVC 'nin davranışı, <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> burada açıklanan kurallara ek olarak bir karmaşıklık katmanı ekler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-689">The behavior of MVC's <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> adds a layer of complexity in addition to the rules described here:</span></span>

* <span data-ttu-id="dddbb-690">`IUrlHelper` her zaman geçerli istekten çevresel değerler olarak yol değerlerini sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-690">`IUrlHelper` always provides the route values from the current request as ambient values.</span></span>
* <span data-ttu-id="dddbb-691">[Iurlhelper. Action](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Action*) , `action` `controller` Geliştirici tarafından geçersiz kılınmadığı sürece her zaman geçerli ve yol değerlerini açık değerler olarak kopyalar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-691">[IUrlHelper.Action](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Action*) always copies the current `action` and `controller` route values as explicit values unless overridden by the developer.</span></span>
* <span data-ttu-id="dddbb-692">[Iurlhelper. Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*) , `page` geçersiz kılınmadığı sürece geçerli yol değerini açık bir değer olarak kopyalar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-692">[IUrlHelper.Page](xref:Microsoft.AspNetCore.Mvc.UrlHelperExtensions.Page*) always copies the current `page` route value as an explicit value unless overridden.</span></span> <!--by the user-->
* <span data-ttu-id="dddbb-693">`IUrlHelper.Page` her zaman geçersiz `handler` `null` kılınmadığı sürece açık değerler olarak geçerli yol değerini geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-693">`IUrlHelper.Page` always overrides the current `handler` route value with `null` as an explicit values unless overridden.</span></span>

<span data-ttu-id="dddbb-694">MVC kendi kurallarını takip ettiğinden, kullanıcılar genellikle çevresel değerlerinin davranış ayrıntılarına göre görünür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-694">Users are often surprised by the behavioral details of ambient values, because MVC doesn't seem to follow its own rules.</span></span> <span data-ttu-id="dddbb-695">Geçmiş ve uyumluluk nedenleriyle,,, ve gibi belirli rota `action` değerlerinin `controller` `page` `handler` kendi özel durum davranışları vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-695">For historical and compatibility reasons, certain route values such as `action`, `controller`, `page`, and `handler` have their own special-case behavior.</span></span>

<span data-ttu-id="dddbb-696">Tarafından sunulan eşdeğer işlevsellik `LinkGenerator.GetPathByAction` ve `LinkGenerator.GetPathByPage` Bu bozuklukları `IUrlHelper` uyumluluk açısından çoğaltır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-696">The equivalent functionality provided by `LinkGenerator.GetPathByAction` and `LinkGenerator.GetPathByPage` duplicates these anomalies of `IUrlHelper` for compatibility.</span></span>

### <a name="url-generation-process"></a><span data-ttu-id="dddbb-697">URL oluşturma işlemi</span><span class="sxs-lookup"><span data-stu-id="dddbb-697">URL generation process</span></span>

<span data-ttu-id="dddbb-698">Aday uç noktalar kümesi bulunduğunda, URL oluşturma algoritması:</span><span class="sxs-lookup"><span data-stu-id="dddbb-698">Once the set of candidate endpoints are found, the URL generation algorithm:</span></span>

* <span data-ttu-id="dddbb-699">Son noktaları yinelemeli olarak işler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-699">Processes the endpoints iteratively.</span></span>
* <span data-ttu-id="dddbb-700">İlk başarılı sonucu döndürür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-700">Returns the first successful result.</span></span>

<span data-ttu-id="dddbb-701">Bu işlemdeki ilk adıma **Rota değeri ınvalidation** adı verilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-701">The first step in this process is called **route value invalidation**.</span></span>  <span data-ttu-id="dddbb-702">Rota değeri geçersiz kılma, yönlendirmenin çevresel değerlerden hangi rota değerlerinin kullanılması gerektiğini ve göz ardı edileceğine yönelik bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-702">Route value invalidation is the process by which routing decides which route values from the ambient values should be used and which should be ignored.</span></span> <span data-ttu-id="dddbb-703">Her ortam değeri, açık değerlerle birlikte değerlendirilir ve yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-703">Each ambient value is considered and either combined with the explicit values, or ignored.</span></span>

<span data-ttu-id="dddbb-704">Ortam değerlerinin rolünü düşünmenin en iyi yolu, bazı yaygın durumlarda, yazma uygulama geliştiricilerinin kaydedilmesini denedikleri değerlerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-704">The best way to think about the role of ambient values is that they attempt to save application developers typing, in some common cases.</span></span> <span data-ttu-id="dddbb-705">Geleneksel olarak, ortam değerlerinin yararlı olduğu senaryolar MVC ile ilgilidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-705">Traditionally, the scenarios where ambient values are helpful are related to MVC:</span></span>

* <span data-ttu-id="dddbb-706">Aynı denetleyicideki başka bir eyleme bağlantı sırasında, denetleyicinin adının belirtilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-706">When linking to another action in the same controller, the controller name doesn't need to be specified.</span></span>
* <span data-ttu-id="dddbb-707">Aynı alandaki başka bir denetleyiciye bağlanırken, alan adının belirtilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-707">When linking to another controller in the same area, the area name doesn't need to be specified.</span></span>
* <span data-ttu-id="dddbb-708">Aynı eylem yöntemine bağlanırken, rota değerlerinin belirtilmesi gerekmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-708">When linking to the same action method, route values don't need to be specified.</span></span>
* <span data-ttu-id="dddbb-709">Uygulamanın başka bir bölümüne bağlanırken, uygulamanın o bölümünde anlamı olmayan rota değerlerini yürütmek istemezsiniz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-709">When linking to another part of the app, you don't want to carry over route values that have no meaning in that part of the app.</span></span>

<span data-ttu-id="dddbb-710">Ya da `LinkGenerator` `IUrlHelper` Bu dönüşe yapılan çağrılar `null` genellikle yol değeri doğrulaması anlaşılmadığında oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-710">Calls to `LinkGenerator` or `IUrlHelper` that return `null` are usually caused by not understanding route value invalidation.</span></span> <span data-ttu-id="dddbb-711">Yolun sorunu çözüp çözmediğini görmek için daha fazla yol değeri belirtip yol değerlerini geçersiz kılma sorunlarını giderin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-711">Troubleshoot route value invalidation by explicitly specifying more of the route values to see if that solves the problem.</span></span>

<span data-ttu-id="dddbb-712">Rota değeri geçersiz kılma, uygulamanın URL şeması, soldan sağa doğru oluşturulmuş bir hiyerarşi ile hiyerarşik olduğu varsayımıyla birlikte çalışıyor.</span><span class="sxs-lookup"><span data-stu-id="dddbb-712">Route value invalidation works on the assumption that the app's URL scheme is hierarchical, with a hierarchy formed from left-to-right.</span></span> <span data-ttu-id="dddbb-713">`{controller}/{action}/{id?}`Bu uygulamada nasıl çalıştığını daha kolay bir şekilde öğrenmek için temel denetleyici yönlendirme şablonunu göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-713">Consider the basic controller route template `{controller}/{action}/{id?}` to get an intuitive sense of how this works in practice.</span></span> <span data-ttu-id="dddbb-714">Bir değer **değişikliği** , sağda görünen tüm rota değerlerini **geçersiz kılar** .</span><span class="sxs-lookup"><span data-stu-id="dddbb-714">A **change** to a value **invalidates** all of the route values that appear to the right.</span></span> <span data-ttu-id="dddbb-715">Bu, hiyerarşiyle ilgili varsayımını yansıtır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-715">This reflects the assumption about hierarchy.</span></span> <span data-ttu-id="dddbb-716">Uygulamanın için bir ortam değeri varsa `id` ve işlem için farklı bir değer belirtiyorsa `controller` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-716">If the app has an ambient value for `id`, and the operation specifies a different value for the `controller`:</span></span>

* <span data-ttu-id="dddbb-717">`id``{controller}`, öğesinin solunda olduğu için yeniden kullanılamaz `{id?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-717">`id` won't be reused because `{controller}` is to the left of `{id?}`.</span></span>

<span data-ttu-id="dddbb-718">Bu ilkeyi gösteren bazı örnekler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-718">Some examples demonstrating this principle:</span></span>

* <span data-ttu-id="dddbb-719">Açık değerler için bir değer içeriyorsa `id` , ortam değeri `id` yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-719">If the explicit values contain a value for `id`, the ambient value for `id` is ignored.</span></span> <span data-ttu-id="dddbb-720">Ve için çevresel değerler `controller` `action` kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-720">The ambient values for `controller` and `action` can be used.</span></span>
* <span data-ttu-id="dddbb-721">Açık değerler için bir değer içeriyorsa `action` , için herhangi bir ortam değeri `action` yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-721">If the explicit values contain a value for `action`, any ambient value for `action` is ignored.</span></span> <span data-ttu-id="dddbb-722">İçin çevresel değerler `controller` kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-722">The ambient values for `controller` can be used.</span></span> <span data-ttu-id="dddbb-723">İçin açık değeri, `action` için çevresel değerden farklıysa `action` , `id` değer kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-723">If the explicit value for `action` is different from the ambient value for `action`, the `id` value won't be used.</span></span>  <span data-ttu-id="dddbb-724">İçin açık değeri, `action` için çevresel değer ile aynıysa `action` , `id` değer kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-724">If the explicit value for `action` is the same as the ambient value for `action`, the `id` value can be used.</span></span>
* <span data-ttu-id="dddbb-725">Açık değerler için bir değer içeriyorsa `controller` , için herhangi bir ortam değeri `controller` yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-725">If the explicit values contain a value for `controller`, any ambient value for `controller` is ignored.</span></span> <span data-ttu-id="dddbb-726">İçin açık değeri, `controller` için çevresel değerden farklıysa `controller` , `action` ve `id` değerleri kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-726">If the explicit value for `controller` is different from the ambient value for `controller`, the `action` and `id` values won't be used.</span></span> <span data-ttu-id="dddbb-727">İçin açık değeri, `controller` için çevresel değer ile aynıysa `controller` , `action` ve `id` değerleri kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-727">If the explicit value for `controller` is the same as the ambient value for `controller`, the `action` and `id` values can be used.</span></span>

<span data-ttu-id="dddbb-728">Bu işlem, öznitelik yollarının ve adanmış geleneksel yolların varlığı tarafından daha karmaşıktır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-728">This process is further complicated by the existence of attribute routes and dedicated conventional routes.</span></span> <span data-ttu-id="dddbb-729">`{controller}/{action}/{id?}`Yönlendirme parametrelerini kullanarak bir hiyerarşiyi belirtmek gibi denetleyici geleneksel yolları.</span><span class="sxs-lookup"><span data-stu-id="dddbb-729">Controller conventional routes such as `{controller}/{action}/{id?}` specify a hierarchy using route parameters.</span></span> <span data-ttu-id="dddbb-730">[Özel geleneksel yollar](xref:mvc/controllers/routing#dcr) ve denetleyicilere ve sayfalara yönelik [öznitelik yolları](xref:mvc/controllers/routing#ar) için :::no-loc(Razor)::: :</span><span class="sxs-lookup"><span data-stu-id="dddbb-730">For [dedicated conventional routes](xref:mvc/controllers/routing#dcr) and [attribute routes](xref:mvc/controllers/routing#ar) to controllers and :::no-loc(Razor)::: Pages:</span></span>

* <span data-ttu-id="dddbb-731">Yol değerlerinin bir hiyerarşisi vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-731">There is a hierarchy of route values.</span></span>
* <span data-ttu-id="dddbb-732">Bunlar şablonda görünmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-732">They don't appear in the template.</span></span>

<span data-ttu-id="dddbb-733">Bu gibi durumlarda, URL oluşturma **gerekli değerler** kavramını tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-733">For these cases, URL generation defines the **required values** concept.</span></span> <span data-ttu-id="dddbb-734">Denetleyiciler ve sayfalar tarafından oluşturulan uç noktalar, :::no-loc(Razor)::: Rota değeri geçersiz kılma çalışmasına izin veren gerekli değerlere sahiptir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-734">Endpoints created by controllers and :::no-loc(Razor)::: Pages have required values specified that allow route value invalidation to work.</span></span>

<span data-ttu-id="dddbb-735">Ayrıntı olarak yol değeri geçersiz kılma algoritması:</span><span class="sxs-lookup"><span data-stu-id="dddbb-735">The route value invalidation algorithm in detail:</span></span>

* <span data-ttu-id="dddbb-736">Gerekli değer adları, yol parametreleriyle birleştirilir ve soldan sağa işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-736">The required value names are combined with the route parameters, then processed from left-to-right.</span></span>
* <span data-ttu-id="dddbb-737">Her parametre için çevresel değer ve açık değer karşılaştırılır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-737">For each parameter, the ambient value and explicit value are compared:</span></span>
    * <span data-ttu-id="dddbb-738">Çevresel değer ve açık değer aynıysa işlem devam eder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-738">If the ambient value and explicit value are the same, the process continues.</span></span>
    * <span data-ttu-id="dddbb-739">Çevresel değer varsa ve açık değer değilse, URL oluşturulurken çevresel değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-739">If the ambient value is present and the explicit value isn't, the ambient value is used when generating the URL.</span></span>
    * <span data-ttu-id="dddbb-740">Çevresel değer yoksa ve açık değer ise, ortam değerini ve sonraki tüm ortam değerlerini reddedin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-740">If the ambient value isn't present and the explicit value is, reject the ambient value and all subsequent ambient values.</span></span>
    * <span data-ttu-id="dddbb-741">Çevresel değer ve açık değer varsa ve iki değer farklıysa, ortam değerini ve sonraki tüm ortam değerlerini reddedin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-741">If the ambient value and the explicit value are present, and the two values are different, reject the ambient value and all subsequent ambient values.</span></span>

<span data-ttu-id="dddbb-742">Bu noktada, URL oluşturma işlemi yol kısıtlamalarını değerlendirmek için hazırlayın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-742">At this point, the URL generation operation is ready to evaluate route constraints.</span></span> <span data-ttu-id="dddbb-743">Kabul edilen değerler kümesi, kısıtlamalara sunulan parametre varsayılan değerleriyle birleştirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-743">The set of accepted values is combined with the parameter default values, which is provided to constraints.</span></span> <span data-ttu-id="dddbb-744">Kısıtlamaların tümü başarılı olursa işlem devam eder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-744">If the constraints all pass, the operation continues.</span></span>

<span data-ttu-id="dddbb-745">Ardından, **kabul edilen değerler** yol şablonunu genişletmek için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-745">Next, the **accepted values** can be used to expand the route template.</span></span> <span data-ttu-id="dddbb-746">Yol şablonu işlenir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-746">The route template is processed:</span></span>

* <span data-ttu-id="dddbb-747">Soldan sağa.</span><span class="sxs-lookup"><span data-stu-id="dddbb-747">From left-to-right.</span></span>
* <span data-ttu-id="dddbb-748">Her parametrenin kabul edilen değeri değiştirildi.</span><span class="sxs-lookup"><span data-stu-id="dddbb-748">Each parameter has its accepted value substituted.</span></span>
* <span data-ttu-id="dddbb-749">Aşağıdaki özel durumlar ile:</span><span class="sxs-lookup"><span data-stu-id="dddbb-749">With the following special cases:</span></span>
  * <span data-ttu-id="dddbb-750">Kabul edilen değerlerde bir değer eksikse ve parametrenin varsayılan değeri varsa, varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-750">If the accepted values is missing a value and the parameter has a default value, the default value is used.</span></span>
  * <span data-ttu-id="dddbb-751">Kabul edilen değerlerde bir değer eksikse ve parametresi isteğe bağlıdır, işleme devam eder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-751">If the accepted values is missing a value and the parameter is optional, processing continues.</span></span>
  * <span data-ttu-id="dddbb-752">Eksik bir isteğe bağlı parametrenin sağında herhangi bir rota parametresinin değeri varsa, işlem başarısız olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-752">If any route parameter to the right of a missing optional parameter has a value, the operation fails.</span></span>
  * <!-- review default-valued parameters optional parameters --> <span data-ttu-id="dddbb-753">Ardışık varsayılan değerli parametreler ve isteğe bağlı parametreler mümkün olduğunda daraltılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-753">Contiguous default-valued parameters and optional parameters are collapsed where possible.</span></span>

<span data-ttu-id="dddbb-754">Yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-754">Values explicitly provided that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="dddbb-755">Aşağıdaki tabloda, yol şablonu kullanılırken sonuç gösterilmektedir `{controller}/{action}/{id?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-755">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="dddbb-756">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-756">Ambient Values</span></span>                     | <span data-ttu-id="dddbb-757">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-757">Explicit Values</span></span>                        | <span data-ttu-id="dddbb-758">Sonuç</span><span class="sxs-lookup"><span data-stu-id="dddbb-758">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="dddbb-759">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-759">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-760">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-760">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="dddbb-761">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-761">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-762">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-762">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="dddbb-763">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="dddbb-763">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="dddbb-764">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-764">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="dddbb-765">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-765">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-766">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="dddbb-766">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

### <a name="problems-with-route-value-invalidation"></a><span data-ttu-id="dddbb-767">Rota değeri geçersiz kılma sorunları</span><span class="sxs-lookup"><span data-stu-id="dddbb-767">Problems with route value invalidation</span></span>

<span data-ttu-id="dddbb-768">ASP.NET Core 3,0 itibariyle, önceki ASP.NET Core sürümlerde kullanılan bazı URL oluşturma şemaları, URL oluşturma ile iyi çalışmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-768">As of ASP.NET Core 3.0, some URL generation schemes used in earlier ASP.NET Core versions don't work well with URL generation.</span></span> <span data-ttu-id="dddbb-769">ASP.NET Core takım, gelecekteki sürümlerde bu ihtiyaçları karşılamak için özellik eklemeyi planlıyor.</span><span class="sxs-lookup"><span data-stu-id="dddbb-769">The ASP.NET Core team plans to add features to address these needs in a future release.</span></span> <span data-ttu-id="dddbb-770">Şimdilik en iyi çözüm eski yönlendirmeyi kullanmaktır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-770">For now the best solution is to use legacy routing.</span></span>

<span data-ttu-id="dddbb-771">Aşağıdaki kod, yönlendirme tarafından desteklenmeyen bir URL oluşturma şemasına bir örnek gösterir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-771">The following code shows an example of a URL generation scheme that's not supported by routing.</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupUnsupported.cs?name=snippet)]

<span data-ttu-id="dddbb-772">Yukarıdaki kodda, `culture` Yerelleştirme için Route parametresi kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-772">In the preceding code, the `culture` route parameter is used for localization.</span></span> <span data-ttu-id="dddbb-773">Bunun için `culture` parametre her zaman bir çevresel değer olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-773">The desire is to have the `culture` parameter always accepted as an ambient value.</span></span> <span data-ttu-id="dddbb-774">Ancak, `culture` gerekli değerlerin çalışma şekli nedeniyle parametre bir çevresel değer olarak kabul edilmez:</span><span class="sxs-lookup"><span data-stu-id="dddbb-774">However, the `culture` parameter is not accepted as an ambient value because of the way required values work:</span></span>

* <span data-ttu-id="dddbb-775">`"default"`Yol şablonunda, `culture` yol parametresi solunda bulunur `controller` , bu nedenle değişiklikler `controller` geçersiz olmaz `culture` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-775">In the `"default"` route template, the `culture` route parameter is to the left of `controller`, so changes to `controller` won't invalidate `culture`.</span></span>
* <span data-ttu-id="dddbb-776">`"blog"`Yol şablonunda, `culture` yol parametresi, `controller` gerekli değerlerde görüntülenen, öğesinin sağında kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-776">In the `"blog"` route template, the `culture` route parameter is considered to be to the right of `controller`, which appears in the required values.</span></span>

## <a name="configuring-endpoint-metadata"></a><span data-ttu-id="dddbb-777">Uç nokta meta verilerini yapılandırma</span><span class="sxs-lookup"><span data-stu-id="dddbb-777">Configuring endpoint metadata</span></span>

<span data-ttu-id="dddbb-778">Aşağıdaki bağlantılar, uç nokta meta verilerini yapılandırma hakkında bilgi sağlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-778">The following links provide information on configuring endpoint metadata:</span></span>

* [<span data-ttu-id="dddbb-779">Uç nokta yönlendirme ile CORS 'yi etkinleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-779">Enable Cors with endpoint routing</span></span>](xref:security/cors#enable-cors-with-endpoint-routing)
* <span data-ttu-id="dddbb-780">Özel bir öznitelik kullanan [ıauthorizationpolicyprovider örneği](https://github.com/dotnet/AspNetCore/tree/release/3.1/src/Security/samples/CustomPolicyProvider) `[MinimumAgeAuthorize]`</span><span class="sxs-lookup"><span data-stu-id="dddbb-780">[IAuthorizationPolicyProvider sample](https://github.com/dotnet/AspNetCore/tree/release/3.1/src/Security/samples/CustomPolicyProvider) using a custom `[MinimumAgeAuthorize]` attribute</span></span>
* <span data-ttu-id="dddbb-781">[[Yetkilendir] özniteliğiyle test kimlik doğrulaması](xref:security/authentication/identity#test-identity)</span><span class="sxs-lookup"><span data-stu-id="dddbb-781">[Test authentication with the [Authorize] attribute](xref:security/authentication/identity#test-identity)</span></span>
* <xref:Microsoft.AspNetCore.Builder.AuthorizationEndpointConventionBuilderExtensions.RequireAuthorization*>
* <span data-ttu-id="dddbb-782">[[Yetkilendir] özniteliğiyle düzeni seçme](xref:security/authorization/limitingidentitybyscheme#selecting-the-scheme-with-the-authorize-attribute)</span><span class="sxs-lookup"><span data-stu-id="dddbb-782">[Selecting the scheme with the [Authorize] attribute](xref:security/authorization/limitingidentitybyscheme#selecting-the-scheme-with-the-authorize-attribute)</span></span>
* <span data-ttu-id="dddbb-783">[[Yetkilendir] özniteliğini kullanarak ilke uygulama](xref:security/authorization/policies#apply-policies-to-mvc-controllers)</span><span class="sxs-lookup"><span data-stu-id="dddbb-783">[Apply policies using the [Authorize] attribute](xref:security/authorization/policies#apply-policies-to-mvc-controllers)</span></span>
* <xref:security/authorization/roles>

<a name="hostmatch"></a>

## <a name="host-matching-in-routes-with-requirehost"></a><span data-ttu-id="dddbb-784">RequireHost ile yollarla eşleşen ana bilgisayar</span><span class="sxs-lookup"><span data-stu-id="dddbb-784">Host matching in routes with RequireHost</span></span>

<span data-ttu-id="dddbb-785"><xref:Microsoft.AspNetCore.Builder.RoutingEndpointConventionBuilderExtensions.RequireHost*> rotaya belirtilen Konağı gerektiren bir kısıtlama uygular.</span><span class="sxs-lookup"><span data-stu-id="dddbb-785"><xref:Microsoft.AspNetCore.Builder.RoutingEndpointConventionBuilderExtensions.RequireHost*> applies a constraint to the route which requires the specified host.</span></span> <span data-ttu-id="dddbb-786">`RequireHost`Veya [[Host]](xref:Microsoft.AspNetCore.Routing.HostAttribute) parametresi şu olabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-786">The `RequireHost` or [[Host]](xref:Microsoft.AspNetCore.Routing.HostAttribute) parameter can be:</span></span>

* <span data-ttu-id="dddbb-787">Ana bilgisayar: `www.domain.com` , `www.domain.com` herhangi bir bağlantı noktasıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-787">Host: `www.domain.com`, matches `www.domain.com` with any port.</span></span>
* <span data-ttu-id="dddbb-788">Joker karakterle,, `*.domain.com` `www.domain.com` `subdomain.domain.com` veya `www.subdomain.domain.com` herhangi bir bağlantı noktasında eşleşen ana bilgisayar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-788">Host with wildcard: `*.domain.com`, matches `www.domain.com`, `subdomain.domain.com`, or `www.subdomain.domain.com` on any port.</span></span>
* <span data-ttu-id="dddbb-789">Bağlantı noktası: `*:5000` , bağlantı noktası 5000 ile herhangi bir konak ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-789">Port: `*:5000`, matches port 5000 with any host.</span></span>
* <span data-ttu-id="dddbb-790">Konak ve bağlantı noktası: `www.domain.com:5000` veya `*.domain.com:5000` , ana bilgisayar ve bağlantı noktasıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-790">Host and port: `www.domain.com:5000` or `*.domain.com:5000`, matches host and port.</span></span>

<span data-ttu-id="dddbb-791">Veya kullanılarak birden çok parametre belirtilebilir `RequireHost` `[Host]` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-791">Multiple parameters can be specified using `RequireHost` or `[Host]`.</span></span> <span data-ttu-id="dddbb-792">Kısıtlama, parametrelerden herhangi biri için geçerli olan konaklarla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-792">The constraint  matches hosts valid for any of the parameters.</span></span> <span data-ttu-id="dddbb-793">Örneğin,, `[Host("domain.com", "*.domain.com")]` ve ile eşleşir `domain.com` `www.domain.com` `subdomain.domain.com` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-793">For example, `[Host("domain.com", "*.domain.com")]` matches `domain.com`, `www.domain.com`, and `subdomain.domain.com`.</span></span>

<span data-ttu-id="dddbb-794">Aşağıdaki kod, `RequireHost` rotada belirtilen Konağı gerektirmek için kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-794">The following code uses `RequireHost` to require the specified host on the route:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupRequireHost.cs?name=snippet)]

<span data-ttu-id="dddbb-795">Aşağıdaki kod, `[Host]` belirtilen konaklardan herhangi birini gerektirmek için denetleyicideki özniteliğini kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-795">The following code uses the `[Host]` attribute on the controller to require any of the specified hosts:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/Controllers/ProductController.cs?name=snippet)]

<span data-ttu-id="dddbb-796">`[Host]`Özniteliği hem denetleyici hem de eylem yöntemine uygulandığında:</span><span class="sxs-lookup"><span data-stu-id="dddbb-796">When the `[Host]` attribute is applied to both the controller and action method:</span></span>

* <span data-ttu-id="dddbb-797">Eylem üzerindeki özniteliği kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-797">The attribute on the action is used.</span></span>
* <span data-ttu-id="dddbb-798">Denetleyici özniteliği yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-798">The controller attribute is ignored.</span></span>

## <a name="performance-guidance-for-routing"></a><span data-ttu-id="dddbb-799">Yönlendirme için performans Kılavuzu</span><span class="sxs-lookup"><span data-stu-id="dddbb-799">Performance guidance for routing</span></span>

<span data-ttu-id="dddbb-800">Yönlendirmenin çoğu, performansı artırmak için ASP.NET Core 3,0 ' de güncelleştirildi.</span><span class="sxs-lookup"><span data-stu-id="dddbb-800">Most of routing was updated in ASP.NET Core 3.0 to increase performance.</span></span>

<span data-ttu-id="dddbb-801">Bir uygulamada performans sorunları olduğunda, yönlendirme genellikle sorun olarak şüpheli.</span><span class="sxs-lookup"><span data-stu-id="dddbb-801">When an app has performance problems, routing is often suspected as the problem.</span></span> <span data-ttu-id="dddbb-802">Yönlendirme nedeni, denetleyiciler ve sayfalar gibi çerçevelerin, :::no-loc(Razor)::: kendi günlük iletilerinde çerçeve içinde harcanan süreyi raporlamadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-802">The reason routing is suspected is that frameworks like controllers and :::no-loc(Razor)::: Pages report the amount of time spent inside the framework in their logging messages.</span></span> <span data-ttu-id="dddbb-803">Denetleyiciler tarafından bildirilen zaman ve isteğin toplam süresi arasında önemli bir fark olduğunda:</span><span class="sxs-lookup"><span data-stu-id="dddbb-803">When there's a significant difference between the time reported by controllers and the total time of the request:</span></span>

* <span data-ttu-id="dddbb-804">Geliştiriciler, uygulamanın kodunu sorunun kaynağı olarak ortadan kaldırır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-804">Developers eliminate their app code as the source of the problem.</span></span>
* <span data-ttu-id="dddbb-805">Yönlendirmenin neden olduğunu varsaymak yaygındır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-805">It's common to assume routing is the cause.</span></span>

<span data-ttu-id="dddbb-806">Yönlendirme, binlerce uç nokta kullanılarak performansa göre test edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-806">Routing is performance tested using thousands of endpoints.</span></span> <span data-ttu-id="dddbb-807">Tipik bir uygulamanın çok büyük bir performans sorunuyla karşılaşmasının pek olası olması normaldir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-807">It's unlikely that a typical app will encounter a performance problem just by being too large.</span></span> <span data-ttu-id="dddbb-808">Yavaş yönlendirme performansının en yaygın temel nedenlerinden biri genellikle hatalı davranan özel bir ara yazılım olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-808">The most common root cause of slow routing performance is usually a badly-behaving custom middleware.</span></span>

<span data-ttu-id="dddbb-809">Aşağıdaki kod örneğinde, gecikme kaynağını daraltmak için temel bir teknik gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-809">This following code sample demonstrates a basic technique for narrowing down the source of delay:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupDelay.cs?name=snippet)]

<span data-ttu-id="dddbb-810">Zaman yönlendirme:</span><span class="sxs-lookup"><span data-stu-id="dddbb-810">To time routing:</span></span>

* <span data-ttu-id="dddbb-811">Yukarıdaki kodda gösterilen zamanlama ara yazılımı kopyasıyla her bir ara yazılımı ayırma.</span><span class="sxs-lookup"><span data-stu-id="dddbb-811">Interleave each middleware with a copy of the timing middleware shown in the preceding code.</span></span>
* <span data-ttu-id="dddbb-812">Zamanlama verilerinin kodla ilişkilendirilmesi için benzersiz bir tanımlayıcı ekleyin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-812">Add a unique identifier to correlate the timing data with the code.</span></span>

<span data-ttu-id="dddbb-813">Bu, örneğin, çok önemli olduğunda gecikmeyi daraltmak için temel bir yoldur `10ms` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-813">This is a basic way to narrow down the delay when it's significant, for example, more than `10ms`.</span></span>  <span data-ttu-id="dddbb-814">`Time 2` `Time 1` Raporlar, ara yazılım içinde harcanan süreyi çıkararak `UseRouting` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-814">Subtracting `Time 2` from `Time 1` reports the time spent inside the `UseRouting` middleware.</span></span>

<span data-ttu-id="dddbb-815">Aşağıdaki kod, önceki zamanlama koduna yönelik daha küçük bir yaklaşım kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-815">The following code uses a more compact approach to the preceding timing code:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupSW.cs?name=snippetSW)]

[!code-csharp[](routing/samples/3.x/RoutingSample/StartupSW.cs?name=snippet)]

### <a name="potentially-expensive-routing-features"></a><span data-ttu-id="dddbb-816">Potansiyel olarak pahalı yönlendirme özellikleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-816">Potentially expensive routing features</span></span>

<span data-ttu-id="dddbb-817">Aşağıdaki listede, temel yol şablonlarıyla karşılaştırıldığında görece maliyetli yönlendirme özellikleri hakkında bazı Öngörüler sunulmaktadır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-817">The following list provides some insight into routing features that are relatively expensive compared with basic route templates:</span></span>

* <span data-ttu-id="dddbb-818">Normal ifadeler: karmaşık ifadeler yazmak veya az miktarda giriş ile uzun süre çalışma süresi olması mümkündür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-818">Regular expressions: It's possible to write regular expressions that are complex, or have long running time with a small amount of input.</span></span>

* <span data-ttu-id="dddbb-819">Karmaşık segmentler ( `{x}-{y}-{z}` ):</span><span class="sxs-lookup"><span data-stu-id="dddbb-819">Complex segments (`{x}-{y}-{z}`):</span></span> 
  * <span data-ttu-id="dddbb-820">, Normal bir URL yolu segmentini ayrıştırmaktan önemli ölçüde daha pahalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-820">Are significantly more expensive than parsing a regular URL path segment.</span></span>
  * <span data-ttu-id="dddbb-821">Daha fazla sayıda alt dizenin ayrılması sonucunu elde edin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-821">Result in many more substrings being allocated.</span></span>
  * <span data-ttu-id="dddbb-822">Karmaşık kesim mantığı ASP.NET Core 3,0 yönlendirme performansı güncelleştirmesinde güncelleştirilmedi.</span><span class="sxs-lookup"><span data-stu-id="dddbb-822">The complex segment logic was not updated in ASP.NET Core 3.0 routing performance update.</span></span>

* <span data-ttu-id="dddbb-823">Zaman uyumlu veri erişimi: birçok karmaşık uygulamanın, yönlendirmenin bir parçası olarak veritabanı erişimi vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-823">Synchronous data access: Many complex apps have database access as part of their routing.</span></span> <span data-ttu-id="dddbb-824">ASP.NET Core 2,2 ve önceki yönlendirme, veritabanı erişim yönlendirmesini desteklemek için doğru genişletilebilirlik noktaları sağlamayabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-824">ASP.NET Core 2.2 and earlier routing might not provide the right extensibility points to support database access routing.</span></span> <span data-ttu-id="dddbb-825">Örneğin, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> , ve <xref:Microsoft.AspNetCore.Mvc.ActionConstraints.IActionConstraint> zaman uyumludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-825">For example, <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, and <xref:Microsoft.AspNetCore.Mvc.ActionConstraints.IActionConstraint> are synchronous.</span></span> <span data-ttu-id="dddbb-826">Ve gibi genişletilebilirlik noktaları <xref:Microsoft.AspNetCore.Routing.MatcherPolicy> <xref:Microsoft.AspNetCore.Routing.EndpointSelectorContext> zaman uyumsuzdur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-826">Extensibility points such as <xref:Microsoft.AspNetCore.Routing.MatcherPolicy> and <xref:Microsoft.AspNetCore.Routing.EndpointSelectorContext> are asynchronous.</span></span>

## <a name="guidance-for-library-authors"></a><span data-ttu-id="dddbb-827">Kitaplık yazarları için kılavuz</span><span class="sxs-lookup"><span data-stu-id="dddbb-827">Guidance for library authors</span></span>

<span data-ttu-id="dddbb-828">Bu bölüm, yönlendirmenin üzerine binen üst düzey kitaplık yazarları için yönergeler içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-828">This section contains guidance for library authors building on top of routing.</span></span> <span data-ttu-id="dddbb-829">Bu ayrıntılar, uygulama geliştiricilerinin yönlendirmeyi genişleten kitaplıkları ve çerçeveleri kullanarak iyi bir deneyim aldığından emin olmak için tasarlanmıştır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-829">These details are intended to ensure that app developers have a good experience using libraries and frameworks that extend routing.</span></span>

### <a name="define-endpoints"></a><span data-ttu-id="dddbb-830">Uç noktaları tanımlama</span><span class="sxs-lookup"><span data-stu-id="dddbb-830">Define endpoints</span></span>

<span data-ttu-id="dddbb-831">URL eşleştirmesi için yönlendirmeyi kullanan bir çerçeve oluşturmak için, en üstünde yapı yapan bir kullanıcı deneyimi tanımlayarak başlayın <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-831">To create a framework that uses routing for URL matching, start by defining a user experience that builds on top of <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span>

<span data-ttu-id="dddbb-832">Üzerine derleme **yapın** <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-832">**DO** build on top of <xref:Microsoft.AspNetCore.Routing.IEndpointRouteBuilder>.</span></span> <span data-ttu-id="dddbb-833">Bu, kullanıcıların kendi çatısını diğer ASP.NET Core özellikleriyle karışıklık olmadan oluşturma olanağı sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-833">This allows users to compose your framework with other ASP.NET Core features without confusion.</span></span> <span data-ttu-id="dddbb-834">Her ASP.NET Core şablonu yönlendirme içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-834">Every ASP.NET Core template includes routing.</span></span> <span data-ttu-id="dddbb-835">Yönlendirmenin mevcut olduğunu ve kullanıcılara tanıdık olduğunu varsayın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-835">Assume routing is present and familiar for users.</span></span>

```csharp
app.UseEndpoints(endpoints =>
{
    // Your framework
    endpoints.MapMyFramework(...);

    endpoints.MapHealthChecks("/healthz");
});
```

<span data-ttu-id="dddbb-836">Bu, uygulayan bir çağrıdan Sealed somut **tür döndürün** `MapMyFramework(...)` <xref:Microsoft.AspNetCore.Builder.IEndpointConventionBuilder> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-836">**DO** return a sealed concrete type from a call to `MapMyFramework(...)` that implements <xref:Microsoft.AspNetCore.Builder.IEndpointConventionBuilder>.</span></span> <span data-ttu-id="dddbb-837">Çoğu Framework `Map...` yöntemi bu kalıbı izler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-837">Most framework `Map...` methods follow this pattern.</span></span> <span data-ttu-id="dddbb-838">`IEndpointConventionBuilder`Arabirim:</span><span class="sxs-lookup"><span data-stu-id="dddbb-838">The `IEndpointConventionBuilder` interface:</span></span>

* <span data-ttu-id="dddbb-839">Meta verilerin bir kısmını bilme olanağı tanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-839">Allows composability of metadata.</span></span>
* <span data-ttu-id="dddbb-840">, Çeşitli uzantı yöntemleriyle hedeflenmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-840">Is targeted by a variety of extension methods.</span></span>

<span data-ttu-id="dddbb-841">Kendi türünü bildirmek, çerçeveye özgü işlevselliği oluşturucuya eklemenize olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-841">Declaring your own type allows you to add your own framework-specific functionality to the builder.</span></span> <span data-ttu-id="dddbb-842">Çerçeve tarafından tanımlanan bir oluşturucuyu sarmadan ve çağrıları iletecek.</span><span class="sxs-lookup"><span data-stu-id="dddbb-842">It's ok to wrap a framework-declared builder and forward calls to it.</span></span>

```csharp
app.UseEndpoints(endpoints =>
{
    // Your framework
    endpoints.MapMyFramework(...).RequireAuthorization()
                                 .WithMyFrameworkFeature(awesome: true);

    endpoints.MapHealthChecks("/healthz");
});
```

<span data-ttu-id="dddbb-843">**CONSIDER** Kendinizinkini yazmayı düşünün <xref:Microsoft.AspNetCore.Routing.EndpointDataSource> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-843">**CONSIDER** writing your own <xref:Microsoft.AspNetCore.Routing.EndpointDataSource>.</span></span> <span data-ttu-id="dddbb-844">`EndpointDataSource` , uç nokta koleksiyonunu bildirmek ve güncelleştirmek için alt düzey temel değer.</span><span class="sxs-lookup"><span data-stu-id="dddbb-844">`EndpointDataSource` is the low-level primitive for declaring and updating a collection of endpoints.</span></span> <span data-ttu-id="dddbb-845">`EndpointDataSource` , denetleyiciler ve sayfalar tarafından kullanılan güçlü bir API 'dir :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-845">`EndpointDataSource` is a powerful API used by controllers and :::no-loc(Razor)::: Pages.</span></span>

<span data-ttu-id="dddbb-846">Yönlendirme testlerinin, güncelleştirme olmayan bir veri kaynağına ilişkin [temel bir örneği](https://github.com/aspnet/AspNetCore/blob/master/src/Http/Routing/test/testassets/RoutingSandbox/Framework/FrameworkEndpointDataSource.cs#L17) vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-846">The routing tests have a [basic example](https://github.com/aspnet/AspNetCore/blob/master/src/Http/Routing/test/testassets/RoutingSandbox/Framework/FrameworkEndpointDataSource.cs#L17) of a non-updating data source.</span></span>

<span data-ttu-id="dddbb-847">Varsayılan olarak **kaydetmeyi denemeyin** `EndpointDataSource` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-847">**DO NOT** attempt to register an `EndpointDataSource` by default.</span></span> <span data-ttu-id="dddbb-848">Kullanıcıların çatısını kaydetmesini gerektir <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-848">Require users to register your framework in <xref:Microsoft.AspNetCore.Builder.EndpointRoutingApplicationBuilderExtensions.UseEndpoints*>.</span></span> <span data-ttu-id="dddbb-849">Yönlendirmenin felseı, varsayılan olarak hiçbir şey dahil değildir ve `UseEndpoints` uç noktaların kaydedileceği yerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-849">The philosophy of routing is that nothing is included by default, and that `UseEndpoints` is the place to register endpoints.</span></span>

### <a name="creating-routing-integrated-middleware"></a><span data-ttu-id="dddbb-850">Yönlendirme ile tümleşik ara yazılım oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-850">Creating routing-integrated middleware</span></span>

<span data-ttu-id="dddbb-851">Meta veri türlerini arabirim olarak tanımlamayı **düşünün** .</span><span class="sxs-lookup"><span data-stu-id="dddbb-851">**CONSIDER** defining metadata types as an interface.</span></span>

<span data-ttu-id="dddbb-852">Meta veri türlerini sınıflar ve yöntemlerde öznitelik olarak kullanmayı **mümkün hale getirir** .</span><span class="sxs-lookup"><span data-stu-id="dddbb-852">**DO** make it possible to use metadata types as an attribute on classes and methods.</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/ICoolMetadata.cs?name=snippet2)]

<span data-ttu-id="dddbb-853">Denetleyiciler ve sayfalar gibi çerçeveler :::no-loc(Razor)::: , meta veri özniteliklerinin türlere ve yöntemlere uygulanmasını destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-853">Frameworks like controllers and :::no-loc(Razor)::: Pages support applying metadata attributes to types and methods.</span></span> <span data-ttu-id="dddbb-854">Meta veri türleri bildirirseniz:</span><span class="sxs-lookup"><span data-stu-id="dddbb-854">If you declare metadata types:</span></span>

* <span data-ttu-id="dddbb-855">Bunları [öznitelik](/dotnet/csharp/programming-guide/concepts/attributes/)olarak erişilebilir yapın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-855">Make them accessible as [attributes](/dotnet/csharp/programming-guide/concepts/attributes/).</span></span>
* <span data-ttu-id="dddbb-856">Çoğu Kullanıcı öznitelikleri uygulamayla tanıdık gelecektir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-856">Most users are familiar with applying attributes.</span></span>

<span data-ttu-id="dddbb-857">Bir arabirim olarak bir meta veri türü bildirmek, başka bir esneklik katmanı ekler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-857">Declaring a metadata type as an interface adds another layer of flexibility:</span></span>

* <span data-ttu-id="dddbb-858">Arabirimler birleştirilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-858">Interfaces are composable.</span></span>
* <span data-ttu-id="dddbb-859">Geliştiriciler, birden çok ilkeyi birleştiren kendi türlerini bildirebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-859">Developers can declare their own types that combine multiple policies.</span></span>

<span data-ttu-id="dddbb-860">Aşağıdaki örnekte gösterildiği gibi meta verileri **geçersiz kılmak mümkün hale gelir** :</span><span class="sxs-lookup"><span data-stu-id="dddbb-860">**DO** make it possible to override metadata, as shown in the following example:</span></span>

[!code-csharp[](routing/samples/3.x/RoutingSample/ICoolMetadata.cs?name=snippet)]

<span data-ttu-id="dddbb-861">Bu yönergeleri izlemek için en iyi yol, **işaretleyici meta verileri** tanımlamayı önleyemaktır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-861">The best way to follow these guidelines is to avoid defining **marker metadata** :</span></span>

* <span data-ttu-id="dddbb-862">Yalnızca meta veri türünün varolup olmadığını göz atın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-862">Don't just look for the presence of a metadata type.</span></span>
* <span data-ttu-id="dddbb-863">Meta verilerde bir özellik tanımlayın ve özelliği denetleyin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-863">Define a property on the metadata and check the property.</span></span>

<span data-ttu-id="dddbb-864">Meta veri koleksiyonu sıralanmıştır ve önceliğe göre geçersiz kılmayı destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-864">The metadata collection is ordered and supports overriding by priority.</span></span> <span data-ttu-id="dddbb-865">Denetleyiciler söz konusu olduğunda, eylem yöntemindeki meta veriler en özeldir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-865">In the case of controllers, metadata on the action method is most specific.</span></span>

<span data-ttu-id="dddbb-866">Ara yazılımı, yönlendirme olmadan ve ile **yararlı hale getirin** .</span><span class="sxs-lookup"><span data-stu-id="dddbb-866">**DO** make middleware useful with and without routing.</span></span>

```csharp
app.UseRouting();

app.UseAuthorization(new AuthorizationPolicy() { ... });

app.UseEndpoints(endpoints =>
{
    // Your framework
    endpoints.MapMyFramework(...).RequireAuthorization();
});
```

<span data-ttu-id="dddbb-867">Bu yönergeye örnek olarak, ara yazılımı göz önünde bulundurun `UseAuthorization` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-867">As an example of this guideline, consider the `UseAuthorization` middleware.</span></span> <span data-ttu-id="dddbb-868">Yetkilendirme ara yazılımı, bir geri dönüş ilkesi geçirmenize olanak sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-868">The authorization middleware allows you to pass in a fallback policy.</span></span> <!-- shown where?  (shown here) --> <span data-ttu-id="dddbb-869">Belirtilmişse, geri dönüş ilkesi her ikisi için de geçerlidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-869">The fallback policy, if specified, applies to both:</span></span>

* <span data-ttu-id="dddbb-870">Belirli bir ilke olmadan uç noktalar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-870">Endpoints without a specified policy.</span></span>
* <span data-ttu-id="dddbb-871">Bir uç noktasıyla eşleşmeyen istekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-871">Requests that don't match an endpoint.</span></span>

<span data-ttu-id="dddbb-872">Bu, yetkilendirme ara yazılımını yönlendirme bağlamı dışında yararlı hale getirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-872">This makes the authorization middleware useful outside of the context of routing.</span></span> <span data-ttu-id="dddbb-873">Yetkilendirme ara yazılımı geleneksel ara yazılım programlama için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-873">The authorization middleware can be used for traditional middleware programming.</span></span>

[!INCLUDE[](~/includes/dbg-route.md)]

::: moniker-end

::: moniker range="= aspnetcore-2.2"

<span data-ttu-id="dddbb-874">Yönlendirme, istek URI 'Lerini uç noktalarla eşleştirmekten ve gelen istekleri bu uç noktalara gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-874">Routing is responsible for mapping request URIs to endpoints and dispatching incoming requests to those endpoints.</span></span> <span data-ttu-id="dddbb-875">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-875">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="dddbb-876">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-876">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="dddbb-877">Uygulamadan yönlendirme bilgilerini kullanarak, yönlendirme, uç noktalarıyla eşlenen URL 'Ler de oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-877">Using route information from the app, routing is also able to generate URLs that map to endpoints.</span></span>

<span data-ttu-id="dddbb-878">ASP.NET Core 2,2 ' de en son yönlendirme senaryolarını kullanmak için, ' de MVC Hizmetleri kaydının [Uyumluluk sürümünü](xref:mvc/compatibility-version) belirtin `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-878">To use the latest routing scenarios in ASP.NET Core 2.2, specify the [compatibility version](xref:mvc/compatibility-version) to the MVC services registration in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc()
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

<span data-ttu-id="dddbb-879">Bu <xref:Microsoft.AspNetCore.Mvc.MvcOptions.EnableEndpointRouting> seçenek yönlendirmenin <xref:Microsoft.AspNetCore.Routing.IRouter> ASP.NET Core 2,1 veya önceki bir sürümünün uç nokta tabanlı Logic mi yoksa tabanlı mantığını mı kullanması gerektiğini belirler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-879">The <xref:Microsoft.AspNetCore.Mvc.MvcOptions.EnableEndpointRouting> option determines if routing should internally use endpoint-based logic or the <xref:Microsoft.AspNetCore.Routing.IRouter>-based logic of ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="dddbb-880">Uyumluluk sürümü 2,2 veya üzeri bir değere ayarlandığında, varsayılan değer olur `true` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-880">When the compatibility version is set to 2.2 or later, the default value is `true`.</span></span> <span data-ttu-id="dddbb-881">`false`Önceki yönlendirme mantığını kullanmak için değeri olarak ayarlayın:</span><span class="sxs-lookup"><span data-stu-id="dddbb-881">Set the value to `false` to use the prior routing logic:</span></span>

```csharp
// Use the routing logic of ASP.NET Core 2.1 or earlier:
services.AddMvc(options => options.EnableEndpointRouting = false)
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_2);
```

<span data-ttu-id="dddbb-882">Tabanlı yönlendirme hakkında daha fazla bilgi için <xref:Microsoft.AspNetCore.Routing.IRouter> [bu konunun ASP.NET Core 2,1 sürümüne](?view=aspnetcore-2.1)bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-882">For more information on <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, see the [ASP.NET Core 2.1 version of this topic](?view=aspnetcore-2.1).</span></span>

> [!IMPORTANT]
> <span data-ttu-id="dddbb-883">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-883">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="dddbb-884">ASP.NET Core MVC yönlendirme hakkında bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-884">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="dddbb-885">Sayfalardaki yönlendirme kuralları hakkında daha fazla bilgi için :::no-loc(Razor)::: bkz <xref:razor-pages/razor-pages-conventions> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-885">For information on routing conventions in :::no-loc(Razor)::: Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="dddbb-886">[Örnek kodu görüntüleme veya indirme](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceği](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="dddbb-886">[View or download sample code](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="dddbb-887">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-887">Routing basics</span></span>

<span data-ttu-id="dddbb-888">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-888">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="dddbb-889">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-889">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="dddbb-890">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-890">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="dddbb-891">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-891">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="dddbb-892">Geliştiriciler, genellikle [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda uygulamanın yüksek trafikte daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-892">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span> <span data-ttu-id="dddbb-893">Özelleştirilmiş durumlar örnekleri, blog ve eticaret uç noktaları içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-893">Specialized situations examples include, blog and ecommerce endpoints.</span></span>

<span data-ttu-id="dddbb-894">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-894">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="dddbb-895">Bu, örneğin, al ve POST gibi birçok işlemin aynı mantıksal kaynakta aynı URL 'YI kullanması anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-895">This means that many operations, for example, GET, and POST, on the same logical resource use the same URL.</span></span> <span data-ttu-id="dddbb-896">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-896">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

<span data-ttu-id="dddbb-897">:::no-loc(Razor)::: Sayfalar uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-897">:::no-loc(Razor)::: Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="dddbb-898">Sayfa yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-898">Additional conventions are available that allow you to customize :::no-loc(Razor)::: Pages routing behavior.</span></span> <span data-ttu-id="dddbb-899">Daha fazla bilgi için <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-899">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="dddbb-900">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-900">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="dddbb-901">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-901">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="dddbb-902">Yönlendirme *endpoints* `Endpoint` , bir uygulamadaki mantıksal uç noktaları temsil etmek için uç noktaları () kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-902">Routing uses *endpoints* (`Endpoint`) to represent logical endpoints in an app.</span></span>

<span data-ttu-id="dddbb-903">Bir uç nokta, istekleri işlemek için bir temsilci ve rastgele meta veri koleksiyonunu tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-903">An endpoint defines a delegate to process requests and a collection of arbitrary metadata.</span></span> <span data-ttu-id="dddbb-904">Meta veriler, her bir uç noktaya eklenen ilkelere ve yapılandırmaya bağlı olarak çapraz kesme sorunları uygulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-904">The metadata is used implement cross-cutting concerns based on policies and configuration attached to each endpoint.</span></span>

<span data-ttu-id="dddbb-905">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-905">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="dddbb-906">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-906">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="dddbb-907">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-907">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="dddbb-908"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-908"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="dddbb-909">MVC/sayfalar gibi uygulama modelleri, :::no-loc(Razor)::: yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm uç noktalarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-909">App models, such as MVC/:::no-loc(Razor)::: Pages, register all of their endpoints, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="dddbb-910">Yönlendirme gerçekleştirme, yönlendirme kararlarını, ara yazılım ardışık düzeninde istediğiniz yere getirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-910">The routing implementation makes routing decisions wherever desired in the middleware pipeline.</span></span>
* <span data-ttu-id="dddbb-911">Bir yönlendirme ara yazılımı, belirli bir istek URI 'SI için yönlendirme ara yazılımı uç noktası kararının sonucunu inceleyebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-911">Middleware that appears after a Routing Middleware can inspect the result of the Routing Middleware's endpoint decision for a given request URI.</span></span>
* <span data-ttu-id="dddbb-912">Uygulamadaki tüm uç noktaları, ara yazılım ardışık düzeninde herhangi bir yerde listelemek mümkündür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-912">It's possible to enumerate all of the endpoints in the app anywhere in the middleware pipeline.</span></span>
* <span data-ttu-id="dddbb-913">Bir uygulama, uç nokta bilgilerine göre URL 'Ler oluşturmak için (örneğin, yeniden yönlendirme veya bağlantılar için) yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-913">An app can use routing to generate URLs (for example, for redirection or links) based on endpoint information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="dddbb-914">URL oluşturma, rastgele genişletilebilirliği destekleyen adreslere dayalıdır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-914">URL generation is based on addresses, which support arbitrary extensibility:</span></span>

  * <span data-ttu-id="dddbb-915">Bağlantı Oluşturucu API 'SI ( <xref:Microsoft.AspNetCore.Routing.LinkGenerator> ), URL oluşturmak için [bağımlılık ekleme (dı)](xref:fundamentals/dependency-injection) kullanılarak her yerde çözülebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-915">The Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>) can be resolved anywhere using [dependency injection (DI)](xref:fundamentals/dependency-injection) to generate URLs.</span></span>
  * <span data-ttu-id="dddbb-916">Bağlantı Oluşturucu API 'sinin dı aracılığıyla kullanılamadığı durumlarda <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> URL 'leri derlemek için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-916">Where the Link Generator API isn't available via DI, <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>

> [!NOTE]
> <span data-ttu-id="dddbb-917">ASP.NET Core 2,2 ' de uç nokta yönlendirme yayını ile, uç nokta bağlama MVC/ :::no-loc(Razor)::: sayfa eylemleri ve sayfaları ile sınırlıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-917">With the release of endpoint routing in ASP.NET Core 2.2, endpoint linking is limited to MVC/:::no-loc(Razor)::: Pages actions and pages.</span></span> <span data-ttu-id="dddbb-918">Uç nokta bağlama yeteneklerinin genişletmeleri, gelecek sürümlerde planlanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-918">The expansions of endpoint-linking capabilities is planned for future releases.</span></span>

<span data-ttu-id="dddbb-919">Yönlendirme, sınıfı tarafından bulunan [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-919">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="dddbb-920">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve sayfalar uygulamalarında yönlendirmeyi işler :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-920">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and :::no-loc(Razor)::: Pages apps.</span></span> <span data-ttu-id="dddbb-921">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-921">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="dddbb-922">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-922">URL matching</span></span>

<span data-ttu-id="dddbb-923">URL eşleştirme, yönlendirmenin bir *uç noktaya* gelen isteği gönderdiği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-923">URL matching is the process by which routing dispatches an incoming request to an *endpoint*.</span></span> <span data-ttu-id="dddbb-924">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-924">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="dddbb-925">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-925">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="dddbb-926">Uç nokta yönlendirmesinde yönlendirme sistemi tüm gönderme kararlarından sorumludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-926">The routing system in endpoint routing is responsible for all dispatching decisions.</span></span> <span data-ttu-id="dddbb-927">Ara yazılım, seçili uç noktaya göre ilkeler uyguladığı için, yönlendirme sisteminde güvenlik ilkelerinin dağıtımını etkileyebilecek veya uygulamanın uygulanmasını etkileyebilecek herhangi bir kararın olması önemlidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-927">Since the middleware applies policies based on the selected endpoint, it's important that any decision that can affect dispatching or the application of security policies is made inside the routing system.</span></span>

<span data-ttu-id="dddbb-928">Uç nokta temsilcisi yürütüldüğünde, [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özellikleri, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-928">When the endpoint delegate is executed, the properties of [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) are set to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="dddbb-929">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-929">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="dddbb-930">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-930">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="dddbb-931">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-931">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="dddbb-932"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> Her rotayla durum verilerinin ilişkilendirilmesini desteklemek için sağlanır, böylece uygulama hangi yolun eşleştiğini temel alarak kararlar alabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-932"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="dddbb-933">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="dddbb-933">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="dddbb-934">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-934">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="dddbb-935">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-935">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="dddbb-936">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-936">Routes can be nested inside of one another.</span></span> <span data-ttu-id="dddbb-937"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers>Özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-937">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="dddbb-938">Genellikle, içindeki ilk öğe <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-938">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="dddbb-939">İçindeki son öğe, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-939">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation-with-linkgenerator"></a><span data-ttu-id="dddbb-940">LinkGenerator ile URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-940">URL generation with LinkGenerator</span></span>

<span data-ttu-id="dddbb-941">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-941">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="dddbb-942">Bu, uç noktalarınız ve bunlara erişen URL 'Ler arasında mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-942">This allows for a logical separation between your endpoints and the URLs that access them.</span></span>

<span data-ttu-id="dddbb-943">Endpoint Routing, bağlantı Oluşturucu API 'SI ( <xref:Microsoft.AspNetCore.Routing.LinkGenerator> ) içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-943">Endpoint routing includes the Link Generator API (<xref:Microsoft.AspNetCore.Routing.LinkGenerator>).</span></span> <span data-ttu-id="dddbb-944"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> , [dı](xref:fundamentals/dependency-injection)'den alınabilecek bir tek hizmettir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-944"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is a singleton service that can be retrieved from [DI](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="dddbb-945">API, yürütülen bir istek bağlamı dışında kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-945">The API can be used outside of the context of an executing request.</span></span> <span data-ttu-id="dddbb-946"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper> <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> [Etiket Yardımcıları](xref:mvc/views/tag-helpers/intro), HTML Yardımcıları ve [eylem sonuçları](xref:mvc/controllers/actions)gibi mvc 'nin ve senaryolarına yönelik senaryolar, bağlantı oluşturma özellikleri sağlamak için bağlantı oluşturucuyu kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-946">MVC's <xref:Microsoft.AspNetCore.Mvc.IUrlHelper> and scenarios that rely on <xref:Microsoft.AspNetCore.Mvc.IUrlHelper>, such as [Tag Helpers](xref:mvc/views/tag-helpers/intro), HTML Helpers, and [Action Results](xref:mvc/controllers/actions), use the link generator to provide link generating capabilities.</span></span>

<span data-ttu-id="dddbb-947">Bağlantı Oluşturucu, bir *Adres* ve *Adres şemaları* kavramıyla desteklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-947">The link generator is backed by the concept of an *address* and *address schemes*.</span></span> <span data-ttu-id="dddbb-948">Adres şeması, bağlantı oluşturma için göz önünde bulundurmanız gereken uç noktaları belirlemenin bir yoludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-948">An address scheme is a way of determining the endpoints that should be considered for link generation.</span></span> <span data-ttu-id="dddbb-949">Örneğin, çok sayıda kullanıcının yol adı ve yol değerleri senaryoları, MVC/ :::no-loc(Razor)::: sayfalardan bir adres düzeni olarak uygulanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-949">For example, the route name and route values scenarios many users are familiar with from MVC/:::no-loc(Razor)::: Pages are implemented as an address scheme.</span></span>

<span data-ttu-id="dddbb-950">Bağlantı Oluşturucu, MVC/ :::no-loc(Razor)::: Pages eylemlerine ve sayfalarına aşağıdaki genişletme yöntemleri aracılığıyla bağlanabilir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-950">The link generator can link to MVC/:::no-loc(Razor)::: Pages actions and pages via the following extension methods:</span></span>

* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetPathByAction*>
* <xref:Microsoft.AspNetCore.Routing.ControllerLinkGeneratorExtensions.GetUriByAction*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetPathByPage*>
* <xref:Microsoft.AspNetCore.Routing.PageLinkGeneratorExtensions.GetUriByPage*>

<span data-ttu-id="dddbb-951">Bu yöntemlerin aşırı yüklemesi, içeren bağımsız değişkenleri kabul eder `HttpContext` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-951">An overload of these methods accepts arguments that include the `HttpContext`.</span></span> <span data-ttu-id="dddbb-952">Bu yöntemler işlevsel olarak eşdeğerdir, `Url.Action` `Url.Page` ancak ek esneklik ve seçenekler sunar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-952">These methods are functionally equivalent to `Url.Action` and `Url.Page` but offer additional flexibility and options.</span></span>

<span data-ttu-id="dddbb-953">`GetPath*`Yöntemler, `Url.Action` `Url.Page` mutlak bir yol içeren bir URI oluşturabilen ve ' a benzerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-953">The `GetPath*` methods are most similar to `Url.Action` and `Url.Page` in that they generate a URI containing an absolute path.</span></span> <span data-ttu-id="dddbb-954">`GetUri*`Yöntemler her zaman bir düzen ve konak içeren mutlak BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-954">The `GetUri*` methods always generate an absolute URI containing a scheme and host.</span></span> <span data-ttu-id="dddbb-955">Bir öğesini kabul eden yöntemler, `HttpContext` yürütülmekte olan istek bağlamında BIR URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-955">The methods that accept an `HttpContext` generate a URI in the context of the executing request.</span></span> <span data-ttu-id="dddbb-956">Ortam yolu değerleri, URL taban yolu, şeması ve yürütülen istekten ana bilgisayar, geçersiz kılınmadıkça kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-956">The ambient route values, URL base path, scheme, and host from the executing request are used unless overridden.</span></span>

<span data-ttu-id="dddbb-957"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> bir adresle çağırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-957"><xref:Microsoft.AspNetCore.Routing.LinkGenerator> is called with an address.</span></span> <span data-ttu-id="dddbb-958">URI oluşturma iki adımda gerçekleşir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-958">Generating a URI occurs in two steps:</span></span>

1. <span data-ttu-id="dddbb-959">Adres, adresle eşleşen bir uç nokta listesine bağlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-959">An address is bound to a list of endpoints that match the address.</span></span>
1. <span data-ttu-id="dddbb-960">Her uç nokta `RoutePattern` , sağlanan değerlerle eşleşen bir yol deseninin bulunana kadar değerlendirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-960">Each endpoint's `RoutePattern` is evaluated until a route pattern that matches the supplied values is found.</span></span> <span data-ttu-id="dddbb-961">Elde edilen çıktı, bağlantı oluşturucuya sağlanan diğer URI parçalarıyla birleştirilir ve döndürülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-961">The resulting output is combined with the other URI parts supplied to the link generator and returned.</span></span>

<span data-ttu-id="dddbb-962">Tarafından sunulan yöntemler, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> herhangi bir adres türü için standart bağlantı oluşturma yeteneklerini destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-962">The methods provided by <xref:Microsoft.AspNetCore.Routing.LinkGenerator> support standard link generation capabilities for any type of address.</span></span> <span data-ttu-id="dddbb-963">Bağlantı oluşturucuyu kullanmanın en kolay yolu, belirli bir adres türü için işlem gerçekleştiren genişletme yöntemlerine yöneliktir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-963">The most convenient way to use the link generator is through extension methods that perform operations for a specific address type.</span></span>

| <span data-ttu-id="dddbb-964">Genişletme yöntemi</span><span class="sxs-lookup"><span data-stu-id="dddbb-964">Extension Method</span></span>   | <span data-ttu-id="dddbb-965">Açıklama</span><span class="sxs-lookup"><span data-stu-id="dddbb-965">Description</span></span>                                                         |
| ------------------ | ------------------------------------------------------------------- |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetPathByAddress*> | <span data-ttu-id="dddbb-966">Belirtilen değerleri temel alarak mutlak bir yola sahip bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-966">Generates a URI with an absolute path based on the provided values.</span></span> |
| <xref:Microsoft.AspNetCore.Routing.LinkGenerator.GetUriByAddress*> | <span data-ttu-id="dddbb-967">Belirtilen değerleri temel alarak mutlak bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-967">Generates an absolute URI based on the provided values.</span></span>             |

> [!WARNING]
> <span data-ttu-id="dddbb-968">Çağırma yöntemlerinin aşağıdaki etkilerine dikkat edin <xref:Microsoft.AspNetCore.Routing.LinkGenerator> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-968">Pay attention to the following implications of calling <xref:Microsoft.AspNetCore.Routing.LinkGenerator> methods:</span></span>
>
> * <span data-ttu-id="dddbb-969">`GetUri*`Gelen isteklerin üstbilgisini doğrulayan bir uygulama yapılandırmasında uzantı yöntemlerini dikkatle kullanın `Host` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-969">Use `GetUri*` extension methods with caution in an app configuration that doesn't validate the `Host` header of incoming requests.</span></span> <span data-ttu-id="dddbb-970">`Host`Gelen isteklerin üstbilgisi doğrulandıktan sonra, güvenilir olmayan istek girişi, bir görünüm/sayfada URI 'ler halinde istemciye geri gönderilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-970">If the `Host` header of incoming requests isn't validated, untrusted request input can be sent back to the client in URIs in a view/page.</span></span> <span data-ttu-id="dddbb-971">Tüm üretim uygulamalarının, `Host` üst bilgisini bilinen geçerli değerlere karşı doğrulamak için kendi sunucusunu yapılandırmasını öneririz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-971">We recommend that all production apps configure their server to validate the `Host` header against known valid values.</span></span>
>
> * <span data-ttu-id="dddbb-972"><xref:Microsoft.AspNetCore.Routing.LinkGenerator>Veya ile birlikte ara yazılım içinde dikkatli kullanın `Map` `MapWhen` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-972">Use <xref:Microsoft.AspNetCore.Routing.LinkGenerator> with caution in middleware in combination with `Map` or `MapWhen`.</span></span> <span data-ttu-id="dddbb-973">`Map*` yürütülen isteğin temel yolunu değiştirir ve bu da bağlantı oluşturma çıktısını etkiler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-973">`Map*` changes the base path of the executing request, which affects the output of link generation.</span></span> <span data-ttu-id="dddbb-974">Tüm <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API 'ler temel yol belirtilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-974">All of the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> APIs allow specifying a base path.</span></span> <span data-ttu-id="dddbb-975">Bağlantı oluşturma işlemi geri almak için her zaman boş bir temel yol belirtin `Map*` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-975">Always specify an empty base path to undo `Map*`'s affect on link generation.</span></span>

## <a name="differences-from-earlier-versions-of-routing"></a><span data-ttu-id="dddbb-976">Yönlendirmenin önceki sürümlerinden farklılıklar</span><span class="sxs-lookup"><span data-stu-id="dddbb-976">Differences from earlier versions of routing</span></span>

<span data-ttu-id="dddbb-977">ASP.NET Core 2,2 veya üzeri ve daha önceki yönlendirme sürümlerindeki ASP.NET Core uç nokta yönlendirmesi arasında birkaç fark vardır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-977">A few differences exist between endpoint routing in ASP.NET Core 2.2 or later and earlier versions of routing in ASP.NET Core:</span></span>

* <span data-ttu-id="dddbb-978">Uç nokta yönlendirme sistemi <xref:Microsoft.AspNetCore.Routing.IRouter> , devralma dahil olmak üzere tabanlı genişletilebilirliği desteklemez <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-978">The endpoint routing system doesn't support <xref:Microsoft.AspNetCore.Routing.IRouter>-based extensibility, including inheriting from <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>

* <span data-ttu-id="dddbb-979">Uç nokta yönlendirme [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim)desteklemez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-979">Endpoint routing doesn't support [WebApiCompatShim](https://www.nuget.org/packages/Microsoft.AspNetCore.Mvc.WebApiCompatShim).</span></span> <span data-ttu-id="dddbb-980">Uyumluluk Shim 'yi kullanmaya devam etmek için 2,1 [Uyumluluk sürümünü](xref:mvc/compatibility-version) ( `.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)` ) kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-980">Use the 2.1 [compatibility version](xref:mvc/compatibility-version) (`.SetCompatibilityVersion(CompatibilityVersion.Version_2_1)`) to continue using the compatibility shim.</span></span>

* <span data-ttu-id="dddbb-981">Uç nokta yönlendirmesinde, geleneksel yollar kullanılırken oluşturulan URI 'lerin büyük küçük harfleri için farklı davranış vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-981">Endpoint Routing has different behavior for the casing of generated URIs when using conventional routes.</span></span>

  <span data-ttu-id="dddbb-982">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="dddbb-982">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="dddbb-983">Aşağıdaki rotayı kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="dddbb-983">Suppose you generate a link to an action using the following route:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "blog", new { id = 17, });
  ```

  <span data-ttu-id="dddbb-984"><xref:Microsoft.AspNetCore.Routing.IRouter>Tabanlı yönlendirme ile, bu kod, `/blog/ReadPost/17` belirtilen rota değerinin büyük küçük harflerini BELIRTEN bir URI oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-984">With <xref:Microsoft.AspNetCore.Routing.IRouter>-based routing, this code generates a URI of `/blog/ReadPost/17`, which respects the casing of the provided route value.</span></span> <span data-ttu-id="dddbb-985">ASP.NET Core 2,2 veya üzeri bir nokta yönlendirme `/Blog/ReadPost/17` ("blog" büyük harfli) üretir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-985">Endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` ("Blog" is capitalized).</span></span> <span data-ttu-id="dddbb-986">Endpoint Routing, `IOutboundParameterTransformer` Bu davranışı genel olarak özelleştirmek veya URL eşleme için farklı kurallar uygulamak üzere kullanılabilecek arabirimi sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-986">Endpoint routing provides the `IOutboundParameterTransformer` interface that can be used to customize this behavior globally or to apply different conventions for mapping URLs.</span></span>

  <span data-ttu-id="dddbb-987">Daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="dddbb-987">For more information, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

* <span data-ttu-id="dddbb-988">Geleneksel yolların bulunduğu MVC/sayfalar tarafından kullanılan bağlantı üretimi, :::no-loc(Razor)::: mevcut olmayan bir denetleyiciye/eyleme veya sayfaya bağlantı kurmaya çalışırken farklı şekilde davranır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-988">Link Generation used by MVC/:::no-loc(Razor)::: Pages with conventional routes behaves differently when attempting to link to an controller/action or page that doesn't exist.</span></span>

  <span data-ttu-id="dddbb-989">Aşağıdaki varsayılan yol şablonunu göz önünde bulundurun:</span><span class="sxs-lookup"><span data-stu-id="dddbb-989">Consider the following default route template:</span></span>

  ```csharp
  app.UseMvc(routes =>
  {
      routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
  });
  ```

  <span data-ttu-id="dddbb-990">Aşağıdaki ile varsayılan şablonu kullanarak bir eyleme bağlantı oluşturduğunuzu varsayalım:</span><span class="sxs-lookup"><span data-stu-id="dddbb-990">Suppose you generate a link to an action using the default template with the following:</span></span>

  ```csharp
  var link = Url.Action("ReadPost", "Blog", new { id = 17, });
  ```

  <span data-ttu-id="dddbb-991">`IRouter`Tabanlı yönlendirme ile, `/Blog/ReadPost/17` `BlogController` mevcut olmasa veya bir eylem yöntemine sahip olmasalar bile sonuç her zaman olur `ReadPost` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-991">With `IRouter`-based routing, the result is always `/Blog/ReadPost/17`, even if the `BlogController` doesn't exist or doesn't have a `ReadPost` action method.</span></span> <span data-ttu-id="dddbb-992">Beklendiği gibi, ASP.NET Core 2,2 veya sonraki bir sürümde uç nokta yönlendirme `/Blog/ReadPost/17` eylem yöntemi varsa üretir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-992">As expected, endpoint routing in ASP.NET Core 2.2 or later produces `/Blog/ReadPost/17` if the action method exists.</span></span> <span data-ttu-id="dddbb-993">*Ancak, uç nokta yönlendirme, eylem yoksa boş bir dize oluşturur.*</span><span class="sxs-lookup"><span data-stu-id="dddbb-993">*However, endpoint routing produces an empty string if the action doesn't exist.*</span></span> <span data-ttu-id="dddbb-994">Kavramsal olarak, uç nokta yönlendirme, eylem mevcut değilse uç noktanın var olduğunu varsaymaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-994">Conceptually, endpoint routing doesn't assume that the endpoint exists if the action doesn't exist.</span></span>

* <span data-ttu-id="dddbb-995">Bağlantı oluşturma *çevresel değeri ınvalidation algoritması* , uç nokta yönlendirme ile kullanıldığında farklı davranır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-995">The link generation *ambient value invalidation algorithm* behaves differently when used with endpoint routing.</span></span>

  <span data-ttu-id="dddbb-996">*Çevresel değer ınvalidation* , şu anda yürütülmekte olan isteğin (çevresel değerler) hangi rota değerlerinin bağlantı oluşturma işlemlerinde kullanılabileceğini belirleyen algoritmadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-996">*Ambient value invalidation* is the algorithm that decides which route values from the currently executing request (the ambient values) can be used in link generation operations.</span></span> <span data-ttu-id="dddbb-997">Geleneksel yönlendirme, farklı bir eyleme bağlanılırken ek yol değerlerini her zaman geçersiz kıldı.</span><span class="sxs-lookup"><span data-stu-id="dddbb-997">Conventional routing always invalidated extra route values when linking to a different action.</span></span> <span data-ttu-id="dddbb-998">ASP.NET Core 2,2 sürümünden önce öznitelik yönlendirmesinde bu davranış yoktu.</span><span class="sxs-lookup"><span data-stu-id="dddbb-998">Attribute routing didn't have this behavior prior to the release of ASP.NET Core 2.2.</span></span> <span data-ttu-id="dddbb-999">ASP.NET Core 'nin önceki sürümlerinde, aynı yol parametresi adlarını kullanan başka bir eyleme bağlantılar, bağlantı oluşturma hatalarıyla sonuçlandı.</span><span class="sxs-lookup"><span data-stu-id="dddbb-999">In earlier versions of ASP.NET Core, links to another action that use the same route parameter names resulted in link generation errors.</span></span> <span data-ttu-id="dddbb-1000">ASP.NET Core 2,2 veya sonraki sürümlerde, yönlendirme biçimlerinin her ikisi de başka bir eyleme bağlanırken değerleri geçersiz kılar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1000">In ASP.NET Core 2.2 or later, both forms of routing invalidate values when linking to another action.</span></span>

  <span data-ttu-id="dddbb-1001">ASP.NET Core 2,1 veya önceki sürümlerde aşağıdaki örneği göz önünde bulundurun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1001">Consider the following example in ASP.NET Core 2.1 or earlier.</span></span> <span data-ttu-id="dddbb-1002">Başka bir eyleme (veya başka bir sayfaya) bağlanırken, yol değerleri istenmeyen yollarla yeniden kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1002">When linking to another action (or another page), route values can be reused in undesirable ways.</span></span>

  <span data-ttu-id="dddbb-1003">*/Pages/Store/Product.exe* :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1003">In */Pages/Store/Product.cshtml* :</span></span>

  ```cshtml
  @page "{id}"
  @Url.Page("/Login")
  ```

  <span data-ttu-id="dddbb-1004">*/Pages/Login.exe* içinde:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1004">In */Pages/Login.cshtml* :</span></span>

  ```cshtml
  @page "{id?}"
  ```

  <span data-ttu-id="dddbb-1005">URI `/Store/Product/18` ASP.NET Core 2,1 veya daha önceki bir sürümdeyse, tarafından Store/Info sayfasında oluşturulan bağlantı `@Url.Page("/Login")` olur `/Login/18` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1005">If the URI is `/Store/Product/18` in ASP.NET Core 2.1 or earlier, the link generated on the Store/Info page by `@Url.Page("/Login")` is `/Login/18`.</span></span> <span data-ttu-id="dddbb-1006">`id`16 değeri, bağlantı hedefi uygulamanın tamamen farklı bir parçası olsa da yeniden kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1006">The `id` value of 18 is reused, even though the link destination is different part of the app entirely.</span></span> <span data-ttu-id="dddbb-1007">`id`Sayfa bağlamındaki yol değeri `/Login` büyük olasılıkla bir mağaza ürün kimliği değeri değil, BIR kullanıcı kimliği değeridir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1007">The `id` route value in the context of the `/Login` page is probably a user ID value, not a store product ID value.</span></span>

  <span data-ttu-id="dddbb-1008">ASP.NET Core 2,2 veya sonraki bir sürümü ile Endpoint Routing de sonuç olur `/Login` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1008">In endpoint routing with ASP.NET Core 2.2 or later, the result is `/Login`.</span></span> <span data-ttu-id="dddbb-1009">Bağlantılı hedef farklı bir eylem veya sayfa olduğunda çevresel değerler yeniden kullanılmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1009">Ambient values aren't reused when the linked destination is a different action or page.</span></span>

* <span data-ttu-id="dddbb-1010">Gidiş dönüşü yol parametresi sözdizimi: çift yıldız ( `**` ) catch-all parametre sözdizimi kullanılırken eğik çizgiler kodlanmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1010">Round-tripping route parameter syntax: Forward slashes aren't encoded when using a double-asterisk (`**`) catch-all parameter syntax.</span></span>

  <span data-ttu-id="dddbb-1011">Bağlantı oluşturma sırasında, yönlendirme sistemi, eğik çizgiler hariç, bir çift yıldız ( `**` ) catch-all parametresinde (örneğin,) yakalanan değeri kodlar `{**myparametername}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1011">During link generation, the routing system encodes the value captured in a double-asterisk (`**`) catch-all parameter (for example, `{**myparametername}`) except the forward slashes.</span></span> <span data-ttu-id="dddbb-1012">Çift yıldız yakalama, `IRouter` ASP.NET Core 2,2 veya üzeri sürümlerde tabanlı yönlendirme ile desteklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1012">The double-asterisk catch-all is supported with `IRouter`-based routing in ASP.NET Core 2.2 or later.</span></span>

  <span data-ttu-id="dddbb-1013">ASP.NET Core () öğesinin önceki sürümlerindeki tek yıldız catch-all parametre sözdizimi `{*myparametername}` desteklenmeye devam eder ve eğik çizgi kodlandı.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1013">The single asterisk catch-all parameter syntax in prior versions of ASP.NET Core (`{*myparametername}`) remains supported, and forward slashes are encoded.</span></span>

  | <span data-ttu-id="dddbb-1014">Yol</span><span class="sxs-lookup"><span data-stu-id="dddbb-1014">Route</span></span>              | <span data-ttu-id="dddbb-1015">İle oluşturulan bağlantı</span><span class="sxs-lookup"><span data-stu-id="dddbb-1015">Link generated with</span></span><br>`Url.Action(new { category = "admin/products" })`&hellip; |
  | ------------------ | --------------------------------------------------------------------- |
  | `/search/{*page}`  | <span data-ttu-id="dddbb-1016">`/search/admin%2Fproducts` (eğik çizgi kodlandı)</span><span class="sxs-lookup"><span data-stu-id="dddbb-1016">`/search/admin%2Fproducts` (the forward slash is encoded)</span></span>             |
  | `/search/{**page}` | `/search/admin/products`                                              |

### <a name="middleware-example"></a><span data-ttu-id="dddbb-1017">Ara yazılım örneği</span><span class="sxs-lookup"><span data-stu-id="dddbb-1017">Middleware example</span></span>

<span data-ttu-id="dddbb-1018">Aşağıdaki örnekte, bir ara yazılım, <xref:Microsoft.AspNetCore.Routing.LinkGenerator> Mağaza ürünlerini listeleyen bir eylem yöntemine bağlantı oluşturmak için API 'yi kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1018">In the following example, a middleware uses the <xref:Microsoft.AspNetCore.Routing.LinkGenerator> API to create link to an action method that lists store products.</span></span> <span data-ttu-id="dddbb-1019">Bağlantı Oluşturucuyu bir sınıfa ekleme ve çağırarak bir `GenerateLink` uygulamadaki herhangi bir sınıf için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1019">Using the link generator by injecting it into a class and calling `GenerateLink` is available to any class in an app.</span></span>

```csharp
using Microsoft.AspNetCore.Routing;

public class ProductsLinkMiddleware
{
    private readonly LinkGenerator _linkGenerator;

    public ProductsLinkMiddleware(RequestDelegate next, LinkGenerator linkGenerator)
    {
        _linkGenerator = linkGenerator;
    }

    public async Task InvokeAsync(HttpContext httpContext)
    {
        var url = _linkGenerator.GetPathByAction("ListProducts", "Store");

        httpContext.Response.ContentType = "text/plain";

        await httpContext.Response.WriteAsync($"Go to {url} to see our products.");
    }
}
```

### <a name="create-routes"></a><span data-ttu-id="dddbb-1020">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1020">Create routes</span></span>

<span data-ttu-id="dddbb-1021">Çoğu uygulama, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ' de tanımlanan benzer uzantı yöntemlerinden birini çağırarak veya arayarak yollar oluşturur <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1021">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="dddbb-1022"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder>Uzantı yöntemlerinden herhangi biri bir örneği oluşturur <xref:Microsoft.AspNetCore.Routing.Route> ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1022">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="dddbb-1023"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yol işleyici parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1023"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="dddbb-1024"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yalnızca tarafından işlenen rotaları ekler <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1024"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="dddbb-1025">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-1025">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="dddbb-1026">Aşağıdaki kod örneği, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan bir çağrının örneğidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1026">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="dddbb-1027">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1027">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="dddbb-1028">Örneğin, yol `/Products/Details/17` Şu yol değerlerini oluşturur: `{ controller = Products, action = Details, id = 17 }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1028">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="dddbb-1029">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1029">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="dddbb-1030">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1030">Route parameters are named.</span></span> <span data-ttu-id="dddbb-1031">Küme ayraçları içinde parametre adının çevreleme tarafından tanımlanan parametreler `{ ... }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1031">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="dddbb-1032">Yukarıdaki şablon URL yoluyla da eşleştirebilir `/` ve değerleri üretebilir `{ controller = Home, action = Index }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1032">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="dddbb-1033">Bu durum, `{controller}` ve `{action}` Rota parametrelerinin varsayılan değerleri olduğu ve `id` route parametresinin isteğe bağlı olması nedeniyle oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1033">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="dddbb-1034">Bir eşittir işareti ( `=` ), yol parametre adından sonra gelen bir değer, parametre için varsayılan bir değer tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1034">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="dddbb-1035">`?`Yol parametre adından sonra bir soru işareti () isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1035">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="dddbb-1036">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1036">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="dddbb-1037">İsteğe bağlı parametreler, karşılık gelen bir URL yol kesimi yoksa rota değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1037">Optional parameters don't produce a route value if there is no corresponding URL path segment.</span></span> <span data-ttu-id="dddbb-1038">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1038">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="dddbb-1039">Aşağıdaki örnekte, Route parametresi tanımı `{id:int}` yol parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlar `id` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1039">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="dddbb-1040">Bu şablon, gibi bir URL yoluyla eşleşir `/Products/Details/17` `/Products/Details/Apples` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1040">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="dddbb-1041">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> , yol değerlerini uygulayıp onları doğrulamak için inceler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1041">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="dddbb-1042">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1042">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="dddbb-1043">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1043">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="dddbb-1044"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, Ve için değer kabul etmenin ek aşırı yüklemeleri `constraints` `dataTokens` `defaults` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1044">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="dddbb-1045">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1045">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="dddbb-1046">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekler eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1046">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="dddbb-1047">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1047">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="dddbb-1048">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1048">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="dddbb-1049">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1049">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{**article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="dddbb-1050">Önceki şablon, benzer bir URL yoluyla eşleşir `/Blog/All-About-Routing/Introduction` ve değerleri ayıklar `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1050">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="dddbb-1051">Ve için varsayılan yol değerleri `controller` , `action` şablonda karşılık gelen hiçbir yol parametresi olmasa bile rota tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1051">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="dddbb-1052">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1052">Default values can be specified in the route template.</span></span> <span data-ttu-id="dddbb-1053">Route `article` parametresi, yol parametre adından önce bir çift yıldız işareti () görünümüne göre *catch-all* olarak tanımlanır `**` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1053">The `article` route parameter is defined as a *catch-all* by the appearance of an double asterisk (`**`) before the route parameter name.</span></span> <span data-ttu-id="dddbb-1054">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1054">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="dddbb-1055">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1055">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="dddbb-1056">Önceki şablon, benzer bir URL yoluyla eşleşir `/en-US/Products/5` ve değerleri `{ controller = Products, action = Details, id = 5 }` ve veri belirteçlerini ayıklar `{ locale = en-US }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1056">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="dddbb-1058">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1058">Route class URL generation</span></span>

<span data-ttu-id="dddbb-1059">Sınıf, yol <xref:Microsoft.AspNetCore.Routing.Route> değerlerini bir kümesini rota şablonuyla BIRLEŞTIREREK URL oluşturma işlemi de gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1059">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="dddbb-1060">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1060">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="dddbb-1061">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1061">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="dddbb-1062">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="dddbb-1062">What values would be produced?</span></span> <span data-ttu-id="dddbb-1063">Bu, URL oluşturma işlevinin sınıfında nasıl çalıştığına ilişkin kaba bir eştir <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1063">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="dddbb-1064">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1064">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="dddbb-1065">Yol değerleriyle `{ controller = Products, action = List }` , URL `/Products/List` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1065">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="dddbb-1066">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1066">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="dddbb-1067">`id`, İsteğe bağlı bir yol parametresi olduğundan, URL 'si için bir değer olmadan başarıyla oluşturulur `id` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1067">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="dddbb-1068">Yol değerleriyle `{ controller = Home, action = Index }` , URL `/` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1068">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="dddbb-1069">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1069">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="dddbb-1070">Her iki URL de aşağıdaki yol tanımıyla (ve) birlikte gidiş dönüş, `/Home/Index` `/` URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1070">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="dddbb-1071">ASP.NET Core MVC kullanan bir uygulama <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> , doğrudan yönlendirmeye çağırmak yerine URL 'ler oluşturmak için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1071">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="dddbb-1072">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1072">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="dddbb-1073">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1073">Use Routing Middleware</span></span>

<span data-ttu-id="dddbb-1074">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1074">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="dddbb-1075">İçindeki hizmet kapsayıcısına yönlendirme Ekle `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1075">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="dddbb-1076">Yolların yönteminde yapılandırılması gerekir `Startup.Configure` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1076">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="dddbb-1077">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1077">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="dddbb-1078"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Yalnızca HTTP GET isteklerini eşleştirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1078"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="dddbb-1079">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1079">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="dddbb-1080">URI</span><span class="sxs-lookup"><span data-stu-id="dddbb-1080">URI</span></span>                    | <span data-ttu-id="dddbb-1081">Yanıt</span><span class="sxs-lookup"><span data-stu-id="dddbb-1081">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="dddbb-1082">Herkese!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1082">Hello!</span></span> <span data-ttu-id="dddbb-1083">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="dddbb-1083">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="dddbb-1084">Herkese!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1084">Hello!</span></span> <span data-ttu-id="dddbb-1085">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="dddbb-1085">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="dddbb-1086">Herkese!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1086">Hello!</span></span> <span data-ttu-id="dddbb-1087">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="dddbb-1087">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="dddbb-1088">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1088">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="dddbb-1089">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1089">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="dddbb-1090">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1090">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="dddbb-1091">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1091">The request falls through, no match.</span></span>              |

<span data-ttu-id="dddbb-1092">Framework, yollar () oluşturmak için bir genişletme yöntemleri kümesi sağlar <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1092">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="dddbb-1093">`Map[Verb]`Yöntemler, yöntem ADıNDAKI http fiili ile rotayı sınırlandırmak için kısıtlamalar kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1093">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="dddbb-1094">Örnek için bkz. <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1094">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="dddbb-1095">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1095">Route template reference</span></span>

<span data-ttu-id="dddbb-1096">Küme ayraçları () içindeki belirteçler `{ ... }` , yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1096">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="dddbb-1097">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1097">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="dddbb-1098">Örneğin, `{controller=Home}{action=Index}` ve arasında değişmez değer olmadığından geçerli bir yol değil `{controller}` `{action}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1098">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="dddbb-1099">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1099">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="dddbb-1100">Yol parametrelerinden (örneğin, `{id}` ) ve yol ayırıcısından farklı bir metin, `/` URL içindeki metinle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1100">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="dddbb-1101">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1101">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="dddbb-1102">Bir sabit yol parametresi sınırlayıcısından ( `{` veya) eşleştirmek için `}` , karakteri (veya) tekrarlayarak sınırlayıcıdan kaçış `{{` `}}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1102">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="dddbb-1103">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1103">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="dddbb-1104">Örneğin, şablonu göz önünde bulundurun `files/{filename}.{ext?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1104">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="dddbb-1105">Hem hem de için değerler olduğunda `filename` `ext` her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1105">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="dddbb-1106">URL 'de yalnızca bir değeri `filename` varsa, sondaki nokta ( `.` ) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1106">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="dddbb-1107">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1107">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="dddbb-1108">`*` `**` URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak bir yıldız işareti () veya çift yıldız işareti () kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1108">You can use an asterisk (`*`) or double asterisk (`**`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="dddbb-1109">Bunlara *catch-all* parametreleri denir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1109">These are called a *catch-all* parameters.</span></span> <span data-ttu-id="dddbb-1110">Örneğin, `blog/{**slug}` ile başlayan `/blog` ve bunu izleyen bir değere sahip olan ve yol değerine atanan HERHANGI bir URI ile eşleşir `slug` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1110">For example, `blog/{**slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="dddbb-1111">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1111">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="dddbb-1112">Yol ayırıcısı () karakterleri de dahil olmak üzere bir URL oluşturmak için, catch-all parametresi uygun karakterleri de çıkar `/` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1112">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="dddbb-1113">Örneğin, `foo/{*path}` yol değerlerini içeren yol `{ path = "my/path" }` oluşturulur `foo/my%2Fpath` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1113">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="dddbb-1114">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1114">Note the escaped forward slash.</span></span> <span data-ttu-id="dddbb-1115">Yol ayırıcı karakterlerini yuvarlaklaştırmak için `**` Rota parametresi önekini kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1115">To round-trip path separator characters, use the `**` route parameter prefix.</span></span> <span data-ttu-id="dddbb-1116">İle rota `foo/{**path}` `{ path = "my/path" }` oluşturulur `foo/my/path` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1116">The route `foo/{**path}` with `{ path = "my/path" }` generates `foo/my/path`.</span></span>

<span data-ttu-id="dddbb-1117">Yol parametreleri, parametre adından sonra bir eşittir işaretiyle () ayırarak varsayılan değer belirtilerek belirlenmiş *varsayılan değerlere* sahip olabilir `=` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1117">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="dddbb-1118">Örneğin, `{controller=Home}` `Home` için varsayılan değer olarak tanımlar `controller` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1118">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="dddbb-1119">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1119">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="dddbb-1120">Yol parametreleri `?` , içinde olduğu gibi parametre adının sonuna bir soru işareti () eklenerek isteğe bağlı olarak yapılır `id?` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1120">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="dddbb-1121">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden, &mdash; isteğe bağlı bir parametre yalnızca Istek URL 'si tarafından bir değer sağlandığı zaman bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1121">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="dddbb-1122">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1122">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="dddbb-1123">`:`Yol parametre adından sonra bir iki nokta () ve kısıtlama adı eklemek, bir rota parametresinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1123">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="dddbb-1124">Kısıtlama bağımsız değişkenler gerektiriyorsa, `(...)` kısıtlama adından sonra parantez () içine alınır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1124">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="dddbb-1125">Birden çok satır içi kısıtlama, başka bir iki nokta ( `:` ) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1125">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="dddbb-1126">Kısıtlama adı ve bağımsız değişkenler, <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> URL işlemede kullanılmak üzere bir örneği oluşturmak için hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1126">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="dddbb-1127">Örneğin, yol şablonu `blog/{article:minlength(10)}` `minlength` bağımsız değişkenle bir kısıtlama belirtir `10` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1127">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="dddbb-1128">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1128">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="dddbb-1129">Yol parametrelerinde Ayrıca, bağlantı oluştururken ve URL 'Ler ile eşleşen eylemler ve sayfalar için bir parametrenin değerini dönüştüren parametre dönüştürücüler bulunabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1129">Route parameters may also have parameter transformers, which transform a parameter's value when generating links and matching actions and pages to URLs.</span></span> <span data-ttu-id="dddbb-1130">LIKE kısıtlamaları, yol parametre adından sonra iki nokta üst üste ( `:` ) ve transformatör adı eklenerek, parametre dönüştürücüleri bir rota parametresine eklenebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1130">Like constraints, parameter transformers can be added inline to a route parameter by adding a colon (`:`) and transformer name after the route parameter name.</span></span> <span data-ttu-id="dddbb-1131">Örneğin, yol şablonu `blog/{article:slugify}` bir `slugify` transformatör belirtir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1131">For example, the route template `blog/{article:slugify}` specifies a `slugify` transformer.</span></span> <span data-ttu-id="dddbb-1132">Parametre dönüştürücüler hakkında daha fazla bilgi için bkz. [Parameter transformatör başvurusu](#parameter-transformer-reference) bölümü.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1132">For more information on parameter transformers, see the [Parameter transformer reference](#parameter-transformer-reference) section.</span></span>

<span data-ttu-id="dddbb-1133">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1133">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="dddbb-1134">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1134">Route Template</span></span>                           | <span data-ttu-id="dddbb-1135">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="dddbb-1135">Example Matching URI</span></span>    | <span data-ttu-id="dddbb-1136">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="dddbb-1136">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="dddbb-1137">Yalnızca tek bir yolla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1137">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="dddbb-1138">İle eşleşir ve `Page` ayarlanır `Home` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1138">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="dddbb-1139">İle eşleşir ve `Page` ayarlanır `Contact` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1139">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="dddbb-1140">`Products`Denetleyiciye ve `List` eyleme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1140">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="dddbb-1141">`Products`Denetleyici ve `Details` eyleme eşlenir ( `id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1141">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="dddbb-1142">`Home`Denetleyiciye ve `Index` yönteme eşlenir ( `id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1142">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="dddbb-1143">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1143">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="dddbb-1144">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1144">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="dddbb-1145">İstekleri eşleme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1145">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="reserved-routing-names"></a><span data-ttu-id="dddbb-1146">Ayrılmış yönlendirme adları</span><span class="sxs-lookup"><span data-stu-id="dddbb-1146">Reserved routing names</span></span>

<span data-ttu-id="dddbb-1147">Aşağıdaki anahtar sözcükler ayrılmış isimlerdir ve yol adları veya parametreler olarak kullanılamaz:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1147">The following keywords are reserved names and can't be used as route names or parameters:</span></span>

* `action`
* `area`
* `controller`
* `handler`
* `page`

## <a name="route-constraint-reference"></a><span data-ttu-id="dddbb-1148">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1148">Route constraint reference</span></span>

<span data-ttu-id="dddbb-1149">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1149">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="dddbb-1150">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1150">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="dddbb-1151">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1151">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="dddbb-1152">Örneğin, <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> bir ISTEğI http fiiline bağlı olarak kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1152">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="dddbb-1153">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1153">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="dddbb-1154">**Giriş doğrulaması** için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1154">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="dddbb-1155">**Giriş doğrulaması** için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1155">If constraints are used for **input validation** , invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="dddbb-1156">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1156">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="dddbb-1157">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1157">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="dddbb-1158">Kısıtlaması</span><span class="sxs-lookup"><span data-stu-id="dddbb-1158">Constraint</span></span> | <span data-ttu-id="dddbb-1159">Örnek</span><span class="sxs-lookup"><span data-stu-id="dddbb-1159">Example</span></span> | <span data-ttu-id="dddbb-1160">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1160">Example matches</span></span> | <span data-ttu-id="dddbb-1161">Notlar</span><span class="sxs-lookup"><span data-stu-id="dddbb-1161">Notes</span></span> |
|------------|---------|-----------------|-------|
| `int` | `{id:int}` | <span data-ttu-id="dddbb-1162">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1162">`123456789`, `-123456789`</span></span> | <span data-ttu-id="dddbb-1163">Herhangi bir tamsayıyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1163">Matches any integer.</span></span>|
| `bool` | `{active:bool}` | <span data-ttu-id="dddbb-1164">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1164">`true`, `FALSE`</span></span> | <span data-ttu-id="dddbb-1165">Veya ile eşleşir `true` `false` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1165">Matches `true` or `false`.</span></span> <span data-ttu-id="dddbb-1166">Büyük/küçük harf duyarsız.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1166">Case-insensitive.</span></span>|
| `datetime` | `{dob:datetime}` | <span data-ttu-id="dddbb-1167">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1167">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="dddbb-1168">`DateTime`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1168">Matches a valid `DateTime` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1169">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1169">See preceding warning.</span></span>|
| `decimal` | `{price:decimal}` | <span data-ttu-id="dddbb-1170">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1170">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="dddbb-1171">`decimal`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1171">Matches a valid `decimal` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1172">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1172">See  preceding warning.</span></span>|
| `double` | `{weight:double}` | <span data-ttu-id="dddbb-1173">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1173">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="dddbb-1174">`double`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1174">Matches a valid `double` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1175">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1175">See  preceding warning.</span></span>|
| `float` | `{weight:float}` | <span data-ttu-id="dddbb-1176">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1176">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="dddbb-1177">`float`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1177">Matches a valid `float` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1178">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1178">See  preceding warning.</span></span>|
| `guid` | `{id:guid}` | <span data-ttu-id="dddbb-1179">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1179">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="dddbb-1180">Geçerli bir `Guid` değerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1180">Matches a valid `Guid` value.</span></span>|
| `long` | `{ticks:long}` | <span data-ttu-id="dddbb-1181">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1181">`123456789`, `-123456789`</span></span> | <span data-ttu-id="dddbb-1182">Geçerli bir `long` değerle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1182">Matches a valid `long` value.</span></span>|
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="dddbb-1183">Dize en az 4 karakter olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1183">String must be at least 4 characters.</span></span>|
| `maxlength(value)` | `{filename:maxlength(8)}` | `MyFile` | <span data-ttu-id="dddbb-1184">Dizede en fazla 8 karakter vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1184">String has maximum of 8 characters.</span></span>|
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="dddbb-1185">Dize tam olarak 12 karakter uzunluğunda olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1185">String must be exactly 12 characters long.</span></span>|
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="dddbb-1186">Dize en az 8 olmalı ve en fazla 16 karakter uzunluğunda olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1186">String must be at least 8 and has maximum of 16 characters.</span></span>|
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="dddbb-1187">Tamsayı değeri en az 18 olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1187">Integer value must be at least 18.</span></span>|
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="dddbb-1188">Tamsayı değeri üst sınırı 120.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1188">Integer value maximum of 120.</span></span>|
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="dddbb-1189">Tamsayı değeri en az 18 ve en fazla 120 olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1189">Integer value must be at least 18 and maximum of 120.</span></span>|
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="dddbb-1190">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır `a` - `z` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1190">String must consist of one or more alphabetical characters `a`-`z`.</span></span> <span data-ttu-id="dddbb-1191">Büyük/küçük harf duyarsız.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1191">Case-insensitive.</span></span>|
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="dddbb-1192">Dize, normal ifadeyle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1192">String must match the regular expression.</span></span> <span data-ttu-id="dddbb-1193">Normal ifade tanımlama hakkında ipuçlarına bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1193">See tips about defining a regular expression.</span></span>|
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="dddbb-1194">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1194">Used to enforce that a non-parameter value is present during URL generation.</span></span>|

<span data-ttu-id="dddbb-1195">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1195">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="dddbb-1196">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1196">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="dddbb-1197">URL 'YI doğrulayan ve bir CLR türüne (veya gibi) dönüştürülen yol kısıtlamaları `int` `DateTime` , her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1197">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="dddbb-1198">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1198">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="dddbb-1199">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1199">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="dddbb-1200">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1200">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="dddbb-1201">Örneğin, `float` kısıtlama yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1201">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="dddbb-1202">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1202">Regular expressions</span></span>

<span data-ttu-id="dddbb-1203">ASP.NET Core Framework, `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` normal ifade oluşturucusuna ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1203">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="dddbb-1204"><xref:System.Text.RegularExpressions.RegexOptions>Bu üyelerin açıklaması için bkz..</span><span class="sxs-lookup"><span data-stu-id="dddbb-1204">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="dddbb-1205">Normal ifadeler, Yönlendirme ve C# dili tarafından kullanılanlarla benzerlik olan sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1205">Regular expressions use delimiters and tokens similar to those used by routing and the C# language.</span></span> <span data-ttu-id="dddbb-1206">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1206">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="dddbb-1207">Yönlendirmenin normal ifadesini kullanmak için `^\d{3}-\d{2}-\d{4}$` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1207">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing:</span></span>

* <span data-ttu-id="dddbb-1208">İfade, `\` dize içinde, kaynak kodunda çift ters eğik çizgi olarak belirtilen tek ters eğik çizgi karakterleri içermelidir `\\` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1208">The expression must have the single backslash `\` characters provided in the string as double backslash `\\` characters in the source code.</span></span>
* <span data-ttu-id="dddbb-1209">Normal ifade, `\\` dize kaçış karakterinden kaçınmak için bizim için olmalıdır `\` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1209">The regular expression must us `\\` in order to escape the `\` string escape character.</span></span>
* <span data-ttu-id="dddbb-1210">Normal ifade, tam `\\` [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanılırken gerekli değildir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1210">The regular expression doesn't require `\\` when using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string).</span></span>

<span data-ttu-id="dddbb-1211">Yönlendirme parametresi sınırlayıcı karakterleri,,,, `{` `}` `[` `]` ifadesindeki karakterleri çift `{{` `}` `[[` `]]` kaçış,,,,,,,,,,,.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1211">To escape routing parameter delimiter characters `{`, `}`, `[`, `]`, double the characters in the expression `{{`, `}`, `[[`, `]]`.</span></span> <span data-ttu-id="dddbb-1212">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1212">The following table shows a regular expression and the escaped version:</span></span>

| <span data-ttu-id="dddbb-1213">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="dddbb-1213">Regular Expression</span></span>    | <span data-ttu-id="dddbb-1214">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="dddbb-1214">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="dddbb-1215">Yönlendirmelerde kullanılan normal ifadeler, genellikle giriş işareti `^` karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1215">Regular expressions used in routing often start with the caret `^` character and match starting position of the string.</span></span> <span data-ttu-id="dddbb-1216">İfadeler, genellikle dolar işareti `$` karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1216">The expressions often end with the dollar sign `$` character and match end of the string.</span></span> <span data-ttu-id="dddbb-1217">`^`Ve `$` karakterleri, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1217">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="dddbb-1218">`^`Ve karakterleri olmadan `$` normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1218">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="dddbb-1219">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1219">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="dddbb-1220">Expression</span><span class="sxs-lookup"><span data-stu-id="dddbb-1220">Expression</span></span>   | <span data-ttu-id="dddbb-1221">Dize</span><span class="sxs-lookup"><span data-stu-id="dddbb-1221">String</span></span>    | <span data-ttu-id="dddbb-1222">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-1222">Match</span></span> | <span data-ttu-id="dddbb-1223">Yorum</span><span class="sxs-lookup"><span data-stu-id="dddbb-1223">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1224">hello</span><span class="sxs-lookup"><span data-stu-id="dddbb-1224">hello</span></span>     | <span data-ttu-id="dddbb-1225">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1225">Yes</span></span>   | <span data-ttu-id="dddbb-1226">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1226">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1227">123abc456</span><span class="sxs-lookup"><span data-stu-id="dddbb-1227">123abc456</span></span> | <span data-ttu-id="dddbb-1228">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1228">Yes</span></span>   | <span data-ttu-id="dddbb-1229">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1229">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1230">MZ</span><span class="sxs-lookup"><span data-stu-id="dddbb-1230">mz</span></span>        | <span data-ttu-id="dddbb-1231">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1231">Yes</span></span>   | <span data-ttu-id="dddbb-1232">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="dddbb-1232">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1233">MZ</span><span class="sxs-lookup"><span data-stu-id="dddbb-1233">MZ</span></span>        | <span data-ttu-id="dddbb-1234">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1234">Yes</span></span>   | <span data-ttu-id="dddbb-1235">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="dddbb-1235">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="dddbb-1236">hello</span><span class="sxs-lookup"><span data-stu-id="dddbb-1236">hello</span></span>     | <span data-ttu-id="dddbb-1237">Hayır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1237">No</span></span>    | <span data-ttu-id="dddbb-1238">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1238">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="dddbb-1239">123abc456</span><span class="sxs-lookup"><span data-stu-id="dddbb-1239">123abc456</span></span> | <span data-ttu-id="dddbb-1240">Hayır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1240">No</span></span>    | <span data-ttu-id="dddbb-1241">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1241">See `^` and `$` above</span></span> |

<span data-ttu-id="dddbb-1242">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1242">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="dddbb-1243">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1243">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="dddbb-1244">Örneğin, `{action:regex(^(list|get|create)$)}` yalnızca `action` Rota değeri `list` ,, veya ile eşleşir `get` `create` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1244">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="dddbb-1245">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1245">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="dddbb-1246">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1246">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="dddbb-1247">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="dddbb-1247">Custom route constraints</span></span>

<span data-ttu-id="dddbb-1248">Yerleşik yol kısıtlamalarına ek olarak, arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1248">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="dddbb-1249"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>Arabirim, `Match` `true` kısıtlama karşılanıp Aksi takdirde döndüren tek bir yöntemi içerir `false` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1249">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="dddbb-1250">Özel bir kullanmak için <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> yol kısıtlama türü, uygulamanın uygulamanın hizmet kapsayıcısında kayıtlı olması gerekir <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1250">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="dddbb-1251"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, Yol kısıtlama anahtarlarını <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> Bu kısıtlamaları doğrulayan uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1251">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="dddbb-1252">Bir uygulama <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> , `Startup.ConfigureServices` hizmetlerin bir parçası olarak ' de güncelleştirilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan ile yapılandırma `services.Configure<RouteOptions>` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1252">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="dddbb-1253">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1253">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="dddbb-1254">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1254">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="dddbb-1255">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1255">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="parameter-transformer-reference"></a><span data-ttu-id="dddbb-1256">Parametre transformatörü başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1256">Parameter transformer reference</span></span>

<span data-ttu-id="dddbb-1257">Parametre dönüştürücüler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1257">Parameter transformers:</span></span>

* <span data-ttu-id="dddbb-1258">Bir bağlantısını oluştururken yürütün <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1258">Execute when generating a link for a <xref:Microsoft.AspNetCore.Routing.Route>.</span></span>
* <span data-ttu-id="dddbb-1259">Uygulayın `Microsoft.AspNetCore.Routing.IOutboundParameterTransformer` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1259">Implement `Microsoft.AspNetCore.Routing.IOutboundParameterTransformer`.</span></span>
* <span data-ttu-id="dddbb-1260">Kullanılarak yapılandırılır <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1260">Are configured using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>.</span></span>
* <span data-ttu-id="dddbb-1261">Parametrenin yol değerini alın ve yeni bir dize değerine dönüştürün.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1261">Take the parameter's route value and transform it to a new string value.</span></span>
* <span data-ttu-id="dddbb-1262">Oluşturulan bağlantıda Dönüştürülmüş değerin kullanılmasına neden olacak.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1262">Result in using the transformed value in the generated link.</span></span>

<span data-ttu-id="dddbb-1263">Örneğin, yol düzeninde bir özel `slugify` parametre transformatörü `blog\{article:slugify}` `Url.Action(new { article = "MyTestArticle" })` oluşturur `blog\my-test-article` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1263">For example, a custom `slugify` parameter transformer in route pattern `blog\{article:slugify}` with `Url.Action(new { article = "MyTestArticle" })` generates `blog\my-test-article`.</span></span>

<span data-ttu-id="dddbb-1264">Bir parametre transformatörü bir yol düzeninde kullanmak için, önce ' de kullanarak bunu <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> yapılandırın `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1264">To use a parameter transformer in a route pattern, configure it first using <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddRouting(options =>
{
    // Replace the type and the name used to refer to it with your own
    // IOutboundParameterTransformer implementation
    options.ConstraintMap["slugify"] = typeof(SlugifyParameterTransformer);
});
```

<span data-ttu-id="dddbb-1265">Parametre dönüştürücüler, bir uç noktanın çözümlediği URI 'yi dönüştürmek için çerçevesi tarafından kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1265">Parameter transformers are used by the framework to transform the URI where an endpoint resolves.</span></span> <span data-ttu-id="dddbb-1266">Örneğin, ASP.NET Core MVC,,, ve ile eşleşecek şekilde kullanılan rota değerini dönüştürmek için parametre dönüştürücüler kullanır `area` `controller` `action` `page` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1266">For example, ASP.NET Core MVC uses parameter transformers to transform the route value used to match an `area`, `controller`, `action`, and `page`.</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller:slugify=Home}/{action:slugify=Index}/{id?}");
```

<span data-ttu-id="dddbb-1267">Önceki yol ile, eylem `SubscriptionManagementController.GetAll` URI ile eşleştirilir `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1267">With the preceding route, the action `SubscriptionManagementController.GetAll` is matched with the URI `/subscription-management/get-all`.</span></span> <span data-ttu-id="dddbb-1268">Bir parametre transformatörü bir bağlantı oluşturmak için kullanılan rota değerlerini değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1268">A parameter transformer doesn't change the route values used to generate a link.</span></span> <span data-ttu-id="dddbb-1269">Örneğin, `Url.Action("GetAll", "SubscriptionManagement")` çıktılar `/subscription-management/get-all` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1269">For example, `Url.Action("GetAll", "SubscriptionManagement")` outputs `/subscription-management/get-all`.</span></span>

<span data-ttu-id="dddbb-1270">ASP.NET Core, oluşturulan yollarla bir parametre dönüştürücüler kullanmak için API kuralları sağlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1270">ASP.NET Core provides API conventions for using a parameter transformers with generated routes:</span></span>

* <span data-ttu-id="dddbb-1271">ASP.NET Core MVC 'nin `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API kuralı vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1271">ASP.NET Core MVC has the `Microsoft.AspNetCore.Mvc.ApplicationModels.RouteTokenTransformerConvention` API convention.</span></span> <span data-ttu-id="dddbb-1272">Bu kural, uygulamadaki tüm öznitelik yollarına belirtilen bir parametre transformatörü uygular.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1272">This convention applies a specified parameter transformer to all attribute routes in the app.</span></span> <span data-ttu-id="dddbb-1273">Parametre transformatörü, öznitelik yol belirteçlerini değiştirildiklerinde dönüştürür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1273">The parameter transformer transforms attribute route tokens as they are replaced.</span></span> <span data-ttu-id="dddbb-1274">Daha fazla bilgi için bkz. [belirteç değişimini özelleştirmek için bir parametre transformatörü kullanma](xref:mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1274">For more information, see [Use a parameter transformer to customize token replacement](xref:mvc/controllers/routing#use-a-parameter-transformer-to-customize-token-replacement).</span></span>
* <span data-ttu-id="dddbb-1275">:::no-loc(Razor)::: Sayfaların `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API kuralı vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1275">:::no-loc(Razor)::: Pages has the `Microsoft.AspNetCore.Mvc.ApplicationModels.PageRouteTransformerConvention` API convention.</span></span> <span data-ttu-id="dddbb-1276">Bu kural, belirtilen bir parametre transformatörü otomatik olarak bulunan tüm :::no-loc(Razor)::: sayfalara uygular.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1276">This convention applies a specified parameter transformer to all automatically discovered :::no-loc(Razor)::: Pages.</span></span> <span data-ttu-id="dddbb-1277">Parametre transformatörü, sayfa yollarının klasör ve dosya adı segmentlerini dönüştürür :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1277">The parameter transformer transforms the folder and file name segments of :::no-loc(Razor)::: Pages routes.</span></span> <span data-ttu-id="dddbb-1278">Daha fazla bilgi için bkz. [sayfa yollarını özelleştirmek için bir parametre transformatörü kullanma](xref:razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1278">For more information, see [Use a parameter transformer to customize page routes](xref:razor-pages/razor-pages-conventions#use-a-parameter-transformer-to-customize-page-routes).</span></span>

## <a name="url-generation-reference"></a><span data-ttu-id="dddbb-1279">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1279">URL generation reference</span></span>

<span data-ttu-id="dddbb-1280">Aşağıdaki örnek, yol değerlerinin ve bir sözlüğünün bir sözlüğüne verilen bir yolun bağlantısının nasıl oluşturulacağını gösterir <xref:Microsoft.AspNetCore.Routing.RouteCollection> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1280">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="dddbb-1281"><xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath>Önceki örnek sonunda oluşturulan `/package/create/123` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1281">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="dddbb-1282">Sözlük, `operation` `id` "paket yolunu izle" şablonunun ve rota değerlerini sağlar `package/{operation}/{id}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1282">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="dddbb-1283">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1283">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="dddbb-1284">Oluşturucunun ikinci parametresi <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> bir *ortam değerleri* koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1284">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="dddbb-1285">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1285">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="dddbb-1286">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1286">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="dddbb-1287">ASP.NET Core MVC uygulamasının `About` öğesinin eyleminde, `HomeController` `Index` &mdash; ortam değeri kullanılan eyleme bağlamak için denetleyici yol değerini belirtmeniz gerekmez `Home` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1287">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="dddbb-1288">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1288">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="dddbb-1289">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1289">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="dddbb-1290">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1290">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="dddbb-1291">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1291">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="dddbb-1292">Aşağıdaki tabloda, yol şablonu kullanılırken sonuç gösterilmektedir `{controller}/{action}/{id?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1292">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="dddbb-1293">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1293">Ambient Values</span></span>                     | <span data-ttu-id="dddbb-1294">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1294">Explicit Values</span></span>                        | <span data-ttu-id="dddbb-1295">Sonuç</span><span class="sxs-lookup"><span data-stu-id="dddbb-1295">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="dddbb-1296">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1296">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-1297">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1297">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="dddbb-1298">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1298">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-1299">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1299">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="dddbb-1300">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1300">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="dddbb-1301">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1301">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="dddbb-1302">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1302">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-1303">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1303">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="dddbb-1304">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1304">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="dddbb-1305">Bağlantı oluşturma yalnızca ve için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur `controller` `action` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1305">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="dddbb-1306">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1306">Complex segments</span></span>

<span data-ttu-id="dddbb-1307">Karmaşık segmentler (örneğin `[Route("/x{token}y")]` ), sabit değerli olmayan değişmez değer ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1307">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="dddbb-1308">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1308">See [this code](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="dddbb-1309">[Kod örneği](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1309">The [code sample](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>
<!-- While that code is no longer used by ASP.NET Core for complex segment matching, it provides a good match to the current algorithm. The [current code](https://github.com/dotnet/AspNetCore/blob/91514c9af7e0f4c44029b51f05a01c6fe4c96e4c/src/Http/Routing/src/Matching/DfaMatcherBuilder.cs#L227-L244) is too abstracted from matching to be useful for understanding complex segment matching.
-->

::: moniker-end

::: moniker range="< aspnetcore-2.2"

<span data-ttu-id="dddbb-1310">Yönlendirme, istek URI 'Lerini rota işleyicileriyle eşleştirmekten ve gelen istekleri gönderen sorumludur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1310">Routing is responsible for mapping request URIs to route handlers and dispatching an incoming requests.</span></span> <span data-ttu-id="dddbb-1311">Yollar uygulamada tanımlanır ve uygulama başlatıldığında yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1311">Routes are defined in the app and configured when the app starts.</span></span> <span data-ttu-id="dddbb-1312">Yol, isteğe bağlı olarak istekte bulunan URL 'den değerleri ayıklayabilir ve bu değerler, istek işleme için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1312">A route can optionally extract values from the URL contained in the request, and these values can then be used for request processing.</span></span> <span data-ttu-id="dddbb-1313">Uygulamadan yapılandırılan yollar kullanıldığında, yönlendirme, yol işleyicileriyle eşlenen URL 'Ler oluşturabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1313">Using configured routes from the app, routing is able to generate URLs that map to route handlers.</span></span>

<span data-ttu-id="dddbb-1314">ASP.NET Core 2,1 ' de en son yönlendirme senaryolarını kullanmak için, ' de MVC Hizmetleri kaydının [Uyumluluk sürümünü](xref:mvc/compatibility-version) belirtin `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1314">To use the latest routing scenarios in ASP.NET Core 2.1, specify the [compatibility version](xref:mvc/compatibility-version) to the MVC services registration in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc()
    .SetCompatibilityVersion(CompatibilityVersion.Version_2_1);
```

> [!IMPORTANT]
> <span data-ttu-id="dddbb-1315">Bu belge, alt düzey ASP.NET Core yönlendirmeyi içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1315">This document covers low-level ASP.NET Core routing.</span></span> <span data-ttu-id="dddbb-1316">ASP.NET Core MVC yönlendirme hakkında bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-1316">For information on ASP.NET Core MVC routing, see <xref:mvc/controllers/routing>.</span></span> <span data-ttu-id="dddbb-1317">Sayfalardaki yönlendirme kuralları hakkında daha fazla bilgi için :::no-loc(Razor)::: bkz <xref:razor-pages/razor-pages-conventions> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-1317">For information on routing conventions in :::no-loc(Razor)::: Pages, see <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="dddbb-1318">[Örnek kodu görüntüleme veya indirme](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([nasıl indirileceği](xref:index#how-to-download-a-sample))</span><span class="sxs-lookup"><span data-stu-id="dddbb-1318">[View or download sample code](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples) ([how to download](xref:index#how-to-download-a-sample))</span></span>

## <a name="routing-basics"></a><span data-ttu-id="dddbb-1319">Yönlendirme temelleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1319">Routing basics</span></span>

<span data-ttu-id="dddbb-1320">Çoğu uygulama, URL 'Lerin okunabilir ve anlamlı olması için temel ve açıklayıcı bir yönlendirme şeması seçmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1320">Most apps should choose a basic and descriptive routing scheme so that URLs are readable and meaningful.</span></span> <span data-ttu-id="dddbb-1321">Varsayılan geleneksel yol `{controller=Home}/{action=Index}/{id?}` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1321">The default conventional route `{controller=Home}/{action=Index}/{id?}`:</span></span>

* <span data-ttu-id="dddbb-1322">Temel ve açıklayıcı bir yönlendirme düzenini destekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1322">Supports a basic and descriptive routing scheme.</span></span>
* <span data-ttu-id="dddbb-1323">, UI tabanlı uygulamalar için kullanışlı bir başlangıç noktasıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1323">Is a useful starting point for UI-based apps.</span></span>

<span data-ttu-id="dddbb-1324">Geliştiriciler, [öznitelik yönlendirme](xref:mvc/controllers/routing#attribute-routing) veya adanmış geleneksel yollar kullanarak özel durumlarda (örneğin, blog ve e-ticaret uç noktaları) bir uygulamanın yüksek trafik bölümlerine daha fazla terse yolu ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1324">Developers commonly add additional terse routes to high-traffic areas of an app in specialized situations (for example, blog and ecommerce endpoints) using [attribute routing](xref:mvc/controllers/routing#attribute-routing) or dedicated conventional routes.</span></span>

<span data-ttu-id="dddbb-1325">Web API 'Leri, uygulamanın işlevselliğini HTTP fiilleri tarafından temsil edilen bir kaynak kümesi olarak modellemek için öznitelik yönlendirmeyi kullanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1325">Web APIs should use attribute routing to model the app's functionality as a set of resources where operations are represented by HTTP verbs.</span></span> <span data-ttu-id="dddbb-1326">Bu, aynı mantıksal kaynaktaki birçok işlemin (örneğin, GET, POST) aynı URL 'YI kullanacağı anlamına gelir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1326">This means that many operations (for example, GET, POST) on the same logical resource will use the same URL.</span></span> <span data-ttu-id="dddbb-1327">Öznitelik yönlendirme, bir API 'nin Genel uç nokta yerleşimini dikkatle tasarlamak için gereken bir denetim düzeyi sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1327">Attribute routing provides a level of control that's needed to carefully design an API's public endpoint layout.</span></span>

<span data-ttu-id="dddbb-1328">:::no-loc(Razor)::: Sayfalar uygulamalar, bir uygulamanın *Sayfalar* klasöründe adlandırılmış kaynaklara hizmeti sağlamak için varsayılan geleneksel yönlendirmeyi kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1328">:::no-loc(Razor)::: Pages apps use default conventional routing to serve named resources in the *Pages* folder of an app.</span></span> <span data-ttu-id="dddbb-1329">Sayfa yönlendirme davranışını özelleştirmenizi sağlayan ek kurallar mevcuttur :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1329">Additional conventions are available that allow you to customize :::no-loc(Razor)::: Pages routing behavior.</span></span> <span data-ttu-id="dddbb-1330">Daha fazla bilgi için <xref:razor-pages/index> ve <xref:razor-pages/razor-pages-conventions> bölümlerine bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1330">For more information, see <xref:razor-pages/index> and <xref:razor-pages/razor-pages-conventions>.</span></span>

<span data-ttu-id="dddbb-1331">URL oluşturma desteği, uygulamanın, uygulamayı birbirine bağlamak için sabit kodlama URL 'Leri olmadan geliştirilebilmesine izin verir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1331">URL generation support allows the app to be developed without hard-coding URLs to link the app together.</span></span> <span data-ttu-id="dddbb-1332">Bu destek, temel bir yönlendirme yapılandırmasıyla başlayıp uygulamanın kaynak düzeni belirlendikten sonra yolların değiştirilmesini sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1332">This support allows for starting with a basic routing configuration and modifying the routes after the app's resource layout is determined.</span></span>

<span data-ttu-id="dddbb-1333">Yönlendirme, ' nin rotalar uygulamalarını kullanır <xref:Microsoft.AspNetCore.Routing.IRouter> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1333">Routing uses routes implementations of <xref:Microsoft.AspNetCore.Routing.IRouter> to:</span></span>

* <span data-ttu-id="dddbb-1334">Gelen istekleri *Rota işleyicilerine* eşleyin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1334">Map incoming requests to *route handlers*.</span></span>
* <span data-ttu-id="dddbb-1335">Yanıtlarda kullanılan URL 'Leri oluşturun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1335">Generate the URLs used in responses.</span></span>

<span data-ttu-id="dddbb-1336">Varsayılan olarak, bir uygulama tek bir yollar koleksiyonuna sahiptir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1336">By default, an app has a single collection of routes.</span></span> <span data-ttu-id="dddbb-1337">Bir istek geldiğinde koleksiyondaki yollar, koleksiyonda var olan sırada işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1337">When a request arrives, the routes in the collection are processed in the order that they exist in the collection.</span></span> <span data-ttu-id="dddbb-1338">Çerçeve koleksiyondaki her bir rotada yöntemini çağırarak, bir gelen istek URL 'sini koleksiyondaki bir yola eşleştirmeye çalışır <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1338">The framework attempts to match an incoming request URL to a route in the collection by calling the <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> method on each route in the collection.</span></span> <span data-ttu-id="dddbb-1339">Yanıt, yönlendirme bilgilerine göre URL (örneğin, yeniden yönlendirme veya bağlantılar için) oluşturmak için yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1339">A response can use routing to generate URLs (for example, for redirection or links) based on route information and thus avoid hard-coded URLs, which helps maintainability.</span></span>

<span data-ttu-id="dddbb-1340">Yönlendirme sistemi aşağıdaki özelliklere sahiptir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1340">The routing system has the following characteristics:</span></span>

* <span data-ttu-id="dddbb-1341">Yol şablonu sözdizimi, simgeleştirilmiş yol parametrelerine sahip yolları tanımlamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1341">Route template syntax is used to define routes with tokenized route parameters.</span></span>
* <span data-ttu-id="dddbb-1342">Geleneksel stil ve öznitelik stili uç nokta yapılandırmasına izin verilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1342">Conventional-style and attribute-style endpoint configuration is permitted.</span></span>
* <span data-ttu-id="dddbb-1343"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> bir URL parametresinin belirli bir uç nokta kısıtlaması için geçerli bir değer içerip içermediğini belirlemekte kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1343"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint> is used to determine whether a URL parameter contains a valid value for a given endpoint constraint.</span></span>
* <span data-ttu-id="dddbb-1344">MVC/sayfalar gibi uygulama modelleri, :::no-loc(Razor)::: yönlendirme senaryolarının öngörülebilir bir uygulaması olan tüm yollarını kaydeder.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1344">App models, such as MVC/:::no-loc(Razor)::: Pages, register all of their routes, which have a predictable implementation of routing scenarios.</span></span>
* <span data-ttu-id="dddbb-1345">Yanıt, yönlendirme bilgilerine göre URL (örneğin, yeniden yönlendirme veya bağlantılar için) oluşturmak için yönlendirmeyi kullanabilir ve bu sayede bakım yapılmasına yardımcı olan sabit kodlanmış URL 'Lerden kaçınabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1345">A response can use routing to generate URLs (for example, for redirection or links) based on route information and thus avoid hard-coded URLs, which helps maintainability.</span></span>
* <span data-ttu-id="dddbb-1346">URL oluşturma, rastgele genişletilebilirliği destekleyen yollara dayalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1346">URL generation is based on routes, which support arbitrary extensibility.</span></span> <span data-ttu-id="dddbb-1347"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper> URL 'Ler oluşturmak için yöntemler sunar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1347"><xref:Microsoft.AspNetCore.Mvc.IUrlHelper> offers methods to build URLs.</span></span>
<!-- fix [middleware](xref:fundamentals/middleware/index) -->
<span data-ttu-id="dddbb-1348">Yönlendirme, sınıfı tarafından bulunan [Ara yazılım](xref:fundamentals/middleware/index) ardışık düzenine bağlıdır <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1348">Routing is connected to the [middleware](xref:fundamentals/middleware/index) pipeline by the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> class.</span></span> <span data-ttu-id="dddbb-1349">[ASP.NET Core MVC](xref:mvc/overview) , yapılandırma kapsamında bir ara yazılım ardışık düzenine yönlendirme ekler ve MVC ve sayfalar uygulamalarında yönlendirmeyi işler :::no-loc(Razor)::: .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1349">[ASP.NET Core MVC](xref:mvc/overview) adds routing to the middleware pipeline as part of its configuration and handles routing in MVC and :::no-loc(Razor)::: Pages apps.</span></span> <span data-ttu-id="dddbb-1350">Tek başına bileşen olarak yönlendirmeyi nasıl kullanacağınızı öğrenmek için [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1350">To learn how to use routing as a standalone component, see the [Use Routing Middleware](#use-routing-middleware) section.</span></span>

### <a name="url-matching"></a><span data-ttu-id="dddbb-1351">URL eşleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-1351">URL matching</span></span>

<span data-ttu-id="dddbb-1352">URL eşleştirme, yönlendirmenin bir *işleyiciye* gelen istek gönderdiğine yönelik işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1352">URL matching is the process by which routing dispatches an incoming request to a *handler*.</span></span> <span data-ttu-id="dddbb-1353">Bu işlem, URL yolundaki verileri temel alır, ancak istekteki verileri göz önünde bulundurmanız için genişletilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1353">This process is based on data in the URL path but can be extended to consider any data in the request.</span></span> <span data-ttu-id="dddbb-1354">Ayrı işleyicilere istek gönderme özelliği, bir uygulamanın boyutunu ve karmaşıklığını ölçeklendirmeye yönelik anahtardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1354">The ability to dispatch requests to separate handlers is key to scaling the size and complexity of an app.</span></span>

<span data-ttu-id="dddbb-1355">Gelen istekler, <xref:Microsoft.AspNetCore.Builder.RouterMiddleware> <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> sırayla her bir yoldaki yöntemi çağıran öğesini girer.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1355">Incoming requests enter the <xref:Microsoft.AspNetCore.Builder.RouterMiddleware>, which calls the <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> method on each route in sequence.</span></span> <span data-ttu-id="dddbb-1356"><xref:Microsoft.AspNetCore.Routing.IRouter>Örnek, [routecontext. Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler*) değerini null olmayan olarak ayarlayarak isteğin *işleneceğini* seçer <xref:Microsoft.AspNetCore.Http.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1356">The <xref:Microsoft.AspNetCore.Routing.IRouter> instance chooses whether to *handle* the request by setting the [RouteContext.Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler*) to a non-null <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span> <span data-ttu-id="dddbb-1357">Bir yol istek için bir işleyici ayarlarsa, yol işleme duraklar ve işleyici isteği işlemek için çağrılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1357">If a route sets a handler for the request, route processing stops, and the handler is invoked to process the request.</span></span> <span data-ttu-id="dddbb-1358">İsteği işlemek için yol işleyicisi bulunmazsa, ara yazılım istek ardışık düzeninde sonraki bir ara yazılım için isteği kapatır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1358">If no route handler is found to process the request, the middleware hands the request off to the next middleware in the request pipeline.</span></span>

<span data-ttu-id="dddbb-1359">' Ye birincil giriş, <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> geçerli istekle Ilişkili [routecontext. HttpContext](xref:Microsoft.AspNetCore.Routing.RouteContext.HttpContext*) ' dir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1359">The primary input to <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> is the [RouteContext.HttpContext](xref:Microsoft.AspNetCore.Routing.RouteContext.HttpContext*) associated with the current request.</span></span> <span data-ttu-id="dddbb-1360">[Routecontext. Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler) ve [Routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData*) , bir yol eşleştirdikten sonra çıkış olarak ayarlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1360">The [RouteContext.Handler](xref:Microsoft.AspNetCore.Routing.RouteContext.Handler) and [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData*) are outputs set after a route is matched.</span></span>

<span data-ttu-id="dddbb-1361">Çağıran bir eşleşme <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> de [routecontext. RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) özelliklerinin özelliklerini, bu nedenle yapılan istek işlemeye bağlı olarak uygun değerlere ayarlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1361">A match that calls <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> also sets the properties of the [RouteContext.RouteData](xref:Microsoft.AspNetCore.Routing.RouteContext.RouteData) to appropriate values based on the request processing performed thus far.</span></span>

<span data-ttu-id="dddbb-1362">[RouteData. Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) , rotada oluşturulan *yol değerlerinin* bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1362">[RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values*) is a dictionary of *route values* produced from the route.</span></span> <span data-ttu-id="dddbb-1363">Bu değerler genellikle URL 'YI simgeleştirerek belirlenir ve Kullanıcı girişini kabul etmek ya da uygulama içinde daha fazla kararlar almak için kullanılabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1363">These values are usually determined by tokenizing the URL and can be used to accept user input or to make further dispatching decisions inside the app.</span></span>

<span data-ttu-id="dddbb-1364">[RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) , eşleşen rotayla ilgili ek verilerin bir özellik çantadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1364">[RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) is a property bag of additional data related to the matched route.</span></span> <span data-ttu-id="dddbb-1365"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> Her rotayla durum verilerinin ilişkilendirilmesini desteklemek için sağlanır, böylece uygulama hangi yolun eşleştiğini temel alarak kararlar alabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1365"><xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*> are provided to support associating state data with each route so that the app can make decisions based on which route matched.</span></span> <span data-ttu-id="dddbb-1366">Bu değerler, geliştirici tarafından tanımlanır ve yönlendirme davranışını herhangi bir **şekilde etkilemez.**</span><span class="sxs-lookup"><span data-stu-id="dddbb-1366">These values are developer-defined and do **not** affect the behavior of routing in any way.</span></span> <span data-ttu-id="dddbb-1367">Ayrıca, [RouteData. DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) içinde bulunan değerler her türlü türden olabilir. Bu, [veri](xref:Microsoft.AspNetCore.Routing.RouteData.Values)dizeleri arasında dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1367">Additionally, values stashed in [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*) can be of any type, in contrast to [RouteData.Values](xref:Microsoft.AspNetCore.Routing.RouteData.Values), which must be convertible to and from strings.</span></span>

<span data-ttu-id="dddbb-1368">[RouteData. yönlendiriciler](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) , isteği başarıyla eşleştirirken geçen yolların bir listesidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1368">[RouteData.Routers](xref:Microsoft.AspNetCore.Routing.RouteData.Routers) is a list of the routes that took part in successfully matching the request.</span></span> <span data-ttu-id="dddbb-1369">Yollar bir diğerinin içinde iç içe olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1369">Routes can be nested inside of one another.</span></span> <span data-ttu-id="dddbb-1370"><xref:Microsoft.AspNetCore.Routing.RouteData.Routers>Özelliği, bir eşleşme ile sonuçlanan yolların mantıksal ağacı aracılığıyla yolu yansıtır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1370">The <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> property reflects the path through the logical tree of routes that resulted in a match.</span></span> <span data-ttu-id="dddbb-1371">Genellikle, içindeki ilk öğe <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> yol koleksiyonudur ve URL oluşturma için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1371">Generally, the first item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route collection and should be used for URL generation.</span></span> <span data-ttu-id="dddbb-1372">İçindeki son öğe, <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> eşleşen yol işleyicisidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1372">The last item in <xref:Microsoft.AspNetCore.Routing.RouteData.Routers> is the route handler that matched.</span></span>

<a name="lg"></a>

### <a name="url-generation"></a><span data-ttu-id="dddbb-1373">URL oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1373">URL generation</span></span>

<span data-ttu-id="dddbb-1374">URL oluşturma, yönlendirmenin bir yol değerleri kümesine göre bir URL yolu oluşturabileceği işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1374">URL generation is the process by which routing can create a URL path based on a set of route values.</span></span> <span data-ttu-id="dddbb-1375">Bu, yönlendirme işleyicileri ve bunlara erişen URL 'Ler arasındaki mantıksal bir ayrım sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1375">This allows for a logical separation between route handlers and the URLs that access them.</span></span>

<span data-ttu-id="dddbb-1376">URL oluşturma benzer bir yinelemeli işlem izler, ancak yol koleksiyonu metoduna çağıran kullanıcı veya çerçeve kodu ile başlar <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1376">URL generation follows a similar iterative process, but it starts with user or framework code calling into the <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> method of the route collection.</span></span> <span data-ttu-id="dddbb-1377">Her *yol* , <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> null olmayan bir değer döndürülene kadar metodu sırayla çağırılır <xref:Microsoft.AspNetCore.Routing.VirtualPathData> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1377">Each *route* has its <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> method called in sequence until a non-null <xref:Microsoft.AspNetCore.Routing.VirtualPathData> is returned.</span></span>

<span data-ttu-id="dddbb-1378">Birincil girdileri <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1378">The primary inputs to <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> are:</span></span>

* [<span data-ttu-id="dddbb-1379">VirtualPathContext. HttpContext</span><span class="sxs-lookup"><span data-stu-id="dddbb-1379">VirtualPathContext.HttpContext</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext)
* [<span data-ttu-id="dddbb-1380">VirtualPathContext. Values</span><span class="sxs-lookup"><span data-stu-id="dddbb-1380">VirtualPathContext.Values</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values)
* [<span data-ttu-id="dddbb-1381">VirtualPathContext. AmbientValues</span><span class="sxs-lookup"><span data-stu-id="dddbb-1381">VirtualPathContext.AmbientValues</span></span>](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues)

<span data-ttu-id="dddbb-1382">Yollar birincil olarak, tarafından belirtilen yol değerlerini kullanır ve <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> bir URL oluşturma ve hangi değerlerin dahil edileceğini belirlemek için.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1382">Routes primarily use the route values provided by <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> and <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> to decide whether it's possible to generate a URL and what values to include.</span></span> <span data-ttu-id="dddbb-1383">, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> Geçerli istekle eşleştirmeden üretilmiş olan rota değerleri kümesidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1383">The <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues> are the set of route values that were produced from matching the current request.</span></span> <span data-ttu-id="dddbb-1384">Buna karşılık, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> geçerli işlem için Istenen URL 'nin nasıl oluşturulacağını belirten rota değerlerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1384">In contrast, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values> are the route values that specify how to generate the desired URL for the current operation.</span></span> <span data-ttu-id="dddbb-1385">, <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext> Bir yolun geçerli bağlamla ilişkili hizmetleri veya ek verileri alması durumunda sağlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1385">The <xref:Microsoft.AspNetCore.Routing.VirtualPathContext.HttpContext> is provided in case a route should obtain services or additional data associated with the current context.</span></span>

> [!TIP]
> <span data-ttu-id="dddbb-1386">Virtualpathcontext [. Values](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values*) öğesini [Virtualpathcontext. AmbientValues](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues*)için bir geçersiz kılma kümesi olarak düşünün.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1386">Think of [VirtualPathContext.Values](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.Values*) as a set of overrides for the [VirtualPathContext.AmbientValues](xref:Microsoft.AspNetCore.Routing.VirtualPathContext.AmbientValues*).</span></span> <span data-ttu-id="dddbb-1387">URL oluşturma, aynı rota veya yol değerlerini kullanan bağlantılar için URL 'Ler oluşturmak üzere geçerli istekten yol değerlerini yeniden kullanmayı dener.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1387">URL generation attempts to reuse route values from the current request to generate URLs for links using the same route or route values.</span></span>

<span data-ttu-id="dddbb-1388">Çıkışı <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> bir <xref:Microsoft.AspNetCore.Routing.VirtualPathData> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1388">The output of <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> is a <xref:Microsoft.AspNetCore.Routing.VirtualPathData>.</span></span> <span data-ttu-id="dddbb-1389"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> , öğesinin bir paraleldir <xref:Microsoft.AspNetCore.Routing.RouteData> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1389"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> is a parallel of <xref:Microsoft.AspNetCore.Routing.RouteData>.</span></span> <span data-ttu-id="dddbb-1390"><xref:Microsoft.AspNetCore.Routing.VirtualPathData><xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath>Çıkış URL 'si için ve yol tarafından ayarlanması gereken bazı ek özellikler içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1390"><xref:Microsoft.AspNetCore.Routing.VirtualPathData> contains the <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> for the output URL and some additional properties that should be set by the route.</span></span>

<span data-ttu-id="dddbb-1391">[VirtualPathData. VirtualPath](xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath*) özelliği, yol tarafından üretilen *sanal yolu* içerir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1391">The [VirtualPathData.VirtualPath](xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath*) property contains the *virtual path* produced by the route.</span></span> <span data-ttu-id="dddbb-1392">Gereksinimlerinize bağlı olarak, yolu daha fazla işlem yapmanız gerekebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1392">Depending on your needs, you may need to process the path further.</span></span> <span data-ttu-id="dddbb-1393">Oluşturulan URL 'YI HTML 'de işlemek istiyorsanız, uygulamanın temel yolunu ekleyin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1393">If you want to render the generated URL in HTML, prepend the base path of the app.</span></span>

<span data-ttu-id="dddbb-1394">[VirtualPathData. Router](xref:Microsoft.AspNetCore.Routing.VirtualPathData.Router*) , URL 'yi başarıyla oluşturmuş olan yola bir başvurudur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1394">The [VirtualPathData.Router](xref:Microsoft.AspNetCore.Routing.VirtualPathData.Router*) is a reference to the route that successfully generated the URL.</span></span>

<span data-ttu-id="dddbb-1395">[VirtualPathData. DataTokens](xref:Microsoft.AspNetCore.Routing.VirtualPathData.DataTokens*) özellikleri, URL 'yi oluşturan rotayla ilgili ek verilerin bir sözlüğüdür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1395">The [VirtualPathData.DataTokens](xref:Microsoft.AspNetCore.Routing.VirtualPathData.DataTokens*) properties is a dictionary of additional data related to the route that generated the URL.</span></span> <span data-ttu-id="dddbb-1396">Bu, [RouteData. Datatoken](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*)'ların paraleldir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1396">This is the parallel of [RouteData.DataTokens](xref:Microsoft.AspNetCore.Routing.RouteData.DataTokens*).</span></span>

### <a name="create-routes"></a><span data-ttu-id="dddbb-1397">Yolları oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1397">Create routes</span></span>

<span data-ttu-id="dddbb-1398">Yönlendirme <xref:Microsoft.AspNetCore.Routing.Route> sınıfı sınıfının standart uygulamasını sağlar <xref:Microsoft.AspNetCore.Routing.IRouter> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1398">Routing provides the <xref:Microsoft.AspNetCore.Routing.Route> class as the standard implementation of <xref:Microsoft.AspNetCore.Routing.IRouter>.</span></span> <span data-ttu-id="dddbb-1399"><xref:Microsoft.AspNetCore.Routing.Route> çağrıldığında URL yoluyla eşleştirilecek desenleri tanımlamak için *yol şablonu* sözdizimini kullanır <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1399"><xref:Microsoft.AspNetCore.Routing.Route> uses the *route template* syntax to define patterns to match against the URL path when <xref:Microsoft.AspNetCore.Routing.IRouter.RouteAsync*> is called.</span></span> <span data-ttu-id="dddbb-1400"><xref:Microsoft.AspNetCore.Routing.Route> çağrıldığında bir URL oluşturmak için aynı rota şablonunu kullanır <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1400"><xref:Microsoft.AspNetCore.Routing.Route> uses the same route template to generate a URL when <xref:Microsoft.AspNetCore.Routing.IRouter.GetVirtualPath*> is called.</span></span>

<span data-ttu-id="dddbb-1401">Çoğu uygulama, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> ' de tanımlanan benzer uzantı yöntemlerinden birini çağırarak veya arayarak yollar oluşturur <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1401">Most apps create routes by calling <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> or one of the similar extension methods defined on <xref:Microsoft.AspNetCore.Routing.IRouteBuilder>.</span></span> <span data-ttu-id="dddbb-1402"><xref:Microsoft.AspNetCore.Routing.IRouteBuilder>Uzantı yöntemlerinden herhangi biri bir örneği oluşturur <xref:Microsoft.AspNetCore.Routing.Route> ve bunu yol koleksiyonuna ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1402">Any of the <xref:Microsoft.AspNetCore.Routing.IRouteBuilder> extension methods create an instance of <xref:Microsoft.AspNetCore.Routing.Route> and add it to the route collection.</span></span>

<span data-ttu-id="dddbb-1403"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yol işleyici parametresini kabul etmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1403"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> doesn't accept a route handler parameter.</span></span> <span data-ttu-id="dddbb-1404"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> yalnızca tarafından işlenen rotaları ekler <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1404"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> only adds routes that are handled by the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span> <span data-ttu-id="dddbb-1405">Varsayılan işleyici bir ' dir `IRouter` ve işleyici isteği işleyemeyebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1405">The default handler is an `IRouter`, and the handler might not handle the request.</span></span> <span data-ttu-id="dddbb-1406">Örneğin, ASP.NET Core MVC genellikle yalnızca kullanılabilir bir denetleyici ve eylemle eşleşen istekleri işleyen varsayılan bir işleyici olarak yapılandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1406">For example, ASP.NET Core MVC is typically configured as a default handler that only handles requests that match an available controller and action.</span></span> <span data-ttu-id="dddbb-1407">MVC 'de yönlendirme hakkında daha fazla bilgi için bkz <xref:mvc/controllers/routing> ..</span><span class="sxs-lookup"><span data-stu-id="dddbb-1407">To learn more about routing in MVC, see <xref:mvc/controllers/routing>.</span></span>

<span data-ttu-id="dddbb-1408">Aşağıdaki kod örneği, <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> tipik bir ASP.NET Core MVC yol tanımı tarafından kullanılan bir çağrının örneğidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1408">The following code example is an example of a <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> call used by a typical ASP.NET Core MVC route definition:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="dddbb-1409">Bu şablon bir URL yoluyla eşleşir ve yol değerlerini ayıklar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1409">This template matches a URL path and extracts the route values.</span></span> <span data-ttu-id="dddbb-1410">Örneğin, yol `/Products/Details/17` Şu yol değerlerini oluşturur: `{ controller = Products, action = Details, id = 17 }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1410">For example, the path `/Products/Details/17` generates the following route values: `{ controller = Products, action = Details, id = 17 }`.</span></span>

<span data-ttu-id="dddbb-1411">Rota değerleri, URL yolunun kesimlere bölünüyor ve rota şablonundaki *yol parametresi* adı ile her bir segmenti eşleştirerek belirlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1411">Route values are determined by splitting the URL path into segments and matching each segment with the *route parameter* name in the route template.</span></span> <span data-ttu-id="dddbb-1412">Yol parametreleri olarak adlandırılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1412">Route parameters are named.</span></span> <span data-ttu-id="dddbb-1413">Küme ayraçları içinde parametre adının çevreleme tarafından tanımlanan parametreler `{ ... }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1413">The parameters defined by enclosing the parameter name in braces `{ ... }`.</span></span>

<span data-ttu-id="dddbb-1414">Yukarıdaki şablon URL yoluyla da eşleştirebilir `/` ve değerleri üretebilir `{ controller = Home, action = Index }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1414">The preceding template could also match the URL path `/` and produce the values `{ controller = Home, action = Index }`.</span></span> <span data-ttu-id="dddbb-1415">Bu durum, `{controller}` ve `{action}` Rota parametrelerinin varsayılan değerleri olduğu ve `id` route parametresinin isteğe bağlı olması nedeniyle oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1415">This occurs because the `{controller}` and `{action}` route parameters have default values and the `id` route parameter is optional.</span></span> <span data-ttu-id="dddbb-1416">Bir eşittir işareti ( `=` ), yol parametre adından sonra gelen bir değer, parametre için varsayılan bir değer tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1416">An equals sign (`=`) followed by a value after the route parameter name defines a default value for the parameter.</span></span> <span data-ttu-id="dddbb-1417">`?`Yol parametre adından sonra bir soru işareti () isteğe bağlı bir parametre tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1417">A question mark (`?`) after the route parameter name defines an optional parameter.</span></span>

<span data-ttu-id="dddbb-1418">Yol parametreleri varsayılan değer ile *her zaman* rota değeri oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1418">Route parameters with a default value *always* produce a route value when the route matches.</span></span> <span data-ttu-id="dddbb-1419">İsteğe bağlı parametreler, karşılık gelen bir URL yol kesimi yoksa rota değeri oluşturmaz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1419">Optional parameters don't produce a route value if there is no corresponding URL path segment.</span></span> <span data-ttu-id="dddbb-1420">Yol şablonu senaryolarının ve sözdiziminin kapsamlı bir açıklaması için [yol şablonu başvurusu](#route-template-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1420">See the [Route template reference](#route-template-reference) section for a thorough description of route template scenarios and syntax.</span></span>

<span data-ttu-id="dddbb-1421">Aşağıdaki örnekte, Route parametresi tanımı `{id:int}` yol parametresi için bir [yol kısıtlaması](#route-constraint-reference) tanımlar `id` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1421">In the following example, the route parameter definition `{id:int}` defines a [route constraint](#route-constraint-reference) for the `id` route parameter:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id:int}");
```

<span data-ttu-id="dddbb-1422">Bu şablon, gibi bir URL yoluyla eşleşir `/Products/Details/17` `/Products/Details/Apples` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1422">This template matches a URL path like `/Products/Details/17` but not `/Products/Details/Apples`.</span></span> <span data-ttu-id="dddbb-1423">Yol kısıtlamaları <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> , yol değerlerini uygulayıp onları doğrulamak için inceler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1423">Route constraints implement <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> and inspect route values to verify them.</span></span> <span data-ttu-id="dddbb-1424">Bu örnekte, yol değeri `id` bir tamsayıya dönüştürülebilir olmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1424">In this example, the route value `id` must be convertible to an integer.</span></span> <span data-ttu-id="dddbb-1425">Framework tarafından sunulan yol kısıtlamalarının açıklaması için bkz. [route-Constraint-Reference](#route-constraint-reference) .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1425">See [route-constraint-reference](#route-constraint-reference) for an explanation of route constraints provided by the framework.</span></span>

<span data-ttu-id="dddbb-1426"><xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*>, Ve için değer kabul etmenin ek aşırı yüklemeleri `constraints` `dataTokens` `defaults` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1426">Additional overloads of <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> accept values for `constraints`, `dataTokens`, and `defaults`.</span></span> <span data-ttu-id="dddbb-1427">Bu parametrelerin tipik kullanımı, anonim türdeki özellik adlarının yol parametre adlarıyla eşleşen anonim olarak yazılmış bir nesneyi geçirmektir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1427">The typical usage of these parameters is to pass an anonymously typed object, where the property names of the anonymous type match route parameter names.</span></span>

<span data-ttu-id="dddbb-1428">Aşağıdaki <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> örnekler eşdeğer yollar oluşturur:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1428">The following <xref:Microsoft.AspNetCore.Builder.MapRouteRouteBuilderExtensions.MapRoute*> examples create equivalent routes:</span></span>

```csharp
routes.MapRoute(
    name: "default_route",
    template: "{controller}/{action}/{id?}",
    defaults: new { controller = "Home", action = "Index" });

routes.MapRoute(
    name: "default_route",
    template: "{controller=Home}/{action=Index}/{id?}");
```

> [!TIP]
> <span data-ttu-id="dddbb-1429">Kısıtlamaları ve Varsayılanları tanımlamaya yönelik satır içi sözdizimi basit yollar için kullanışlı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1429">The inline syntax for defining constraints and defaults can be convenient for simple routes.</span></span> <span data-ttu-id="dddbb-1430">Ancak, satır içi sözdizimi tarafından desteklenmeyen veri belirteçleri gibi senaryolar vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1430">However, there are scenarios, such as data tokens, that aren't supported by inline syntax.</span></span>

<span data-ttu-id="dddbb-1431">Aşağıdaki örnek birkaç ek senaryoyu göstermektedir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1431">The following example demonstrates a few additional scenarios:</span></span>

```csharp
routes.MapRoute(
    name: "blog",
    template: "Blog/{*article}",
    defaults: new { controller = "Blog", action = "ReadArticle" });
```

<span data-ttu-id="dddbb-1432">Önceki şablon, benzer bir URL yoluyla eşleşir `/Blog/All-About-Routing/Introduction` ve değerleri ayıklar `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1432">The preceding template matches a URL path like `/Blog/All-About-Routing/Introduction` and extracts the values `{ controller = Blog, action = ReadArticle, article = All-About-Routing/Introduction }`.</span></span> <span data-ttu-id="dddbb-1433">Ve için varsayılan yol değerleri `controller` , `action` şablonda karşılık gelen hiçbir yol parametresi olmasa bile rota tarafından üretilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1433">The default route values for `controller` and `action` are produced by the route even though there are no corresponding route parameters in the template.</span></span> <span data-ttu-id="dddbb-1434">Varsayılan değerler yol şablonunda belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1434">Default values can be specified in the route template.</span></span> <span data-ttu-id="dddbb-1435">`article`Route parametresi, yol parametre adından önce bir yıldız işareti () görünümüne göre *catch-all* olarak tanımlanır `*` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1435">The `article` route parameter is defined as a *catch-all* by the appearance of an asterisk (`*`) before the route parameter name.</span></span> <span data-ttu-id="dddbb-1436">Catch-all yol parametreleri, URL yolunun kalanını yakalar ve boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1436">Catch-all route parameters capture the remainder of the URL path and can also match the empty string.</span></span>

<span data-ttu-id="dddbb-1437">Aşağıdaki örnek yol kısıtlamalarını ve veri belirteçlerini ekler:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1437">The following example adds route constraints and data tokens:</span></span>

```csharp
routes.MapRoute(
    name: "us_english_products",
    template: "en-US/Products/{id}",
    defaults: new { controller = "Products", action = "Details" },
    constraints: new { id = new IntRouteConstraint() },
    dataTokens: new { locale = "en-US" });
```

<span data-ttu-id="dddbb-1438">Önceki şablon, benzer bir URL yoluyla eşleşir `/en-US/Products/5` ve değerleri `{ controller = Products, action = Details, id = 5 }` ve veri belirteçlerini ayıklar `{ locale = en-US }` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1438">The preceding template matches a URL path like `/en-US/Products/5` and extracts the values `{ controller = Products, action = Details, id = 5 }` and the data tokens `{ locale = en-US }`.</span></span>

![Yereller Windows belirteçleri](routing/_static/tokens.png)

### <a name="route-class-url-generation"></a><span data-ttu-id="dddbb-1440">Yol sınıfı URL 'SI oluşturma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1440">Route class URL generation</span></span>

<span data-ttu-id="dddbb-1441">Sınıf, yol <xref:Microsoft.AspNetCore.Routing.Route> değerlerini bir kümesini rota şablonuyla BIRLEŞTIREREK URL oluşturma işlemi de gerçekleştirebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1441">The <xref:Microsoft.AspNetCore.Routing.Route> class can also perform URL generation by combining a set of route values with its route template.</span></span> <span data-ttu-id="dddbb-1442">Bu, URL yolunu eşleştirmenin mantıksal bir işlemdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1442">This is logically the reverse process of matching the URL path.</span></span>

> [!TIP]
> <span data-ttu-id="dddbb-1443">URL oluşturmayı daha iyi anlamak için, oluşturmak istediğiniz URL 'YI düşünün ve sonra bir yönlendirme şablonunun bu URL ile nasıl eşleşeceğini düşünün.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1443">To better understand URL generation, imagine what URL you want to generate and then think about how a route template would match that URL.</span></span> <span data-ttu-id="dddbb-1444">Hangi değerler üretilemidir?</span><span class="sxs-lookup"><span data-stu-id="dddbb-1444">What values would be produced?</span></span> <span data-ttu-id="dddbb-1445">Bu, URL oluşturma işlevinin sınıfında nasıl çalıştığına ilişkin kaba bir eştir <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1445">This is the rough equivalent of how URL generation works in the <xref:Microsoft.AspNetCore.Routing.Route> class.</span></span>

<span data-ttu-id="dddbb-1446">Aşağıdaki örnek genel ASP.NET Core MVC varsayılan yolunu kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1446">The following example uses a general ASP.NET Core MVC default route:</span></span>

```csharp
routes.MapRoute(
    name: "default",
    template: "{controller=Home}/{action=Index}/{id?}");
```

<span data-ttu-id="dddbb-1447">Yol değerleriyle `{ controller = Products, action = List }` , URL `/Products/List` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1447">With the route values `{ controller = Products, action = List }`, the URL `/Products/List` is generated.</span></span> <span data-ttu-id="dddbb-1448">Yol değerleri, URL yolunu biçimlendirmek için karşılık gelen yol parametrelerinin yerine kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1448">The route values are substituted for the corresponding route parameters to form the URL path.</span></span> <span data-ttu-id="dddbb-1449">`id`, İsteğe bağlı bir yol parametresi olduğundan, URL 'si için bir değer olmadan başarıyla oluşturulur `id` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1449">Since `id` is an optional route parameter, the URL is successfully generated without a value for `id`.</span></span>

<span data-ttu-id="dddbb-1450">Yol değerleriyle `{ controller = Home, action = Index }` , URL `/` oluşturulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1450">With the route values `{ controller = Home, action = Index }`, the URL `/` is generated.</span></span> <span data-ttu-id="dddbb-1451">Belirtilen yol değerleri varsayılan değerlerle eşleşir ve varsayılan değerlere karşılık gelen segmentler güvenle atlanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1451">The provided route values match the default values, and the segments corresponding to the default values are safely omitted.</span></span>

<span data-ttu-id="dddbb-1452">Her iki URL de aşağıdaki yol tanımıyla (ve) birlikte gidiş dönüş, `/Home/Index` `/` URL oluşturmak için kullanılan rota değerlerini oluşturur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1452">Both URLs generated round-trip with the following route definition (`/Home/Index` and `/`) produce the same route values that were used to generate the URL.</span></span>

> [!NOTE]
> <span data-ttu-id="dddbb-1453">ASP.NET Core MVC kullanan bir uygulama <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> , doğrudan yönlendirmeye çağırmak yerine URL 'ler oluşturmak için kullanılmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1453">An app using ASP.NET Core MVC should use <xref:Microsoft.AspNetCore.Mvc.Routing.UrlHelper> to generate URLs instead of calling into routing directly.</span></span>

<span data-ttu-id="dddbb-1454">URL oluşturma hakkında daha fazla bilgi için [URL oluşturma başvurusu](#url-generation-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1454">For more information on URL generation, see the [Url generation reference](#url-generation-reference) section.</span></span>

## <a name="use-routing-middleware"></a><span data-ttu-id="dddbb-1455">Yönlendirme ara yazılımı kullanma</span><span class="sxs-lookup"><span data-stu-id="dddbb-1455">Use routing middleware</span></span>

<span data-ttu-id="dddbb-1456">Uygulamanın proje dosyasındaki [Microsoft. AspNetCore. app metapackage](xref:fundamentals/metapackage-app) öğesine başvurun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1456">Reference the [Microsoft.AspNetCore.App metapackage](xref:fundamentals/metapackage-app) in the app's project file.</span></span>

<span data-ttu-id="dddbb-1457">İçindeki hizmet kapsayıcısına yönlendirme Ekle `Startup.ConfigureServices` :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1457">Add routing to the service container in `Startup.ConfigureServices`:</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_ConfigureServices&highlight=3)]

<span data-ttu-id="dddbb-1458">Yolların yönteminde yapılandırılması gerekir `Startup.Configure` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1458">Routes must be configured in the `Startup.Configure` method.</span></span> <span data-ttu-id="dddbb-1459">Örnek uygulama aşağıdaki API 'Leri kullanır:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1459">The sample app uses the following APIs:</span></span>

* <xref:Microsoft.AspNetCore.Routing.RouteBuilder>
* <span data-ttu-id="dddbb-1460"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Yalnızca HTTP GET isteklerini eşleştirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1460"><xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>: Matches only HTTP GET requests.</span></span>
* <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_RouteHandler)]

<span data-ttu-id="dddbb-1461">Aşağıdaki tabloda verilen URI 'Ler ile ilgili yanıtlar gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1461">The following table shows the responses with the given URIs.</span></span>

| <span data-ttu-id="dddbb-1462">URI</span><span class="sxs-lookup"><span data-stu-id="dddbb-1462">URI</span></span>                    | <span data-ttu-id="dddbb-1463">Yanıt</span><span class="sxs-lookup"><span data-stu-id="dddbb-1463">Response</span></span>                                          |
| ---------------------- | ------------------------------------------------- |
| `/package/create/3`    | <span data-ttu-id="dddbb-1464">Herkese!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1464">Hello!</span></span> <span data-ttu-id="dddbb-1465">Rota değerleri: [işlem, oluşturma], [kimlik, 3]</span><span class="sxs-lookup"><span data-stu-id="dddbb-1465">Route values: [operation, create], [id, 3]</span></span> |
| `/package/track/-3`    | <span data-ttu-id="dddbb-1466">Herkese!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1466">Hello!</span></span> <span data-ttu-id="dddbb-1467">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="dddbb-1467">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/-3/`   | <span data-ttu-id="dddbb-1468">Herkese!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1468">Hello!</span></span> <span data-ttu-id="dddbb-1469">Rota değerleri: [işlem, izleme], [kimlik,-3]</span><span class="sxs-lookup"><span data-stu-id="dddbb-1469">Route values: [operation, track], [id, -3]</span></span> |
| `/package/track/`      | <span data-ttu-id="dddbb-1470">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1470">The request falls through, no match.</span></span>              |
| `GET /hello/Joe`       | <span data-ttu-id="dddbb-1471">Merhaba, ali!</span><span class="sxs-lookup"><span data-stu-id="dddbb-1471">Hi, Joe!</span></span>                                          |
| `POST /hello/Joe`      | <span data-ttu-id="dddbb-1472">İstek üzerinden geçer, yalnızca HTTP GET ile eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1472">The request falls through, matches HTTP GET only.</span></span> |
| `GET /hello/Joe/Smith` | <span data-ttu-id="dddbb-1473">İstek, eşleşme olmadan üzerinden yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1473">The request falls through, no match.</span></span>              |

<span data-ttu-id="dddbb-1474">Tek bir yol yapılandırıyorsanız, <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*> bir örneği geçirme çağrısı yapın `IRouter` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1474">If you're configuring a single route, call <xref:Microsoft.AspNetCore.Builder.RoutingBuilderExtensions.UseRouter*> passing in an `IRouter` instance.</span></span> <span data-ttu-id="dddbb-1475">Kullanmanız gerekmez <xref:Microsoft.AspNetCore.Routing.RouteBuilder> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1475">You won't need to use <xref:Microsoft.AspNetCore.Routing.RouteBuilder>.</span></span>

<span data-ttu-id="dddbb-1476">Framework, yollar () oluşturmak için bir genişletme yöntemleri kümesi sağlar <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions> :</span><span class="sxs-lookup"><span data-stu-id="dddbb-1476">The framework provides a set of extension methods for creating routes (<xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions>):</span></span>

* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareDelete*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareGet*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewarePut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapMiddlewareVerb*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPost*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapPut*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>
* <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>

<span data-ttu-id="dddbb-1477">Gibi listelenen yöntemlerin bazıları için <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> gerekir <xref:Microsoft.AspNetCore.Http.RequestDelegate> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1477">Some of listed methods, such as <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*>, require a <xref:Microsoft.AspNetCore.Http.RequestDelegate>.</span></span> <span data-ttu-id="dddbb-1478"><xref:Microsoft.AspNetCore.Http.RequestDelegate>Yol eşleştiğinde *yol işleyicisi* olarak kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1478">The <xref:Microsoft.AspNetCore.Http.RequestDelegate> is used as the *route handler* when the route matches.</span></span> <span data-ttu-id="dddbb-1479">Bu ailedeki diğer yöntemler, yönlendirme işleyicisi olarak kullanılmak üzere bir ara yazılım ardışık düzeni yapılandırmaya olanak tanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1479">Other methods in this family allow configuring a middleware pipeline for use as the route handler.</span></span> <span data-ttu-id="dddbb-1480">Yöntemi gibi `Map*` bir işleyiciyi kabul etmez, <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*> öğesini kullanır <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1480">If the `Map*` method doesn't accept a handler, such as <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapRoute*>, it uses the <xref:Microsoft.AspNetCore.Routing.RouteBuilder.DefaultHandler*>.</span></span>

<span data-ttu-id="dddbb-1481">`Map[Verb]`Yöntemler, yöntem ADıNDAKI http fiili ile rotayı sınırlandırmak için kısıtlamalar kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1481">The `Map[Verb]` methods use constraints to limit the route to the HTTP Verb in the method name.</span></span> <span data-ttu-id="dddbb-1482">Örnek için bkz. <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> ve <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1482">For example, see <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapGet*> and <xref:Microsoft.AspNetCore.Routing.RequestDelegateRouteBuilderExtensions.MapVerb*>.</span></span>

## <a name="route-template-reference"></a><span data-ttu-id="dddbb-1483">Rota şablonu başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1483">Route template reference</span></span>

<span data-ttu-id="dddbb-1484">Küme ayraçları () içindeki belirteçler `{ ... }` , yol eşleştirildiği takdirde bağlanan *Rota parametrelerini* tanımlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1484">Tokens within curly braces (`{ ... }`) define *route parameters* that are bound if the route is matched.</span></span> <span data-ttu-id="dddbb-1485">Bir yol segmentinde birden fazla yol parametresi tanımlayabilirsiniz, ancak bunların bir değişmez değer ile ayrılması gerekir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1485">You can define more than one route parameter in a route segment, but they must be separated by a literal value.</span></span> <span data-ttu-id="dddbb-1486">Örneğin, `{controller=Home}{action=Index}` ve arasında değişmez değer olmadığından geçerli bir yol değil `{controller}` `{action}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1486">For example, `{controller=Home}{action=Index}` isn't a valid route, since there's no literal value between `{controller}` and `{action}`.</span></span> <span data-ttu-id="dddbb-1487">Bu rota parametrelerinin bir adı olmalı ve ek öznitelikler belirtilmiş olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1487">These route parameters must have a name and may have additional attributes specified.</span></span>

<span data-ttu-id="dddbb-1488">Yol parametrelerinden (örneğin, `{id}` ) ve yol ayırıcısından farklı bir metin, `/` URL içindeki metinle eşleşmelidir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1488">Literal text other than route parameters (for example, `{id}`) and the path separator `/` must match the text in the URL.</span></span> <span data-ttu-id="dddbb-1489">Metin eşleştirme, büyük/küçük harfe duyarsızdır ve URL yolunun kodu çözülmüş gösterimine göre yapılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1489">Text matching is case-insensitive and based on the decoded representation of the URLs path.</span></span> <span data-ttu-id="dddbb-1490">Bir sabit yol parametresi sınırlayıcısından ( `{` veya) eşleştirmek için `}` , karakteri (veya) tekrarlayarak sınırlayıcıdan kaçış `{{` `}}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1490">To match a literal route parameter delimiter (`{` or `}`), escape the delimiter by repeating the character (`{{` or `}}`).</span></span>

<span data-ttu-id="dddbb-1491">İsteğe bağlı bir dosya uzantısına sahip bir dosya adı yakalamaya deneyen URL desenlerinin ek konuları vardır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1491">URL patterns that attempt to capture a file name with an optional file extension have additional considerations.</span></span> <span data-ttu-id="dddbb-1492">Örneğin, şablonu göz önünde bulundurun `files/{filename}.{ext?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1492">For example, consider the template `files/{filename}.{ext?}`.</span></span> <span data-ttu-id="dddbb-1493">Hem hem de için değerler olduğunda `filename` `ext` her iki değer de doldurulur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1493">When values for both `filename` and `ext` exist, both values are populated.</span></span> <span data-ttu-id="dddbb-1494">URL 'de yalnızca bir değeri `filename` varsa, sondaki nokta ( `.` ) isteğe bağlı olduğundan yol eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1494">If only a value for `filename` exists in the URL, the route matches because the trailing period (`.`) is  optional.</span></span> <span data-ttu-id="dddbb-1495">Aşağıdaki URL 'Ler bu rota ile eşleşiyor:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1495">The following URLs match this route:</span></span>

* `/files/myFile.txt`
* `/files/myFile`

<span data-ttu-id="dddbb-1496">`*`URI 'nin geri kalanına bağlamak için bir yol parametresinin öneki olarak yıldız işareti () kullanabilirsiniz.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1496">You can use the asterisk (`*`) as a prefix to a route parameter to bind to the rest of the URI.</span></span> <span data-ttu-id="dddbb-1497">Buna *catch-all* parametresi denir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1497">This is called a *catch-all* parameter.</span></span> <span data-ttu-id="dddbb-1498">Örneğin, `blog/{*slug}` ile başlayan `/blog` ve bunu izleyen bir değere sahip olan ve yol değerine atanan HERHANGI bir URI ile eşleşir `slug` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1498">For example, `blog/{*slug}` matches any URI that starts with `/blog` and has any value following it, which is assigned to the `slug` route value.</span></span> <span data-ttu-id="dddbb-1499">Catch-all parametreleri boş dizeyle de aynı olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1499">Catch-all parameters can also match the empty string.</span></span>

<span data-ttu-id="dddbb-1500">Yol ayırıcısı () karakterleri de dahil olmak üzere bir URL oluşturmak için, catch-all parametresi uygun karakterleri de çıkar `/` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1500">The catch-all parameter escapes the appropriate characters when the route is used to generate a URL, including path separator (`/`) characters.</span></span> <span data-ttu-id="dddbb-1501">Örneğin, `foo/{*path}` yol değerlerini içeren yol `{ path = "my/path" }` oluşturulur `foo/my%2Fpath` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1501">For example, the route `foo/{*path}` with route values `{ path = "my/path" }` generates `foo/my%2Fpath`.</span></span> <span data-ttu-id="dddbb-1502">Atlanan eğik çizgiye göz önünde edin.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1502">Note the escaped forward slash.</span></span>

<span data-ttu-id="dddbb-1503">Yol parametreleri, parametre adından sonra bir eşittir işaretiyle () ayırarak varsayılan değer belirtilerek belirlenmiş *varsayılan değerlere* sahip olabilir `=` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1503">Route parameters may have *default values* designated by specifying the default value after the parameter name separated by an equals sign (`=`).</span></span> <span data-ttu-id="dddbb-1504">Örneğin, `{controller=Home}` `Home` için varsayılan değer olarak tanımlar `controller` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1504">For example, `{controller=Home}` defines `Home` as the default value for `controller`.</span></span> <span data-ttu-id="dddbb-1505">Parametresi için URL 'de hiçbir değer yoksa varsayılan değer kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1505">The default value is used if no value is present in the URL for the parameter.</span></span> <span data-ttu-id="dddbb-1506">Yol parametreleri `?` , içinde olduğu gibi parametre adının sonuna bir soru işareti () eklenerek isteğe bağlı olarak yapılır `id?` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1506">Route parameters are made optional by appending a question mark (`?`) to the end of the parameter name, as in `id?`.</span></span> <span data-ttu-id="dddbb-1507">İsteğe bağlı değerler ve varsayılan yol parametreleri arasındaki fark, varsayılan değere sahip bir yol parametresinin her zaman bir değer ürettiğinden, &mdash; isteğe bağlı bir parametre yalnızca Istek URL 'si tarafından bir değer sağlandığı zaman bir değere sahip olur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1507">The difference between optional values and default route parameters is that a route parameter with a default value always produces a value&mdash;an optional parameter has a value only when a value is provided by the request URL.</span></span>

<span data-ttu-id="dddbb-1508">Rota parametrelerinin URL 'den bağlanan rota değeriyle eşleşmesi gereken kısıtlamaları olabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1508">Route parameters may have constraints that must match the route value bound from the URL.</span></span> <span data-ttu-id="dddbb-1509">`:`Yol parametre adından sonra bir iki nokta () ve kısıtlama adı eklemek, bir rota parametresinde bir *satır içi kısıtlamayı* belirtir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1509">Adding a colon (`:`) and constraint name after the route parameter name specifies an *inline constraint* on a route parameter.</span></span> <span data-ttu-id="dddbb-1510">Kısıtlama bağımsız değişkenler gerektiriyorsa, `(...)` kısıtlama adından sonra parantez () içine alınır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1510">If the constraint requires arguments, they're enclosed in parentheses (`(...)`) after the constraint name.</span></span> <span data-ttu-id="dddbb-1511">Birden çok satır içi kısıtlama, başka bir iki nokta ( `:` ) ve kısıtlama adı eklenerek belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1511">Multiple inline constraints can be specified by appending another colon (`:`) and constraint name.</span></span>

<span data-ttu-id="dddbb-1512">Kısıtlama adı ve bağımsız değişkenler, <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> URL işlemede kullanılmak üzere bir örneği oluşturmak için hizmetine geçirilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1512">The constraint name and arguments are passed to the <xref:Microsoft.AspNetCore.Routing.IInlineConstraintResolver> service to create an instance of <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> to use in URL processing.</span></span> <span data-ttu-id="dddbb-1513">Örneğin, yol şablonu `blog/{article:minlength(10)}` `minlength` bağımsız değişkenle bir kısıtlama belirtir `10` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1513">For example, the route template `blog/{article:minlength(10)}` specifies a `minlength` constraint with the argument `10`.</span></span> <span data-ttu-id="dddbb-1514">Yol kısıtlamaları ve Framework tarafından sunulan kısıtlamaların bir listesi hakkında daha fazla bilgi için, [route kısıtlama başvurusu](#route-constraint-reference) bölümüne bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1514">For more information on route constraints and a list of the constraints provided by the framework, see the [Route constraint reference](#route-constraint-reference) section.</span></span>

<span data-ttu-id="dddbb-1515">Aşağıdaki tabloda örnek yol şablonları ve bunların davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1515">The following table demonstrates example route templates and their behavior.</span></span>

| <span data-ttu-id="dddbb-1516">Rota şablonu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1516">Route Template</span></span>                           | <span data-ttu-id="dddbb-1517">Örnek eşleşen URI</span><span class="sxs-lookup"><span data-stu-id="dddbb-1517">Example Matching URI</span></span>    | <span data-ttu-id="dddbb-1518">İstek URI 'SI&hellip;</span><span class="sxs-lookup"><span data-stu-id="dddbb-1518">The request URI&hellip;</span></span>                                                    |
| ---------------------------------------- | ----------------------- | -------------------------------------------------------------------------- |
| `hello`                                  | `/hello`                | <span data-ttu-id="dddbb-1519">Yalnızca tek bir yolla eşleşir `/hello` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1519">Only matches the single path `/hello`.</span></span>                                     |
| `{Page=Home}`                            | `/`                     | <span data-ttu-id="dddbb-1520">İle eşleşir ve `Page` ayarlanır `Home` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1520">Matches and sets `Page` to `Home`.</span></span>                                         |
| `{Page=Home}`                            | `/Contact`              | <span data-ttu-id="dddbb-1521">İle eşleşir ve `Page` ayarlanır `Contact` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1521">Matches and sets `Page` to `Contact`.</span></span>                                      |
| `{controller}/{action}/{id?}`            | `/Products/List`        | <span data-ttu-id="dddbb-1522">`Products`Denetleyiciye ve `List` eyleme eşlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1522">Maps to the `Products` controller and `List` action.</span></span>                       |
| `{controller}/{action}/{id?}`            | `/Products/Details/123` | <span data-ttu-id="dddbb-1523">`Products`Denetleyici ve `Details` eyleme eşlenir ( `id` 123 olarak ayarlanır).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1523">Maps to the `Products` controller and  `Details` action (`id` set to 123).</span></span> |
| `{controller=Home}/{action=Index}/{id?}` | `/`                     | <span data-ttu-id="dddbb-1524">`Home`Denetleyiciye ve `Index` yönteme eşlenir ( `id` yok sayılır).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1524">Maps to the `Home` controller and `Index` method (`id` is ignored).</span></span>        |

<span data-ttu-id="dddbb-1525">Bir şablon kullanmak genellikle yönlendirmeye en basit yaklaşımdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1525">Using a template is generally the simplest approach to routing.</span></span> <span data-ttu-id="dddbb-1526">Kısıtlamalar ve varsayılanlar, yol şablonu dışında da belirtilebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1526">Constraints and defaults can also be specified outside the route template.</span></span>

> [!TIP]
> <span data-ttu-id="dddbb-1527">İstekleri eşleme gibi yerleşik yönlendirme uygulamalarının nasıl yapıldığını görmek için [günlük kaydını](xref:fundamentals/logging/index) etkinleştirin <xref:Microsoft.AspNetCore.Routing.Route> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1527">Enable [Logging](xref:fundamentals/logging/index) to see how the built-in routing implementations, such as <xref:Microsoft.AspNetCore.Routing.Route>, match requests.</span></span>

## <a name="route-constraint-reference"></a><span data-ttu-id="dddbb-1528">Yol kısıtlama başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1528">Route constraint reference</span></span>

<span data-ttu-id="dddbb-1529">Yol kısıtlamaları, gelen URL 'de bir eşleşme meydana geldiğinde ve URL yolu yol değerlerinde simgeleştirilir yürütülür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1529">Route constraints execute when a match has occurred to the incoming URL and the URL path is tokenized into route values.</span></span> <span data-ttu-id="dddbb-1530">Rota kısıtlamaları genellikle yol şablonu aracılığıyla ilişkili rota değerini inceler ve değerin kabul edilebilir olup olmadığı konusunda bir Evet/Hayır kararı getirir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1530">Route constraints generally inspect the route value associated via the route template and make a yes/no decision about whether or not the value is acceptable.</span></span> <span data-ttu-id="dddbb-1531">Bazı rota kısıtlamaları, isteğin yönlendirilip yönlendirilmeyeceğini göz önünde bulundurmanız için yol değeri dışındaki verileri kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1531">Some route constraints use data outside the route value to consider whether the request can be routed.</span></span> <span data-ttu-id="dddbb-1532">Örneğin, <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> bir ISTEğI http fiiline bağlı olarak kabul edebilir veya reddedebilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1532">For example, the <xref:Microsoft.AspNetCore.Routing.Constraints.HttpMethodRouteConstraint> can accept or reject a request based on its HTTP verb.</span></span> <span data-ttu-id="dddbb-1533">Kısıtlamalar, yönlendirme isteklerinde ve bağlantı oluşturmada kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1533">Constraints are used in routing requests and link generation.</span></span>

> [!WARNING]
> <span data-ttu-id="dddbb-1534">**Giriş doğrulaması** için kısıtlamaları kullanmayın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1534">Don't use constraints for **input validation**.</span></span> <span data-ttu-id="dddbb-1535">**Giriş doğrulaması** için kısıtlamalar kullanılıyorsa, doğru bir hata iletisine sahip *400-Bad isteği* yerine *404-* olmayan bir Yanıt ile geçersiz giriş oluşur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1535">If constraints are used for **input validation** , invalid input results in a *404 - Not Found* response instead of a *400 - Bad Request* with an appropriate error message.</span></span> <span data-ttu-id="dddbb-1536">Yol kısıtlamaları, belirli bir rota için girdileri doğrulamak üzere değil, benzer yolların **belirsizliğini ortadan** kaldırmak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1536">Route constraints are used to **disambiguate** similar routes, not to validate the inputs for a particular route.</span></span>

<span data-ttu-id="dddbb-1537">Aşağıdaki tabloda örnek yol kısıtlamaları ve bunların beklenen davranışları gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1537">The following table demonstrates example route constraints and their expected behavior.</span></span>

| <span data-ttu-id="dddbb-1538">kısıtlama</span><span class="sxs-lookup"><span data-stu-id="dddbb-1538">constraint</span></span> | <span data-ttu-id="dddbb-1539">Örnek</span><span class="sxs-lookup"><span data-stu-id="dddbb-1539">Example</span></span> | <span data-ttu-id="dddbb-1540">Örnek eşleşmeler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1540">Example Matches</span></span> | <span data-ttu-id="dddbb-1541">Notlar</span><span class="sxs-lookup"><span data-stu-id="dddbb-1541">Notes</span></span> |
| ---------- | ------- | --------------- | ----- |
| `int` | `{id:int}` | <span data-ttu-id="dddbb-1542">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1542">`123456789`, `-123456789`</span></span> | <span data-ttu-id="dddbb-1543">Herhangi bir tamsayıyla eşleşir</span><span class="sxs-lookup"><span data-stu-id="dddbb-1543">Matches any integer</span></span> |
| `bool` | `{active:bool}` | <span data-ttu-id="dddbb-1544">`true`, `FALSE`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1544">`true`, `FALSE`</span></span> | <span data-ttu-id="dddbb-1545">Eşleşiyor `true` veya `false` (büyük/küçük harf duyarsız)</span><span class="sxs-lookup"><span data-stu-id="dddbb-1545">Matches `true` or `false` (case-insensitive)</span></span> |
| `datetime` | `{dob:datetime}` | <span data-ttu-id="dddbb-1546">`2016-12-31`, `2016-12-31 7:32pm`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1546">`2016-12-31`, `2016-12-31 7:32pm`</span></span> | <span data-ttu-id="dddbb-1547">`DateTime`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1547">Matches a valid `DateTime` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1548">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1548">See  preceding warning.</span></span>|
| `decimal` | `{price:decimal}` | <span data-ttu-id="dddbb-1549">`49.99`, `-1,000.01`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1549">`49.99`, `-1,000.01`</span></span> | <span data-ttu-id="dddbb-1550">`decimal`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1550">Matches a valid `decimal` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1551">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1551">See  preceding warning.</span></span>|
| `double` | `{weight:double}` | <span data-ttu-id="dddbb-1552">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1552">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="dddbb-1553">`double`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1553">Matches a valid `double` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1554">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1554">See  preceding warning.</span></span>|
| `float` | `{weight:float}` | <span data-ttu-id="dddbb-1555">`1.234`, `-1,001.01e8`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1555">`1.234`, `-1,001.01e8`</span></span> | <span data-ttu-id="dddbb-1556">`float`Sabit kültürün geçerli bir değeriyle eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1556">Matches a valid `float` value in the invariant culture.</span></span> <span data-ttu-id="dddbb-1557">Önceki uyarıya bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1557">See  preceding warning.</span></span>|
| `guid` | `{id:guid}` | <span data-ttu-id="dddbb-1558">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1558">`CD2C1638-1638-72D5-1638-DEADBEEF1638`, `{CD2C1638-1638-72D5-1638-DEADBEEF1638}`</span></span> | <span data-ttu-id="dddbb-1559">Geçerli bir `Guid` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="dddbb-1559">Matches a valid `Guid` value</span></span> |
| `long` | `{ticks:long}` | <span data-ttu-id="dddbb-1560">`123456789`, `-123456789`</span><span class="sxs-lookup"><span data-stu-id="dddbb-1560">`123456789`, `-123456789`</span></span> | <span data-ttu-id="dddbb-1561">Geçerli bir `long` değerle eşleşir</span><span class="sxs-lookup"><span data-stu-id="dddbb-1561">Matches a valid `long` value</span></span> |
| `minlength(value)` | `{username:minlength(4)}` | `Rick` | <span data-ttu-id="dddbb-1562">Dize en az 4 karakter olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1562">String must be at least 4 characters</span></span> |
| `maxlength(value)` | `{filename:maxlength(8)}` | `Richard` | <span data-ttu-id="dddbb-1563">Dize 8 karakterden uzun olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1563">String must be no more than 8 characters</span></span> |
| `length(length)` | `{filename:length(12)}` | `somefile.txt` | <span data-ttu-id="dddbb-1564">Dize tam olarak 12 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1564">String must be exactly 12 characters long</span></span> |
| `length(min,max)` | `{filename:length(8,16)}` | `somefile.txt` | <span data-ttu-id="dddbb-1565">Dize en az 8 ve en fazla 16 karakter uzunluğunda olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1565">String must be at least 8 and no more than 16 characters long</span></span> |
| `min(value)` | `{age:min(18)}` | `19` | <span data-ttu-id="dddbb-1566">Tamsayı değeri en az 18 olmalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1566">Integer value must be at least 18</span></span> |
| `max(value)` | `{age:max(120)}` | `91` | <span data-ttu-id="dddbb-1567">Tamsayı değeri 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1567">Integer value must be no more than 120</span></span> |
| `range(min,max)` | `{age:range(18,120)}` | `91` | <span data-ttu-id="dddbb-1568">Tamsayı değeri en az 18 olmalı ancak 120 ' ten fazla olmamalıdır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1568">Integer value must be at least 18 but no more than 120</span></span> |
| `alpha` | `{name:alpha}` | `Rick` | <span data-ttu-id="dddbb-1569">Dize bir veya daha fazla alfabetik karakterden oluşmalıdır ( `a` - `z` büyük/küçük harfe duyarsız)</span><span class="sxs-lookup"><span data-stu-id="dddbb-1569">String must consist of one or more alphabetical characters (`a`-`z`, case-insensitive)</span></span> |
| `regex(expression)` | `{ssn:regex(^\\d{{3}}-\\d{{2}}-\\d{{4}}$)}` | `123-45-6789` | <span data-ttu-id="dddbb-1570">Dize, normal ifadeyle eşleşmelidir (normal ifade tanımlama hakkında ipuçlarına bakın)</span><span class="sxs-lookup"><span data-stu-id="dddbb-1570">String must match the regular expression (see tips about defining a regular expression)</span></span> |
| `required` | `{name:required}` | `Rick` | <span data-ttu-id="dddbb-1571">URL oluşturma sırasında parametre olmayan bir değerin mevcut olduğunu zorlamak için kullanılır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1571">Used to enforce that a non-parameter value is present during URL generation</span></span> |

<span data-ttu-id="dddbb-1572">Birden çok, iki nokta üst üste sınırlı kısıtlama tek bir parametreye uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1572">Multiple, colon-delimited constraints can be applied to a single parameter.</span></span> <span data-ttu-id="dddbb-1573">Örneğin, aşağıdaki kısıtlama bir parametreyi 1 veya daha büyük bir tamsayı değeriyle kısıtlar:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1573">For example, the following constraint restricts a parameter to an integer value of 1 or greater:</span></span>

```csharp
[Route("users/{id:int:min(1)}")]
public User GetUserById(int id) { }
```

> [!WARNING]
> <span data-ttu-id="dddbb-1574">URL 'YI doğrulayan ve bir CLR türüne (veya gibi) dönüştürülen yol kısıtlamaları `int` `DateTime` , her zaman sabit kültürü kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1574">Route constraints that verify the URL and are converted to a CLR type (such as `int` or `DateTime`) always use the invariant culture.</span></span> <span data-ttu-id="dddbb-1575">Bu kısıtlamalar, URL 'nin yerelleştirilemeyen olduğunu varsayar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1575">These constraints assume that the URL is non-localizable.</span></span> <span data-ttu-id="dddbb-1576">Framework tarafından sunulan yol kısıtlamaları, yol değerlerinde depolanan değerleri değiştirmez.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1576">The framework-provided route constraints don't modify the values stored in route values.</span></span> <span data-ttu-id="dddbb-1577">URL 'den Ayrıştırılan tüm rota değerleri dizeler olarak depolanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1577">All route values parsed from the URL are stored as strings.</span></span> <span data-ttu-id="dddbb-1578">Örneğin, `float` kısıtlama yol değerini bir float öğesine dönüştürmeye çalışır, ancak dönüştürülen değer yalnızca bir float öğesine dönüştürülebileceğini doğrulamak için kullanılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1578">For example, the `float` constraint attempts to convert the route value to a float, but the converted value is used only to verify it can be converted to a float.</span></span>

## <a name="regular-expressions"></a><span data-ttu-id="dddbb-1579">Normal ifadeler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1579">Regular expressions</span></span>

<span data-ttu-id="dddbb-1580">ASP.NET Core Framework, `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` normal ifade oluşturucusuna ekler.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1580">The ASP.NET Core framework adds `RegexOptions.IgnoreCase | RegexOptions.Compiled | RegexOptions.CultureInvariant` to the regular expression constructor.</span></span> <span data-ttu-id="dddbb-1581"><xref:System.Text.RegularExpressions.RegexOptions>Bu üyelerin açıklaması için bkz..</span><span class="sxs-lookup"><span data-stu-id="dddbb-1581">See <xref:System.Text.RegularExpressions.RegexOptions> for a description of these members.</span></span>

<span data-ttu-id="dddbb-1582">Normal ifadeler, Yönlendirme ve C# dili tarafından kullanılanlarla benzerlik olan sınırlayıcıları ve belirteçleri kullanır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1582">Regular expressions use delimiters and tokens similar to those used by Routing and the C# language.</span></span> <span data-ttu-id="dddbb-1583">Normal ifade belirteçlerinin atlanmalıdır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1583">Regular expression tokens must be escaped.</span></span> <span data-ttu-id="dddbb-1584">Yönlendirmenin normal ifadesini kullanmak için `^\d{3}-\d{2}-\d{4}$` , ifadenin `\` `\\` `\` dize kaçış karakterini (tam [dize sabit değerleri](/dotnet/csharp/language-reference/keywords/string)kullanmadıkça) atlamak için C# kaynak dosyasındaki dize (çift ters eğik çizgi) karakterleriyle belirtilen (tek ters eğik çizgi) karakterlere sahip olması gerekir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1584">To use the regular expression `^\d{3}-\d{2}-\d{4}$` in routing, the expression must have the `\` (single backslash) characters provided in the string as `\\` (double backslash) characters in the C# source file in order to escape the `\` string escape character (unless using [verbatim string literals](/dotnet/csharp/language-reference/keywords/string)).</span></span> <span data-ttu-id="dddbb-1585">Yönlendirme parametresi sınırlayıcı karakterlerini ( `{` ,,, `}` ) atlamak için `[` `]` , ifadedeki karakterleri (,,, `{{` ) çift `}` `[[` `]]` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1585">To escape routing parameter delimiter characters (`{`, `}`, `[`, `]`), double the characters in the expression (`{{`, `}`, `[[`, `]]`).</span></span> <span data-ttu-id="dddbb-1586">Aşağıdaki tabloda, bir normal ifade ve kaçan sürümü gösterilmektedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1586">The following table shows a regular expression and the escaped version.</span></span>

| <span data-ttu-id="dddbb-1587">Normal ifade</span><span class="sxs-lookup"><span data-stu-id="dddbb-1587">Regular Expression</span></span>    | <span data-ttu-id="dddbb-1588">Kaçan normal Ifade</span><span class="sxs-lookup"><span data-stu-id="dddbb-1588">Escaped Regular Expression</span></span>     |
| --------------------- | ------------------------------ |
| `^\d{3}-\d{2}-\d{4}$` | `^\\d{{3}}-\\d{{2}}-\\d{{4}}$` |
| `^[a-z]{2}$`          | `^[[a-z]]{{2}}$`               |

<span data-ttu-id="dddbb-1589">Yönlendirmelerde kullanılan normal ifadeler, genellikle şapka işareti ( `^` ) karakteriyle başlar ve dizenin başlangıç konumuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1589">Regular expressions used in routing often start with the caret (`^`) character and match starting position of the string.</span></span> <span data-ttu-id="dddbb-1590">İfadeler, genellikle dolar işareti ( `$` ) karakteriyle biter ve dizenin sonuyla eşleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1590">The expressions often end with the dollar sign (`$`) character and match end of the string.</span></span> <span data-ttu-id="dddbb-1591">`^`Ve `$` karakterleri, normal ifadenin tüm yol parametresi değeri ile eşleştiğinden emin olun.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1591">The `^` and `$` characters ensure that the regular expression match the entire route parameter value.</span></span> <span data-ttu-id="dddbb-1592">`^`Ve karakterleri olmadan `$` normal ifade, dize içindeki herhangi bir alt dizeden eşleşir ve bu genellikle istenmeyen bir ifadedir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1592">Without the `^` and `$` characters, the regular expression match any substring within the string, which is often undesirable.</span></span> <span data-ttu-id="dddbb-1593">Aşağıdaki tabloda örnekler verilmektedir ve bunların eşleşmesinin neden eşleşmediği veya eşleşmemesi açıklanmaktadır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1593">The following table provides examples and explains why they match or fail to match.</span></span>

| <span data-ttu-id="dddbb-1594">Expression</span><span class="sxs-lookup"><span data-stu-id="dddbb-1594">Expression</span></span>   | <span data-ttu-id="dddbb-1595">Dize</span><span class="sxs-lookup"><span data-stu-id="dddbb-1595">String</span></span>    | <span data-ttu-id="dddbb-1596">Eşleştirme</span><span class="sxs-lookup"><span data-stu-id="dddbb-1596">Match</span></span> | <span data-ttu-id="dddbb-1597">Yorum</span><span class="sxs-lookup"><span data-stu-id="dddbb-1597">Comment</span></span>               |
| ------------ | --------- | :---: |  -------------------- |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1598">hello</span><span class="sxs-lookup"><span data-stu-id="dddbb-1598">hello</span></span>     | <span data-ttu-id="dddbb-1599">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1599">Yes</span></span>   | <span data-ttu-id="dddbb-1600">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1600">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1601">123abc456</span><span class="sxs-lookup"><span data-stu-id="dddbb-1601">123abc456</span></span> | <span data-ttu-id="dddbb-1602">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1602">Yes</span></span>   | <span data-ttu-id="dddbb-1603">Alt dize eşleşmeleri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1603">Substring matches</span></span>     |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1604">MZ</span><span class="sxs-lookup"><span data-stu-id="dddbb-1604">mz</span></span>        | <span data-ttu-id="dddbb-1605">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1605">Yes</span></span>   | <span data-ttu-id="dddbb-1606">Eşleşen ifadesi</span><span class="sxs-lookup"><span data-stu-id="dddbb-1606">Matches expression</span></span>    |
| `[a-z]{2}`   | <span data-ttu-id="dddbb-1607">MZ</span><span class="sxs-lookup"><span data-stu-id="dddbb-1607">MZ</span></span>        | <span data-ttu-id="dddbb-1608">Evet</span><span class="sxs-lookup"><span data-stu-id="dddbb-1608">Yes</span></span>   | <span data-ttu-id="dddbb-1609">Büyük/küçük harfe duyarlı değil</span><span class="sxs-lookup"><span data-stu-id="dddbb-1609">Not case sensitive</span></span>    |
| `^[a-z]{2}$` | <span data-ttu-id="dddbb-1610">hello</span><span class="sxs-lookup"><span data-stu-id="dddbb-1610">hello</span></span>     | <span data-ttu-id="dddbb-1611">Hayır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1611">No</span></span>    | <span data-ttu-id="dddbb-1612">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1612">See `^` and `$` above</span></span> |
| `^[a-z]{2}$` | <span data-ttu-id="dddbb-1613">123abc456</span><span class="sxs-lookup"><span data-stu-id="dddbb-1613">123abc456</span></span> | <span data-ttu-id="dddbb-1614">Hayır</span><span class="sxs-lookup"><span data-stu-id="dddbb-1614">No</span></span>    | <span data-ttu-id="dddbb-1615">Bkz. `^` ve `$` üzeri</span><span class="sxs-lookup"><span data-stu-id="dddbb-1615">See `^` and `$` above</span></span> |

<span data-ttu-id="dddbb-1616">Normal ifade sözdizimi hakkında daha fazla bilgi için bkz. [.NET Framework normal ifadeler](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span><span class="sxs-lookup"><span data-stu-id="dddbb-1616">For more information on regular expression syntax, see [.NET Framework Regular Expressions](/dotnet/standard/base-types/regular-expression-language-quick-reference).</span></span>

<span data-ttu-id="dddbb-1617">Bir parametreyi bilinen olası değerler kümesiyle kısıtlamak için, normal bir ifade kullanın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1617">To constrain a parameter to a known set of possible values, use a regular expression.</span></span> <span data-ttu-id="dddbb-1618">Örneğin, `{action:regex(^(list|get|create)$)}` yalnızca `action` Rota değeri `list` ,, veya ile eşleşir `get` `create` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1618">For example, `{action:regex(^(list|get|create)$)}` only matches the `action` route value to `list`, `get`, or `create`.</span></span> <span data-ttu-id="dddbb-1619">Kısıtlama sözlüğüne geçirilirse dize `^(list|get|create)$` eşdeğerdir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1619">If passed into the constraints dictionary, the string `^(list|get|create)$` is equivalent.</span></span> <span data-ttu-id="dddbb-1620">Bilinen kısıtlamalardan biriyle eşleşmeyen kısıtlama sözlüğüne (bir şablon içinde satır içi değil) geçirilen kısıtlamalar da normal ifadeler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1620">Constraints that are passed in the constraints dictionary (not inline within a template) that don't match one of the known constraints are also treated as regular expressions.</span></span>

## <a name="custom-route-constraints"></a><span data-ttu-id="dddbb-1621">Özel yol kısıtlamaları</span><span class="sxs-lookup"><span data-stu-id="dddbb-1621">Custom Route Constraints</span></span>

<span data-ttu-id="dddbb-1622">Yerleşik yol kısıtlamalarına ek olarak, arabirimi uygulayarak özel yol kısıtlamaları oluşturulabilir <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1622">In addition to the built-in route constraints, custom route constraints can be created by implementing the <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface.</span></span> <span data-ttu-id="dddbb-1623"><xref:Microsoft.AspNetCore.Routing.IRouteConstraint>Arabirim, `Match` `true` kısıtlama karşılanıp Aksi takdirde döndüren tek bir yöntemi içerir `false` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1623">The <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> interface contains a single method, `Match`, which returns `true` if the constraint is satisfied and `false` otherwise.</span></span>

<span data-ttu-id="dddbb-1624">Özel bir kullanmak için <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> yol kısıtlama türü, uygulamanın uygulamanın hizmet kapsayıcısında kayıtlı olması gerekir <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1624">To use a custom <xref:Microsoft.AspNetCore.Routing.IRouteConstraint>, the route constraint type must be registered with the app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> in the app's service container.</span></span> <span data-ttu-id="dddbb-1625"><xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap>, Yol kısıtlama anahtarlarını <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> Bu kısıtlamaları doğrulayan uygulamalarla eşleyen bir sözlüktür.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1625">A <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> is a dictionary that maps route constraint keys to <xref:Microsoft.AspNetCore.Routing.IRouteConstraint> implementations that validate those constraints.</span></span> <span data-ttu-id="dddbb-1626">Bir uygulama <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> , `Startup.ConfigureServices` hizmetlerin bir parçası olarak ' de güncelleştirilir [. AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) çağrısı veya <xref:Microsoft.AspNetCore.Routing.RouteOptions> doğrudan ile yapılandırma `services.Configure<RouteOptions>` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1626">An app's <xref:Microsoft.AspNetCore.Routing.RouteOptions.ConstraintMap> can be updated in `Startup.ConfigureServices` either as part of a [services.AddRouting](xref:Microsoft.Extensions.DependencyInjection.RoutingServiceCollectionExtensions.AddRouting*) call or by configuring <xref:Microsoft.AspNetCore.Routing.RouteOptions> directly with `services.Configure<RouteOptions>`.</span></span> <span data-ttu-id="dddbb-1627">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1627">For example:</span></span>

```csharp
services.AddRouting(options =>
{
    options.ConstraintMap.Add("customName", typeof(MyCustomConstraint));
});
```

<span data-ttu-id="dddbb-1628">Kısıtlama daha sonra, kısıtlama türü kaydedilirken belirtilen ad kullanılarak yollara her zamanki şekilde uygulanabilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1628">The constraint can then be applied to routes in the usual manner, using the name specified when registering the constraint type.</span></span> <span data-ttu-id="dddbb-1629">Örneğin:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1629">For example:</span></span>

```csharp
[HttpGet("{id:customName}")]
public ActionResult<string> Get(string id)
```

## <a name="url-generation-reference"></a><span data-ttu-id="dddbb-1630">URL oluşturma başvurusu</span><span class="sxs-lookup"><span data-stu-id="dddbb-1630">URL generation reference</span></span>

<span data-ttu-id="dddbb-1631">Aşağıdaki örnek, yol değerlerinin ve bir sözlüğünün bir sözlüğüne verilen bir yolun bağlantısının nasıl oluşturulacağını gösterir <xref:Microsoft.AspNetCore.Routing.RouteCollection> .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1631">The following example shows how to generate a link to a route given a dictionary of route values and a <xref:Microsoft.AspNetCore.Routing.RouteCollection>.</span></span>

[!code-csharp[](routing/samples/2.x/RoutingSample/Startup.cs?name=snippet_Dictionary)]

<span data-ttu-id="dddbb-1632"><xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath>Önceki örnek sonunda oluşturulan `/package/create/123` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1632">The <xref:Microsoft.AspNetCore.Routing.VirtualPathData.VirtualPath> generated at the end of the preceding sample is `/package/create/123`.</span></span> <span data-ttu-id="dddbb-1633">Sözlük, `operation` `id` "paket yolunu izle" şablonunun ve rota değerlerini sağlar `package/{operation}/{id}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1633">The dictionary supplies the `operation` and `id` route values of the "Track Package Route" template, `package/{operation}/{id}`.</span></span> <span data-ttu-id="dddbb-1634">Ayrıntılar için, [yönlendirme ara yazılımı kullanma](#use-routing-middleware) bölümünde veya [örnek uygulamada](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples)örnek koda bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1634">For details, see the sample code in the [Use Routing Middleware](#use-routing-middleware) section or the [sample app](https://github.com/dotnet/AspNetCore.Docs/tree/master/aspnetcore/fundamentals/routing/samples).</span></span>

<span data-ttu-id="dddbb-1635">Oluşturucunun ikinci parametresi <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> bir *ortam değerleri* koleksiyonudur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1635">The second parameter to the <xref:Microsoft.AspNetCore.Routing.VirtualPathContext> constructor is a collection of *ambient values*.</span></span> <span data-ttu-id="dddbb-1636">Ortam değerleri, bir geliştiricinin bir istek bağlamı içinde belirtmesi gereken değer sayısını sınırlandırdığından kullanım için uygundur.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1636">Ambient values are convenient to use because they limit the number of values a developer must specify within a request context.</span></span> <span data-ttu-id="dddbb-1637">Geçerli isteğin geçerli yol değerleri, bağlantı oluşturma için çevresel değerler olarak kabul edilir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1637">The current route values of the current request are considered ambient values for link generation.</span></span> <span data-ttu-id="dddbb-1638">ASP.NET Core MVC uygulamasının `About` öğesinin eyleminde, `HomeController` `Index` &mdash; ortam değeri kullanılan eyleme bağlamak için denetleyici yol değerini belirtmeniz gerekmez `Home` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1638">In an ASP.NET Core MVC app's `About` action of the `HomeController`, you don't need to specify the controller route value to link to the `Index` action&mdash;the ambient value of `Home` is used.</span></span>

<span data-ttu-id="dddbb-1639">Bir parametreyle eşleşmeyen çevresel değerler yok sayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1639">Ambient values that don't match a parameter are ignored.</span></span> <span data-ttu-id="dddbb-1640">Ayrıca, açıkça sağlanmış bir değer çevresel değeri geçersiz kıldığında çevresel değerler de yoksayılır.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1640">Ambient values are also ignored when an explicitly provided value overrides the ambient value.</span></span> <span data-ttu-id="dddbb-1641">Eşleştirme, URL 'de soldan sağa doğru gerçekleşir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1641">Matching occurs from left to right in the URL.</span></span>

<span data-ttu-id="dddbb-1642">Açık olarak sağlanmış ancak yolun bir segmentiyle eşleşmeyen değerler sorgu dizesine eklenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1642">Values explicitly provided but that don't match a segment of the route are added to the query string.</span></span> <span data-ttu-id="dddbb-1643">Aşağıdaki tabloda, yol şablonu kullanılırken sonuç gösterilmektedir `{controller}/{action}/{id?}` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1643">The following table shows the result when using the route template `{controller}/{action}/{id?}`.</span></span>

| <span data-ttu-id="dddbb-1644">Çevresel değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1644">Ambient Values</span></span>                     | <span data-ttu-id="dddbb-1645">Açık değerler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1645">Explicit Values</span></span>                        | <span data-ttu-id="dddbb-1646">Sonuç</span><span class="sxs-lookup"><span data-stu-id="dddbb-1646">Result</span></span>                  |
| ---------------------------------- | -------------------------------------- | ----------------------- |
| <span data-ttu-id="dddbb-1647">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1647">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-1648">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1648">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="dddbb-1649">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1649">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-1650">denetleyici = "Order", Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1650">controller = "Order", action = "About"</span></span> | `/Order/About`          |
| <span data-ttu-id="dddbb-1651">denetleyici = "giriş", renk = "kırmızı"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1651">controller = "Home", color = "Red"</span></span> | <span data-ttu-id="dddbb-1652">Action = "hakkında"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1652">action = "About"</span></span>                       | `/Home/About`           |
| <span data-ttu-id="dddbb-1653">denetleyici = "giriş"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1653">controller = "Home"</span></span>                | <span data-ttu-id="dddbb-1654">Action = "hakkında", color = "Red"</span><span class="sxs-lookup"><span data-stu-id="dddbb-1654">action = "About", color = "Red"</span></span>        | `/Home/About?color=Red` |

<span data-ttu-id="dddbb-1655">Bir rotada bir parametreye karşılık gelen bir varsayılan değer varsa ve bu değer açıkça sağlanmışsa, varsayılan değerle eşleşmelidir:</span><span class="sxs-lookup"><span data-stu-id="dddbb-1655">If a route has a default value that doesn't correspond to a parameter and that value is explicitly provided, it must match the default value:</span></span>

```csharp
routes.MapRoute("blog_route", "blog/{*slug}",
    defaults: new { controller = "Blog", action = "ReadPost" });
```

<span data-ttu-id="dddbb-1656">Bağlantı oluşturma yalnızca ve için eşleşen değerler sağlandığında bu yol için bir bağlantı oluşturur `controller` `action` .</span><span class="sxs-lookup"><span data-stu-id="dddbb-1656">Link generation only generates a link for this route when the matching values for `controller` and `action` are provided.</span></span>

## <a name="complex-segments"></a><span data-ttu-id="dddbb-1657">Karmaşık segmentler</span><span class="sxs-lookup"><span data-stu-id="dddbb-1657">Complex segments</span></span>

<span data-ttu-id="dddbb-1658">Karmaşık segmentler (örneğin `[Route("/x{token}y")]` ), sabit değerli olmayan değişmez değer ile sağdan sola eşleştirilirken işlenir.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1658">Complex segments (for example `[Route("/x{token}y")]`) are processed by matching up literals from right to left in a non-greedy way.</span></span> <span data-ttu-id="dddbb-1659">Karmaşık segmentlerin nasıl eşleştirileceği hakkında ayrıntılı bir açıklama için [bu koda](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) bakın.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1659">See [this code](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) for a detailed explanation of how complex segments are matched.</span></span> <span data-ttu-id="dddbb-1660">[Kod örneği](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) ASP.NET Core tarafından kullanılmaz, ancak karmaşık segmentler hakkında iyi bir açıklama sağlar.</span><span class="sxs-lookup"><span data-stu-id="dddbb-1660">The [code sample](https://github.com/dotnet/aspnetcore/blob/v2.2.13/src/Http/Routing/src/Patterns/RoutePatternMatcher.cs#L293) is not used by ASP.NET Core, but it provides a good explanation of complex segments.</span></span>

::: moniker-end
